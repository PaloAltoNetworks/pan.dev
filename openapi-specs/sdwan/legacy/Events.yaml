components:
  schemas:
    Address:
      properties:
        city:
          description: 'The name of the city. Max size = 100.

            '
          type: string
        country:
          description: 'The name of the country. Max size = 100.

            '
          type: string
        post_code:
          description: 'The postal code. Max size = 100.

            '
          type: string
        state:
          description: 'The name of the state. Max size = 100.

            '
          type: string
        street:
          description: 'The name of the street. Max size = 100.

            '
          type: string
        street2:
          description: 'The name of the street. Max size = 100.

            '
          type: string
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items:
            $ref: '#/components/schemas/AggregateData'
          type: array
        field:
          description: Field
          type: string
        name:
          description: 'The name of the query response.

            '
          type: string
        operator:
          description: Operator
          enum:
          - max
          - count
          - min
          - avg
          - sum
          type: string
        statistic:
          description: 'QoS application aggregate statistics.

            '
          type: string
        unit:
          description: 'The unit.

            '
          type: string
    AggregateData:
      properties:
        value:
          description: 'Value of the aggregate data.

            '
          type: number
        view:
          description: 'View the aggregate date.

            '
          type: object
    AnynetLinkQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of anynet links that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number anynet links deleted for a time frame.

            '
          type: boolean
        group_by:
          description: Group By
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the anynet links by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    AnynetLinkQueryFilter:
      properties:
        admin_up:
          description: Admin Up
          type: boolean
        ep1_site_id:
          description: Ep1 Site Id
          type: string
        ep1_site_role:
          description: Ep1 Site Role
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep1_wan_interface_id:
          description: Ep1 Wan Interface Id
          type: string
        ep2_site_id:
          description: Ep2 Site Id
          type: string
        ep2_site_role:
          description: Ep2 Site Role
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep2_wan_interface_id:
          description: Ep2 Wan Interface Id
          type: string
        events:
          description: Events
          items:
            $ref: '#/components/schemas/AnynetlinkEventState'
          type: array
        id:
          description: Id
          type: string
        type:
          description: Type
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpn_events:
          description: Vpn Events
          items:
            $ref: '#/components/schemas/VPNEndpointEventState'
          type: array
    AnynetLinkQueryFilterV2N1:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        ep1_site_id:
          description: "The endpoint 1 site ID.         \n"
          type: string
        ep1_site_role:
          description: 'The endpoint 1 site role.

            '
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep1_wan_interface_id:
          description: "The endpoint 1 WAN interface ID.          \n"
          type: string
        ep2_site_id:
          description: 'The endpoint  2 site ID.

            '
          type: string
        ep2_site_role:
          description: 'The endpoint 2 site role.

            '
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep2_wan_interface_id:
          description: 'The endpoint 2 WAN interface ID.

            '
          type: string
        events:
          description: 'The anynet event states information.

            '
          items:
            $ref: '#/components/schemas/AnynetlinkEventStateV2N1'
          type: array
        id:
          description: 'The anynet link ID.

            '
          type: string
        type:
          description: "Anynet link type. \n"
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpn_events:
          description: 'The VPN endpoint event states.

            '
          items:
            $ref: '#/components/schemas/VPNEndpointEventState'
          type: array
    AnynetlinkEvent:
      properties:
        admin_up:
          description: Admin Up
          type: boolean
        ep1_site_id:
          description: Ep1 Site Id
          type: string
        ep1_site_role:
          description: Ep1 Site Role
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep1_wan_interface_id:
          description: Ep1 Wan Interface Id
          type: string
        ep2_site_id:
          description: Ep2 Site Id
          type: string
        ep2_site_role:
          description: Ep2 Site Role
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep2_wan_interface_id:
          description: Ep2 Wan Interface Id
          type: string
        events:
          description: Events
          items:
            $ref: '#/components/schemas/AnynetlinkEventState'
          type: array
        id:
          description: Id
          type: string
        type:
          description: Type
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpn_events:
          description: Vpn Events
          items:
            $ref: '#/components/schemas/VPNEndpointEventState'
          type: array
    AnynetlinkEventState:
      properties:
        error_vpnlinks:
          description: Error Vpnlinks
          items:
            description: Error Vpnlinks
            type: string
          type: array
        event_code:
          description: Event Code
          type: string
        event_id:
          description: Event Id
          type: string
        id:
          description: Id
          type: string
        priority:
          description: Priority
          format: int32
          type: integer
        site_ids:
          description: Site Ids
          items:
            description: Site Ids
            type: string
          type: array
        standing:
          description: Standing
          type: boolean
        suppressed:
          description: Suppressed
          type: boolean
        suppressed_info:
          $ref: '#/components/schemas/SuppressedInfo'
          description: Suppressed Info
        timestamp:
          description: Timestamp
          format: int64
          type: integer
    AnynetlinkEventStateV2N1:
      properties:
        error_vpnlinks:
          description: 'The vpn link error messages.

            '
          items:
            description: Error Vpnlinks
            type: string
          type: array
        event_code:
          description: 'The event code.

            '
          type: string
        event_id:
          description: "The event ID.         \n"
          type: string
        id:
          description: 'The anynet link ID.

            '
          type: string
        priority:
          description: "The event priority.          \n"
          format: int32
          type: integer
        site_ids:
          description: 'The site IDs.

            '
          items:
            description: Site Ids
            type: string
          type: array
        standing:
          description: 'The standing event. True or False.

            '
          type: boolean
        suppressed:
          description: 'The events are suppressed. True or False.

            '
          type: boolean
        suppressed_info:
          $ref: '#/components/schemas/SuppressedInfo'
          description: 'Information on the suppressed event - event ID, suppressed
            time, summary event IDs etc.

            '
        timestamp:
          description: 'The event time stamp.

            '
          format: int64
          type: integer
    AnynetlinkEventV2N1:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        ep1_site_id:
          description: "The endpoint 1 site ID.         \n"
          type: string
        ep1_site_role:
          description: 'The endpoint 1 site role.

            '
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep1_wan_interface_id:
          description: "The endpoint 1 WAN interface ID.          \n"
          type: string
        ep2_site_id:
          description: 'The endpoint  2 site ID.

            '
          type: string
        ep2_site_role:
          description: 'The endpoint 2 site role.

            '
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep2_wan_interface_id:
          description: 'The endpoint 2 WAN interface ID.

            '
          type: string
        events:
          description: 'The anynet event states information.

            '
          items:
            $ref: '#/components/schemas/AnynetlinkEventStateV2N1'
          type: array
        id:
          description: 'The anynet link ID.

            '
          type: string
        type:
          description: "Anynet link type. \n"
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpn_events:
          description: 'The VPN endpoint event states.

            '
          items:
            $ref: '#/components/schemas/VPNEndpointEventState'
          type: array
    BaseEventState:
      properties:
        event_code:
          description: "The event code. \n"
          type: string
        event_id:
          description: "The event ID.        \n"
          type: string
        id:
          description: Id
          type: string
        priority:
          description: "The priority set for the event. \n"
          format: int32
          type: integer
        standing:
          description: 'The standing event. True or False.

            '
          type: boolean
        suppressed:
          description: 'The events are suppressed. True or False.

            '
          type: boolean
        suppressed_info:
          $ref: '#/components/schemas/SuppressedInfo'
          description: 'The information on the suppressed events.

            '
        timestamp:
          description: 'The time stamp of the event.

            '
          format: int64
          type: integer
    ElementEvent:
      properties:
        events:
          description: 'The details of the events occurred.

            '
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        id:
          description: 'The element ID.

            '
          type: string
        name:
          description: 'The name of the element.

            '
          type: string
        role:
          description: 'The element role.

            '
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        spoke_cluster_status:
          description: 'The spoke cluster status.

            '
          enum:
          - init
          - active
          - backup
          - unknown
          type: string
    ElementQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: Aggregate
        description:
          description: 'The description of the element.

            '
        dest_page:
          description: Dest Page
          format: int32
          type: integer
        getDeleted:
          description: GetDeleted
          type: boolean
        group_by:
          description: Group By
          items:
            description: Group By
            type: string
          type: array
        id:
          description: Id
          type: string
        last_query_ts:
          description: Last Query Ts
          format: int64
          type: integer
        limit:
          description: Limit
          format: int32
          type: integer
        next_query:
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: Retrieved Fields
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: Retrieved Fields Mask
          type: boolean
        sort_params:
          type: object
        total_count:
          description: Total Count
          format: int64
          type: integer
    ElementQueryFilter:
      properties:
        admin_action:
          description: "The values of admin actions that can be performed. \n"
          enum:
          - ALLOCATE
          - CLAIM
          - BOUND
          - UNBOUND
          - DECLAIM
          - RETURN
          - REVOKE
          - RETIRE
          - STOCK
          - UPGRADE
          - REMANUFACTURE
          - DEFAULT
          - INVALID
          type: string
        allowed_roles:
          description: "The allowed roles.       \n"
          items:
            description: Allowed Roles
            enum:
            - NONE
            - HUB
            - SPOKE
            - PA_WE_BR
            - PA_WE_DC
            - PA_CONN
            type: string
          type: array
        cluster_insertion_mode:
          description: "The element ID. \n"
          type: string
        cluster_member_id:
          description: 'The cluster member ID.

            '
          type: string
        connected:
          description: 'The element is connected. True or False.

            '
          type: boolean
        deployment_op:
          description: 'The deployment values.

            '
          enum:
          - DECLAIM
          - UPGRADE
          - ABORT_UPGRADE
          - DEFAULT
          - INVALID
          - REBIND
          - READY
          - IN_SERVICE
          - REUNBIND
          - RENEW_CIC
          - ABORT_CELLULARMODULE_FIRMWARE_UPGRADE
          - FIPS_AUTO
          - NON_FIPS_AUTO
          - FIPS_MANUAL
          - NON_FIPS_MANUAL
          - GET_MAC_ADDRESS_TABLE
          type: string
        description:
          description: 'The description of the element. Max length = 256.

            '
        events:
          description: 'The details of the events occurred.

            '
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        hw_id:
          description: 'The hardware ID.

            '
          type: string
        id:
          description: Id
          type: string
        model_name:
          description: "The model name of the device. \n"
          type: string
        name:
          description: 'The name of the element. Max length = 128.

            '
          type: string
        role:
          description: 'The element role.

            '
          enum:
          - PA_WE_DC
          - PA_WE_BR
          - NONE
          - PA_CONN
          - SPOKE
          - HUB
          type: string
        serial_number:
          description: 'The serial number.

            '
          type: string
        site_id:
          description: 'The site ID.

            '
          type: string
        software_version:
          description: 'The software version.

            '
          type: string
        spoke_cluster_status:
          description: 'The spoke cluster status.

            '
          enum:
          - init
          - active
          - backup
          - unknown
          type: string
        state:
          description: 'The element state.

            '
          enum:
          - ACTIVE_INIT
          - ACTIVE_INVENTORY_IN_PROGRESS
          - ACTIVE_INVENTORY_OPERATION_FAILED
          - ACTIVE_INVENTORY_GET_PENDING
          - ACTIVE_INVENTORY_GET_COMPLETE
          - ACTIVE_INVENTORY_POST_PENDING
          - ACTIVE_INTF_CONFIG_GET_FAILED
          - ACTIVE_INTF_STATE_GET_FAILED
          - ACTIVE_READY
          - ACTIVE_BOUND_IN_PROGRESS
          - ACTIVE_UNBOUND_IN_PROGRESS
          - ACTIVE_UNBOUND_FROM_SITE_FAILED
          - ACTIVE_BOUND_TO_SITE
          - ACTIVE_BOUND_TO_SITE_FAILED
          - OOS
          - ACTIVE_DECLAIM_IN_PROGRESS
          - ACTIVE_DECLAIMED
          type: string
      required:
      - site_id
    Error:
      properties:
        _error:
          items:
            $ref: '#/components/schemas/ErrorResponse'
          type: array
    ErrorResponse:
      properties:
        code:
          description: 'The error code.

            '
          type: string
        message:
          description: 'The error message.

            '
          type: string
    EventBody:
      properties:
        acknowledged:
          description: 'If event is acknowledged. Value = True/False.

            '
          type: boolean
        acknowledgement_info:
          description: 'If acknowledged event information.

            '
          type: string
        cleared:
          description: 'If the event is cleared. Value = True/False.

            '
          type: boolean
        code:
          description: 'The event code.

            '
          type: string
        correlation_id:
          description: 'The event correlation ID.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        entity_ref:
          description: 'The event reference.

            '
          type: string
        id:
          type: string
        info:
          description: 'The details of the hub cluster ID.

            '
          properties:
            hub_cluster_id:
              type: string
          type: object
        notes:
          description: 'The event notes.

            '
          type: string
        policy_info:
          description: 'The event policy information; time the policy was applied,
            policy rule ID, and policy set ID.

            '
          properties:
            policy_applied_time:
              type: string
            policyrule_id:
              type: string
            policyset_id:
              type: string
          type: object
        priority:
          description: 'The event priority.

            '
          type: string
        severity:
          description: 'The event severity level.

            '
          type: string
        site_id:
          description: 'The site ID.

            '
          type: string
        suppressed:
          description: 'If the event is suppressed. Value = True/False.

            '
          type: string
        suppressed_info:
          description: 'Information on the suppressed event - event ID, suppressed
            time, summary event IDs etc.

            '
          properties:
            event_ids:
              type: string
            other_reasons:
              type: string
            summary_event_ids:
              type: string
            suppressed_time:
              type: string
          type: object
        time:
          description: 'The time and date when the event occurred.

            '
          type: string
        type:
          description: 'The event type.

            '
          type: string
      type: object
    EventCodesData:
      properties:
        count:
          type: integer
        description:
          description: 'The event description.

            '
        items:
          items:
            properties:
              category:
                type: string
              code:
                type: string
              description:
                type: string
              display_name:
                type: string
              entity_ref:
                type: string
              resource_types:
                items:
                  type: string
                type: array
              suppress:
                type: boolean
              type:
                enum:
                - alarm
                - alert
                type: string
            type: object
          type: array
      type: object
    EventQueryRequestV3_5:
      properties:
        _offset:
          description: 'Event offset time and date information.

            '
          format: date-time
          type: string
        acknowledged:
          description: 'If event is acknowledged. Value = True/False.

            '
          type: boolean
        end_time:
          description: 'The event end time.

            '
          format: date-time
          type: string
        limit:
          properties:
            count:
              type: integer
            sort_on:
              type: string
            sort_order:
              enum:
              - ascending
              - descending
              type: string
          type: object
        priority:
          description: 'The event priority.

            '
          items:
            enum:
            - p1
            - p2
            - p3
            - p4
            type: string
          type: array
        query:
          description: 'The query details of event category, code, correlation ID,
            entitity reference, site, and type.

            '
          properties:
            category:
              items:
                type: string
              type: array
            code:
              items:
                type: string
              type: array
            correlation_id:
              items:
                type: string
              type: array
            entity_ref:
              items:
                type: string
              type: array
            site:
              items:
                type: string
              type: array
            type:
              items:
                type: string
              type: array
          type: object
        severity:
          description: 'The event severity level.

            '
          items:
            enum:
            - critical
            - major
            - minor
            type: string
          type: array
        start_time:
          description: 'The event start time.

            '
          format: date-time
          type: string
        suppressed:
          description: 'If the event is suppressed. Value = True/False.

            '
          type: boolean
        suppressed_info:
          description: 'Information on the suppressed event - event ID, suppressed
            time, summary event IDs etc.

            '
          properties:
            event_ids:
              items:
                type: string
              type: array
          type: object
        time_type:
          description: 'The event time type.

            '
          enum:
          - raised
          - created
          type: string
        view:
          description: 'The event viewing details.

            '
          properties:
            individual:
              enum:
              - site
              - code
              - category
              - entity
              - correlation
              - tenant
              type: string
            summary:
              type: boolean
          type: object
      type: object
    EventQueryRequestV3_6:
      properties:
        _offset:
          description: 'Event offset time and date information.

            '
          format: date-time
          type: string
        acknowledged:
          description: 'If event is acknowledged. Value = True/False.

            '
          type: boolean
        dest_page:
          description: 'The destination page.

            '
          type: integer
        element_cluster_roles:
          description: 'The element cluster values.

            '
          items:
            enum:
            - HUB
            - SPOKE
            - NONE
            type: string
          type: array
        end_time:
          description: 'The event end time.

            '
          format: date-time
          type: string
        limit:
          properties:
            count:
              type: integer
            sort_on:
              type: string
            sort_order:
              enum:
              - ascending
              - descending
              type: string
          type: object
        priority:
          description: 'The event priority.

            '
          items:
            enum:
            - p1
            - p2
            - p3
            - p4
            type: string
          type: array
        query:
          description: 'The query details of event category, code, correlation ID,
            entitity reference, site, and type.

            '
          properties:
            category:
              items:
                type: string
              type: array
            code:
              items:
                type: string
              type: array
            correlation_id:
              items:
                type: string
              type: array
            entity_ref:
              items:
                type: string
              type: array
            site:
              items:
                type: string
              type: array
            type:
              items:
                type: string
              type: array
          type: object
        severity:
          description: 'The event severity level.

            '
          items:
            enum:
            - critical
            - major
            - minor
            type: string
          type: array
        start_time:
          description: 'The event start time.

            '
          format: date-time
          type: string
        suppressed:
          description: 'If the event is suppressed. Value = True/False.

            '
          type: boolean
        suppressed_info:
          description: 'Information on the suppressed event - event ID, suppressed
            time, summary event IDs etc.

            '
          properties:
            event_ids:
              items:
                type: string
              type: array
          type: object
        temporal_scope:
          description: 'Temporal scope of the event.

            '
          enum:
          - include_standing
          - standing_only
          type: string
        time_type:
          description: 'The event time type.

            '
          enum:
          - raised
          - created
          type: string
        view:
          description: 'The event viewing details.

            '
          properties:
            individual:
              enum:
              - site
              - code
              - category
              - entity
              - correlation
              - tenant
              type: string
            summary:
              type: boolean
          type: object
      type: object
    EventQueryResponseV3_5:
      properties:
        _request_id:
          description: 'The event query request ID.

            '
          type: string
        _status_code:
          description: 'The status code of the event.

            '
          type: integer
        included_count:
          type: integer
        items:
          description: 'The details of the event query.

            '
          items:
            properties:
              _created_on_utc:
                format: date-time
                type: string
              _etag:
                type: string
              _updated_on_utc:
                format: date-time
                type: string
              acknowledged:
                type: boolean
              acknowledgement_info:
                properties:
                  acknowledged_by:
                    type: string
                  acknowledgement_time:
                    format: date-time
                    type: string
                type: object
              cleared:
                type: boolean
              code:
                type: string
              correlation_id:
                type: string
              entity_ref:
                type: string
              id:
                type: string
              ident:
                type: string
              info:
                type: object
              policy_info:
                properties:
                  policyrule_id:
                    type: string
                  policyset_id:
                    type: string
                type: object
              priority:
                type: string
              severity:
                type: string
              standing:
                type: boolean
              suppressed:
                type: boolean
              suppressed_info:
                properties:
                  event_id:
                    type: string
                  other_reason:
                    type: string
                  suppressed_time:
                    format: date-time
                    type: string
                type: object
              time:
                format: date-time
                type: string
              type:
                type: string
            type: object
          type: array
        total_count:
          description: 'The event query total count.

            '
          type: integer
      type: object
    EventQueryResponseV3_6:
      properties:
        _request_id:
          description: 'The event query request ID.

            '
          type: string
        _status_code:
          description: 'The status code of the event.

            '
          type: integer
        dest_page:
          description: 'The destination page.

            '
          type: integer
        included_count:
          description: 'The event included count.

            '
          type: integer
        items:
          description: 'The details of the event query.

            '
          items:
            properties:
              _created_on_utc:
                format: date-time
                type: string
              _etag:
                type: string
              _updated_on_utc:
                format: date-time
                type: string
              acknowledged:
                type: boolean
              acknowledgement_info:
                properties:
                  acknowledged_by:
                    type: string
                  acknowledgement_time:
                    format: date-time
                    type: string
                type: object
              cleared:
                type: boolean
              code:
                type: string
              correlation_id:
                type: string
              element_cluster_roles:
                items:
                  enum:
                  - HUB
                  - SPOKE
                  - NONE
                  type: string
                type: array
              entity_ref:
                type: string
              id:
                type: string
              ident:
                type: string
              info:
                type: object
              policy_info:
                properties:
                  policyrule_id:
                    type: string
                  policyset_id:
                    type: string
                type: object
              priority:
                type: string
              severity:
                type: string
              standing:
                type: boolean
              suppressed:
                type: boolean
              suppressed_info:
                properties:
                  event_id:
                    type: string
                  other_reason:
                    type: string
                  suppressed_time:
                    format: date-time
                    type: string
                type: object
              time:
                format: date-time
                type: string
              type:
                type: string
            type: object
          type: array
        total_count:
          description: 'The event query total count.

            '
          type: integer
      type: object
    EventsOperationsRequestBody:
      properties:
        bulk_acknowledge:
          description: 'The details of acknowledged events.

            '
          properties:
            acknowledge:
              type: boolean
            event_ids:
              items:
                type: string
              type: array
          type: object
      type: object
    EventsOperationsResponseBody:
      properties:
        _status_code:
          description: 'The status code of the event.

            '
          type: integer
      type: object
    EventsSummaryBodyV2_0:
      properties:
        element_cluster_role:
          description: 'The element cluster role.

            '
          enum:
          - HUB
          - SPOKE
          - NONE
          type: string
        end_time:
          description: 'The event end time.

            '
          type: string
        group_by:
          description: 'The event grouping.

            '
          items:
            enum:
            - priority
            - code
            - tenant
            type: string
          type: array
        query:
          description: 'The query details of event code and type.

            '
          properties:
            code:
              items:
                type: string
              type: array
            type:
              items:
                enum:
                - alarm
                - alert
                type: string
              type: array
          type: object
        start_time:
          description: 'The event start time.

            '
          type: string
      type: object
    EventsSummaryBodyV2_1:
      properties:
        clients:
          items:
            type: string
          type: array
        element_cluster_role:
          enum:
          - HUB
          - SPOKE
          - NONE
          type: string
        end_time:
          type: string
        group_by:
          items:
            enum:
            - priority
            - code
            - tenant
            type: string
          type: array
        query:
          properties:
            code:
              items:
                type: string
              type: array
            type:
              items:
                enum:
                - alarm
                - alert
                type: string
              type: array
          type: object
        start_time:
          type: string
      type: object
    EventsSummaryData:
      properties:
        _status_code:
          description: 'The status code of the event.

            '
          example: 200
          type: integer
        count:
          description: 'The event count.

            '
          type: integer
        data:
          description: 'The details of the event code, priority, and tenant IDs.

            '
          items:
            properties:
              code:
                type: string
              priority:
                type: string
              tenant_ids:
                type: string
            type: object
          type: array
        type:
          description: 'The event type.

            '
          enum:
          - alarm
          - alert
          type: string
      type: object
    EventsSummaryTenantData:
      properties:
        _status_code:
          description: 'The status code of the event.

            '
          example: 200
          type: integer
        count:
          description: 'The event count.

            '
          type: integer
        data:
          description: 'The details of the event code, priority, and tenant IDs.

            '
          items:
            properties:
              alarms:
                properties:
                  p1:
                    type: integer
                  p2:
                    type: integer
                  p3:
                    type: integer
                  p4:
                    type: integer
                  p5:
                    type: integer
                type: object
              alerts:
                properties:
                  p1:
                    type: integer
                  p2:
                    type: integer
                  p3:
                    type: integer
                  p4:
                    type: integer
                  p5:
                    type: integer
                type: object
              tenant_id:
                type: string
            type: object
          type: array
        type:
          description: 'The event type.

            '
          enum:
          - alarm
          - alert
          type: string
      type: object
    GetEventResponseBody:
      properties:
        _created_on_utc:
          description: 'The event created time and date.

            '
          format: date-time
          type: string
        _etag:
          description: 'The etag.

            '
          type: integer
        _status_code:
          description: 'The event status code.

            '
          type: integer
        _updated_on_utc:
          description: 'The event updated time and date.

            '
          format: date-time
          type: string
        code:
          description: 'The event code.

            '
          type: string
        correlation_id:
          description: 'The event correlation ID.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        entity_ref:
          description: 'The event reference.

            '
          type: string
        id:
          type: string
        info:
          properties:
            process_name:
              type: string
          type: object
        notes:
          description: 'The event notes.

            '
          type: string
        policy_info:
          description: 'The event policy information; time the policy was applied,
            policy rule ID, and policy set ID.

            '
          properties:
            policy_applied_time:
              format: date-time
              type: string
            policyrule_id:
              type: string
            policyset_id:
              type: string
          type: object
        priority:
          description: 'The event priority.

            '
          type: string
        severity:
          description: 'The event severity level.

            '
          type: string
        site_id:
          description: 'The site ID.

            '
          type: string
        suppressed:
          description: 'If the event is suppressed. Value = True/False.

            '
          type: boolean
        suppressed_info:
          description: 'Information on the suppressed event - event ID, suppressed
            time, summary event IDs etc.

            '
          properties:
            event_ids:
              items:
                type: string
              type: array
            other_reasons:
              items:
                type: string
              type: array
            summary_event_ids:
              items:
                type: string
              type: array
            suppressed_time:
              format: date-time
              type: string
          type: object
        time:
          description: 'The time and date when the event occurred.

            '
          format: date-time
          type: string
        type:
          description: "The event type.           \n"
          type: string
      type: object
    InterfaceEvent:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        bound_interfaces:
          description: 'The bound interfaces.

            '
          items:
            description: Bound Interfaces
            type: string
          type: array
        element_id:
          description: 'The element ID.

            '
          type: string
        events:
          description: 'The details of the events occurred.

            '
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        id:
          description: "The interface ID.    \n"
          type: string
        ipv4_addresses:
          description: 'The IPv4 addresses configured for the interface.

            '
          items:
            description: Ipv4 Addresses
            type: string
          type: array
        name:
          description: 'The name of the interface.

            '
          type: string
        parent:
          description: 'The parent interface.

            '
          type: string
        site_wan_interface_ids:
          description: 'The site WAN interface IDs.

            '
          items:
            description: Site Wan Interface Ids
            type: string
          type: array
        type:
          description: 'The interface type.

            '
          enum:
          - SERVICE_LINK
          - LOOPBACK
          - BYPASSPAIR
          - SUBINTERFACE
          - PPPOE
          - PORTCHANNEL
          - PORT
          - VIRTUAL_INTERFACE
          - CELLULAR
          - SWITCH_PORT
          - SVI
          - PORT_CHANNEL
          - NGFW_SDWANINTERFACE
          type: string
        used_for:
          description: 'The value of where the interface is used.

            '
          enum:
          - NONE
          - PUBLIC
          - PRIVATE
          - LAN
          - PRIVATE_L2
          - PRIVATE_WAN
          type: string
    InterfaceQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of interfaces that match the query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number interfaces deleted for a time frame.

            '
          type: boolean
        group_by:
          description: Group By
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the interfaces by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    InterfaceQueryFilter:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        bound_interfaces:
          description: 'The bound interfaces.

            '
          items:
            description: Bound Interfaces
            type: string
          type: array
        element_id:
          description: 'The element ID.

            '
          type: string
        events:
          description: 'The details of the events occurred.

            '
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        id:
          description: "The interface ID.    \n"
          type: string
        ipv4_addresses:
          description: 'The IPv4 addresses configured for the interface.

            '
          items:
            description: Ipv4 Addresses
            type: string
          type: array
        name:
          description: 'The name of the interface.

            '
          type: string
        parent:
          description: 'The parent interface.

            '
          type: string
        site_wan_interface_ids:
          description: 'The site WAN interface IDs.

            '
          items:
            description: Site Wan Interface Ids
            type: string
          type: array
        type:
          description: 'The interface type.

            '
          enum:
          - SERVICE_LINK
          - LOOPBACK
          - BYPASSPAIR
          - SUBINTERFACE
          - PPPOE
          - PORTCHANNEL
          - PORT
          - VIRTUAL_INTERFACE
          - CELLULAR
          - SWITCH_PORT
          - SVI
          - PORT_CHANNEL
          - NGFW_SDWANINTERFACE
          type: string
        used_for:
          description: 'The value of where the interface is used.

            '
          enum:
          - NONE
          - PUBLIC
          - PRIVATE
          - LAN
          - PRIVATE_L2
          - PRIVATE_WAN
          type: string
    ListQueryResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        deleted_count:
          description: 'The deleted number.

            '
          format: int64
          type: integer
        deleted_ids:
          description: 'The deleted IDs.

            '
          items:
            type: string
          type: array
        description:
          description: 'Description of the query. Max size = 256.

            '
        id:
          description: 'The ID.

            '
          type: string
        next_query:
          description: 'Details of the next query.

            '
          type: object
        tenant_id:
          description: 'The tenant ID.

            '
          type: string
        total_count:
          description: 'Total number.

            '
          format: int64
          type: integer
      type: object
    Location:
      properties:
        description:
          description: 'The decsription of the location.

            '
        latitude:
          description: 'The latitude of the location. Max range = 90.

            '
          format: float
          type: number
        longitude:
          description: 'The longitude of the location. Max range = 180.

            '
          format: float
          type: number
    SiteEvent:
      properties:
        address:
          $ref: '#/components/schemas/Address'
          description: 'The address of the site where the event occurred.

            '
        admin_state:
          description: 'The site mode of operation, can be in one of three states:
            Active - the site is in Control mode and network policy is applied. Monitor
            - the site is in Analytics mode, no network policy is applied. Disabled:
            the site is disabled.

            '
          type: string
        element_cluster_role:
          description: 'This attribute describes the type i.e. SPOKE or HUB.

            '
          type: string
        events:
          description: 'The details of the events occurred.

            '
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        id:
          description: 'The site ID.

            '
          type: string
        location:
          $ref: '#/components/schemas/Location'
          description: 'The geographical cordinates for the site address.

            '
        name:
          description: 'The site name (Max size = 128).

            '
          type: string
    SiteQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of sites that match the query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number sites deleted for a time frame.

            '
          type: boolean
        group_by:
          description: Group By
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the sites by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    SiteQueryFilter:
      properties:
        address:
          $ref: '#/components/schemas/Address'
          description: 'The address of the site.

            '
        admin_state:
          description: 'The site mode of operation, can be in one of three states:
            Active - the site is in Control mode and network policy is applied. Monitor
            - the site is in Analytics mode, no network policy is applied. Disabled:
            the site is disabled.

            '
          type: string
        description:
          description: 'Description for the site (Max size = 1024).

            '
        element_cluster_role:
          description: 'This attribute describes the type i.e. SPOKE or HUB.

            '
          type: string
        events:
          description: Events
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        id:
          description: 'The ID.

            '
          type: string
        location:
          $ref: '#/components/schemas/Location'
          description: 'The geographical cordinates for the site address.

            '
        name:
          description: 'The site name (Max size = 128).

            '
          type: string
        policy_set_id:
          description: 'The ID for the Original Network Policy Set. Can be retrieved
            using policysets API.

            '
          type: string
        security_policyset_id:
          description: 'The ID for the Original Security Policy Set. Can be retrieved
            using securitypolicysets API.

            '
          type: string
        service_binding:
          description: 'Bind a site to a set of domains. Can be retrieved using sericebindingmaps
            API.

            '
          type: string
    SuppressedInfo:
      properties:
        root_cause_event_ids:
          description: 'The event IDs of the root causes.

            '
          items:
            description: Root Cause Event Ids
            type: string
          type: array
        suppressed_time:
          description: 'The suppressed event time.

            '
          format: date
          type: string
    VPNEndpointEventState:
      properties:
        al_id:
          description: Al Id
          type: string
        eid:
          description: Eid
          type: string
        event_code:
          description: 'The event code.

            '
          type: string
        event_id:
          description: "The event ID.         \n"
          type: string
        id:
          description: 'The anynet link ID.

            '
          type: string
        priority:
          description: "The event priority.          \n"
          format: int32
          type: integer
        site_id:
          description: Site Id
          type: string
        standing:
          description: 'The standing event. True or False.

            '
          type: boolean
        suppressed:
          description: 'The events are suppressed. True or False.

            '
          type: boolean
        suppressed_info:
          $ref: '#/components/schemas/SuppressedInfo'
          description: 'Information on the suppressed event - event ID, suppressed
            time, summary event IDs etc.

            '
        timestamp:
          description: 'The event time stamp.

            '
          format: int64
          type: integer
        vep_id:
          description: Vep Id
          type: string
        vpnlink_id:
          description: Vpnlink Id
          type: string
    WANInterfaceEvent:
      properties:
        events:
          description: 'The details of the WAN events occurred.

            '
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        id:
          description: 'The WAN interface ID.

            '
          type: string
        name:
          description: 'The name of the WAN interface.

            '
          type: string
        network_id:
          description: 'The network ID.

            '
          type: string
        type:
          description: 'The WAN interface type.

            '
          enum:
          - PUBLIC_WAN
          - PRIVATE_WAN
          type: string
    WANInterfaceQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of app defs that match the query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number app definitions deleted for a time frame.

            '
          type: boolean
        group_by:
          description: Group By
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the app definitions by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    WANInterfaceQueryFilter:
      properties:
        events:
          description: 'The details of the WAN events occurred.

            '
          items:
            $ref: '#/components/schemas/BaseEventState'
          type: array
        id:
          description: 'The WAN interface ID.

            '
          type: string
        name:
          description: 'The name of the WAN interface.

            '
          type: string
        network_id:
          description: 'The network ID.

            '
          type: string
        type:
          description: 'The WAN interface type.

            '
          enum:
          - PUBLIC_WAN
          - PRIVATE_WAN
          type: string
info:
  contact: {}
  description: List of APIs used to retrieve events, manage the event policy rules
    that include managing of event correlation policy rules and sets and querying
    of the rules and sets.
  title: Events
  version: Latest
openapi: 3.0.0
paths:
  /v2.0/api/tenants/{tenant_id}/anynetlinks/correlationevents/query:
    post:
      description: Query AnynetLink correlation events.
      operationId: post-v2.0-api-tenants-tenant_id-anynetlinks-correlationevents-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/AnynetLinkQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_QUERY_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query AnynetLink Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/anynetlinks/{anynetlink_id}/correlationevents:
    get:
      description: Get anynetlink correlation events.
      operationId: get-v2.0-api-tenants-tenant_id-anynetlinks-anynetlink_id-correlationevents
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The anynet link ID.
        in: path
        name: anynetlink_id
        required: true
        schema:
          description: Anynetlink Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnynetlinkEvent'
          description: Successful Operation
      summary: Get AnynetLink Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/elements/correlationevents/query:
    post:
      description: Query element correlation events.
      operationId: post-v2.0-api-tenants-tenant_id-elements-correlationevents-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElementQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/ElementQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_QUERY_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Element Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/eventcodes:
    get:
      description: List the names and descriptions of all event codes.
      operationId: get-v2.0-api-tenants-tenant_id-eventcodes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventCodesData'
          description: Successful Operation
      summary: Get Event Codes (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/events/operations:
    post:
      description: Get all the event operations.
      operationId: post-v2.0-api-tenants-tenant_id-events-operations
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventsOperationsRequestBody'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventsOperationsResponseBody'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_REQUEST_ERROR_0001
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request Invalid query or params
      summary: Get Event Operations (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/events/summary:
    post:
      description: Get all the event summaries.
      operationId: post-v2.0-api-tenants-tenant_id-events-summary
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventsSummaryBodyV2_0'
      responses:
        '200':
          content:
            application/json:
              schema:
                oneOf:
                - $ref: '#/components/schemas/EventsSummaryData'
                - $ref: '#/components/schemas/EventsSummaryTenantData'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_REQUEST_ERROR_0001
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Event Summary (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/interfaces/correlationevents/query:
    post:
      description: Query interface correlation events.
      operationId: post-v2.0-api-tenants-tenant_id-interfaces-correlationevents-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InterfaceQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/InterfaceQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Interface Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/sites/correlationevents/query:
    post:
      description: Query site correlation events.
      operationId: post-v2.0-api-tenants-tenant_id-sites-correlationevents-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SiteQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/SiteQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_QUERY_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query SIte Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/correlationevents:
    get:
      description: Get site correlation events.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-correlationevents
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteEvent'
          description: Successful Operation
      summary: Get Site Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/correlationevents:
    get:
      description: Get element events.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-correlationevents
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementEvent'
          description: Successful Operation
      summary: Get Element Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/interfaces/{interface_id}/correlationevents:
    get:
      description: Get interface correlation events.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-interfaces-interface_id-correlationevents
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The interface ID.
        in: path
        name: interface_id
        required: true
        schema:
          description: Interface Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InterfaceEvent'
          description: Successful Operation
      summary: Get Interface Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/waninterfaces/{wan_interface_id}/correlationevents:
    get:
      description: Get wan interface correlation events.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-waninterfaces-wan_interface_id-correlationevents
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The wan interface ID.
        in: path
        name: wan_interface_id
        required: true
        schema:
          description: Wan Interface Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WANInterfaceEvent'
          description: Successful Operation
      summary: Get WAN Interface Correlation Events (v2.0)
      tags:
      - Events
  /v2.0/api/tenants/{tenant_id}/waninterfaces/correlationevents/query:
    post:
      description: Query WAN interface correlation events.
      operationId: post-v2.0-api-tenants-tenant_id-waninterfaces-correlationevents-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WANInterfaceQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/WANInterfaceQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_QUERY_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query WAN Interface Correlation Events (v2.0)
      tags:
      - Events
  /v2.1/api/tenants/{tenant_id}/anynetlinks/correlationevents/query:
    post:
      description: Query AnynetLink correlation events.
      operationId: post-v2.1-api-tenants-tenant_id-anynetlinks-correlationevents-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/AnynetLinkQueryFilterV2N1'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_QUERY_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query AnynetLink Correlation Events (v2.1)
      tags:
      - Events
  /v2.1/api/tenants/{tenant_id}/anynetlinks/{anynetlink_id}/correlationevents:
    get:
      description: Get anynetlink correlation events.
      operationId: get-v2.1-api-tenants-tenant_id-anynetlinks-anynetlink_id-correlationevents
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The anynet link ID.
        in: path
        name: anynetlink_id
        required: true
        schema:
          description: Anynetlink Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnynetlinkEventV2N1'
          description: Successful Operation
      summary: Get AnynetLink Correlation Events (v2.1)
      tags:
      - Events
  /v2.1/api/tenants/{tenant_id}/events/summary:
    post:
      description: Get all the event summaries.
      operationId: post-v2.1-api-tenants-tenant_id-events-summary
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventsSummaryBodyV2_1'
      responses:
        '200':
          content:
            application/json:
              schema:
                oneOf:
                - $ref: '#/components/schemas/EventsSummaryData'
                - $ref: '#/components/schemas/EventsSummaryTenantData'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_REQUEST_ERROR_0001
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request Invalid query or params
      summary: Get Event Summary (v2.0)
      tags:
      - Events
  /v2.3/api/tenants/{tenant_id}/events/{event_id}:
    get:
      description: Get all the events.
      operationId: get-v2.3-api-tenants-tenant_id-events-event_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The event ID.
        in: path
        name: event_id
        required: true
        schema:
          description: Event Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GetEventResponseBody'
          description: Successful Operation
      summary: Get all Events (v2.3)
      tags:
      - Events
    put:
      description: Update event details.
      operationId: put-v2.3-api-tenants-tenant_id-events-event_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The event ID.
        in: path
        name: event_id
        required: true
        schema:
          description: Event Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventBody'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventBody'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_REQUEST_ERROR_0001
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Event Details (v2.3)
      tags:
      - Events
  /v3.5/api/tenants/{tenant_id}/events/query:
    post:
      description: Query events for supporting pagination.
      operationId: post-v3.5-api-tenants-tenant_id-events-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventQueryRequestV3_5'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventQueryResponseV3_5'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_REQUEST_ERROR_0001
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Events for Pagination Support (v3.5)
      tags:
      - Events
  /v3.6/api/tenants/{tenant_id}/events/query:
    post:
      description: Query events for supporting pagination.
      operationId: post-v3.6-api-tenants-tenant_id-events-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EventQueryRequestV3_6'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EventQueryResponseV3_6'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_REQUEST_ERROR_0001
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Events for Pagination Support (v3.6)
      tags:
      - Events
servers:
- url: https://api.us.cloudgenix.com
tags:
- name: Events
