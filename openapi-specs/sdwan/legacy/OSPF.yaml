components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items:
            $ref: '#/components/schemas/AggregateData'
          type: array
        field:
          description: Field
          type: string
        name:
          description: 'The name of the query response.

            '
          type: string
        operator:
          description: Operator
          enum:
          - max
          - count
          - min
          - avg
          - sum
          type: string
        statistic:
          description: 'QoS application aggregate statistics.

            '
          type: string
        unit:
          description: 'The unit.

            '
          type: string
    AggregateData:
      properties:
        value:
          description: 'Value of the aggregate data.

            '
          type: number
        view:
          description: 'View the aggregate date.

            '
          type: object
    Area:
      properties:
        area_id:
          description: 'The area over which the OSPF parameters can be applied. Identifier
            for the area in x.x.x.x format. This is the identifier that each neighbor
            must accept to be part of the same area.

            '
          type: integer
        area_type:
          description: 'The OSPF config area type.

            '
          enum:
          - NORMAL
          - STUB
          - NSSA
          type: string
    DiscoveredNeighbor:
      properties:
        neighbor_id:
          description: 'The discovered neighbor ID.

            '
          type: string
        status:
          description: 'Status of the configuration.

            '
          type: string
    Error:
      properties:
        _error:
          items:
            $ref: '#/components/schemas/ErrorResponse'
          type: array
    ErrorResponse:
      properties:
        code:
          description: 'The error code.

            '
          type: string
        message:
          description: 'The error message.

            '
          type: string
    InterfaceConfig:
      properties:
        area_id:
          description: 'Identifier for the area in x.x.x.x format.

            '
          type: integer
        discovered_neighbors:
          description: 'Info on the discovered neighbors.

            '
          items:
            $ref: '#/components/schemas/DiscoveredNeighbor'
          type: array
        interface_id:
          description: 'Associated area interface ID.

            '
          type: string
    InterfaceOspfConfig:
      properties:
        area_id:
          description: 'Identifier for the area in x.x.x.x format.

            '
          type: integer
        interface_id:
          description: 'Associated area interface ID.

            '
          type: string
        ospf_config_override:
          $ref: '#/components/schemas/OspfGlobalConfig'
          description: 'The global config to override the global configurations for
            the selected interface.

            '
    ListQueryResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        deleted_count:
          description: 'The deleted number.

            '
          format: int64
          type: integer
        deleted_ids:
          description: 'The deleted IDs.

            '
          items:
            type: string
          type: array
        description:
          description: 'Description of the query. Max size = 256.

            '
        id:
          description: 'The ID.

            '
          type: string
        next_query:
          description: 'Details of the next query.

            '
          type: object
        tenant_id:
          description: 'The tenant ID.

            '
          type: string
        total_count:
          description: 'Total number.

            '
          format: int64
          type: integer
      type: object
    ListResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        id:
          description: 'The ID.

            '
          type: string
        tenant_id:
          description: 'The tenant ID.

            '
          type: string
      type: object
    OSPFGlobalConfig:
      properties:
        cost:
          description: 'The Cost range, which is 1 - 65535.

            '
          type: integer
        dead_interval:
          description: 'The number of seconds that a neighbor router waits for a hello
            packet from the device before declaring the router down. range is 1-65535,
            Default is 40.

            '
          format: int32
          type: integer
        hello_interval:
          description: 'The interval in seconds at which the OSPF process sends hello
            packets to its directly connected neighbors (the range is 1 - 65535; the
            default is 10).

            '
          format: int32
          type: integer
        id:
          description: 'The OSPF config ID.

            '
          type: string
        md5_key_id:
          description: 'MD5 Key ID between 1 and 255 and an MD5 Secret of up to 16
            characters. The MD5 password you specify will be applied to the messages
            exchanged with the peer.

            '
          type: integer
        md5_secret:
          description: 'The MD5 secret key of 16 characters. The MD5 password specified
            is applied to the messages exchanged with the peer.

            '
          type: string
        prefix_adv_type_to_lan:
          description: "The prefix advertisement type to LAN. \n"
          type: string
        retransmit_interval:
          description: 'The length of time, in seconds, that OSPF waits to receive
            a link-state advertisement (LSA) from a neighbor before retransmitting
            it (the range is 1 - 65535; the default is 5).

            '
          format: int32
          type: integer
        router_id:
          description: 'The Router ID. IP address and ION device''s OSPF ID.

            '
          type: string
        transmit_delay:
          description: "The length of time an LSA is delayed seconds before being\
            \ sent out of an interface (range is 1 - 65535; default is 1). \n"
          format: int32
          type: integer
    OSPFGlobalConfigScreen:
      properties:
        cost:
          description: 'Cost: Range(max = 65535L, message = OSPF_CONFIG_INVALID_COST,
            min = 1L) '
          type: integer
        dead_interval:
          description: 'Dead Interval: Range(max = 65535L, error = OSPF_CONFIG_INVALID_DEAD_INTERVAL:
            Invalid Dead Interval.Value needs to be between 1 and 65535., min = 1L) '
          format: int32
          type: integer
        hello_interval:
          description: 'Hello Interval: Range(max = 65535L, error = OSPF_CONFIG_INVALID_HELLO_INTERVAL:
            Invalid Hello Interval.Value needs to be between 1 and 65535., min = 1L) '
          format: int32
          type: integer
        id:
          description: Id
          type: string
        md5_key_id:
          description: 'Md5 Key Id: Range(max = 255L, message = OSPF_CONFIG_MD5_KEY_ID_LENGTH_EXCEEDS,
            min = 1L) '
          type: integer
        md5_secret:
          description: 'Md5 Secret: Size(max = 16, error = OSPF_CONFIG_MD5_LENGTH_EXCEEDS:
            Max length of MD5 secret is 16., min = 1) '
          type: string
        prefix_adv_type_to_lan:
          description: Prefix Adv Type To Lan
          type: string
        retransmit_interval:
          description: 'Retransmit Interval: Range(max = 65535L, error = OSPF_CONFIG_INVALID_RETRANSMIT_INTERVAL:
            Invalid retransmit interval.Value needs to be between 1 and 65535., min
            = 1L) '
          format: int32
          type: integer
        router_id:
          description: 'Router Id: IPAddress(allowEmpty = true, allowLinkLocal = false,
            allowNull = true, bcast = DENY, defaultRoute = false, dnsCheck = false,
            error = OSPF_CONFIG_INVALID_ROUTER_ID: Invalid Router Id, type = IP) '
          type: string
        transmit_delay:
          description: 'Transmit Delay: Range(max = 65535L, error = OSPF_CONFIG_INVALID_TRANSMIT_DELAY:
            Invalid transmit delay.Value needs to be between 1 and 65535., min = 1L) '
          format: int32
          type: integer
    OspfConfig:
      properties:
        areas:
          description: 'Areas: Valid '
          items:
            $ref: '#/components/schemas/Area'
          type: array
        description:
          description: 'Description: Size(max = 1024, error = DESCRIPTION_EXCEEDS_LIMIT:
            Description length exceeds limit, min = 0) '
          type: string
        id:
          description: Id
          type: string
        interfaces:
          description: 'Interfaces: Valid '
          items:
            $ref: '#/components/schemas/InterfaceOspfConfig'
          type: array
        name:
          description: 'Name: Required(error = NAME_REQD: Please provide resource
            name.) Size(max = 128, error = NAME_EXCEEDS_LIMIT: Name of the resource
            exceeds limit., min = 0) '
          type: string
        prefix_adv_route_map_id:
          description: Prefix Adv Route Map Id
          type: string
        prefix_adv_type_to_lan:
          description: Prefix Adv Type To Lan
          type: string
        redistribute_bgp:
          description: Redistribute Bgp
          type: boolean
        redistribute_route_map_id:
          description: Redistribute Route Map Id
          type: string
        router_id:
          description: 'Router Id: IPAddress(allowEmpty = true, allowLinkLocal = false,
            allowNull = true, bcast = DENY, defaultRoute = false, dnsCheck = false,
            message = OSPF_CONFIG_INVALID_ROUTER_ID, type = IP) '
          type: string
        scope:
          description: Scope
          type: string
        shutdown:
          description: Shutdown
          type: boolean
        tags:
          description: 'Tags: ListString(allowDuplicate = false, allowEmpty = true,
            allowNull = true, length = 1024, listMaxSize = 10, error = INVALID_TAG:
            Maximum 10 Unique tags of length 1024 each are allowed, noTrim = false,
            regex = [^,\\s]+, required = false) '
          items:
            description: Tags
            type: string
          type: array
        vrf_context_id:
          description: 'Vrf Context Id: NotNull(message = OSPF_CONFIG_NO_VRF_CONTEXT) '
          type: string
      required:
      - vrf_context_id
      - name
    OspfConfigQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of OSPF configs that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number OSPF configs deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the OSPF configs for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the OSPF configs by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    OspfConfigQueryFilter:
      properties:
        areas:
          description: 'Info on the OSPF config areas.

            '
          items:
            $ref: '#/components/schemas/Area'
          type: array
        description:
          description: 'The OSPF config description. Max size = 1024.

            '
        id:
          description: "The OSPF config ID.  \n"
          type: string
        interfaces:
          description: "Info on the OSPF config interfaces. \n"
          items:
            $ref: '#/components/schemas/InterfaceOspfConfig'
          type: array
        name:
          description: "The OSPF config name. Max size = 128.   \n"
          type: string
        prefix_adv_route_map_id:
          description: 'The prefix advertisement route map ID.

            '
          type: string
        prefix_adv_type_to_lan:
          description: 'The prefix advertisement type to LAN.

            '
          type: string
        redistribute_bgp:
          description: 'Redistribute to advertise all the BGP Prefixes into OSPF.

            '
          type: boolean
        redistribute_route_map_id:
          description: 'The redistribute BGP Prefix in OSPF route map ID.

            '
          type: string
        router_id:
          description: 'The Router ID. IP address and ION device''s OSPF ID.

            '
          type: string
        scope:
          description: 'Local or Global. Local indicates that prefixes will not be
            advertised.

            '
          type: string
        shutdown:
          description: 'True or False = the created peer is used or not.

            '
          type: boolean
        tags:
          description: 'An information field that can be added to identify the OSPF
            config. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        vrf_context_id:
          description: 'The attached VRF (Virtual Routing and Forwarding) ID.

            '
          type: string
      required:
      - vrf_context_id
      - name
    OspfConfigScreen:
      properties:
        areas:
          description: 'Areas: Valid '
          items:
            $ref: '#/components/schemas/Area'
          type: array
        description:
          description: 'Description: Size(max = 1024, error = DESCRIPTION_EXCEEDS_LIMIT:
            Description length exceeds limit, min = 0) '
          type: string
        id:
          description: Id
          type: string
        interfaces:
          description: 'Interfaces: Valid '
          items:
            $ref: '#/components/schemas/InterfaceOspfConfig'
          type: array
        name:
          description: 'Name: Required(error = NAME_REQD: Please provide resource
            name.) Size(max = 128, error = NAME_EXCEEDS_LIMIT: Name of the resource
            exceeds limit., min = 0) '
          type: string
        prefix_adv_route_map_id:
          description: Prefix Adv Route Map Id
          type: string
        prefix_adv_type_to_lan:
          description: Prefix Adv Type To Lan
          type: string
        redistribute_bgp:
          description: Redistribute Bgp
          type: boolean
        redistribute_route_map_id:
          description: Redistribute Route Map Id
          type: string
        router_id:
          description: 'Router Id: IPAddress(allowEmpty = true, allowLinkLocal = false,
            allowNull = true, bcast = DENY, defaultRoute = false, dnsCheck = false,
            message = OSPF_CONFIG_INVALID_ROUTER_ID, type = IP) '
          type: string
        scope:
          description: Scope
          type: string
        shutdown:
          description: Shutdown
          type: boolean
        tags:
          description: 'Tags: ListString(allowDuplicate = false, allowEmpty = true,
            allowNull = true, length = 1024, listMaxSize = 10, error = INVALID_TAG:
            Maximum 10 Unique tags of length 1024 each are allowed, noTrim = false,
            regex = [^,\\s]+, required = false) '
          items:
            description: Tags
            type: string
          type: array
        vrf_context_id:
          description: 'Vrf Context Id: NotNull(message = OSPF_CONFIG_NO_VRF_CONTEXT) '
          type: string
      required:
      - vrf_context_id
      - name
    OspfDiscoveredNeighbor:
      properties:
        discovered_neighbors_count:
          description: 'The number of OSPF discovered neighbors.

            '
          type: integer
        id:
          description: 'The OSPF discovered neighbor ID.

            '
          type: string
        interfaces:
          description: 'The interfaces name.

            '
          items:
            $ref: '#/components/schemas/InterfaceConfig'
          type: array
        ospf_config_id:
          description: 'The OSPF config ID.

            '
          type: string
        vrf_context_id:
          description: 'The attached VRF (Virtual Routing and Forwarding) ID.

            '
          type: string
    OspfDiscoveredNeighborFilter:
      properties:
        discovered_neighbors_count:
          description: 'The number of OSPF discovered neighbors.

            '
          type: integer
        id:
          description: 'The OSPF discovered neighbor ID.

            '
          type: string
        interfaces:
          description: 'The interfaces name.

            '
          items:
            $ref: '#/components/schemas/InterfaceConfig'
          type: array
        ospf_config_id:
          description: 'The OSPF config ID.

            '
          type: string
        vrf_context_id:
          description: "The attached VRF (Virtual Routing and Forwarding) ID. \n"
          type: string
    OspfDiscoveredNeighborQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of OSPF discovered neighbors that match
            the query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number OSPF discovered neighbors deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the OSPF discovered neighbors for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the OSPF discovered neighbors by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    OspfGlobalConfig:
      properties:
        cost:
          description: 'The Cost range, which is 1 - 65535.

            '
          type: integer
        dead_interval:
          description: 'The number of seconds that a neighbor router waits for a hello
            packet from the device before declaring the router down. range is 1-65535,
            Default is 40.

            '
          type: integer
        hello_interval:
          description: 'The interval in seconds at which the OSPF process sends hello
            packets to its directly connected neighbors (the range is 1 - 65535; the
            default is 10).

            '
          type: integer
        md5_key_id:
          description: 'MD5 Key ID between 1 and 255 and an MD5 Secret of up to 16
            characters. The MD5 password you specify will be applied to the messages
            exchanged with the peer.

            '
          type: integer
        md5_secret:
          description: 'The MD5 secret key of 16 characters. The MD5 password specified
            is applied to the messages exchanged with the peer.

            '
          type: string
        retransmit_interval:
          description: 'The length of time, in seconds, that OSPF waits to receive
            a link-state advertisement (LSA) from a neighbor before retransmitting
            it (the range is 1 - 65535; the default is 5).

            '
          type: integer
        transmit_delay:
          description: 'The length of time an LSA is delayed seconds before being
            sent out of an interface (range is 1 - 65535; default is 1).

            '
          type: integer
    OspfReachablePrefixes:
      properties:
        id:
          description: 'The OSPF reachable prefix ID.

            '
          type: string
        ospf_config_id:
          description: 'The OSPF config ID.

            '
          type: string
        reachable_ipv4_prefixes:
          description: 'The reachable IPv4 prefixes for OSPF.

            '
          items:
            $ref: '#/components/schemas/PrefixInfo'
          type: array
        reachable_ipv4_prefixes_count:
          description: "The reachable IPv4 prefixes count for OSPF. \n"
          format: int32
          type: integer
        redistribute:
          description: 'True or False.

            '
          type: boolean
        vrf_context_id:
          description: 'The attached VRF (Virtual Routing and Forwarding) ID.

            '
          type: string
    OspfReachablePrefixesFilter:
      properties:
        id:
          description: 'The OSPF reachable prefix ID.

            '
          type: string
        ospf_config_id:
          description: 'The OSPF config ID.

            '
          type: string
        reachable_ipv4_prefixes:
          description: 'The reachable IPv4 prefixes for OSPF.

            '
          items:
            $ref: '#/components/schemas/PrefixInfo'
          type: array
        reachable_ipv4_prefixes_count:
          description: "The reachable IPv4 prefixes count for OSPF. \n"
          format: int32
          type: integer
        redistribute:
          description: 'True or False.

            '
          type: boolean
        vrf_context_id:
          description: 'The attached VRF (Virtual Routing and Forwarding) ID.

            '
          type: string
    OspfReachablePrefixesQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of OSPF reachable prefixes that match
            the query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number OSPF reachable prefixes deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the OSPF reachable prefixes for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the OSPF reachable prefixes by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    PrefixInfo:
      properties:
        as_path:
          description: 'The AS path of the IPv4/IPv6 prefix.

            '
          items:
            description: As Path
            format: int64
            type: integer
          type: array
        network:
          description: 'The network of the IPv4/IPv6 prefix.

            '
          type: string
        nexthop:
          description: 'The next-hop of the IPv4/IPv6 prefix.

            '
          type: string
info:
  contact: {}
  description: List of Prisma SD-WAN APIs used to support the Open Shortest Path First
    (OSPF) routing protocols with the L3 switches towards the Branch sites and Aggregation
    Layer at the campus and Data Center sites.
  title: OSPF
  version: Latest
openapi: 3.0.0
paths:
  /v2.0/api/tenants/{tenant_id}/ospfconfigs/query:
    post:
      description: Query the Open Shortest Path First (OSPF) routing configurations.
      operationId: post-v2.0-api-tenants-tenant_id-ospfconfigs-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OspfConfigQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OspfConfigQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_INPUT
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query OSPF Config (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/ospfdiscoveredneighbors/query:
    post:
      description: Queries the OSPF discovered neighbors that match the query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ospfdiscoveredneighbors-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OspfDiscoveredNeighborQuery'
        description: Query Details for OSPF discovered neighbors
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OspfDiscoveredNeighborFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: OSPF_CONFIG_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query OSPF Discovered Neighbors (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/ospfreachableprefixes/query:
    post:
      description: Queries the OSPF reachable prefixes that match the query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ospfreachableprefixes-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OspfReachablePrefixesQuery'
        description: Query Details for OSPF reachable prefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OspfReachablePrefixesFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_QUERY_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query OSPF Reachable Prefixes (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ospfconfigs:
    get:
      description: Lists all the OSPF configurations.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfconfigs
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OspfConfig'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get all OSPF Configs (v2.0)
      tags:
      - OSPF
    post:
      description: Create an OSPF configuration.
      operationId: post-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfconfigs
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OspfConfigScreen'
        description: Details of the OSPF config to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OspfConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: OSPF_CONFIG_PREFIX_ADV_TYPE_TO_LAN_NOT_SUPPORTED_IN_HUB
                  - code: OSPF_CONFIG_INVALID_PREFIX_ADV_TO_LAN
                  - code: OSPF_CONFIG_PREFIX_ADV_ROUTE_MAP_ID_NOT_SUPPORTED
                  - code: OSPF_CONFIG_PREFIX_ADV_ROUTE_MAP_ID_NOT_FOUND
                  - code: OSPF_CONFIG_INVALID_REDISTRIBUTE_ROUTE_MAP_ID
                  - code: OSPF_CONFIG_REDISTRIBUTE_ROUTE_MAP_ID_NOT_FOUND
                  - code: OSPF_CONFIG_NETWORK_SCOPE_NOT_SUPPORTED_IN_HUB_SW_VERSION
                  - code: OSPF_CONFIG_INVALID_NETWORK_SCOPE
                  - code: OSPF_CONFIG_VRF_CONTEXT_ID_NOT_IN_VRF_PROFILE
                  - code: OSPF_CONFIG_VRF_CONTEXT_ALREADY_ASSIGNED
                  - code: OSPF_CONFIG_DUPLICATE_AREA_ID
                  - code: OSPF_CONFIG_AREA_ID_ZERO_MUST_HAVE_TYPE_NORMAL
                  - code: OSPF_CONFIG_NO_INTERFACE_ASSIGNED_TO_VRF
                  - code: OSPF_CONFIG_INVALID_INTERFACE
                  - code: OSPF_CONFIG_INVALID_INTERFACE_ONLY_SUPPORT_LAN
                  - code: OSPF_CONFIG_INVALID_INTERFACE_ONLY_SUPPORT_PRIVATE
                  - code: OSPF_CONFIG_NOT_MATCHING_AREA_ID
                  - code: OSPF_CONFIG_NEED_BOTH_MD5_KEY_AND_SECRET
                  - code: OSPF_CONFIG_INVALID_DEAD_INTERVAL_WITH_RESPECT_TO_HELLO_INTERVAL
                  - code: OSPF_CONFIG_NO_VRF_CONTEXT
                  - code: OSPF_CONFIG_INVALID_ROUTER_ID
                  - code: OSPF_CONFIG_INVALID_AREA_TYPE
                  - code: OSPF_CONFIG_MD5_KEY_ID_LENGTH_EXCEEDS
                  - code: OSPF_CONFIG_MD5_LENGTH_EXCEEDS
                  - code: OSPF_CONFIG_INVALID_RETRANSMIT_INTERVAL
                  - code: OSPF_CONFIG_INVALID_TRANSMIT_DELAY
                  - code: OSPF_CONFIG_INVALID_HELLO_INTERVAL
                  - code: OSPF_CONFIG_INVALID_DEAD_INTERVAL
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create OSPF Config (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ospfconfigs/{ospf_config_id}:
    delete:
      description: Delete an existing OSPF configuration.
      operationId: delete-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfconfigs-ospf_config_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The OSPF config ID.
        in: path
        name: ospf_config_id
        required: true
        schema:
          description: Ospf Config Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OspfConfig'
                      type: array
                  type: object
          description: Successful Operation
      summary: Delete an OSPF Config (v2.0)
      tags:
      - OSPF
    get:
      description: List an OSPF configuration.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfconfigs-ospf_config_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The OSPF config ID.
        in: path
        name: ospf_config_id
        required: true
        schema:
          description: Ospf Config Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OspfConfig'
          description: Successful Operation
      summary: Get an OSPF Config (v2.0)
      tags:
      - OSPF
    put:
      description: Update an existing OSPF configuration.
      operationId: put-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfconfigs-ospf_config_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The OSPF config ID.
        in: path
        name: ospf_config_id
        required: true
        schema:
          description: Ospf Config Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OspfConfigScreen'
        description: Details of the OSPF config to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OspfConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: OSPF_CONFIG_PREFIX_ADV_TYPE_TO_LAN_NOT_SUPPORTED_IN_HUB
                  - code: OSPF_CONFIG_INVALID_PREFIX_ADV_TO_LAN
                  - code: OSPF_CONFIG_PREFIX_ADV_ROUTE_MAP_ID_NOT_SUPPORTED
                  - code: OSPF_CONFIG_PREFIX_ADV_ROUTE_MAP_ID_NOT_FOUND
                  - code: OSPF_CONFIG_INVALID_REDISTRIBUTE_ROUTE_MAP_ID
                  - code: OSPF_CONFIG_REDISTRIBUTE_ROUTE_MAP_ID_NOT_FOUND
                  - code: OSPF_CONFIG_NETWORK_SCOPE_NOT_SUPPORTED_IN_HUB_SW_VERSION
                  - code: OSPF_CONFIG_INVALID_NETWORK_SCOPE
                  - code: OSPF_CONFIG_VRF_CONTEXT_ID_NOT_IN_VRF_PROFILE
                  - code: OSPF_CONFIG_VRF_CONTEXT_ALREADY_ASSIGNED
                  - code: OSPF_CONFIG_DUPLICATE_AREA_ID
                  - code: OSPF_CONFIG_AREA_ID_ZERO_MUST_HAVE_TYPE_NORMAL
                  - code: OSPF_CONFIG_NO_INTERFACE_ASSIGNED_TO_VRF
                  - code: OSPF_CONFIG_INVALID_INTERFACE
                  - code: OSPF_CONFIG_INVALID_INTERFACE_ONLY_SUPPORT_LAN
                  - code: OSPF_CONFIG_INVALID_INTERFACE_ONLY_SUPPORT_PRIVATE
                  - code: OSPF_CONFIG_NOT_MATCHING_AREA_ID
                  - code: OSPF_CONFIG_NEED_BOTH_MD5_KEY_AND_SECRET
                  - code: OSPF_CONFIG_INVALID_DEAD_INTERVAL_WITH_RESPECT_TO_HELLO_INTERVAL
                  - code: OSPF_CONFIG_NO_VRF_CONTEXT
                  - code: OSPF_CONFIG_INVALID_ROUTER_ID
                  - code: OSPF_CONFIG_INVALID_AREA_TYPE
                  - code: OSPF_CONFIG_MD5_KEY_ID_LENGTH_EXCEEDS
                  - code: OSPF_CONFIG_MD5_LENGTH_EXCEEDS
                  - code: OSPF_CONFIG_INVALID_RETRANSMIT_INTERVAL
                  - code: OSPF_CONFIG_INVALID_TRANSMIT_DELAY
                  - code: OSPF_CONFIG_INVALID_HELLO_INTERVAL
                  - code: OSPF_CONFIG_INVALID_DEAD_INTERVAL
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update an OSPF Config (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ospfconfigs/{ospf_config_id}/ospfdiscoveredneighbors:
    get:
      description: Lists all the OSPF discovered neighbors.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfconfigs-ospf_config_id-ospfdiscoveredneighbors
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The OSPF config ID.
        in: path
        name: ospf_config_id
        required: true
        schema:
          description: Ospf Config Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OspfDiscoveredNeighbor'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get all OSPF Discovered Neighbors (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ospfconfigs/{ospf_config_id}/ospfreachableprefixes:
    get:
      description: Lists all the OSPF reachable prefixes.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfconfigs-ospf_config_id-ospfreachableprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The OSPF config ID.
        in: path
        name: ospf_config_id
        required: true
        schema:
          description: Ospf Config Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OspfReachablePrefixes'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get all OSPF Reachable Prefixes (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ospfglobalconfigs:
    get:
      description: Lists all the OSPF configs from NB.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfglobalconfigs
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/OSPFGlobalConfig'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get all OSPF Configs from NB (v2.0)
      tags:
      - OSPF
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ospfglobalconfigs/{ospf_config_id}:
    get:
      description: List a specific OSPF config from NB.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfglobalconfigs-ospf_config_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The OSPF config ID.
        in: path
        name: ospf_config_id
        required: true
        schema:
          description: Ospf Config Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OSPFGlobalConfig'
          description: Successful Operation
      summary: Get OSPF Config from NB (v2.0)
      tags:
      - OSPF
    put:
      description: Updates an OSPF configuration.
      operationId: put-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ospfglobalconfigs-ospf_config_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The OSPF config ID.
        in: path
        name: ospf_config_id
        required: true
        schema:
          description: Ospf Config Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OSPFGlobalConfigScreen'
        description: Details of the ospf config to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OSPFGlobalConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: OSPF_CONFIG_INVALID_HELLO_INTERVAL
                  - code: OSPF_CONFIG_INVALID_DEAD_INTERVAL
                  - code: OSPF_CONFIG_PREFIX_ADV_NOT_SUPPORTED_FOR_HUB
                  - code: OSPF_CONFIG_INVALID_ROUTER_ID
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update OSPF Config (v2.0))
      tags:
      - OSPF
servers:
- url: https://api.us.cloudgenix.com
tags:
- name: OSPF
