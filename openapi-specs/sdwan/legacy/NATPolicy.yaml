components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items: {$ref: '#/components/schemas/AggregateData'}
          type: array
        field: {description: Field, type: string}
        name: {description: 'The name of the query response.

            ', type: string}
        operator:
          description: Operator
          enum: [max, count, min, avg, sum]
          type: string
        statistic: {description: 'QoS application aggregate statistics.

            ', type: string}
        unit: {description: 'The unit.

            ', type: string}
    AggregateData:
      properties:
        value: {description: 'Value of the aggregate data.

            ', type: number}
        view: {description: 'View the aggregate date.

            ', type: object}
    BasePolicyPrefixS:
      properties:
        description: {description: "The description for the source/destination prefixes.\
            \ \n"}
        id: {description: 'The ID of the source/destination prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IP prefixes.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: Ipv6 Prefixes
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        name: {description: "The name of the source/destination prefix.          \
            \ \n", type: string}
        tags:
          description: 'A information field that can be added to identify the source/destination
            prefix.

            '
          items: {description: Tags, type: string}
          type: array
    BasePolicySetStack:
      properties:
        description: {description: 'The description of the policy set stack. Max size
            = 256.

            '}
        id: {description: "The ID of the policy set stack. \n", type: string}
        name: {description: "The name of the policy set stack. Max size = 128.   \
            \ \n", type: string}
        policyset_ids:
          description: "The ID of the policy set.                   \n"
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: "A information field that can be added to identify the policy\
            \ set stack. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    BaseQueryDTO:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: Aggregate}
        dest_page: {description: Dest Page, format: int32, type: integer}
        getDeleted: {description: GetDeleted, type: boolean}
        group_by:
          description: Group By
          items: {description: Group By, type: string}
          type: array
        id: {description: Id, type: string}
        last_query_ts: {description: Last Query Ts, format: int64, type: integer}
        limit: {description: Limit, format: int32, type: integer}
        next_query: {type: object}
        query_params: {$ref: '#/components/schemas/QueryParams', description: Query
            Params, type: object}
        retrieved_fields:
          description: Retrieved Fields
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: Retrieved Fields Mask, type: boolean}
        sort_params:
          properties:
            <field_name>:
              enum: [asc, desc]
              type: string
          type: object
        total_count: {description: Total Count, format: int64, type: integer}
    BulkResponse:
      properties:
        count: {description: 'The total number.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
        type: {description: "The type. \n", type: string}
    Error:
      properties:
        _error:
          items: {$ref: '#/components/schemas/ErrorResponse'}
          type: array
    ErrorResponse:
      properties:
        code: {description: 'The error code.

            ', type: string}
        message: {description: 'The error message.

            ', type: string}
    GlobalPrefixQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of global prefixes that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number global prefixes deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the global prefixs for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the global prefixes by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    GlobalPrefixQueryFilter:
      properties:
        description: {description: 'The description of the security policy global
            prefix. Max size = 256.

            '}
        id: {description: 'The ID of the security policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the security policy global prefix. Max size
            = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    ListQueryResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        deleted_count: {description: 'The deleted number.

            ', format: int64, type: integer}
        deleted_ids:
          description: 'The deleted IDs.

            '
          items: {type: string}
          type: array
        description: {description: 'Description of the query. Max size = 256.

            '}
        id: {description: 'The ID.

            ', type: string}
        next_query: {description: 'Details of the next query.

            ', type: object}
        tenant_id: {description: 'The tenant ID.

            ', type: string}
        total_count: {description: 'Total number.

            ', format: int64, type: integer}
      type: object
    ListResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
        tenant_id: {description: 'The tenant ID.

            ', type: string}
      type: object
    NATAction:
      properties:
        nat_pool_id: {description: 'The ID of the the NAT pool used in policy rules
            and bound to interfaces.

            ', type: string}
        port: {description: 'The port range of the action in the NAT policy rule.

            ', type: integer}
        protocols:
          description: 'The protocol of the action for the NAT policy rule. Supports
            max 4 protocols.

            '
          items: {description: Protocols, type: string}
          type: array
        type:
          description: 'The type of action for NAT policy.

            '
          enum: [NO_NAT, SOURCE_NAT_DYNAMIC, SOURCE_NAT_STATIC, DESTINATION_NAT_DYNAMIC,
            DESTINATION_NAT_STATIC, ALG_DISABLE]
          type: string
      required: [type]
    NATGlobalPrefix:
      properties:
        description: {description: 'The description for the global NAT prefix. Max
            size = 256.

            '}
        id: {description: 'The ID of the global NAT prefix.

            ', type: string}
        ipv4_prefixes:
          description: "Lists the IP addresses. \n"
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: "The name of the global NAT prefix. Max size = 128.  \
            \           \n", type: string}
        tags:
          description: "A information field that can be added to identify the global\
            \ NAT prefix. Maximum 10 unique tags of length 1024 each are allowed.\
            \      \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATLocalPrefix:
      properties:
        description: {description: 'The description for the local NAT prefix. Max
            size = 256.

            '}
        id: {description: 'The ID of the local NAT prefix.

            ', type: string}
        name: {description: "The name of the local NAT prefix. Max size = 128.   \
            \          \n", type: string}
        tags:
          description: 'A information field that can be added to identify the local
            NAT prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicyPool:
      properties:
        description: {description: 'The description for the NAT policy pool. Max size
            = 256.

            '}
        id: {description: 'The ID of the NAT policy pool.

            ', type: string}
        name: {description: "The name of the NAT policy pool. Max size = 128.    \
            \         \n", type: string}
        tags:
          description: 'A information field that can be added to identify the NAT
            policy pool. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicyPoolQueryFilter:
      properties:
        description: {description: 'The description for the NAT policy pool. Max size
            = 256.

            '}
        id: {description: 'The ID of the NAT policy pool.

            ', type: string}
        name: {description: "The name of the NAT policy pool. Max size = 128.    \
            \         \n", type: string}
        tags:
          description: 'A information field that can be added to identify the NAT
            policy pool. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicyRule:
      properties:
        actions:
          description: 'The actions for the configured NAT policy rule/set. A maximum
            4 actions can be specified in a NAT policy rule/set.

            '
          items: {$ref: '#/components/schemas/NATAction'}
          type: array
        description: {description: 'The description of the NAT policy rule/set. Max
            size = 256.

            '}
        destination_ports:
          description: "The start and end port for Port Ranges. A maximum of 16 port\
            \ ranges is allowed, and port ranges can only be added for TCP or UDP\
            \ protocols.            \n"
          items: {$ref: '#/components/schemas/PortRange'}
          type: array
        destination_prefixes: {$ref: '#/components/schemas/BasePolicyPrefixS', description: 'Lists
            the details of the destination prefixes.

            '}
        destination_prefixes_id: {description: 'The ID of the destination prefix.

            ', type: string}
        destination_zone: {$ref: '#/components/schemas/NATPolicyZone', description: "List\
            \ the details of the destination zone. \n"}
        destination_zone_id: {description: 'The ID of the destination zone. Destination
            zone is based on the direction of the traffic with respect to the ION
            device.

            ', type: string}
        disabled: {description: 'Lists if the NAT policy rule/set is disabled.

            ', type: boolean}
        disabled_reason: {description: 'Reason the NAT policy rule/set is disabled.

            ', type: string}
        enabled: {description: 'Lists if the NAT policy rule/set is enabled.

            ', type: boolean}
        id: {description: "The ID of the policy rule/set.        \n", type: string}
        inactive: {description: 'Lists if the NAT policy rule/set is inactive.

            ', type: boolean}
        inactive_reason: {description: 'Reason the NAT policy rule/set is inactive.

            ', type: string}
        name: {description: 'The name of the NAT policy rule/set.

            ', type: string}
        natpolicypools:
          description: 'Lists the details of the NAT policy pools.

            '
          items: {$ref: '#/components/schemas/NATPolicyPool'}
          type: array
        policyset_id: {description: "The ID of the NAT policy set. NAT policy sets\
            \ contain policy rules and are a part of NAT policy set stacks.      \
            \      \n", type: string}
        protocol: {description: 'The protocol for the NAT policy set. TCP or UDP.

            ', type: integer}
        region: {description: Region, type: string}
        source_ports:
          description: 'List the details of the source port range. A maximum of 16
            port ranges are allowed, and port ranges can only be added for TCP or
            UDP protocols.

            '
          items: {$ref: '#/components/schemas/PortRange'}
          type: array
        source_prefixes: {$ref: '#/components/schemas/BasePolicyPrefixS', description: 'List
            the details of the source prefixes.

            '}
        source_prefixes_id: {description: 'The ID of the source prefixes.

            ', type: string}
        source_zone: {$ref: '#/components/schemas/NATPolicyZone', description: "Lists\
            \ the details of the source zone used in policy rules and bound to interfaces.\
            \  \n"}
        source_zone_id: {description: "The ID of the NAT policy source zone. Source\
            \ zones identify traffic that is sourced from a zone.        \n", type: string}
        tags:
          description: 'A information field that can be added to identify the NAT
            policy rule/set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        tenant_id: {description: 'ID of the tenant.

            ', type: string}
      required: [actions, name, tenant_id]
    NATPolicyRuleQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of NAT policy rules that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of NAT policy rules deleted for a time
            frame.

            ', type: boolean}
        group_by:
          description: 'Group the NAT policy rules for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the NAT policy rules by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    NATPolicyRuleQueryFilter:
      properties:
        actions:
          description: 'The actions for the configured NAT policy rule. A maximum
            4 actions can be specified in a NAT policy rule.

            '
          items: {$ref: '#/components/schemas/NATAction'}
          type: array
        description: {description: 'The description of the NAT policy rule. Max size
            = 256.

            '}
        destination_ports:
          description: "The start and end port for Port Ranges. A maximum of 16 port\
            \ ranges is allowed, and port ranges can only be added for TCP or UDP\
            \ protocols.            \n"
          items: {$ref: '#/components/schemas/PortRange'}
          type: array
        destination_prefixes_id: {description: 'The ID of the destination prefix.

            ', type: string}
        destination_zone_id: {description: 'The ID of the destination zone. Destination
            zone is based on the direction of the traffic with respect to the ION
            device.

            ', type: string}
        enabled: {description: 'Lists if the NAT policy rule is enabled.

            ', type: boolean}
        id: {description: "The ID of the policy rule.        \n", type: string}
        name: {description: 'The name of the NAT policy rule.

            ', type: string}
        policyset_id: {description: "The ID of the NAT policy set. NAT policy sets\
            \ contain policy rules and are a part of NAT policy set stacks.      \
            \      \n", type: string}
        protocol: {description: 'The protocol for the NAT policy rule. TCP or UDP.

            ', type: integer}
        source_ports:
          description: 'The start value of the port range. A maximum of 16 port ranges
            are allowed, and port ranges can only be added for TCP or UDP protocols.

            '
          items: {$ref: '#/components/schemas/PortRange'}
          type: array
        source_prefixes_id: {description: 'The ID of the source prefixes.

            ', type: string}
        source_zone_id: {description: "The ID of the NAT policy source zone. Source\
            \ zones identify traffic that is sourced from a zone.        \n", type: string}
        tags:
          description: 'A information field that can be added to identify the NAT
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [actions, name]
    NATPolicySet:
      properties:
        clone_from: {description: 'The policy set that has been cloned from.

            ', type: string}
        description: {description: 'The description of the NAT policy set. Max size
            = 256.

            '}
        destination_zone_policyrule_order:
          description: "The destination zone where the policy rule order is clasified.\
            \            \n"
          items: {description: Destination Zone Policyrule Order, type: string}
          type: array
        disabled: {description: "Lists if the NAT policy sets are disabled. \n", type: boolean}
        disabled_reason: {description: 'Reason the NAT policy set is disabled.

            ', type: string}
        id: {description: "The ID of the policy set.        \n", type: string}
        inactive: {description: 'Lists if the NAT policy set is inactive.

            ', type: boolean}
        inactive_reason: {description: "Reason the NAT policy set is inactive.   \
            \        \n", type: string}
        name: {description: 'The name of the NAT policy set. Max size = 128.

            ', type: string}
        policy_req_version: {description: 'The policy version.

            ', type: string}
        policy_rules:
          description: "Lists the details of the policy rules. \n"
          items: {$ref: '#/components/schemas/NATPolicyRule'}
          type: array
        region: {description: 'The region of the site.

            ', type: string}
        send_to_element: {description: Send To Element, type: boolean}
        source_zone_policyrule_order:
          description: "The order of the NAT policy source zone rules.          \n"
          items: {description: Source Zone Policyrule Order, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the NAT
            policy set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        tenant_id: {description: 'ID of the tenant.

            ', type: string}
        update_order: {description: Update Order, type: boolean}
      required: [name, tenant_id]
    NATPolicySetQueryFilter:
      properties:
        clone_from: {description: 'The policy set that has been cloned from.

            ', type: string}
        description: {description: 'The description of the NAT policy set. Max size
            = 256.

            '}
        destination_zone_policyrule_order:
          description: 'The destination zone where the policy rule order is clasified.

            '
          items: {description: Destination Zone Policyrule Order, type: string}
          type: array
        id: {description: "The ID of the policy set.        \n", type: string}
        name: {description: "The name of the NAT policy set. Max size = 128.    \n",
          type: string}
        source_zone_policyrule_order:
          description: "The order of the NAT policy source zone rules.       \n"
          items: {description: Source Zone Policyrule Order, type: string}
          type: array
        tags:
          description: "A information field that can be added to identify the NAT\
            \ policy set. Maximum 10 unique tags of length 1024 each are allowed.\
            \ \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicySetStack:
      properties:
        default_policysetstack: {description: "The default policy set stack.     \
            \   \n", type: boolean}
        description: {description: "The ID of the policy set stack.              \
            \     \n"}
        id: {description: Id, type: string}
        name: {description: "The name of the policy set stack. Max size = 128.   \
            \ \n", type: string}
        policyset_ids:
          description: 'The ID of the policy set.

            '
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the policy
            set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicySetStackPOSTScreen:
      properties:
        default_policysetstack: {description: "The default policy set stack.     \
            \   \n", type: boolean}
        description: {description: 'The description of the policy set stack. Max size
            = 256.

            '}
        id: {description: 'The ID of the policy set stack.

            ', type: string}
        name: {description: "The name of the policy set stack. Max size = 128.   \
            \ \n", type: string}
        policyset_ids:
          description: "The ID of the policy set.                   \n"
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: "A information field that can be added to identify the policy\
            \ set stack. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicySetStackQueryFilter:
      properties:
        default_policysetstack: {description: "The default policy set stack.     \
            \   \n", type: boolean}
        description: {description: 'The description of the policy set stack. Max size
            = 256.

            '}
        id: {description: 'The ID of the policy set stack.

            ', type: string}
        name: {description: "The name of the policy set stack. Max size = 128.   \
            \ \n", type: string}
        policyset_ids:
          description: "The ID of the policy set.                   \n"
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: "A information field that can be added to identify the policy\
            \ set stack. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicySetStackScreen:
      properties:
        default_policysetstack: {description: "The default policy set stack.     \
            \   \n", type: boolean}
        description: {description: 'The description of the policy set stack. Max size
            = 256.

            '}
        id: {description: "The ID of the policy set stack. \n", type: string}
        name: {description: "The name of the policy set stack. Max size = 128.   \
            \ \n", type: string}
        policyset_ids:
          description: "The ID of the policy set.                   \n"
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: "A information field that can be added to identify the policy\
            \ set stack. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    NATPolicyZone:
      properties:
        default_for_public_interfaces: {description: "The default source/destination\
            \ zone for public interfaces.        \n", type: boolean}
        description: {description: 'The description for the source/destination zone.
            Max size = 128.

            '}
        disabled: {description: 'Lists if the source/destination zone set is disabled.

            ', type: boolean}
        disabled_reason: {description: 'Reason the source/destination zone is disabled.

            ', type: string}
        id: {description: "The ID of the source/destination zone. \n", type: string}
        inactive: {description: 'Lists if the source/destination zone is inactive.

            ', type: boolean}
        inactive_reason: {description: "Reason the source/destination zone is inactive.\
            \           \n", type: string}
        name: {description: 'The name of the source/destination zone.

            ', type: string}
        region: {description: 'The region of the site.

            ', type: string}
        tags:
          description: "A information field that can be added to identify the source/destination\
            \ zone. \n"
          items: {description: Tags, type: string}
          type: array
        tenant_id: {description: 'ID of the tenant.

            ', type: string}
      required: [name, tenant_id]
    NATPolicyZoneQueryFilter:
      properties:
        default_for_public_interfaces: {description: "The default NAT policy zone\
            \ for public interfaces.        \n", type: boolean}
        description: {description: "The description of the NAT policy zone. Max size\
            \ = 256.               \n"}
        id: {description: 'The ID of the NAT policy zone.

            ', type: string}
        name: {description: "The name of the NAT policy zone. Max size = 128.    \n",
          type: string}
        tags:
          description: "A information field that can be added to identify the NAT\
            \ policy zone. Maximum 10 unique tags of length 1024 each are allowed.\
            \ \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    PolicySetStackQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of policy set stacks that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of policy set stacks deleted for a time
            frame.

            ', type: boolean}
        group_by:
          description: 'Group the policy set stacks for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "List the policy set stacks by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    PortRange:
      properties:
        from: {description: 'The start port of the destination/source port range.

            ', type: integer}
        to: {description: 'The end port of the destination/source port range.

            ', type: integer}
    QueryParams:
      properties:
        <field_name>|comparator:
          additionalProperties:
            properties:
              eq: {type: string}
              gte: {type: string}
              in:
                items: {type: string}
                type: array
              lte: {type: string}
              neq: {type: string}
            type: object
          type: object
    SiteNATPrefixAssociation:
      properties:
        id: {description: 'The ID of the NAT site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'List the ipv4 prefixes for the site.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The ID of the prefix.               \n", type: string}
        tags:
          description: "A information field that can be added to identify the NAT\
            \ site prefix. Maximum 10 unique tags of length 1024 each are allowed.\
            \ \n"
          items: {description: Tags, type: string}
          type: array
    SitePrefixAssociationQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of prefix associations that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of prefix associations deleted for a
            time frame.

            ', type: boolean}
        group_by:
          description: 'Group the prefix associations for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the prefix associations by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SitePrefixAssociationQueryFilter:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        site_id: {description: 'The site ID binding the prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
info:
  contact: {}
  description: List of APIs used to manage the NAT policy rules.
  title: NAT Policy
  version: Latest
openapi: 3.0.0
paths:
  /v2.0/api/tenants/{tenant_id}/natglobalprefixes:
    get:
      description: List all the global NAT prefixes.
      operationId: get-v2.0-api-tenants-tenant_id-natglobalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATGlobalPrefix'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Global NAT Prefixes (v2.0)
      tags: [NAT Policy]
    post:
      description: Create a new NAT global prefix.
      operationId: post-v2.0-api-tenants-tenant_id-natglobalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATGlobalPrefix'}
        description: Details of the NATprefix  to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAT_PREFIX_INVALID_NAME}
                  - {code: NAT_PREFIX_NAME_REQUIRED}
                  - {code: NAT_PREFIX_INVALID_DESCRIPTION}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create NAT Global Prefix (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natglobalprefixes/query:
    post:
      description: Queries all the global prefixes.
      operationId: post-v2.0-api-tenants-tenant_id-natglobalprefixes-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GlobalPrefixQuery'}
        description: Details of the query for Global Prefix
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/GlobalPrefixQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Global Prefixes (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natglobalprefixes/{prefix_id}:
    delete:
      description: Delete a NAT global prefix.
      operationId: delete-v2.0-api-tenants-tenant_id-natglobalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the global NAT prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: RESOURCE_IN_USE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete NAT Global Prefix (v2.0)
      tags: [NAT Policy]
    get:
      description: List a specific NAT global prefix.
      operationId: get-v2.0-api-tenants-tenant_id-natglobalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the global NAT prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATGlobalPrefix'}
          description: Successful Operation
      summary: Get NAT Global Prefix (v2.0)
      tags: [NAT Policy]
    put:
      description: Update a specific NAT global prefix.
      operationId: put-v2.0-api-tenants-tenant_id-natglobalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the global NAT prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATGlobalPrefix'}
        description: Details to update the NAT Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAT_PREFIX_INVALID_NAME}
                  - {code: NAT_PREFIX_NAME_REQUIRED}
                  - {code: NAT_PREFIX_INVALID_DESCRIPTION}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update NAT Global Prefix (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natlocalprefixes:
    get:
      description: List all the local NAT prefixes.
      operationId: get-v2.0-api-tenants-tenant_id-natlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATLocalPrefix'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Local NAT Prefixes (v2.0)
      tags: [NAT Policy]
    post:
      description: Create a new NAT local prefix.
      operationId: post-v2.0-api-tenants-tenant_id-natlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATLocalPrefix'}
        description: Details of the NAT  Local prefix  to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAT_PREFIX_INVALID_NAME}
                  - {code: NAT_PREFIX_NAME_REQUIRED}
                  - {code: NAT_PREFIX_INVALID_DESCRIPTION}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Local NAT Prefix (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natlocalprefixes/query:
    post:
      description: Queries all the local prefixes.
      operationId: post-v2.0-api-tenants-tenant_id-natlocalprefixes-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePrefixAssociationQueryFilter'}
        description: Query details for site local prefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePrefixAssociationQuery'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Local Prefixes (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natlocalprefixes/{prefix_id}:
    delete:
      description: Delete a NAT local prefix.
      operationId: delete-v2.0-api-tenants-tenant_id-natlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the local NAT prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_ASSOCIATED_WITH_SITE}
                  - {code: PREFIX_IS_USED_IN_POLICY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete NAT Local Prefix (v2.0)
      tags: [NAT Policy]
    get:
      description: List a specific NAT local prefix.
      operationId: get-v2.0-api-tenants-tenant_id-natlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the local NAT prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATLocalPrefix'}
          description: Successful Operation
      summary: Get NAT Local Prefix (v2.0)
      tags: [NAT Policy]
    put:
      description: Update a specific NAT local prefix.
      operationId: put-v2.0-api-tenants-tenant_id-natlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the local NAT prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATLocalPrefix'}
        description: Details to update the NAT local prefix.
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAT_PREFIX_INVALID_NAME}
                  - {code: NAT_PREFIX_NAME_REQUIRED}
                  - {code: NAT_PREFIX_INVALID_DESCRIPTION}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update NAT Local Prefix (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicypools:
    get:
      description: List all the NAT policy pools.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicypools
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicyPool'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get NAT Policy Pools (v2.0)
      tags: [NAT Policy]
    post:
      description: Create a new NAT policy pool.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicypools
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyPool'}
        description: Details of the NAT Policy Pool  to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyPool'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAT_POLICYPOOL_INVALID_NAME}
                  - {code: NAT_POLICYPOOL_NAME_REQUIRED}
                  - {code: NAT_POLICYPOOL_INVALID_DESCRIPTION}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create NAT Policy Pool (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicypools/query:
    post:
      description: Queries all the NAT policy pools.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicypools-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyPoolQueryFilter'}
        description: Details of the query for NAT policy pools.
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicyPoolQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query NAT Policy Pools (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicypools/{natpolicy_pool_id}:
    delete:
      description: Delete a NAT policy pool.
      operationId: delete-v2.0-api-tenants-tenant_id-natpolicypools-natpolicy_pool_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy pool.
        in: path
        name: natpolicy_pool_id
        required: true
        schema: {description: Natpolicy Pool Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyPool'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAT_POOL_USED_IN_POLICY_RULE}
                  - {code: NAT_POOL_IS_ASSOCIATED_WITH_INTERFACE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete NAT Policy Pool (v2.0)
      tags: [NAT Policy]
    get:
      description: List a specific NAT policy pool.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicypools-natpolicy_pool_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy pool.
        in: path
        name: natpolicy_pool_id
        required: true
        schema: {description: Natpolicy Pool Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyPool'}
          description: Successful Operation
      summary: Get NAT Policy Pool (v2.0)
      tags: [NAT Policy]
    put:
      description: Update a specific NAT policy pool.
      operationId: put-v2.0-api-tenants-tenant_id-natpolicypools-natpolicy_pool_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy pool.
        in: path
        name: natpolicy_pool_id
        required: true
        schema: {description: Natpolicy Pool Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyPool'}
        description: Details to update the NAT Policy Pool.
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyPool'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAT_POLICYPOOL_INVALID_NAME}
                  - {code: NAT_POLICYPOOL_NAME_REQUIRED}
                  - {code: NAT_POLICYPOOL_INVALID_DESCRIPTION}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update NAT Policy Pool (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicyrules/query:
    post:
      description: Queries all the NAT policy rules.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicyrules-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyRuleQuery'}
        description: Details of the query for NAT Policy Rule
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicyRuleQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query NAT Policy Rules (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysets:
    get:
      description: List all the NAT policy sets of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicysets
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicySet'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get NAT Policy Sets of Tenant (v2.0)
      tags: [NAT Policy]
    post:
      description: Create a new NAT policy set.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicysets
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicySet'}
        description: Details of NAT Policy Set to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAT_POLICY_SET_NAME_REQD}
                  - {code: NAT_POLICY_SET_NAME_EXCEEDS_LIMIT}
                  - {code: NAT_POLICY_SET_DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: NAT_POLICY_SET_CONFIG_INVALID_CLONE_FROM}
                  - {code: NAT_SET_DUPLICATE_SOURCE_ZONE_RULES}
                  - {code: NAT_SET_DUPLICATE_DESTINATION_ZONE_RULES}
                  - {code: POLICY_SET_CONFIG_CREATION_FAILED}
                  - {code: NAT_SET_CONFIG_INVALID_CLONE_FROM}
                  - {code: NAT_SET_CONFIG_INVALID_RULE_ORDER}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create NAT Policy Set (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysets/query:
    post:
      description: Queries all the NAT policy sets.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicysets-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicySetQueryFilter'}
        description: Query details for Policy Set
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicySetQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Policy Sets (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysets/{nat_policy_set_id}:
    delete:
      description: Delete a NAT policy set.
      operationId: delete-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAT_SET_DUPLICATE_SOURCE_ZONE_RULES}
                  - {code: NAT_SET_DUPLICATE_DESTINATION_ZONE_RULES}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete NAT Policy Set (v2.0)
      tags: [NAT Policy]
    get:
      description: List a specific NAT policy set of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySet'}
          description: Successful Operation
      summary: Get NAT Policy Set of Tenant (v2.0)
      tags: [NAT Policy]
    put:
      description: Update a NAT policy set.
      operationId: put-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicySet'}
        description: Policy Set Details
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: NAT_POLICY_SET_NAME_REQD}
                  - {code: NAT_POLICY_SET_NAME_EXCEEDS_LIMIT}
                  - {code: NAT_POLICY_SET_DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: NAT_POLICY_SET_CONFIG_INVALID_CLONE_FROM}
                  - {code: NAT_SET_DUPLICATE_SOURCE_ZONE_RULES}
                  - {code: NAT_SET_DUPLICATE_DESTINATION_ZONE_RULES}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update NAT Policy Set (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysets/{nat_policy_set_id}/natpolicyrules:
    get:
      description: List all the policy rules of a policy set.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id-natpolicyrules
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicyRule'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Policy Rules of Policy Set (v2.0)
      tags: [NAT Policy]
    post:
      description: Create a new NAT policy rule.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id-natpolicyrules
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyRule'}
        description: Details of NAT Policy Rule to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyRule'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAT_POLICY_SET_NOT_PRESENT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAT_POLICY_RULE_INVALID}
                  - {code: NAT_POLICY_RULE_NAME_REQD}
                  - {code: NAT_POLICY_RULE_NAME_EXCEEDS_LIMIT}
                  - {code: NAT_POLICY_RULE_DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: NAT_RULE_INVALID_PROTOCOL}
                  - {code: NAT_RULE_SOURCE_PORTS_EXCEEDS_LIMIT}
                  - {code: NAT_RULE_INVALID_SOURCE_ZONE_ID}
                  - {code: NAT_RULE_INVALID_DESTINATION_ZONE_ID}
                  - {code: NAT_RULE_INVALID_DESTINATION_PREFIXES_ID}
                  - {code: NAT_RULE_DESTINATION_PORTS_EXCEEDS_LIMIT}
                  - {code: NAT_RULE_ACTIONS_MISSING}
                  - {code: NAT_RULE_INVALID_SOURCE_PREFIXES_ID}
                  - {code: NAT_RULE_INVALID_ACTION_LIMIT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create NAT Policy Rule (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysets/{nat_policy_set_id}/natpolicyrules/{nat_policy_rule_id}:
    delete:
      description: Delete the NAT policy rule of a tenant.
      operationId: delete-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id-natpolicyrules-nat_policy_rule_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      - description: ID of the NAT policy rule.
        in: path
        name: nat_policy_rule_id
        required: true
        schema: {description: Nat Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyRule'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAT_RULE_SET_NOT_PRESENT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete NAT Policy Rule of Tenant (v2.0)
      tags: [NAT Policy]
    get:
      description: List a specific NAT policy rule of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id-natpolicyrules-nat_policy_rule_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      - description: ID of the NAT policy rule.
        in: path
        name: nat_policy_rule_id
        required: true
        schema: {description: Nat Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyRule'}
          description: Successful Operation
      summary: Get NAT Policy Rule of Tenant (v2.0)
      tags: [NAT Policy]
    put:
      description: Update the NAT ;policy rule of a tenant.
      operationId: put-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id-natpolicyrules-nat_policy_rule_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      - description: ID of the NAT policy rule.
        in: path
        name: nat_policy_rule_id
        required: true
        schema: {description: Nat Policy Rule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyRule'}
        description: Details of NAT Policy Rule to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyRule'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAT_POLICY_SET_NOT_PRESENT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAT_POLICY_RULE_INVALID}
                  - {code: NAT_POLICY_RULE_NAME_REQD}
                  - {code: NAT_POLICY_RULE_NAME_EXCEEDS_LIMIT}
                  - {code: NAT_POLICY_RULE_DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: NAT_RULE_INVALID_PROTOCOL}
                  - {code: NAT_RULE_SOURCE_PORTS_EXCEEDS_LIMIT}
                  - {code: NAT_RULE_INVALID_SOURCE_ZONE_ID}
                  - {code: NAT_RULE_INVALID_DESTINATION_ZONE_ID}
                  - {code: NAT_RULE_INVALID_DESTINATION_PREFIXES_ID}
                  - {code: NAT_RULE_DESTINATION_PORTS_EXCEEDS_LIMIT}
                  - {code: NAT_RULE_ACTIONS_MISSING}
                  - {code: NAT_RULE_INVALID_SOURCE_PREFIXES_ID}
                  - {code: NAT_RULE_INVALID_ACTION_LIMIT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update NAT Policy Rule of Tenant (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysets/{nat_policy_set_id}/status:
    get:
      description: List a specific NAT policy set status of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicysets-nat_policy_set_id-status
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set.
        in: path
        name: nat_policy_set_id
        required: true
        schema: {description: Nat Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySet'}
          description: Successful Operation
      summary: Get NAT Policy Set Status of Tenant (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysetstacks:
    get:
      description: List all the NAT policy set stacks of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicysetstacks
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicySetStack'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get NAT Policy Set Stacks of Tenant (v2.0)
      tags: [NAT Policy]
    post:
      description: Create a new NAT policy set stack.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicysetstacks
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicySetStackPOSTScreen'}
        description: Details of NAT Policy Set to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAT_POLICYSET_STACK_NAME_REQD}
                  - {code: NAT_POLICYSET_STACK_NAME_EXCEEDS_LENGTH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create NAT Policy Set Stack (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysetstacks/query:
    post:
      description: Queries all the NAT policy set stacks.
      operationId: post-v2.0-api-tenants-tenant_id-natpolicysetstacks-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PolicySetStackQuery'}
        description: Query Details for NAT Policy Set
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicySetStackQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Policy Set Stacks (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natpolicysetstacks/{natpolicy_set_stack_id}:
    delete:
      description: Delete a NAT policy set stack.
      operationId: delete-v2.0-api-tenants-tenant_id-natpolicysetstacks-natpolicy_set_stack_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set stack.
        in: path
        name: natpolicy_set_stack_id
        required: true
        schema: {description: Natpolicy Set Stack Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/BasePolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAT_POLICYSET_STACK_ID_NOT_FOUND}
                  - {code: RESOURCE_IN_USE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete NAT Policy Set Stack (v2.0)
      tags: [NAT Policy]
    get:
      description: List a specific NAT policy set stack of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-natpolicysetstacks-natpolicy_set_stack_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set stack.
        in: path
        name: natpolicy_set_stack_id
        required: true
        schema: {description: Natpolicy Set Stack Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySetStack'}
          description: Successful Operation
      summary: Get NAT Policy Set Stack (v2.0)
      tags: [NAT Policy]
    put:
      description: Update a NAT policy set stack.
      operationId: put-v2.0-api-tenants-tenant_id-natpolicysetstacks-natpolicy_set_stack_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy set stack.
        in: path
        name: natpolicy_set_stack_id
        required: true
        schema: {description: Natpolicy Set Stack Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicySetStackScreen'}
        description: Details of NAT Policy Set to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAT_POLICYSET_STACK_ID_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update NAT Policy Set Stack (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natzones:
    get:
      description: List all the NAT policy zones.
      operationId: get-v2.0-api-tenants-tenant_id-natzones
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicyZone'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Nat Policy Zones (v2.0)
      tags: [NAT Policy]
    post:
      description: Create a new NAT policy zone.
      operationId: post-v2.0-api-tenants-tenant_id-natzones
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyZone'}
        description: Details of the Nat Policy Zone to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Nat Policy Zone (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natzones/query:
    post:
      description: Queries all the NAT policy zones.
      operationId: post-v2.0-api-tenants-tenant_id-natzones-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/BaseQueryDTO'}
        description: Details to query the Nat Policy Zone.
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NATPolicyZoneQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query NAT Policy Zones (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/natzones/{nat_policy_zone_id}:
    delete:
      description: Delete a NAT policy zone.
      operationId: delete-v2.0-api-tenants-tenant_id-natzones-nat_policy_zone_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy zone.
        in: path
        name: nat_policy_zone_id
        required: true
        schema: {description: Nat Policy Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_ASSOCIATED_WITH_SITE}
                  - {code: PREFIX_IS_USED_IN_POLICY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete NAT Policy Zone (v2.0)
      tags: [NAT Policy]
    get:
      description: List a specific NAT policy zone.
      operationId: get-v2.0-api-tenants-tenant_id-natzones-nat_policy_zone_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy zone.
        in: path
        name: nat_policy_zone_id
        required: true
        schema: {description: Nat Policy Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyZone'}
          description: Successful Operation
      summary: Get NAT Policy Zone (v2.0)
      tags: [NAT Policy]
    put:
      description: Update a specific NAT policy zone.
      operationId: put-v2.0-api-tenants-tenant_id-natzones-nat_policy_zone_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the NAT policy zone.
        in: path
        name: nat_policy_zone_id
        required: true
        schema: {description: Nat Policy Zone Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NATPolicyZone'}
        description: Details to update the Nat Policy Zone.
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NATPolicyZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update NAT Policy Zone (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/natlocalprefixes:
    get:
      description: List all the site NAT prefixes.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-natlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteNATPrefixAssociation'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Site NAT Prefixes (v2.0)
      tags: [NAT Policy]
    post:
      description: Create an association between the site and the NAT prefix.
      operationId: post-v2.0-api-tenants-tenant_id-sites-site_id-natlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteNATPrefixAssociation'}
        description: Details of the site NAT prefix to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteNATPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: SITE_NOT_FOUND}
                  - {code: NAT_LOCALPREFIX_NOT_FOUND}
                  - {code: NAT_PREFIX_INVALID}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Association between Site and NAT Prefix (v2.0)
      tags: [NAT Policy]
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/natlocalprefixes/{prefix_id}:
    delete:
      description: Delete an existing site NAT prefix.
      operationId: delete-v2.0-api-tenants-tenant_id-sites-site_id-natlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the NAT local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteNATPrefixAssociation'}
          description: Successful Operation
      summary: Delete Site NAT Prefix (v2.0)
      tags: [NAT Policy]
    get:
      description: List a site NAT prefix.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-natlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the NAT local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteNATPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_PREFIX_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Site NAT Prefix (v2.0)
      tags: [NAT Policy]
    put:
      description: Update an existing site NAT local prefix association.
      operationId: put-v2.0-api-tenants-tenant_id-sites-site_id-natlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the NAT local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteNATPrefixAssociation'}
        description: Details of the site NAT prefix Association to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteNATPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: SITE_NOT_FOUND}
                  - {code: NAT_LOCALPREFIX_NOT_FOUND}
                  - {code: NAT_PREFIX_INVALID}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Site NAT Local Prefix Association (v2.0)
      tags: [NAT Policy]
servers:
- {url: 'https://api.us.cloudgenix.com'}
tags:
- {name: NAT Policy}
