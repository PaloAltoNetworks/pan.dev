components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items: {$ref: '#/components/schemas/AggregateData'}
          type: array
        field: {description: Field, type: string}
        name: {description: 'The name of the query response.

            ', type: string}
        operator:
          description: Operator
          enum: [max, count, min, avg, sum]
          type: string
        statistic: {description: 'QoS application aggregate statistics.

            ', type: string}
        unit: {description: 'The unit.

            ', type: string}
    AggregateData:
      properties:
        value: {description: 'Value of the aggregate data.

            ', type: number}
        view: {description: 'View the aggregate date.

            ', type: object}
    BaseCustomDHCPOptions:
      properties:
        option_definition: {description: 'Custom option definition. Max length supported
            is 1024.

            ', type: string}
        option_value: {description: 'Custom option value. Max length supported is
            8192.

            ', type: string}
      required: [option_value, option_definition]
    BulkResponse:
      properties:
        count: {description: 'The total number.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
        type: {description: "The type. \n", type: string}
    DHCPRelay:
      properties:
        enabled: {description: "Indicates if DHCP relay is enabled. \n", type: boolean}
        option_82: {$ref: '#/components/schemas/DHCPRelayOption82', description: 'Details
            of option_82 if enabled. The ION device DHCP Relay agent can include additional
            information by using DHCP Option 82, Circuit ID, and Remote ID, which
            the DHCP server uses to allocate the appropriate IP address.

            '}
        server_ips:
          description: 'The DHCP server IP addresses.

            '
          items: {description: Server Ips, type: string}
          type: array
        source_interface: {description: 'The DHCP server source interface.

            ', type: string}
    DHCPRelayOption82:
      properties:
        circuit_id: {description: "The DHCP relay circuit ID. \n", type: string}
        enabled: {description: "Option 82 if enabled. \n", type: boolean}
        reforwarding_policy:
          description: 'The DHCP server reforwarding policy.

            '
          enum: [REPLACE, KEEP, APPEND, DROP]
          type: string
        remote_id: {description: 'The DHCP server remote ID.

            ', type: string}
      required: [reforwarding_policy]
    DHCPServer:
      properties:
        broadcast_address: {description: 'The address of the site.

            ', type: string}
        custom_options:
          description: 'Custom options defined for the DHCP server.

            '
          items: {$ref: '#/components/schemas/BaseCustomDHCPOptions'}
          type: array
        default_lease_time: {description: 'The lease time each client will get if
            a user does not request a particular lease time.

            ', format: int64, type: integer}
        description: {description: 'Description of each instance of the DHCP server,
            per subnet.

            '}
        disabled: {description: 'Uncheck to enable the configuration.

            ', type: boolean}
        dns_servers:
          description: 'The domain name servers for clients to resolve DNS requests.
            Max number is 3.

            '
          items: {description: Dns Servers, type: string}
          type: array
        domain_name: {description: 'The domain name for clients. Max length is 256.

            ', type: string}
        gateway: {description: 'The default gateway for clients.

            ', type: string}
        id: {description: 'The DHCP server ID.

            ', type: string}
        ip_ranges:
          description: 'The valid IPv4 address range for defined subnet.

            '
          items: {$ref: '#/components/schemas/IPRange'}
          type: array
        max_lease_time: {description: 'The maximum lease time the clients can use.

            ', format: int64, type: integer}
        network_context_id: {description: "ID of the network context, if configured.\
            \             \n", type: string}
        static_mappings:
          description: 'IP addresses assigned to clients by statically mapping IPs
            to MAC addresses of the clients.

            '
          items: {$ref: '#/components/schemas/StaticMapping'}
          type: array
        subnet: {description: 'The subnet for which the DHCP server is being configured.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the DHCP
            server.

            '
          items: {description: Tags, type: string}
          type: array
      required: [ip_ranges, subnet]
    ESPGroup:
      properties:
        force_encapsulation: {description: 'The type of encapsulation is force UDP.
            The type of encapsulation has to match the encapsulation configured at
            the standard VPN endpoint.

            ', type: boolean}
        lifesize: {$ref: '#/components/schemas/Lifesize', description: Lifesize}
        lifetime: {description: 'The life time for the ESP Group. The default lifetime
            of an ESP Group is 24 hours.

            ', format: int32, type: integer}
        lifetime_units: {description: Lifetime Units, type: string}
        mode:
          description: 'The IPsec config ESP tunnel mode.

            '
          enum: [TUNNEL, TRANSPORT]
          type: string
        proposals:
          description: "Information on the IPsec proposals. Proposals is a list of\
            \ crypto parameters to be used to secure the ESP sessions between the\
            \ ION device and the endpoint.         \n"
          items: {$ref: '#/components/schemas/Proposals'}
          type: array
        responder_sase_proposals: {$ref: '#/components/schemas/ResponderSaseProposals',
          description: Responder Sase Proposals}
      required: [mode, proposals]
    Error:
      properties:
        _error:
          items: {$ref: '#/components/schemas/ErrorResponse'}
          type: array
    ErrorResponse:
      properties:
        code: {description: 'The error code.

            ', type: string}
        message: {description: 'The error message.

            ', type: string}
    IKEGroup:
      properties:
        aggressive: {description: 'If aggressive mode is selecetd, the source interface
            or endpoint is behind NAT or there are multiple tunnels to the same remote
            endpoint.

            ', type: boolean}
        authentication_multiple: {description: Authentication Multiple, type: integer}
        key_exchange:
          description: 'The key exchange for the IKE group = IKEv1 or IKEv2.

            '
          enum: [IKEV1, IKEV2]
          type: string
        lifetime: {description: 'The life time for the IKE Group. The default lifetime
            of an IKE Group is 72 hours.

            ', format: int32, type: integer}
        lifetime_units: {description: Lifetime Units, type: string}
        port: {description: 'The port number of the communication port. The default
            port is 500. The port number configured in the IKE group has to be the
            same as the port number configured in the standard VPN endpoint IKE group.

            ', format: int32, type: integer}
        proposals:
          description: 'Information on the IPsec proposals. Proposals is a list of
            crypto parameters to be used to secure the IKE sessions between the ION
            device and the endpoint.

            '
          items: {$ref: '#/components/schemas/Proposals'}
          type: array
        reauth: {description: 'The mode for IKEv2. If selected, then a new tunnel
            has to be re-negotiated when the lifetime is reached.

            ', type: boolean}
      required: [key_exchange, proposals]
    IKEV1Params:
      properties:
        xauth_id: {description: 'The xauth ID.

            ', type: string}
        xauth_secret: {description: 'The authentication secret. Length must be between
            4-128.

            ', type: string}
        xauth_secret_encrypted: {description: "The xauth secret. Value = True.   \
            \         \n", type: string}
        xauth_secret_hash: {description: 'The xauth hash. Value = True.

            ', type: string}
        xauth_type:
          description: 'The xauth type.

            '
          enum: [NONE, SECRET]
          type: string
      required: [xauth_type]
    IPRange:
      properties:
        end_ip: {description: "The original end IP address for the defined subnet.\
            \ \n", type: string}
        start_ip: {description: 'The original start IP address for the defined subnet.

            ', type: string}
      required: [end_ip, start_ip]
    IPSECAuthenticationV1:
      properties:
        certificate: {description: 'The certificate authentication for the IPsec Profile.

            ', type: string}
        certificate_profile_id: {description: Certificate Profile Id, type: string}
        comment: {description: Comment, type: string}
        ikev1_params: {$ref: '#/components/schemas/IKEV1Params', description: 'Ikev1
            Params: Valid '}
        local_ca_certificate: {description: "The local CA certificate uploaded for\
            \ certificate authentication.              \n", type: string}
        local_id:
          description: 'The local ID type.

            '
          enum: [LOCAL_IP, DN, HOSTNAME, CUSTOM, NONE]
          type: string
        local_id_custom: {description: 'The custom local ID.

            ', type: string}
        local_pa_certificate_id: {description: Local Pa Certificate Id, type: string}
        pa_master_key_id: {description: Pa Master Key Id, type: string}
        passphrase: {description: Passphrase, type: string}
        passphrase_encrypted: {description: 'Passphrase Encrypted: JsonIgnore(value
            = true) ', type: string}
        peer_id_check:
          description: Peer Id Check
          enum: [EXACT, WILDCARD]
          type: string
        permit_peer_id_mismatch: {description: Permit Peer Id Mismatch, type: boolean}
        private_key: {description: 'The private key file uploaded for certificate
            authentication.

            ', type: string}
        private_key_encrypted: {description: 'Private Key Encrypted: JsonIgnore(value
            = true) ', type: string}
        remote_ca_certificate: {description: "The standard VPN endpoint CA certificate\
            \ uploaded.            \n", type: string}
        remote_id: {description: "The ID for the standard VPN endpoint in the Remote\
            \ ID field. \n", type: string}
        secret: {description: "The password for PSK authentication.           \n",
          type: string}
        secret_encrypted: {description: 'Secret Encrypted: JsonIgnore(value = true) ',
          type: string}
        secret_hash: {description: 'Secret Hash: JsonIgnore(value = true) ', type: string}
        strict_validation_peer_extended_key_use: {description: Strict Validation Peer
            Extended Key Use, type: boolean}
        type:
          description: 'The authentication type = PSK or Certificates.

            '
          enum: [NONE, PSK, X509]
          type: string
        x509Objects: {$ref: '#/components/schemas/X509Objects', description: 'X509Objects:
            JsonIgnore(value = true) '}
      required: [local_id, type]
    IPSECProfile:
      properties:
        authentication: {$ref: '#/components/schemas/IPSECAuthenticationV1', description: 'The
            details of the authentication mode for the IPsec Profile.

            '}
        description: {description: 'The description of the IPsec profile. Max size
            = 256.

            '}
        dpd_delay: {description: "The DPD delay time in seconds (should be between\
            \ 1-60 seconds) for IKEv1.               \n", format: int32, type: integer}
        dpd_enable: {description: "If DPD is enabled for the IPsec profile. \n", type: boolean}
        dpd_timeout: {description: 'The configured DPD timeout period.

            ', format: int32, type: integer}
        esp_group: {$ref: '#/components/schemas/ESPGroup', description: 'The ESP group
            defined for the IPsec profile.

            '}
        id: {description: "The ID of the IPsec profile.             \n", type: string}
        ike_group: {$ref: '#/components/schemas/IKEGroup', description: "The IKE group\
            \ defined on the IPsec profile. \n"}
        name: {description: "The name of the IPsec profile. Max size = 128.      \
            \       \n", type: string}
        tags:
          description: "An information field that can be added to identify the IPsec\
            \ profile. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [authentication, ike_group, esp_group, name]
    IPSECProfileQueryFilter:
      properties:
        authentication: {$ref: '#/components/schemas/IPSECAuthenticationV1', description: 'The
            details of the authentication mode for the IPsec Profile.

            '}
        description: {description: 'The description of the IPsec profile. Max size
            = 256.

            '}
        dpd_delay: {description: "The DPD delay time in seconds (should be between\
            \ 1-60 seconds) for IKEv1.               \n", format: int32, type: integer}
        dpd_enable: {description: "If DPD is enabled for the IPsec profile. \n", type: boolean}
        dpd_timeout: {description: 'The configured DPD timeout period.

            ', format: int32, type: integer}
        esp_group: {$ref: '#/components/schemas/ESPGroup', description: 'The ESP group
            defined for the IPsec profile.

            '}
        id: {description: "The ID of the IPsec profile.             \n", type: string}
        ike_group: {$ref: '#/components/schemas/IKEGroup', description: "The IKE group\
            \ defined on the IPsec profile. \n"}
        name: {description: "The name of the IPsec profile. Max size = 128.      \
            \       \n", type: string}
        tags:
          description: 'An information field that can be added to identify the IPsec
            profile. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [authentication, ike_group, esp_group, name]
    IPSECProfileQueryFilterV2N2:
      properties:
        authentication: {$ref: '#/components/schemas/IPSECAuthenticationV1', description: 'The
            details of the authentication mode for the IPsec Profile.

            '}
        description: {description: 'The description of the IPsec profile. Max size
            = 256.

            '}
        dpd_delay: {description: "The DPD delay time in seconds (should be between\
            \ 1-60 seconds) for IKEv1.               \n", format: int32, type: integer}
        dpd_enable: {description: "If DPD is enabled for the IPsec profile. \n", type: boolean}
        dpd_timeout: {description: 'The configured DPD timeout period.

            ', format: int32, type: integer}
        esp_group: {$ref: '#/components/schemas/ESPGroup', description: 'The ESP group
            defined for the IPsec profile.

            '}
        id: {description: "The ID of the IPsec profile.             \n", type: string}
        ike_group: {$ref: '#/components/schemas/IKEGroup', description: "The IKE group\
            \ defined on the IPsec profile. \n"}
        name: {description: 'The name of the IPsec profile. Max size = 128.

            ', type: string}
        tags:
          description: "An information field that can be added to identify the IPsec\
            \ profile. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
        used_for:
          description: "IPSec profile used for values.              \n"
          enum: [fabric_vpn]
          type: string
      required: [authentication, ike_group, esp_group, name]
    IPSECProfileScreen:
      properties:
        authentication: {$ref: '#/components/schemas/IPSECAuthenticationV1', description: 'The
            details of the authentication mode for the IPsec Profile.

            '}
        description: {description: 'The description of the IPsec profile. Max size
            = 256.

            '}
        dpd_delay: {description: "The DPD delay time in seconds (should be between\
            \ 1-60 seconds) for IKEv1.               \n", format: int32, type: integer}
        dpd_enable: {description: "If DPD is enabled for the IPsec profile. \n", type: boolean}
        dpd_timeout: {description: 'The configured DPD timeout period.

            ', format: int32, type: integer}
        esp_group: {$ref: '#/components/schemas/ESPGroup', description: 'The ESP group
            defined for the IPsec profile.

            '}
        id: {description: "The ID of the IPsec profile.             \n", type: string}
        ike_group: {$ref: '#/components/schemas/IKEGroup', description: "The IKE group\
            \ defined on the IPsec profile. \n"}
        name: {description: "The name of the IPsec profile. Max size = 128.      \
            \       \n", type: string}
        tags:
          description: "An information field that can be added to identify the IPsec\
            \ profile. Maximum 10 unique tags of length 1024 each are allowed.   \
            \    \n"
          items: {description: Tags, type: string}
          type: array
      required: [authentication, ike_group, esp_group, name]
    IPSECProfileScreenV2N2:
      properties:
        authentication: {$ref: '#/components/schemas/IPSECAuthenticationV1', description: 'The
            details of the authentication mode for the IPsec Profile.

            '}
        description: {description: 'The description of the IPsec profile. Max size
            = 256.

            '}
        dpd_delay: {description: "The DPD delay time in seconds (should be between\
            \ 1-60 seconds) for IKEv1.               \n", format: int32, type: integer}
        dpd_enable: {description: "If DPD is enabled for the IPsec profile. \n", type: boolean}
        dpd_timeout: {description: 'The configured DPD timeout period.

            ', format: int32, type: integer}
        esp_group: {$ref: '#/components/schemas/ESPGroup', description: 'The ESP group
            defined for the IPsec profile.

            '}
        id: {description: "The ID of the IPsec profile.             \n", type: string}
        ike_group: {$ref: '#/components/schemas/IKEGroup', description: "The IKE group\
            \ defined on the IPsec profile. \n"}
        name: {description: 'The name of the IPsec profile. Max size = 128.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the IPsec
            profile. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        used_for:
          description: "IPSec profile used for values.               \n"
          enum: [fabric_vpn]
          type: string
      required: [authentication, ike_group, esp_group, name]
    IPSECProfileV2N2:
      properties:
        authentication: {$ref: '#/components/schemas/IPSECAuthenticationV1', description: 'The
            details of the authentication mode for the IPsec Profile.

            '}
        description: {description: 'The description of the IPsec profile. Max size
            = 256.

            '}
        dpd_delay: {description: "The DPD delay time in seconds (should be between\
            \ 1-60 seconds) for IKEv1.               \n", format: int32, type: integer}
        dpd_enable: {description: "If DPD is enabled for the IPsec profile. \n", type: boolean}
        dpd_timeout: {description: 'The configured DPD timeout period.

            ', format: int32, type: integer}
        esp_group: {$ref: '#/components/schemas/ESPGroup', description: 'The ESP group
            defined for the IPsec profile.

            '}
        id: {description: "The ID of the IPsec profile.             \n", type: string}
        ike_group: {$ref: '#/components/schemas/IKEGroup', description: "The IKE group\
            \ defined on the IPsec profile. \n"}
        name: {description: 'The name of the IPsec profile. Max size = 128.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the IPsec
            profile. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        used_for:
          description: "IPSec profile used for values.               \n"
          enum: [fabric_vpn]
          type: string
      required: [authentication, ike_group, esp_group, name]
    LANNetworkQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of LAN networks that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of LAN networks deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the LAN networks for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the LAN networks by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    LANNetworkQueryFilterResponse:
      properties:
        description: {description: 'The description of the LAN network. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN network. \n", type: string}
        ipv4_config: {$ref: '#/components/schemas/LanNetworkIPv4Config', description: 'The
            Ipv4 config of the LAN network. Maz size = 256.

            '}
        name: {description: 'The name of the LAN network. Max size = 128.

            ', type: string}
        network_context_id: {description: 'The network context ID.

            ', type: string}
        scope:
          description: 'The scope of the network.

            '
          enum: [GLOBAL, LOCAL]
          type: string
        site_id: {description: 'The ID of the site.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the LAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [site_id, scope, name]
    LANNetworkQueryFilterV3N2:
      properties:
        description: {description: 'The description of the LAN network. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN network. \n", type: string}
        ipv4_config: {$ref: '#/components/schemas/LanNetworkIPv4Config', description: 'The
            Ipv4 config of the LAN network. Maz size = 256.

            '}
        ipv6_config: {$ref: '#/components/schemas/LanNetworkIPv6Config', description: 'The
            Ipv6 config of the LAN network. Maz size = 256.

            '}
        name: {description: 'The name of the LAN network. Max size = 128.

            ', type: string}
        network_context_id: {description: 'The network context ID.

            ', type: string}
        scope:
          description: 'The scope of the network.

            '
          enum: [GLOBAL, LOCAL]
          type: string
        tags:
          description: 'An information field that can be added to identify the LAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [scope, name]
    LANNetworkQueryFilterV3N3:
      properties:
        description: {description: 'The description of the LAN network. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN network. \n", type: string}
        ipv4_config: {$ref: '#/components/schemas/LanNetworkIPv4Config', description: 'The
            Ipv4 config of the LAN network. Maz size = 256.

            '}
        ipv6_config: {$ref: '#/components/schemas/LanNetworkIPv6Config', description: 'The
            Ipv6 config of the LAN network. Maz size = 256.

            '}
        name: {description: 'The name of the LAN network. Max size = 128.

            ', type: string}
        network_context_id: {description: 'The network context ID.

            ', type: string}
        scope:
          description: 'The scope of the network.

            '
          enum: [GLOBAL, LOCAL]
          type: string
        tags:
          description: 'An information field that can be added to identify the LAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        vrf_context_id: {description: "The vrf context id. \n", type: string}
      required: [scope, name]
    LANNetworkScreenV3N2:
      properties:
        description: {description: 'The description of the LAN network. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN network. \n", type: string}
        ipv4_config: {$ref: '#/components/schemas/LanNetworkIPv4Config', description: 'The
            Ipv4 config of the LAN network. Maz size = 256.

            '}
        ipv6_config: {$ref: '#/components/schemas/LanNetworkIPv6Config', description: 'The
            Ipv6 config of the LAN network. Maz size = 256.

            '}
        name: {description: 'The name of the LAN network. Max size = 128.

            ', type: string}
        network_context_id: {description: 'The network context ID.

            ', type: string}
        scope:
          description: 'The scope of the network.

            '
          enum: [GLOBAL, LOCAL]
          type: string
        tags:
          description: 'An information field that can be added to identify the LAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [scope, name]
    LANNetworkScreenV3N3:
      properties:
        description: {description: 'The description of the LAN network. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN network. \n", type: string}
        ipv4_config: {$ref: '#/components/schemas/LanNetworkIPv4Config', description: 'The
            Ipv4 config of the LAN network. Maz size = 256.

            '}
        ipv6_config: {$ref: '#/components/schemas/LanNetworkIPv6Config', description: 'The
            Ipv6 config of the LAN network. Maz size = 256.

            '}
        name: {description: 'The name of the LAN network. Max size = 128.

            ', type: string}
        network_context_id: {description: 'The network context ID.

            ', type: string}
        scope:
          description: 'The scope of the network.

            '
          enum: [GLOBAL, LOCAL]
          type: string
        tags:
          description: 'An information field that can be added to identify the LAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        vrf_context_id: {description: "The VRF (Virtual Routing and Forwarding) ID.\
            \              \n", type: string}
      required: [scope, name]
    LANNetworkV3N2:
      properties:
        description: {description: 'The description of the LAN network. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN network. \n", type: string}
        ipv4_config: {$ref: '#/components/schemas/LanNetworkIPv4Config', description: 'The
            Ipv4 config of the LAN network. Maz size = 256.

            '}
        ipv6_config: {$ref: '#/components/schemas/LanNetworkIPv6Config', description: 'The
            Ipv6 config of the LAN network. Maz size = 256.

            '}
        name: {description: 'The name of the LAN network. Max size = 128.

            ', type: string}
        network_context_id: {description: 'The network context ID.

            ', type: string}
        scope:
          description: 'The scope of the network.

            '
          enum: [GLOBAL, LOCAL]
          type: string
        tags:
          description: 'An information field that can be added to identify the LAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [scope, name]
    LANNetworkV3N3:
      properties:
        description: {description: 'The description of the LAN network. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN network. \n", type: string}
        ipv4_config: {$ref: '#/components/schemas/LanNetworkIPv4Config', description: 'The
            Ipv4 config of the LAN network. Maz size = 256.

            '}
        ipv6_config: {$ref: '#/components/schemas/LanNetworkIPv6Config', description: 'The
            Ipv6 config of the LAN network. Maz size = 256.

            '}
        name: {description: 'The name of the LAN network. Max size = 128.

            ', type: string}
        network_context_id: {description: 'The network context ID.

            ', type: string}
        scope:
          description: 'The scope of the network.

            '
          enum: [GLOBAL, LOCAL]
          type: string
        tags:
          description: 'An information field that can be added to identify the LAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        vrf_context_id: {description: 'The VRF (Virtual Routing and Forwarding) ID.

            ', type: string}
      required: [scope, name]
    LQMConfig:
      properties:
        hub_site_ids:
          description: 'List of DC Site IDs.

            '
          items: {description: Hub Site Ids, type: string}
          type: array
        inter_packet_gap: {description: 'Allowed values: 50-300 milliseconds.

            ', format: int32, type: integer}
        statistic:
          description: 'WAN interface LQM config statistics.

            '
          enum: [min, max, avg]
          type: string
    LanNetworkIPv4Config:
      properties:
        default_routers:
          description: 'The default routers configured for the network.

            '
          items: {description: Default Routers, type: string}
          type: array
        dhcp_relay: {$ref: '#/components/schemas/DHCPRelay', description: "Details\
            \ of DHCP relay if enabled. \n"}
        dhcp_server: {$ref: '#/components/schemas/DHCPServer', description: "Details\
            \ of DHCP server if configured. \n"}
        prefixes:
          description: 'List of IP addresses for the network.

            '
          items: {description: Prefixes, type: string}
          type: array
    LanNetworkIPv6Config:
      properties:
        default_routers:
          description: 'The LAN config default routers.

            '
          items: {description: Default Routers, type: string}
          type: array
        prefixes:
          description: 'The LAN config IPv6 prefix.

            '
          items: {description: Prefixes, type: string}
          type: array
    Lifesize:
      properties:
        units: {description: Units, type: string}
        value: {description: Value, format: int32, type: integer}
    ListQueryResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        deleted_count: {description: 'The deleted number.

            ', format: int64, type: integer}
        deleted_ids:
          description: 'The deleted IDs.

            '
          items: {type: string}
          type: array
        description: {description: 'Description of the query. Max size = 256.

            '}
        id: {description: 'The ID.

            ', type: string}
        next_query: {description: 'Details of the next query.

            ', type: object}
        tenant_id: {description: 'The tenant ID.

            ', type: string}
        total_count: {description: 'Total number.

            ', format: int64, type: integer}
      type: object
    ListResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
        tenant_id: {description: 'The tenant ID.

            ', type: string}
      type: object
    NetworkContext:
      properties:
        description: {description: 'The description of the LAN segment. Maz size =
            256.

            '}
        id: {description: "The ID of the LAN segment. \n", type: string}
        name: {description: 'The name of the LAN segment. Max size = 128.

            ', type: string}
    NetworkContextQueryFilter:
      properties:
        description: {description: 'The description of the network context config.
            Maz size = 256.

            '}
        id: {description: "The ID of the network context. \n", type: string}
        name: {description: 'The name of the network context config. Max size = 128.

            ', type: string}
      required: [name]
    NetworkContextScreen:
      properties:
        description: {description: 'The description of the network context config.
            Maz size = 256.

            '}
        id: {description: "The ID of the network context. \n", type: string}
        name: {description: 'The name of the network context config. Max size = 128.

            ', type: string}
      required: [name]
    Proposals:
      properties:
        dh_groups:
          description: 'The DH group values.

            '
          enum: [ECP224BP, ECP256, NONE, MODP8192, MODP1024S160, MODP3072, ECP384,
            MODP1024, MODP4096, MODP2048, ECP224, ECP192, ECP521, ECP256BP, ECP384BP,
            MODP2048S224, ECP512BP, MODP768, CURVE25519, MODP2048S256, MODP6144, MODP1536]
          type: string
        encryption:
          description: 'The encryption values.

            '
          enum: [AES256CCM64, NONE, AES192GMAC, TWOFISH192, AES256GCM16, AES192CTR,
            AES256CTR, BLOWFISH256, AES128CCM128, AES256CCM128, AES256GCM128, AES192GCM128,
            AES256CCM96, AES192CCM64, CAMELLIA192, AES128GCM64, AES256, AES128GCM96,
            TWOFISH256, AES192GCM64, AES256GMAC, SERPENT192, AES128CCM64, BLOWFISH192,
            AES192CCM128, TRIPLEDES, AES192, AES256GCM64, SERPENT128, AES256GCM96,
            CAMELLIA128, AES128GCM128, AES128CCM16, AES128, AES128CTR, AES192CCM96,
            AES192GCM96, CAMELLIA256, AES128GMAC, AES128CCM96, SERPENT256, BLOWFISH128,
            AES128GCM16, TWOFISH128]
          type: string
        hash:
          description: 'The hash values.

            '
          enum: [NONE, AES192GMAC, SHA1, AES256GMAC, SHA512, AESXCBC, MD5, SHA256_96,
            AES128GMAC, SHA384, SHA256]
          type: string
        prf:
          description: 'Prf: ValidateEnum(enumClass = classOf[IPSECHash], error =
            IPSECCONFIG_INVALID_HASH: Invalid hashing algorithm specified., nullAllowed
            = true) '
          enum: [MD5, SHA1, SHA256, SHA384, SHA512, SHA256_96, AESXCBC, AES128GMAC,
            AES192GMAC, AES256GMAC, NONE]
          type: string
      required: [hash, encryption, dh_groups]
    ResponderSaseProposals:
      properties:
        dh_group:
          description: Dh Group
          items:
            description: Dh Group
            enum: [NONE, MODP768, MODP1024, MODP1536, MODP2048, MODP3072, MODP4096,
              MODP6144, MODP8192, MODP1024S160, MODP2048S224, MODP2048S256, ECP192,
              ECP224, ECP256, ECP384, ECP521, ECP224BP, ECP256BP, ECP384BP, ECP512BP,
              CURVE25519]
            type: string
          type: array
        encryption:
          description: 'The encryption values.

            '
          items:
            description: Encryption
            enum: [NONE, AES128, AES192, AES256, AES128CTR, AES192CTR, AES256CTR,
              AES128CCM16, AES128CCM64, AES192CCM64, AES256CCM64, AES128CCM96, AES192CCM96,
              AES256CCM96, AES128CCM128, AES192CCM128, AES256CCM128, AES128GCM16,
              AES128GCM64, AES192GCM64, AES256GCM16, AES256GCM64, AES128GCM96, AES192GCM96,
              AES256GCM96, AES128GCM128, AES192GCM128, AES256GCM128, AES128GMAC, AES192GMAC,
              AES256GMAC, TRIPLEDES, BLOWFISH128, BLOWFISH192, BLOWFISH256, CAMELLIA128,
              CAMELLIA192, CAMELLIA256, SERPENT128, SERPENT192, SERPENT256, TWOFISH128,
              TWOFISH192, TWOFISH256]
            type: string
          type: array
        hash:
          description: 'The hash values.

            '
          items:
            description: Hash
            enum: [MD5, SHA1, SHA256, SHA384, SHA512, SHA256_96, AESXCBC, AES128GMAC,
              AES192GMAC, AES256GMAC, NONE]
            type: string
          type: array
    StaticMapping:
      properties:
        ip_address: {description: 'IP addresses assigned mapped to MAC addresses of
            the clients.

            ', type: string}
        mac: {description: "The mac addresses that need to be mapped. \n", type: string}
        name: {description: 'The name of the static mapping. Max length supported
            is 256.

            ', type: string}
      required: [ip_address, name]
    VPNLinkConfiguration:
      properties:
        keep_alive_failure_count: {description: 'The VPN keep alive failure count
            (must be between 3 and 30).

            ', type: integer}
        keep_alive_interval: {description: 'The VPN keep alive timeout interval (must
            be between 100ms-1740,000ms).

            ', type: integer}
      required: [keep_alive_failure_count, keep_alive_interval]
    WANInterfaceLabelQueryFilterV2N5:
      properties:
        bwc_enabled: {description: "The bwc parameters.        \n", type: boolean}
        description: {description: 'The description of the WAN interface label. Maz
            size = 256.

            '}
        id: {description: 'The ID of the WAN interface label.

            ', type: string}
        l3_reachability: {$ref: '#/components/schemas/WANL3Reachability', description: 'The
            WAN L3 reachability information.

            '}
        label: {description: 'The label of the WAN interface.

            ', type: string}
        lqm_enabled: {description: "The link quality monitoring parameters.      \
            \        \n", type: boolean}
        name: {description: 'The name of the WAN interface label. Max size = 128.

            ', type: string}
        probe_profile_id: {description: 'The probe profile ID.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface label. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        use_for_application_reachability_probes: {description: "Indicates if the use\
            \ for application reachability probe is selected.              \n", type: boolean}
        use_for_controller_connections: {description: 'Indicates if the use for controller
            connections is selected.

            ', type: boolean}
        use_lqm_for_non_hub_paths: {description: 'Indicates if the use for lqm for
            non hub paths is enabled.

            ', type: boolean}
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'The configuration of the VPN link.

            '}
    WANInterfaceLabelQueryFilterV2N6:
      properties:
        app_acceleration_enabled: {description: App Acceleration Enabled, type: boolean}
        bwc_enabled: {description: "The bwc parameters.        \n", type: boolean}
        description: {description: 'The description of the WAN interface label. Maz
            size = 256.

            '}
        id: {description: 'The ID of the WAN interface label.

            ', type: string}
        l3_reachability: {$ref: '#/components/schemas/WANL3Reachability', description: 'The
            WAN L3 reachability information.

            '}
        label: {description: 'The label of the WAN interface.

            ', type: string}
        lqm_enabled: {description: "The link quality monitoring parameters.      \
            \        \n", type: boolean}
        name: {description: 'The name of the WAN interface label. Max size = 128.

            ', type: string}
        probe_profile_id: {description: 'The probe profile ID.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface label. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        use_for_application_reachability_probes: {description: "Indicates if the use\
            \ for application reachability probe is selected.              \n", type: boolean}
        use_for_controller_connections: {description: 'Indicates if the use for controller
            connections is selected.

            ', type: boolean}
        use_lqm_for_non_hub_paths: {description: 'Indicates if the use for lqm for
            non hub paths is enabled.

            ', type: boolean}
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'The configuration of the VPN link.

            '}
    WANInterfaceLabelScreenV2N5:
      properties:
        bwc_enabled: {description: "The bwc parameters.        \n", type: boolean}
        description: {description: 'The description of the WAN interface label. Maz
            size = 256.

            '}
        id: {description: 'The ID of the WAN interface label.

            ', type: string}
        l3_reachability: {$ref: '#/components/schemas/WANL3Reachability', description: 'The
            WAN L3 reachability information.

            '}
        label: {description: 'The label of the WAN interface.

            ', type: string}
        lqm_enabled: {description: "The link quality monitoring parameters.      \
            \        \n", type: boolean}
        name: {description: 'The name of the WAN interface label. Max size = 128.

            ', type: string}
        probe_profile_id: {description: 'The probe profile ID.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface label. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        use_for_application_reachability_probes: {description: "Indicates if the use\
            \ for application reachability probe is selected.              \n", type: boolean}
        use_for_controller_connections: {description: 'Indicates if the use for controller
            connections is selected.

            ', type: boolean}
        use_lqm_for_non_hub_paths: {description: 'Indicates if the use for lqm for
            non hub paths is enabled.

            ', type: boolean}
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'The configuration of the VPN link.

            '}
    WANInterfaceLabelScreenV2N6:
      properties:
        app_acceleration_enabled: {description: App Acceleration Enabled, type: boolean}
        bwc_enabled: {description: "The bwc parameters.        \n", type: boolean}
        description: {description: 'The description of the WAN interface label. Maz
            size = 256.

            '}
        id: {description: 'The ID of the WAN interface label.

            ', type: string}
        l3_reachability: {$ref: '#/components/schemas/WANL3Reachability', description: 'The
            WAN L3 reachability information.

            '}
        label: {description: 'The label of the WAN interface.

            ', type: string}
        lqm_enabled: {description: "The link quality monitoring parameters.      \
            \        \n", type: boolean}
        name: {description: 'The name of the WAN interface label. Max size = 128.

            ', type: string}
        probe_profile_id: {description: 'The probe profile ID.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface label. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        use_for_application_reachability_probes: {description: "Indicates if the use\
            \ for application reachability probe is selected.              \n", type: boolean}
        use_for_controller_connections: {description: 'Indicates if the use for controller
            connections is selected.

            ', type: boolean}
        use_lqm_for_non_hub_paths: {description: 'Indicates if the use for lqm for
            non hub paths is enabled.

            ', type: boolean}
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'The configuration of the VPN link.

            '}
    WANInterfaceLabelV2N5:
      properties:
        bwc_enabled: {description: "The bwc parameters.        \n", type: boolean}
        description: {description: 'The description of the WAN interface label. Maz
            size = 256.

            '}
        id: {description: 'The ID of the WAN interface label.

            ', type: string}
        l3_reachability: {$ref: '#/components/schemas/WANL3Reachability', description: 'The
            WAN L3 reachability information.

            '}
        label: {description: 'The label of the WAN interface.

            ', type: string}
        lqm_enabled: {description: "The link quality monitoring parameters.      \
            \        \n", type: boolean}
        name: {description: 'The name of the WAN interface label. Max size = 128.

            ', type: string}
        probe_profile_id: {description: 'The probe profile ID.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface label. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        use_for_application_reachability_probes: {description: "Indicates if the use\
            \ for application reachability probe is selected.              \n", type: boolean}
        use_for_controller_connections: {description: 'Indicates if the use for controller
            connections is selected.

            ', type: boolean}
        use_lqm_for_non_hub_paths: {description: 'Indicates if the use for lqm for
            non hub paths is enabled.

            ', type: boolean}
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'The configuration of the VPN link.

            '}
    WANInterfaceLabelV2N6:
      properties:
        app_acceleration_enabled: {description: App Acceleration Enabled, type: boolean}
        bwc_enabled: {description: "The bwc parameters.        \n", type: boolean}
        description: {description: 'The description of the WAN interface label. Maz
            size = 256.

            '}
        id: {description: 'The ID of the WAN interface label.

            ', type: string}
        l3_reachability: {$ref: '#/components/schemas/WANL3Reachability', description: 'The
            WAN L3 reachability information.

            '}
        label: {description: 'The label of the WAN interface.

            ', type: string}
        lqm_enabled: {description: "The link quality monitoring parameters.      \
            \        \n", type: boolean}
        name: {description: 'The name of the WAN interface label. Max size = 128.

            ', type: string}
        probe_profile_id: {description: 'The probe profile ID.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface label. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        use_for_application_reachability_probes: {description: "Indicates if the use\
            \ for application reachability probe is selected.              \n", type: boolean}
        use_for_controller_connections: {description: 'Indicates if the use for controller
            connections is selected.

            ', type: boolean}
        use_lqm_for_non_hub_paths: {description: 'Indicates if the use for lqm for
            non hub paths is enabled.

            ', type: boolean}
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'The configuration of the VPN link.

            '}
    WANInterfaceQueryFilterV2N5:
      properties:
        bfd_mode:
          description: 'Allowed values: aggressive - For fast failure detection of
            links. This mode is the default mode and is recommended by Prisma SD-WAN.
            non_aggressive - when you want to reduce the amount of probe traffic,
            or for links that are subjected to high loss or poor quality.

            '
          enum: [aggressive, non_aggressive]
          type: string
        bw_config_mode:
          description: 'The bandwidth config mode.

            '
          enum: [auto, manual, manual_bwm_disabled]
          type: string
        bwc_enabled: {description: 'The bwc parameters.

            ', type: boolean}
        cost: {description: 'Cost value for the site WAN interface should be within
            range 0-1024.

            ', type: integer}
        description: {description: 'The description of the WAN interface config.

            '}
        id: {description: 'The ID.

            ', type: string}
        label_id: {description: 'The WAN interface label or Circuit label ID. This
            can be retrieved using the waninterfacelabels API.

            ', type: string}
        link_bw_down: {description: 'The circuit downstream capacity in MB.

            ', format: double, type: number}
        link_bw_up: {description: 'The circuit upstream capacity in MB.

            ', format: double, type: number}
        lqm_config: {$ref: '#/components/schemas/LQMConfig', description: 'The link
            quality monitoring parameters.

            '}
        lqm_enabled: {description: 'The link quality monitoring parameters.

            ', type: boolean}
        name: {description: 'The name of the WAN interface config.

            ', type: string}
        network_id: {description: 'The network ID.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        type:
          description: Type
          enum: [PUBLIC_WAN, PRIVATE_WAN]
          type: string
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'Vpnlink Configuration: Valid Valid '}
      required: [bfd_mode]
    WANInterfaceQueryFilterV2N6:
      properties:
        bfd_mode:
          description: 'Allowed values: aggressive - For fast failure detection of
            links. This mode is the default mode and is recommended by Prisma SD-WAN.
            non_aggressive - when you want to reduce the amount of probe traffic,
            or for links that are subjected to high loss or poor quality.

            '
          enum: [aggressive, non_aggressive]
          type: string
        bw_config_mode:
          description: 'The bandwidth config mode.

            '
          enum: [auto, manual, manual_bwm_disabled]
          type: string
        bwc_enabled: {description: 'The bwc parameters.

            ', type: boolean}
        cost: {description: 'Cost value for the site WAN interface should be within
            range 0-1024.

            ', type: integer}
        description: {description: 'The description of the WAN interface config.

            '}
        id: {description: 'The ID.

            ', type: string}
        label_id: {description: 'The WAN interface label or Circuit label ID. This
            can be retrieved using the waninterfacelabels API.

            ', type: string}
        link_bw_down: {description: 'The circuit downstream capacity in MB.

            ', format: double, type: number}
        link_bw_up: {description: 'The circuit upstream capacity in MB.

            ', format: double, type: number}
        lqm_config: {$ref: '#/components/schemas/LQMConfig', description: 'The link
            quality monitoring parameters.

            '}
        lqm_enabled: {description: 'The link quality monitoring parameters.

            ', type: boolean}
        name: {description: 'The name of the WAN interface config.

            ', type: string}
        network_id: {description: 'The network ID.

            ', type: string}
        site_id: {description: Site Id, type: string}
        tags:
          description: 'An information field that can be added to identify the WAN
            interface. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        type:
          description: Type
          enum: [PUBLIC_WAN, PRIVATE_WAN]
          type: string
        use_for_application_reachability_probes: {description: 'Use For Application
            Reachability Probes: Valid ', type: boolean}
        use_for_controller_connections: {description: 'Use For Controller Connections:
            Valid ', type: boolean}
        vpnlink_configuration: {$ref: '#/components/schemas/VPNLinkConfiguration',
          description: 'Vpnlink Configuration: Valid Valid '}
      required: [bfd_mode]
    WANL3Reachability:
      properties:
        probe_config_ids:
          description: "The probe configuration IDs.        \n"
          items: {description: Probe Config Ids, type: string}
          type: array
        use_element_default: {description: 'The default element used.

            ', type: boolean}
    WANNetwork:
      properties:
        id: {description: "The ID of the WAN network. \n", type: string}
        name: {description: 'The name of the WAN network. Max size = 128.

            ', type: string}
        provider_as_numbers:
          description: 'The provider AS numbers of the WAN network.

            '
          items: {description: Provider As Numbers, type: integer}
          type: array
        type:
          description: 'The type of WAN network.

            '
          enum: [PUBLIC_WAN, PRIVATE_WAN]
          type: string
      required: [type, name]
    WANNetworkQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of WAN networks that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of WAN networks for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the WAN networks for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the WAN networks by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    WANNetworkScreen:
      properties:
        id: {description: "The ID of the WAN network. \n", type: string}
        name: {description: 'The name of the WAN network. Max size = 128.

            ', type: string}
        provider_as_numbers:
          description: 'The provider AS numbers of the WAN network.

            '
          items: {description: Provider As Numbers, type: integer}
          type: array
        type:
          description: 'The type of WAN network.

            '
          enum: [PUBLIC_WAN, PRIVATE_WAN]
          type: string
      required: [type, name]
    WANNetworkScreenV2N1:
      properties:
        description: {description: "The description of the WAN network. Maz size =\
            \ 256.        \n"}
        id: {description: "The ID of the WAN network. \n", type: string}
        name: {description: 'The name of the WAN network. Max size = 128.

            ', type: string}
        provider_as_numbers:
          description: 'The provider AS numbers of the WAN network.

            '
          items: {description: Provider As Numbers, type: integer}
          type: array
        tags:
          description: 'An information field that can be added to identify the WAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        type:
          description: 'The type of WAN network.

            '
          enum: [PUBLIC_WAN, PRIVATE_WAN]
          type: string
      required: [type, name]
    WANNetworkV2N1:
      properties:
        description: {description: "The description of the WAN network. Maz size =\
            \ 256.        \n"}
        id: {description: "The ID of the WAN network. \n", type: string}
        name: {description: 'The name of the WAN network. Max size = 128.

            ', type: string}
        provider_as_numbers:
          description: 'The provider AS numbers of the WAN network.

            '
          items: {description: Provider As Numbers, type: integer}
          type: array
        tags:
          description: 'An information field that can be added to identify the WAN
            network. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        type:
          description: 'The type of WAN network.

            '
          enum: [PUBLIC_WAN, PRIVATE_WAN]
          type: string
      required: [type, name]
    X509Objects:
      properties:
        certHolder: {type: object}
        certificate: {description: Certificate, type: string}
        is_local_ca_cert_set: {description: Is Local Ca Cert Set, type: boolean}
        is_remote_ca_cert_set: {description: Is Remote Ca Cert Set, type: boolean}
        keyPair: {type: object}
        local_ca_certificate: {description: Local Ca Certificate, type: string}
        local_ca_certs_set:
          description: Local Ca Certs Set
          items: {type: object}
          type: array
        passphrase: {description: Passphrase, type: string}
        pkcs12_certificate: {description: Pkcs12 Certificate, type: string}
        privateKey: {description: PrivateKey, type: java.security.privatekey}
        private_key: {description: Private Key, type: string}
        remote_ca_certificate: {description: Remote Ca Certificate, type: string}
        remote_ca_certs_set:
          description: Remote Ca Certs Set
          items: {type: object}
          type: array
info:
  contact: {}
  description: List of APIs used to manage Network constructs in Prisma SD-WAN like
    WAN Networks, LAN Networks, Circuit labels, and Network Contexts. All these are
    used to define a circuit and create policies.
  title: Network Constructs
  version: Latest
openapi: 3.0.0
paths:
  /v2.0/api/tenants/{tenant_id}/networkcontexts:
    get:
      description: List all the LAN segments.
      operationId: get-v2.0-api-tenants-tenant_id-networkcontexts
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NetworkContext'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get LAN Segments (v2.0)
      tags: [Network Constructs]
    post:
      description: Create a new LAN segment.
      operationId: post-v2.0-api-tenants-tenant_id-networkcontexts
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NetworkContext'}
        description: Details of the lan segment model to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NetworkContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NETWORK_CONTEXT_CONFIG_DESCRIPTION_INVALID}
                  - {code: NAME_EXCEEDS_LIMIT}
                  - {code: NETWORK_CONTEXT_CONFIG_NAME_REQD}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create LAN Segment (v2.0)
      tags: [Network Constructs]
  /v2.0/api/tenants/{tenant_id}/networkcontexts/query:
    post:
      description: Query DB for limit number of network contexts that match the query
        parameters.
      operationId: post-v2.0-api-tenants-tenant_id-networkcontexts-query
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NetworkContext'}
        description: Details of the query for lan segment
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/NetworkContextQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for Network Contexts (v2.0)
      tags: [Network Constructs]
  /v2.0/api/tenants/{tenant_id}/networkcontexts/{network_context_id}:
    delete:
      description: Delete a LAN segment.
      operationId: delete-v2.0-api-tenants-tenant_id-networkcontexts-network_context_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the network context.
        in: path
        name: network_context_id
        required: true
        schema: {description: Network Context Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NetworkContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NETWORK_CONTEXT_CONFIG_DESCRIPTION_INVALID}
                  - {code: NAME_EXCEEDS_LIMIT}
                  - {code: NETWORK_CONTEXT_CONFIG_NAME_REQD}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete LAN Segment (v2.0)
      tags: [Network Constructs]
    get:
      description: List a specific LAN segment.
      operationId: get-v2.0-api-tenants-tenant_id-networkcontexts-network_context_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the network context.
        in: path
        name: network_context_id
        required: true
        schema: {description: Network Context Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NetworkContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_LAN_SEGMENTID}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get LAN Segment (v2.0)
      tags: [Network Constructs]
    put:
      description: Update a specific LAN segment.
      operationId: put-v2.0-api-tenants-tenant_id-networkcontexts-network_context_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the network context.
        in: path
        name: network_context_id
        required: true
        schema: {description: Network Context Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/NetworkContextScreen'}
        description: Details of lan segment to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/NetworkContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NETWORK_CONTEXT_CONFIG_DESCRIPTION_INVALID}
                  - {code: NAME_EXCEEDS_LIMIT}
                  - {code: NETWORK_CONTEXT_CONFIG_NAME_REQD}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update LAN Segment (v2.0)
      tags: [Network Constructs]
  /v2.0/api/tenants/{tenant_id}/wannetworks:
    get:
      description: List all the tenant WAN networks.
      operationId: get-v2.0-api-tenants-tenant_id-wannetworks
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANNetwork'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get WAN Networks (v2.0)
      tags: [Network Constructs]
    post:
      description: Create a new WAN network.
      operationId: post-v2.0-api-tenants-tenant_id-wannetworks
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANNetworkScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetwork'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_INPUT}
                  - {code: EXTRA_PROVIDER_ASNUM}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create WAN Network (v2.0)
      tags: [Network Constructs]
  /v2.0/api/tenants/{tenant_id}/wannetworks/query:
    post:
      description: Query the DB for WAN networks that match query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-wannetworks-query
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANNetworkQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANNetworkQuery'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_WAN_NETWORK_EXISTS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for WAN Networks (v2.0)
      tags: [Network Constructs]
  /v2.0/api/tenants/{tenant_id}/wannetworks/{wan_network_id}:
    delete:
      description: Delete an existing WAN network.
      operationId: delete-v2.0-api-tenants-tenant_id-wannetworks-wan_network_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN network.
        in: path
        name: wan_network_id
        required: true
        schema: {description: Wan Network Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetwork'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_WAN_NETWORK_EXISTS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete WAN Network (v2.0)
      tags: [Network Constructs]
    get:
      description: List a WAN network.
      operationId: get-v2.0-api-tenants-tenant_id-wannetworks-wan_network_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN network.
        in: path
        name: wan_network_id
        required: true
        schema: {description: Wan Network Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetwork'}
          description: Successful Operation
      summary: Get WAN Network (v2.0)
      tags: [Network Constructs]
    put:
      description: Update an existing WAN network.
      operationId: put-v2.0-api-tenants-tenant_id-wannetworks-wan_network_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN network.
        in: path
        name: wan_network_id
        required: true
        schema: {description: Wan Network Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANNetworkScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetwork'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_INPUT}
                  - {code: EXTRA_PROVIDER_ASNUM}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update WAN Network (v2.0)
      tags: [Network Constructs]
  /v2.1/api/tenants/{tenant_id}/ipsecprofiles:
    get:
      description: Get the IPsec profile list.
      operationId: get-v2.1-api-tenants-tenant_id-ipsecprofiles
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/IPSECProfile'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get IPSEC Profile List (v2.1)
      tags: [Network Constructs]
    post:
      description: Create a new IPsec profile.
      operationId: post-v2.1-api-tenants-tenant_id-ipsecprofiles
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPSECProfileScreen'}
        description: Details of the IPSEC profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: IPSECCONFIG_INVALID_ENCODED_AUTH_SECRET}
                  - {code: IPSECCONFIG_IKEGROUP_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_DELAY}
                  - {code: IPSECCONFIG_ESPGROUP_REQUIRED}
                  - {code: IPSECCONFIG_AUTHENTICATION_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_TIMEOUT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPSEC Profile (v2.1)
      tags: [Network Constructs]
  /v2.1/api/tenants/{tenant_id}/ipsecprofiles/query:
    post:
      description: Query the DB for limit number of tenant level IPsec profiles that
        match the query parameters.
      operationId: post-v2.1-api-tenants-tenant_id-ipsecprofiles-query
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPSECProfileQueryFilter'}
        description: Query Details for IPSECProfile
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/IPSECProfileQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for IPSEC Profiles (v2.1)
      tags: [Network Constructs]
  /v2.1/api/tenants/{tenant_id}/ipsecprofiles/{profile_id}:
    delete:
      description: Delete an IPsec Profile.
      operationId: delete-v2.1-api-tenants-tenant_id-ipsecprofiles-profile_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the IPsec profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: IPSECCONFIG_AUTO_CREATED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete IPSEC Profile (v2.1)
      tags: [Network Constructs]
    get:
      description: List a specific IPsec Profile.
      operationId: get-v2.1-api-tenants-tenant_id-ipsecprofiles-profile_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the IPsec profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfile'}
          description: Successful Operation
      summary: Get IPSEC Profile (v2.1)
      tags: [Network Constructs]
    put:
      description: Update an IPsec Profile.
      operationId: put-v2.1-api-tenants-tenant_id-ipsecprofiles-profile_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the IPsec profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPSECProfileScreen'}
        description: Details of the IPSEC profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: IPSECCONFIG_INVALID_ENCODED_AUTH_SECRET}
                  - {code: IPSECCONFIG_IKEGROUP_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_DELAY}
                  - {code: IPSECCONFIG_ESPGROUP_REQUIRED}
                  - {code: IPSECCONFIG_AUTHENTICATION_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_TIMEOUT}
                  - {code: IPSECCONFIG_AUTO_CREATED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPSEC Profile (v2.1)
      tags: [Network Constructs]
  /v2.1/api/tenants/{tenant_id}/wannetworks:
    get:
      description: List all the tenant WAN networks.
      operationId: get-v2.1-api-tenants-tenant_id-wannetworks
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANNetworkV2N1'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get WAN Networks (v2.1)
      tags: [Network Constructs]
    post:
      description: Create a new WAN network.
      operationId: post-v2.1-api-tenants-tenant_id-wannetworks
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANNetworkScreenV2N1'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetworkV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_INPUT}
                  - {code: EXTRA_PROVIDER_ASNUM}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create WAN Network (v2.1)
      tags: [Network Constructs]
  /v2.1/api/tenants/{tenant_id}/wannetworks/query:
    post:
      description: Query the DB for WAN networks that match query parameters.
      operationId: post-v2.1-api-tenants-tenant_id-wannetworks-query
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANNetworkQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANNetworkQuery'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_WAN_NETWORK_EXISTS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for WAN Networks (v2.1)
      tags: [Network Constructs]
  /v2.1/api/tenants/{tenant_id}/wannetworks/{wan_network_id}:
    delete:
      description: Delete an existing WAN network.
      operationId: delete-v2.1-api-tenants-tenant_id-wannetworks-wan_network_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN network.
        in: path
        name: wan_network_id
        required: true
        schema: {description: Wan Network Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetworkV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_WAN_NETWORK_EXISTS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete WAN Network (v2.1)
      tags: [Network Constructs]
    get:
      description: List a WAN network.
      operationId: get-v2.1-api-tenants-tenant_id-wannetworks-wan_network_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN network.
        in: path
        name: wan_network_id
        required: true
        schema: {description: Wan Network Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetworkV2N1'}
          description: Successful Operation
      summary: Get WAN Network (v2.1)
      tags: [Network Constructs]
    put:
      description: Update an existing WAN network.
      operationId: put-v2.1-api-tenants-tenant_id-wannetworks-wan_network_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN network.
        in: path
        name: wan_network_id
        required: true
        schema: {description: Wan Network Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANNetworkScreenV2N1'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANNetworkV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_INPUT}
                  - {code: EXTRA_PROVIDER_ASNUM}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update WAN Network (v2.1)
      tags: [Network Constructs]
  /v2.2/api/tenants/{tenant_id}/ipsecprofiles:
    get:
      description: Get the IPsec profile list.
      operationId: get-v2.2-api-tenants-tenant_id-ipsecprofiles
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/IPSECProfileV2N2'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get IPSEC Profile List (v2.2)
      tags: [Network Constructs]
    post:
      description: Create a new IPsec profile.
      operationId: post-v2.2-api-tenants-tenant_id-ipsecprofiles
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPSECProfileScreenV2N2'}
        description: Details of the IPSEC profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfileV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: IPSECCONFIG_INVALID_ENCODED_AUTH_SECRET}
                  - {code: IPSECCONFIG_IKEGROUP_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_DELAY}
                  - {code: IPSECCONFIG_ESPGROUP_REQUIRED}
                  - {code: IPSECCONFIG_AUTHENTICATION_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_TIMEOUT}
                  - {code: IPSECCONFIG_CANNOT_SET_USED_FOR}
                  - {code: VPN_CIPHER_NOT_SUPPORTED_ON_DEVICE}
                  - {code: FIPS_MODE_IPSEC_CONFIG_NOT_SUPPORTED}
                  - {code: GCM_IKE_PROPOSAL_HASH_NOT_NONE}
                  - {code: GCM_IKE_PROPOSAL_PRF_INVALID}
                  - {code: GCM_ESP_PROPOSAL_HASH_NOT_NONE}
                  - {code: GCM_ESP_PROPOSAL_PRF_INVALID}
                  - {code: CBC_IKE_PROPOSAL_PRF_NOT_NONE}
                  - {code: CBC_IKE_PROPOSAL_HASH_INVALID}
                  - {code: CBC_ESP_PROPOSAL_PRF_NOT_NONE}
                  - {code: CBC_ESP_PROPOSAL_HASH_INVALID}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPSEC Profile (v2.2)
      tags: [Network Constructs]
  /v2.2/api/tenants/{tenant_id}/ipsecprofiles/query:
    post:
      description: Query the DB for limit number of tenant level IPsec profiles that
        match the query parameters.
      operationId: post-v2.2-api-tenants-tenant_id-ipsecprofiles-query
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPSECProfileQueryFilterV2N2'}
        description: Query Details for IPSECProfile
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/IPSECProfileQueryFilterV2N2'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for IPSEC Profiles (v2.2)
      tags: [Network Constructs]
  /v2.2/api/tenants/{tenant_id}/ipsecprofiles/{profile_id}:
    delete:
      description: Delete an IPsec Profile.
      operationId: delete-v2.2-api-tenants-tenant_id-ipsecprofiles-profile_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the IPsec profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfileV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: SYSTEM_RESOURCE_MODIFICATION_NOT_ALLOWED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
        '404':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
          description: Resource Not Found
      summary: Delete IPSEC Profile (v2.2)
      tags: [Network Constructs]
    get:
      description: List a specific IPsec Profile.
      operationId: get-v2.2-api-tenants-tenant_id-ipsecprofiles-profile_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the IPsec profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfileV2N2'}
          description: Successful Operation
        '404':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
          description: Resource Not Found
      summary: Get IPSEC Profile (v2.2)
      tags: [Network Constructs]
    put:
      description: Update an IPsec Profile.
      operationId: put-v2.2-api-tenants-tenant_id-ipsecprofiles-profile_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the IPsec profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPSECProfileScreenV2N2'}
        description: Details of the IPSEC profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPSECProfileV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: IPSECCONFIG_INVALID_ENCODED_AUTH_SECRET}
                  - {code: IPSECCONFIG_IKEGROUP_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_DELAY}
                  - {code: IPSECCONFIG_ESPGROUP_REQUIRED}
                  - {code: IPSECCONFIG_AUTHENTICATION_REQUIRED}
                  - {code: IPSECCONFIG_INVALID_DPD_TIMEOUT}
                  - {code: SYSTEM_RESOURCE_MODIFICATION_NOT_ALLOWED}
                  - {code: IPSECCONFIG_AUTO_CREATED}
                  - {code: VPN_CIPHER_NOT_SUPPORTED_ON_DEVICE}
                  - {code: FIPS_MODE_IPSEC_CONFIG_NOT_SUPPORTED}
                  - {code: GCM_IKE_PROPOSAL_HASH_NOT_NONE}
                  - {code: GCM_IKE_PROPOSAL_PRF_INVALID}
                  - {code: GCM_ESP_PROPOSAL_HASH_NOT_NONE}
                  - {code: GCM_ESP_PROPOSAL_PRF_INVALID}
                  - {code: CBC_IKE_PROPOSAL_PRF_NOT_NONE}
                  - {code: CBC_IKE_PROPOSAL_HASH_INVALID}
                  - {code: CBC_ESP_PROPOSAL_PRF_NOT_NONE}
                  - {code: CBC_ESP_PROPOSAL_HASH_INVALID}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
        '404':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/Error'}
          description: Resource Not Found
      summary: Update IPSEC Profile (v2.2)
      tags: [Network Constructs]
  /v2.5/api/tenants/{tenant_id}/waninterfacelabels:
    get:
      description: List all the WAN interface labels for a tenant.
      operationId: get-v2.5-api-tenants-tenant_id-waninterfacelabels
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANInterfaceLabelV2N5'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: WAN_INTERFACE_LABEL_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get WAN Interface Labels (v2.5)
      tags: [Network Constructs]
  /v2.5/api/tenants/{tenant_id}/waninterfacelabels/query:
    post:
      description: Query the DB for WAN interface labels that match query parameters.
      operationId: post-v2.5-api-tenants-tenant_id-waninterfacelabels-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANInterfaceQueryFilterV2N5'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANInterfaceLabelQueryFilterV2N5'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for WAN Interface Labels (v2.5)
      tags: [Network Constructs]
  /v2.5/api/tenants/{tenant_id}/waninterfacelabels/{wantinterface_label_id}:
    get:
      description: List a specific WAN interface label.
      operationId: get-v2.5-api-tenants-tenant_id-waninterfacelabels-wantinterface_label_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN interface label.
        in: path
        name: wantinterface_label_id
        required: true
        schema: {description: Wantinterface Label Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANInterfaceLabelV2N5'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: WAN_INTERFACE_LABEL_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get WAN Interface Label (v2.5)
      tags: [Network Constructs]
    put:
      description: Update a specific WAN interface label.
      operationId: put-v2.5-api-tenants-tenant_id-waninterfacelabels-wantinterface_label_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN interface label.
        in: path
        name: wantinterface_label_id
        required: true
        schema: {description: Wantinterface Label Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANInterfaceLabelScreenV2N5'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANInterfaceLabelV2N5'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: WAN_INTERFACE_LABEL_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: VPNLINK_CONFIG_INVALID_KEEP_ALIVE_INTERVAL}
                  - {code: VPNLINK_CONFIG_INVALID_KEEP_ALIVE_FAILURE_COUNT}
                  - {code: VPNLINK_CONFIG_REQUIRED}
                  - {code: WAN_INTERFACE_LABEL_MISMATCH}
                  - {code: WANINTERFACE_LABEL_USE_FOR_NON_HUB_PATHS_INVALID}
                  - {code: L3_REACHABILITY_MAX_PROBE_CONFIGS_LIMIT_EXCEEDED}
                  - {code: INVALID_PROBE_PROFILE}
                  - {code: WANINTERFACE_L3_REACHABILITY_OVERRIDING_PROBE_CONFIGS_FROM_LABEL_PROFILE}
                  - {code: L3_REACHABILITY_PROBE_CONFIGS_CANONT_BE_SET_ELEMENT_DEFAULT_CONFIGURED}
                  - {code: L3_REACHABILITY_PROBE_CONFIGS_REQUIRED_WHEN_ELEMENT_DEFAULT_NOT_USED}
                  - {code: PROBE_CONFIGS_CANNOT_BE_SPECIFIED_WITHOUT_PROBE_PROFILE}
                  - {code: WANINTERFACE_LABEL_PROBE_PROFILE_DOES_NOT_CONTAIN_PROBE_CONFIGS}
                  - {code: PROBE_CONFIG_VALID_L3_REACHABILITY_PROTOCOL_MISSING}
                  - {code: PROBE_CONFIG_VALID_L3_REACHABILITY_DIRECT_PATH_MISSING}
                  - {code: L3_REACHABILITY_PROBE_CONFIGS_REQUIRED}
                  - {code: L3_REACHABILITY_CAN_NOT_BE_UNSET_FROM_LABEL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update WAN Interface Label (v2.5)
      tags: [Network Constructs]
  /v2.6/api/tenants/{tenant_id}/waninterfacelabels:
    get:
      description: List all the WAN interface labels for a tenant.
      operationId: get-v2.6-api-tenants-tenant_id-waninterfacelabels
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANInterfaceLabelV2N6'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: WAN_INTERFACE_LABEL_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get WAN Interface Labels (v2.6)
      tags: [Network Constructs]
  /v2.6/api/tenants/{tenant_id}/waninterfacelabels/query:
    post:
      description: Query the DB for WAN interface labels that match query parameters.
      operationId: post-v2.6-api-tenants-tenant_id-waninterfacelabels-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANInterfaceQueryFilterV2N6'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/WANInterfaceLabelQueryFilterV2N6'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for WAN Interface Labels (v2.6)
      tags: [Network Constructs]
  /v2.6/api/tenants/{tenant_id}/waninterfacelabels/{wantinterface_label_id}:
    get:
      description: List a specific WAN interface label.
      operationId: get-v2.6-api-tenants-tenant_id-waninterfacelabels-wantinterface_label_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN interface label.
        in: path
        name: wantinterface_label_id
        required: true
        schema: {description: Wantinterface Label Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANInterfaceLabelV2N6'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: WAN_INTERFACE_LABEL_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get WAN Interface Label (v2.6)
      tags: [Network Constructs]
    put:
      description: Update a specific WAN interface label.
      operationId: put-v2.6-api-tenants-tenant_id-waninterfacelabels-wantinterface_label_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the WAN interface label.
        in: path
        name: wantinterface_label_id
        required: true
        schema: {description: Wantinterface Label Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/WANInterfaceLabelScreenV2N6'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/WANInterfaceLabelV2N6'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: WAN_INTERFACE_LABEL_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: VPNLINK_CONFIG_INVALID_KEEP_ALIVE_INTERVAL}
                  - {code: VPNLINK_CONFIG_INVALID_KEEP_ALIVE_FAILURE_COUNT}
                  - {code: VPNLINK_CONFIG_REQUIRED}
                  - {code: WAN_INTERFACE_LABEL_MISMATCH}
                  - {code: WANINTERFACE_LABEL_USE_FOR_NON_HUB_PATHS_INVALID}
                  - {code: L3_REACHABILITY_MAX_PROBE_CONFIGS_LIMIT_EXCEEDED}
                  - {code: INVALID_PROBE_PROFILE}
                  - {code: WANINTERFACE_L3_REACHABILITY_OVERRIDING_PROBE_CONFIGS_FROM_LABEL_PROFILE}
                  - {code: L3_REACHABILITY_PROBE_CONFIGS_CANONT_BE_SET_ELEMENT_DEFAULT_CONFIGURED}
                  - {code: L3_REACHABILITY_PROBE_CONFIGS_REQUIRED_WHEN_ELEMENT_DEFAULT_NOT_USED}
                  - {code: PROBE_CONFIGS_CANNOT_BE_SPECIFIED_WITHOUT_PROBE_PROFILE}
                  - {code: WANINTERFACE_LABEL_PROBE_PROFILE_DOES_NOT_CONTAIN_PROBE_CONFIGS}
                  - {code: PROBE_CONFIG_VALID_L3_REACHABILITY_PROTOCOL_MISSING}
                  - {code: PROBE_CONFIG_VALID_L3_REACHABILITY_DIRECT_PATH_MISSING}
                  - {code: L3_REACHABILITY_PROBE_CONFIGS_REQUIRED}
                  - {code: L3_REACHABILITY_CAN_NOT_BE_UNSET_FROM_LABEL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update WAN Interface Label (v2.6)
      tags: [Network Constructs]
  /v3.2/api/tenants/{tenant_id}/lannetworks/query:
    post:
      description: Query the DB for site LAN networks that match query parameters.
      operationId: post-v3.2-api-tenants-tenant_id-lannetworks-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/LANNetworkQueryFilterResponse'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for Site LAN Networks (v3.2)
      tags: [Network Constructs]
  /v3.2/api/tenants/{tenant_id}/sites/{site_id}/lannetworks:
    get:
      description: List all the LAN networks.
      operationId: get-v3.2-api-tenants-tenant_id-sites-site_id-lannetworks
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/LANNetworkV3N2'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get LAN Networks (v3.2)
      tags: [Network Constructs]
    post:
      description: Create a new LAN network.
      operationId: post-v3.2-api-tenants-tenant_id-sites-site_id-lannetworks
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkScreenV3N2'}
        description: Details of the network to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_INPUT}
                  - {code: NO_LAN_NET_PREFIX}
                  - {code: ONLY_ONE_DEFAULT_ROUTER_SUPPORTED}
                  - {code: INVALID_NETWORK_CONTEXT}
                  - {code: PREFIX_EXACT_MATCH_WITH_OTHER_SITE}
                  - {code: INVALID_IPV4_PREFIX}
                  - {code: SAME_IPV4_PREFIX}
                  - {code: INVALID_IPADDRESS}
                  - {code: DEFAULT_ROUTER_PREFIX_MISMATCH}
                  - {code: SAME_IPV4_DEFAULT_ROUTER_ADDRESS}
                  - {code: LAN_NETWORK_ADD_FAILURE}
                  - {code: INVALID_IPV6_PREFIX}
                  - {code: NO_LAN_NET_V6_PREFIX}
                  - {code: DEFAULT_ROUTER_V6_EXCEEDS_LIMIT}
                  - {code: SAME_IPV6_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create LAN Network (v3.2)
      tags: [Network Constructs]
  /v3.2/api/tenants/{tenant_id}/sites/{site_id}/lannetworks/query:
    post:
      description: Query LAN networks that match query parameters.
      operationId: post-v3.2-api-tenants-tenant_id-sites-site_id-lannetworks-query
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/LANNetworkQueryFilterV3N2'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query LAN Networks (v3.2)
      tags: [Network Constructs]
  /v3.2/api/tenants/{tenant_id}/sites/{site_id}/lannetworks/{lannetwork_id}:
    delete:
      description: Delete an existing LAN network.
      operationId: delete-v3.2-api-tenants-tenant_id-sites-site_id-lannetworks-lannetwork_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the LAN network.
        in: path
        name: lannetwork_id
        required: true
        schema: {description: Lannetwork Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: LAN_NETWORK_IN_USE}
                  - {code: LANNETWORK_IS_USED_IN_SECURITY_ZONES}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete LAN Network (v3.2)
      tags: [Network Constructs]
    get:
      description: List a LAN network.
      operationId: get-v3.2-api-tenants-tenant_id-sites-site_id-lannetworks-lannetwork_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the LAN network.
        in: path
        name: lannetwork_id
        required: true
        schema: {description: Lannetwork Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N2'}
          description: Successful Operation
      summary: Get LAN Network (v3.2)
      tags: [Network Constructs]
    put:
      description: Update an existing LAN network.
      operationId: put-v3.2-api-tenants-tenant_id-sites-site_id-lannetworks-lannetwork_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the LAN network.
        in: path
        name: lannetwork_id
        required: true
        schema: {description: Lannetwork Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkScreenV3N2'}
        description: Details of the network to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: INVALID_JSON_INPUT}
                  - {code: NO_LAN_NET_PREFIX}
                  - {code: ONLY_ONE_DEFAULT_ROUTER_SUPPORTED}
                  - {code: INVALID_NETWORK_CONTEXT}
                  - {code: INVALID_IPV4_PREFIX}
                  - {code: SAME_IPV4_PREFIX}
                  - {code: INVALID_IPADDRESS}
                  - {code: DEFAULT_ROUTER_PREFIX_MISMATCH}
                  - {code: SAME_IPV4_DEFAULT_ROUTER_ADDRESS}
                  - {code: PREFIX_EXACT_MATCH_WITH_OTHER_SITE}
                  - {code: OVERLAPPING_LAN_NET_PREFIXES}
                  - {code: INVALID_IPV6_PREFIX}
                  - {code: NO_LAN_NET_V6_PREFIX}
                  - {code: DEFAULT_ROUTER_V6_EXCEEDS_LIMIT}
                  - {code: SAME_IPV6_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update LAN Network (v3.2)
      tags: [Network Constructs]
  /v3.3/api/tenants/{tenant_id}/lannetworks/query:
    post:
      description: Query the DB for site LAN networks that match query parameters.
      operationId: post-v3.3-api-tenants-tenant_id-lannetworks-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/LANNetworkQueryFilterResponse'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for Site LAN Networks (v3.3)
      tags: [Network Constructs]
  /v3.3/api/tenants/{tenant_id}/sites/{site_id}/lannetworks:
    get:
      description: List all the LAN networks.
      operationId: get-v3.3-api-tenants-tenant_id-sites-site_id-lannetworks
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/LANNetworkV3N3'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get LAN Networks (v3.3)
      tags: [Network Constructs]
    post:
      description: Create a new LAN network.
      operationId: post-v3.3-api-tenants-tenant_id-sites-site_id-lannetworks
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkScreenV3N3'}
        description: Details of the network to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N3'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_INPUT}
                  - {code: NO_LAN_NET_PREFIX}
                  - {code: ONLY_ONE_DEFAULT_ROUTER_SUPPORTED}
                  - {code: INVALID_NETWORK_CONTEXT}
                  - {code: PREFIX_EXACT_MATCH_WITH_OTHER_SITE}
                  - {code: INVALID_IPV4_PREFIX}
                  - {code: SAME_IPV4_PREFIX}
                  - {code: INVALID_IPADDRESS}
                  - {code: DEFAULT_ROUTER_PREFIX_MISMATCH}
                  - {code: SAME_IPV4_DEFAULT_ROUTER_ADDRESS}
                  - {code: LAN_NETWORK_ADD_FAILURE}
                  - {code: INVALID_IPV6_PREFIX}
                  - {code: NO_LAN_NET_V6_PREFIX}
                  - {code: DEFAULT_ROUTER_V6_EXCEEDS_LIMIT}
                  - {code: SAME_IPV6_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create LAN Network (v3.3)
      tags: [Network Constructs]
  /v3.3/api/tenants/{tenant_id}/sites/{site_id}/lannetworks/query:
    post:
      description: Query LAN networks that match query parameters.
      operationId: post-v3.3-api-tenants-tenant_id-sites-site_id-lannetworks-query
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/LANNetworkQueryFilterV3N3'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query LAN Networks (v3.3)
      tags: [Network Constructs]
  /v3.3/api/tenants/{tenant_id}/sites/{site_id}/lannetworks/{lannetwork_id}:
    delete:
      description: Delete an existing LAN network.
      operationId: delete-v3.3-api-tenants-tenant_id-sites-site_id-lannetworks-lannetwork_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the LAN network.
        in: path
        name: lannetwork_id
        required: true
        schema: {description: Lannetwork Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N3'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: LAN_NETWORK_IN_USE}
                  - {code: LANNETWORK_IS_USED_IN_SECURITY_ZONES}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete LAN Network (v3.3)
      tags: [Network Constructs]
    get:
      description: List a LAN network.
      operationId: get-v3.3-api-tenants-tenant_id-sites-site_id-lannetworks-lannetwork_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the LAN network.
        in: path
        name: lannetwork_id
        required: true
        schema: {description: Lannetwork Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N3'}
          description: Successful Operation
      summary: Get LAN Network (v3.3)
      tags: [Network Constructs]
    put:
      description: Update an existing LAN network.
      operationId: put-v3.3-api-tenants-tenant_id-sites-site_id-lannetworks-lannetwork_id
      parameters:
      - description: The ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the LAN network.
        in: path
        name: lannetwork_id
        required: true
        schema: {description: Lannetwork Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/LANNetworkScreenV3N3'}
        description: Details of the network to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/LANNetworkV3N3'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: INVALID_JSON_INPUT}
                  - {code: NO_LAN_NET_PREFIX}
                  - {code: ONLY_ONE_DEFAULT_ROUTER_SUPPORTED}
                  - {code: INVALID_NETWORK_CONTEXT}
                  - {code: INVALID_IPV4_PREFIX}
                  - {code: SAME_IPV4_PREFIX}
                  - {code: INVALID_IPADDRESS}
                  - {code: DEFAULT_ROUTER_PREFIX_MISMATCH}
                  - {code: SAME_IPV4_DEFAULT_ROUTER_ADDRESS}
                  - {code: PREFIX_EXACT_MATCH_WITH_OTHER_SITE}
                  - {code: OVERLAPPING_LAN_NET_PREFIXES}
                  - {code: INVALID_IPV6_PREFIX}
                  - {code: NO_LAN_NET_V6_PREFIX}
                  - {code: DEFAULT_ROUTER_V6_EXCEEDS_LIMIT}
                  - {code: SAME_IPV6_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update LAN Network (v3.3)
      tags: [Network Constructs]
servers:
- {url: 'https://api.us.cloudgenix.com'}
tags:
- {name: Network Constructs}
