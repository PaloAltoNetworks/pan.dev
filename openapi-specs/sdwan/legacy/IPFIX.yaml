components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items: {$ref: '#/components/schemas/AggregateData'}
          type: array
        field: {description: Field, type: string}
        name: {description: 'The name of the query response.

            ', type: string}
        operator:
          description: Operator
          enum: [max, count, min, avg, sum]
          type: string
        statistic: {description: 'QoS application aggregate statistics.

            ', type: string}
        unit: {description: 'The unit.

            ', type: string}
    AggregateData:
      properties:
        value: {description: 'Value of the aggregate data.

            ', type: number}
        view: {description: 'View the aggregate date.

            ', type: object}
    BulkResponse:
      properties:
        count: {description: 'The total number.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
        type: {description: "The type. \n", type: string}
    CollectorConfig:
      properties:
        host: {description: 'The IPv4 address or the FQDN Schema (domain name) of
            the IPFIX collector.

            ', type: string}
        host_port: {description: 'The port number that matches the port on which the
            collector is configured to receive IPFIX records.

            ', format: int32, type: integer}
        ipfixcollectorcontext_id: {description: 'The ID of the IPFIX collector context.
            The device uses the IP address of the interface to which the collector
            context is bound as the source interface to export IPFIX flow records.

            ', type: string}
        max_message_size: {description: Max Message Size, format: int32, type: integer}
        protocol:
          description: 'The protocol type of the collector''s config.

            '
          enum: [tcp, udp]
          type: string
    Error:
      properties:
        _error:
          items: {$ref: '#/components/schemas/ErrorResponse'}
          type: array
    ErrorResponse:
      properties:
        code: {description: 'The error code.

            ', type: string}
        message: {description: 'The error message.

            ', type: string}
    IPFixConfig:
      properties:
        collector_config:
          description: 'Collectors define the third-party applications which consume
            the exported flow records. Maximum 4 collectors can be configured.

            '
          items: {$ref: '#/components/schemas/CollectorConfig'}
          type: array
        description: {description: 'The description of the IPFIX profile configuration.

            '}
        export_cache_timeout: {description: 'Export Cache Timeout specifies the time
            for which the ION device should cache a new flow record before exporting
            it. Value must be within 10 and 600 seconds.The default value is 30 seconds.

            ', type: integer}
        filters:
          description: "The filters configured to select a subset of flows from all\
            \ the observed flows to export to a collector. Maximum of 8 filters per\
            \ IPFIX profile.  \n"
          items: {$ref: '#/components/schemas/IPFixFilter'}
          type: array
        id: {description: 'The ID.

            ', type: string}
        ipfixprofile_id: {description: 'The IPFIX profile configuration ID.

            ', type: string}
        ipfixtemplate_id: {description: 'The IPFIX template ID.

            ', type: string}
        name: {description: 'The IPFIX profile name.

            ', type: string}
        sampler: {$ref: '#/components/schemas/IPFixSampler', description: 'Subset
            of flows to export from all the observed flows. The device forwards this
            subset to the filtering process to perform further selection if filters
            are configured.

            '}
        tags:
          description: "A information field that can be added to identify the IPFIX\
            \ config. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [ipfixprofile_id, name]
    IPFixConfigQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of IPFIX configs that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number IPFIX configs deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the IPFIX configs for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "List the IPFIX configs by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    IPFixConfigQueryFilter:
      properties:
        collector_config:
          description: 'Collectors define the third-party applications which consume
            the exported flow records. Maximum 4 collectors can be configured.

            '
          items: {$ref: '#/components/schemas/CollectorConfig'}
          type: array
        description: {description: 'The description of the collector configuration.

            '}
        element_id: {description: 'The element ID.

            ', type: string}
        export_cache_timeout: {description: 'Export Cache Timeout specifies the time
            for which the ION device should cache a new flow record before exporting
            it. Value must be within 10 and 600 seconds.The default value is 30 seconds.

            ', type: integer}
        filters:
          description: "The filters configured to select a subset of flows from all\
            \ the observed flows to export to a collector. Maximum of 8 filters per\
            \ IPFIX profile.  \n"
          items: {$ref: '#/components/schemas/IPFixFilter'}
          type: array
        id: {description: 'The ID of the collector configuration.

            ', type: string}
        ipfixprofile_id: {description: 'The IPFIX configuration profile ID.

            ', type: string}
        ipfixtemplate_id: {description: 'The IPFIX template ID.

            ', type: string}
        name: {description: 'The IPFIX profile name.

            ', type: string}
        sampler: {$ref: '#/components/schemas/IPFixSampler', description: 'Subset
            of flows to export from all the observed flows. The device forwards this
            subset to the filtering process to perform further selection if filters
            are configured.

            '}
        site_id: {description: 'The site ID.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the IPFIX
            config. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [site_id, ipfixprofile_id, name]
    IPFixConfigScreen:
      properties:
        collector_config:
          description: 'Collectors define the third-party applications which consume
            the exported flow records. Maximum 4 collectors can be configured.

            '
          items: {$ref: '#/components/schemas/CollectorConfig'}
          type: array
        description: {description: 'The description of the IPFIX profile configuration.

            '}
        export_cache_timeout: {description: 'Export Cache Timeout specifies the time
            for which the ION device should cache a new flow record before exporting
            it. Value must be within 10 and 600 seconds.The default value is 30 seconds.

            ', type: integer}
        filters:
          description: "The filters configured to select a subset of flows from all\
            \ the observed flows to export to a collector. Maximum of 8 filters per\
            \ IPFIX profile.  \n"
          items: {$ref: '#/components/schemas/IPFixFilter'}
          type: array
        id: {description: 'The ID.

            ', type: string}
        ipfixprofile_id: {description: 'The IPFIX profile configuration ID.

            ', type: string}
        ipfixtemplate_id: {description: 'The IPFIX template ID.

            ', type: string}
        name: {description: 'The IPFIX profile name.

            ', type: string}
        sampler: {$ref: '#/components/schemas/IPFixSampler', description: 'Subset
            of flows to export from all the observed flows. The device forwards this
            subset to the filtering process to perform further selection if filters
            are configured.

            '}
        tags:
          description: 'A information field that can be added to identify the IPFIX
            config. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [ipfixprofile_id, name]
    IPFixContext:
      properties:
        description: {description: 'The description of the IPFIX context config. Max
            characters = 256.

            '}
        id: {description: 'The ID of the IPFIX context config.

            ', type: string}
        name: {description: 'The name of the IPFIX context config. Max characters
            = 128.

            ', type: string}
      required: [name]
    IPFixContextQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of IPFIX config contexts that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number IPFIX config contexts deleted for a
            time frame.

            ', type: boolean}
        group_by:
          description: 'Group the IPFIX config contexts for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the IPFIX config context by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    IPFixContextQueryFilter:
      properties:
        description: {description: 'The description of the IPFIX context config. Max
            characters = 256.

            '}
        id: {description: 'The ID of the IPFIX context config.

            ', type: string}
        name: {description: 'The name of the IPFIX context config. Max characters
            = 128.

            ', type: string}
      required: [name]
    IPFixContextScreen:
      properties:
        description: {description: 'The description of the IPFIX config. Max characters
            = 256.

            '}
        id: {description: 'The ID of the IPFIX config.

            ', type: string}
        name: {description: 'The name of the IPFIX config. Max characters = 128.

            ', type: string}
      required: [name]
    IPFixFilter:
      properties:
        app_def_ids:
          description: 'The IDs of the applications that filter flow records.

            '
          items: {description: App Def Ids, type: string}
          type: array
        dst_ports:
          description: 'The destination port ranges, if applicable, for TCP and UDP
            protocols.

            '
          items: {$ref: '#/components/schemas/Port'}
          type: array
        dst_prefixes_id: {description: 'The destination prefix ID.

            ', type: string}
        ipfixfiltercontext_ids:
          description: 'The ID of the IPFIX filter context that maps to an interface
            on the ION device.

            '
          items: {description: Ipfixfiltercontext Ids, type: string}
          type: array
        priority_traffic_types:
          description: 'The types of priority traffic for the IPFIX config.

            '
          items:
            description: Priority Traffic Types
            enum: [P1_Video, P1_Audio, P1_Xact, P1_Bulk, P2_Video, P2_Audio, P2_Xact,
              P2_Bulk, P3_Video, P3_Audio, P3_Xact, P3_Bulk, P4_Video, P4_Audio, P4_Xact,
              P4_Bulk]
            type: string
          type: array
        protocols:
          description: 'The possible protocols types for the IPFIX config.

            '
          items:
            description: Protocols
            enum: [tcp, udp, ospf, ipv6, is-is-over-ipv4, ipv6-nonxt, etherip, ipv6-icmp,
              igmp, udplite, ipv6-opts, icmp, esp, crtp, ipip, eigrp, egp, gre, l2tpv3,
              sctp, ip-in-ip, rsvp, pim, scps, mpls-in-ip, ah, vrrp, ipv6-route, igp,
              ipv6-frag]
            type: string
          type: array
        rtp_transport_type:
          description: 'The value of the RTP transport identifier is Prisma SD-WAN
            specific and is identified from the flow.

            '
          enum: [audio, video]
          type: string
        src_ports:
          description: 'The source port for the flow.

            '
          items: {$ref: '#/components/schemas/Port'}
          type: array
        src_prefixes_id: {description: 'The source prefix IDs.

            ', type: string}
        wan_path_direction:
          description: 'The diection of the WAN path.

            '
          enum: [lan_to_wan, wan_to_lan]
          type: string
    IPFixGlobalPrefix:
      properties:
        description: {description: 'The description of the IPFIX global prefix. Max
            characters = 256.

            '}
        id: {description: 'The ID of the IPFIX global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address or IP address subnets.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the IPFIX global prefix. Max characters =
            128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the IPFIX
            global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    IPFixGlobalPrefixScreen:
      properties:
        description: {description: 'The description of the IPFIX global prefix. Max
            characters = 256.

            '}
        id: {description: 'The ID of the IPFIX global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address or IP address subnets.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the IPFIX global prefix. Max characters =
            128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the IPFIX
            global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    IPFixLocalPrefix:
      properties:
        description: {description: 'The description of the IPFIX local prefix. Max
            characters = 256.

            '}
        id: {description: 'The ID of the IPFIX local prefix.

            ', type: string}
        name: {description: "The name of the IPFIX local prefix. Max characters =\
            \ 128.    \n", type: string}
        tags:
          description: "A information field that can be added to identify the IPFIX\
            \ local prefix. Maximum 10 unique tags of length 1024 each are allowed.\
            \ \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    IPFixLocalPrefixScreen:
      properties:
        description: {description: 'The description of the IPFIX local prefix. Max
            characters = 256.

            '}
        id: {description: 'The ID of the IPFIX local prefix.

            ', type: string}
        name: {description: "The name of the IPFIX local prefix. Max characters =\
            \ 128.    \n", type: string}
        tags:
          description: 'A information field that can be added to identify the IPFIX
            local prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    IPFixProfile:
      properties:
        collector_config:
          description: 'Collectors define the third-party applications which consume
            the exported flow records. Maximum 4 collectors can be configured.

            '
          items: {$ref: '#/components/schemas/CollectorConfig'}
          type: array
        description: {description: 'The description of the IPFIX profile configuration.

            '}
        export_cache_timeout: {description: 'Export Cache Timeout specifies the time
            for which the ION device should cache a new flow record before exporting
            it. Value must be within 10 and 600 seconds.The default value is 30 seconds.

            ', format: int32, type: integer}
        filters:
          description: "The filters configured to select a subset of flows from all\
            \ the observed flows to export to a collector. Maximum of 8 filters per\
            \ IPFIX profile.  \n"
          items: {$ref: '#/components/schemas/IPFixFilter'}
          type: array
        id: {description: 'The ID.

            ', type: string}
        ipfixtemplate_id: {description: 'The IPFIX template ID.

            ', type: string}
        name: {description: 'The IPFIX profile name.

            ', type: string}
        sampler: {$ref: '#/components/schemas/IPFixSampler', description: 'Subset
            of flows to export from all the observed flows. The device forwards this
            subset to the filtering process to perform further selection if filters
            are configured.

            '}
        tags:
          description: 'A information field that can be added to identify the IPFIX
            config. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [ipfixtemplate_id, collector_config, name]
    IPFixProfileQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of IPFIX profiles that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number IPFIX profiles deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the IPFIX profiles for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the IPFIX profiles by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    IPFixProfileScreen:
      properties:
        collector_config:
          description: 'Collectors define the third-party applications which consume
            the exported flow records. Maximum 4 collectors can be configured.

            '
          items: {$ref: '#/components/schemas/CollectorConfig'}
          type: array
        description: {description: 'The description of the IPFIX profile configuration.

            '}
        export_cache_timeout: {description: 'Export Cache Timeout specifies the time
            for which the ION device should cache a new flow record before exporting
            it. Value must be within 10 and 600 seconds.The default value is 30 seconds.

            ', format: int32, type: integer}
        filters:
          description: "The filters configured to select a subset of flows from all\
            \ the observed flows to export to a collector. Maximum of 8 filters per\
            \ IPFIX profile.  \n"
          items: {$ref: '#/components/schemas/IPFixFilter'}
          type: array
        id: {description: 'The ID.

            ', type: string}
        ipfixtemplate_id: {description: 'The IPFIX template ID.

            ', type: string}
        name: {description: 'The IPFIX profile name.

            ', type: string}
        sampler: {$ref: '#/components/schemas/IPFixSampler', description: 'Subset
            of flows to export from all the observed flows. The device forwards this
            subset to the filtering process to perform further selection if filters
            are configured.

            '}
        tags:
          description: "A information field that can be added to identify the IPFIX\
            \ config. Maximum 10 unique tags of length 1024 each are allowed.    \
            \     \n"
          items: {description: Tags, type: string}
          type: array
      required: [ipfixtemplate_id, collector_config, name]
    IPFixSampler:
      properties:
        algorithm:
          description: 'A time-based algorithm to configure the duration for sampling.

            '
          enum: [time_based, none]
          type: string
        time_interval: {description: 'The length of the sampling interval during which
            flows are selected. The default value is 5 ms.

            ', type: integer}
        time_spacing: {description: 'The spacing between the end of one sampling interval
            and the start of the next sampling interval. The default value is 5 ms.

            ', type: integer}
    IPFixTemplate:
      properties:
        description: {description: 'The IPFIX profile description when creating an
            IPFIX template.

            '}
        flow_fields:
          description: "The information elements exported by default in the flow record.\
            \  \n"
          items:
            description: Flow Fields
            enum: [INTERFACES, TIME_STAMPS, DST_IPV4_ADDRESS, DST_PORT, SRC_IPV4_ADDRESS,
              SRC_PORT, PROTOCOL, DSCP_MAP, DSCP_LAST, QOS_QUEUE, WAN_PATH, APP_DEF_ID,
              RTP_TRANSPORT_TYPE, TRANSPORT_TCP_WINDOWSIZE, CONNECTION_UNIFLOW_BYTES,
              CONNECTION_UNIFLOW_PACKETS, CONNECTION_BIFLOW_BYTES, CONNECTION_BIFLOW_PACKETS,
              CONNECTION_RTT, CONNECTION_NTT, CONNECTION_SRT, APPLICATION_HOST, CONNECTION_INIT,
              CONNECTION_XACT, CONNECTION_UDPTRT, MEDIA_CODEC, MEDIA_JITTER, MEDIA_LOSS,
              MEDIA_MOS, TROUBLESHOOT_TCP, TROUBLESHOOT_DECISION_MAP]
            type: string
          type: array
        generate_biflow: {description: 'Generate flow report in both directions for
            a flow at an observation point.

            ', type: boolean}
        id: {description: 'The ID of the IPFIX local prefix.

            ', type: string}
        name: {description: 'The name of the IPFIX template. Max size = 128.

            ', type: string}
        option_export_timeout: {description: 'Indicates how often the device sends
            flow templates to a collector. This is only applicable when the connection
            to a collector uses the UDP protocol. The default value is 600 seconds.

            ', format: int32, type: integer}
        options:
          description: 'When selected, Options allows export of additional information
            to the collector that would not be possible with Flow Fields alone. Maz
            size = 8.

            '
          items:
            description: Options
            enum: [APP_DEF_ID_TABLE, LINK_QUALITY_METRICS, WAN_PATH_ID_TABLE, TYPE_INFO_FOR_IPFIX_IE,
              DEVICE_IDENTIFICATION]
            type: string
          type: array
        tags:
          description: 'A information field that can be added to identify the IPFIX
            local prefix. Maximum 10 Unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        template_export_timeout: {description: "Indicates how often the device sends\
            \ the option record information to a collector. This is only applicable\
            \ when the connection to a collector uses the UDP protocol. The default\
            \ value is 600 seconds. \n", format: int32, type: integer}
      required: [options, flow_fields, name]
    IPFixTemplateScreen:
      properties:
        description: {description: 'The IPFIX profile description when creating an
            IPFIX template.

            '}
        flow_fields:
          description: "The information elements exported by default in the flow record.\
            \  \n"
          items:
            description: Flow Fields
            enum: [INTERFACES, TIME_STAMPS, DST_IPV4_ADDRESS, DST_PORT, SRC_IPV4_ADDRESS,
              SRC_PORT, PROTOCOL, DSCP_MAP, DSCP_LAST, QOS_QUEUE, WAN_PATH, APP_DEF_ID,
              RTP_TRANSPORT_TYPE, TRANSPORT_TCP_WINDOWSIZE, CONNECTION_UNIFLOW_BYTES,
              CONNECTION_UNIFLOW_PACKETS, CONNECTION_BIFLOW_BYTES, CONNECTION_BIFLOW_PACKETS,
              CONNECTION_RTT, CONNECTION_NTT, CONNECTION_SRT, APPLICATION_HOST, CONNECTION_INIT,
              CONNECTION_XACT, CONNECTION_UDPTRT, MEDIA_CODEC, MEDIA_JITTER, MEDIA_LOSS,
              MEDIA_MOS, TROUBLESHOOT_TCP, TROUBLESHOOT_DECISION_MAP]
            type: string
          type: array
        generate_biflow: {description: 'Generate flow report in both directions for
            a flow at an observation point.

            ', type: boolean}
        id: {description: 'The ID of the IPFIX local prefix.

            ', type: string}
        name: {description: 'The name of the IPFIX template. Max size = 128.

            ', type: string}
        option_export_timeout: {description: 'Indicates how often the device sends
            flow templates to a collector. This is only applicable when the connection
            to a collector uses the UDP protocol. The default value is 600 seconds.

            ', format: int32, type: integer}
        options:
          description: 'When selected, Options allows export of additional information
            to the collector that would not be possible with Flow Fields alone. Maz
            size = 8.

            '
          items:
            description: Options
            enum: [APP_DEF_ID_TABLE, LINK_QUALITY_METRICS, WAN_PATH_ID_TABLE, TYPE_INFO_FOR_IPFIX_IE,
              DEVICE_IDENTIFICATION]
            type: string
          type: array
        tags:
          description: 'A information field that can be added to identify the IPFIX
            local prefix. Maximum 10 Unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        template_export_timeout: {description: "Indicates how often the device sends\
            \ the option record information to a collector. This is only applicable\
            \ when the connection to a collector uses the UDP protocol. The default\
            \ value is 600 seconds. \n", format: int32, type: integer}
      required: [options, flow_fields, name]
    ListQueryResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        deleted_count: {description: 'The deleted number.

            ', format: int64, type: integer}
        deleted_ids:
          description: 'The deleted IDs.

            '
          items: {type: string}
          type: array
        description: {description: 'Description of the query. Max size = 256.

            '}
        id: {description: 'The ID.

            ', type: string}
        next_query: {description: 'Details of the next query.

            ', type: object}
        tenant_id: {description: 'The tenant ID.

            ', type: string}
        total_count: {description: 'Total number.

            ', format: int64, type: integer}
      type: object
    Port:
      properties:
        end: {description: 'The end port range must be between 1 and 65535.

            ', type: string}
        start: {description: 'The start port range must be between 1 and 65535.

            ', type: string}
    SiteIPFixPrefixAssociation:
      properties:
        id: {description: 'The ID of the IPFIX prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The prefix ID that associates with a site.   \n",
          type: string}
        tags:
          description: "A information field that can be added to identify the prefix.\
            \ Maximum 10 unique tags of length 1024 each are allowed.      \n"
          items: {description: Tags, type: string}
          type: array
    SiteIPFixPrefixAssociationScreen:
      properties:
        id: {description: 'The ID of the IPFIX site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        tags:
          description: 'An information field that can be added to identify the IPFIX
            site prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    SitePrefixAssociationQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of prefix associations that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of prefix associations deleted for a
            time frame.

            ', type: boolean}
        group_by:
          description: 'Group the prefix associations for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the prefix associations by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SitePrefixAssociationQueryFilter:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        site_id: {description: 'The site ID binding the prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
info:
  contact: {}
  description: List of APIs used to manage the IPFIX services.
  title: IPFIX
  version: Latest
openapi: 3.0.0
paths:
  /v2.0/api/tenants/{tenant_id}/ipfix/query:
    post:
      description: Queries the DB for limit number of IPFIX configs that match the
        query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ipfix-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixConfigQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/IPFixConfigQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query DB for IPFIX Configs (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixcollectorcontexts:
    get:
      description: List all the IPFix collector contexts.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixcollectorcontexts
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
      summary: Get IPFix Collector Context (v2.0)
      tags: [IPFIX]
    post:
      description: Create a IPFix collector context.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixcollectorcontexts
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixContextScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFix Collector Context (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixcollectorcontexts/query:
    post:
      description: Queries the DB for limit number of ipfix collector contexts that
        match the query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixcollectorcontexts-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixContextQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/IPFixContextQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query DB for IPFIX Collector Contexts (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixcollectorcontexts/{context_id}:
    delete:
      description: Delete a IPFix collector context.
      operationId: delete-v2.0-api-tenants-tenant_id-ipfixcollectorcontexts-context_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX collector context.
        in: path
        name: context_id
        required: true
        schema: {description: Context Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: IPFIX_COLLECTOR_CONTEXT_IS_USED_IN_IPFIX_PROFILE}
                  - {code: IPFIX_COLLECTOR_CONTEXT_IS_USED_IN_IPFIX_CONFIG}
                  - {code: IPFIX_COLLECTOR_CONTEXT_IS_USED_IN_INTERFACES}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete IPFix Collector Context (v2.0)
      tags: [IPFIX]
    get:
      description: List an IPFix collector context.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixcollectorcontexts-context_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX collector context.
        in: path
        name: context_id
        required: true
        schema: {description: Context Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
      summary: Get IPFix Collector Context (v2.0)
      tags: [IPFIX]
    put:
      description: Update an IPFIX collector context.
      operationId: put-v2.0-api-tenants-tenant_id-ipfixcollectorcontexts-context_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX collector context.
        in: path
        name: context_id
        required: true
        schema: {description: Context Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixContextScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Collector Context (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixfiltercontexts:
    get:
      description: List all the IPFIX filter contexts.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixfiltercontexts
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
      summary: Get IPFIX Filter Context (v2.0)
      tags: [IPFIX]
    post:
      description: Create a IPFIX filter context.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixfiltercontexts
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixContextScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFIX Filter Context (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixfiltercontexts/query:
    post:
      description: Queries the DB for limit number of IPFIX filter contexts that match
        the query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixfiltercontexts-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixContextQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/IPFixContextQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query DB for IPFIX Filter Contexts (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixfiltercontexts/{context_id}:
    delete:
      description: Delete an IPFIX filter context.
      operationId: delete-v2.0-api-tenants-tenant_id-ipfixfiltercontexts-context_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX filter context.
        in: path
        name: context_id
        required: true
        schema: {description: Context Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: IPFIX_FILTER_CONTEXT_IS_USED_IN_IPFIX_PROFILE}
                  - {code: IPFIX_FILTER_CONTEXT_IS_USED_IN_IPFIX_CONFIG}
                  - {code: IPFIX_FILTER_CONTEXT_IS_USED_IN_INTERFACES}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete IPFIX Filter Context (v2.0)
      tags: [IPFIX]
    get:
      description: List an IPFIX filter context.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixfiltercontexts-context_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX filter context.
        in: path
        name: context_id
        required: true
        schema: {description: Context Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
      summary: Get IPFIX Filter Context (v2.0)
      tags: [IPFIX]
    put:
      description: Update an IPFIX filter context.
      operationId: put-v2.0-api-tenants-tenant_id-ipfixfiltercontexts-context_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX filter context.
        in: path
        name: context_id
        required: true
        schema: {description: Context Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixContextScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixContext'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Filter Context (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixglobalprefixes:
    get:
      description: List all the global prefixes.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixglobalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixGlobalPrefix'}
          description: Successful Operation
      summary: Get IPFIX Global Prefix (v2.0)
      tags: [IPFIX]
    post:
      description: Create a IPFIX global prefix.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixglobalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixGlobalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: INVALID_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFIX Global Prefix (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixglobalprefixes/{prefix_id}:
    delete:
      description: Delete an IPFix global prefix.
      operationId: delete-v2.0-api-tenants-tenant_id-ipfixglobalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: IPFIX_PREFIX_IS_USED_IN_IPFIX_PROFILE}
                  - {code: IPFIX_PREFIX_IS_USED_IN_IPFIX_CONFIG}
                  - {code: ENTERPRISE_PREFIX_INVALID_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete IPFIX Global Prefix (v2.0)
      tags: [IPFIX]
    get:
      description: Get an IPFix global prefix.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixglobalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixGlobalPrefix'}
          description: Successful Operation
      summary: Get IPFIX Global Prefix (v2.0)
      tags: [IPFIX]
    put:
      description: Update an IPFix global prefix.
      operationId: put-v2.0-api-tenants-tenant_id-ipfixglobalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixGlobalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: INVALID_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Global Prefix (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixlocalprefixes:
    get:
      description: List all the local prefixes.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixLocalPrefix'}
          description: Successful Operation
      summary: Get IPFIX Local Prefix (v2.0)
      tags: [IPFIX]
    post:
      description: Create a IPFIX local prefix.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixLocalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFIX Local Prefix (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixlocalprefixes/query:
    post:
      description: Queries the DB for limit number of IPFIX site prefix associations
        that match the query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixlocalprefixes-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePrefixAssociationQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePrefixAssociationQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query DB for IPFIX Site Prefix Association (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixlocalprefixes/{prefix_id}:
    delete:
      description: Delete an IPFIX local prefix.
      operationId: delete-v2.0-api-tenants-tenant_id-ipfixlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_ASSOCIATED_WITH_SITE}
                  - {code: IPFIX_PREFIX_IS_USED_IN_IPFIX_PROFILE}
                  - {code: IPFIX_PREFIX_IS_USED_IN_IPFIX_CONFIG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete IPFIX Local Prefix (v2.0)
      tags: [IPFIX]
    get:
      description: Get an IPFIX local prefix.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixLocalPrefix'}
          description: Successful Operation
      summary: Get IPFIX Local Prefix (v2.0)
      tags: [IPFIX]
    put:
      description: Update an IPFIX local prefix.
      operationId: put-v2.0-api-tenants-tenant_id-ipfixlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixLocalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Local Prefix (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixprofiles:
    get:
      description: List all IPFix profiles.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixprofiles
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixProfile'}
          description: Successful Operation
      summary: Get IPFIX Profiles (v2.0)
      tags: [IPFIX]
    post:
      description: Create an IPFIX profile.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixprofiles
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixProfileScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: IPFIX_PROFILE_COLLECTOR_CONFIG_REQUIRED}
                  - {code: IPFIX_PROFILE_TEMPLATE_ID_REQUIRED}
                  - {code: IPFIX_COLLECTOR_INVALID_MAX_MESSAGE_SIZE}
                  - {code: IPFIX_COLLECTOR_INVALID_PROTOCOL}
                  - {code: IPFIX_COLLECTOR_HOST_REQUIRED}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_DUPLICATE_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_INTERVAL}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_SPACING}
                  - {code: IPFIX_FILTERS_MAX_SIZE}
                  - {code: IPFIX_INVALID_EXPORT_CACHE_TIMEOUT}
                  - {code: IPFIX_FILTER_INVALID_CONTEXT_IDS}
                  - {code: IPFIX_FILTER_DST_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_SRC_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_INVALID_WAN_PATH_DIRECTION}
                  - {code: IPFIX_FILTER_INVALID_APPDEF_IDS}
                  - {code: IPFIX_FILTER_INVALID_RTP_TRANSPORT_TYPE}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOLS}
                  - {code: IPFIX_FILTER_DUPLICATE_PROTOCOLS}
                  - {code: IPFIX_FILTER_INVALID_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_DUPLICATE_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOL_PORT}
                  - {code: IPFIX_FILTER_DUPLICATE_PORT_RANGE}
                  - {code: IPFIX_PORT_INVALID_VALUE}
                  - {code: IPFIX_INVALID_FILTER_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_COLLECTOR_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_APPDEF_IDS}
                  - {code: IPFIX_INVALID_PREFIX_IDS}
                  - {code: IPFIX_PORT_INVALID_RANGE}
                  - {code: IPFIX_INVALID_FILTER}
                  - {code: IPFIX_INVALID_TEMPLATE_ID}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_TIME_BASED_CONFIG}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_NONE_CONFIG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFIX Profile (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixprofiles/query:
    post:
      description: Queries the DB for limit number of IPFIX profiles that match the
        query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixprofiles-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixProfileQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteIPFixPrefixAssociation'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query DB for IPFIX Profiles (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixprofiles/{profile_id}:
    delete:
      description: Delete an IPFIX Profile.
      operationId: delete-v2.0-api-tenants-tenant_id-ipfixprofiles-profile_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: IPFIX_PROFILE_IS_USED_IN_IPFIX_CONFIG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete IPFIX Profile (v2.0)
      tags: [IPFIX]
    get:
      description: List an IPFIX profile.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixprofiles-profile_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixProfile'}
          description: Successful Operation
      summary: Get IPFIX Profile(v2.0)
      tags: [IPFIX]
    put:
      description: Update an IPFIX profile.
      operationId: put-v2.0-api-tenants-tenant_id-ipfixprofiles-profile_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX profile.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixProfileScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: IPFIX_PROFILE_COLLECTOR_CONFIG_REQUIRED}
                  - {code: IPFIX_PROFILE_TEMPLATE_ID_REQUIRED}
                  - {code: IPFIX_COLLECTOR_INVALID_MAX_MESSAGE_SIZE}
                  - {code: IPFIX_COLLECTOR_INVALID_PROTOCOL}
                  - {code: IPFIX_COLLECTOR_HOST_REQUIRED}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_DUPLICATE_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_INTERVAL}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_SPACING}
                  - {code: IPFIX_FILTERS_MAX_SIZE}
                  - {code: IPFIX_INVALID_EXPORT_CACHE_TIMEOUT}
                  - {code: IPFIX_FILTER_INVALID_CONTEXT_IDS}
                  - {code: IPFIX_FILTER_DST_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_SRC_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_INVALID_WAN_PATH_DIRECTION}
                  - {code: IPFIX_FILTER_INVALID_APPDEF_IDS}
                  - {code: IPFIX_FILTER_INVALID_RTP_TRANSPORT_TYPE}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOLS}
                  - {code: IPFIX_FILTER_DUPLICATE_PROTOCOLS}
                  - {code: IPFIX_FILTER_INVALID_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_DUPLICATE_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOL_PORT}
                  - {code: IPFIX_FILTER_DUPLICATE_PORT_RANGE}
                  - {code: IPFIX_PORT_INVALID_VALUE}
                  - {code: IPFIX_INVALID_FILTER_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_COLLECTOR_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_APPDEF_IDS}
                  - {code: IPFIX_INVALID_PREFIX_IDS}
                  - {code: IPFIX_PORT_INVALID_RANGE}
                  - {code: IPFIX_INVALID_FILTER}
                  - {code: IPFIX_INVALID_TEMPLATE_ID}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_TIME_BASED_CONFIG}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_NONE_CONFIG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Profile(v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixtemplates:
    get:
      description: List all IPFix templates.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixtemplates
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixTemplate'}
          description: Successful Operation
      summary: Get IPFIX Templates (v2.0)
      tags: [IPFIX]
    post:
      description: Create an IPFIX template.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixtemplates
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixTemplateScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixTemplate'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: IPFIX_TEMPLATE_INVALID_FLOW_FIELDS}
                  - {code: IPFIX_TEMPLATE_FLOW_FIELDS_REQD}
                  - {code: IPFIX_TEMPLATE_INVALID_OPTIONS}
                  - {code: IPFIX_TEMPLATE_INVALID_TEMPLATE_EXPORT_TIMEOUT}
                  - {code: IPFIX_TEMPLATE_INVALID_OPTION_EXPORT_TIMEOUT}
                  - {code: IPFIX_TEMPLATE_DUPLICATE_FLOW_FIELDS}
                  - {code: IPFIX_TEMPLATE_DUPLICATE_OPTIONS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFIX Template (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixtemplates/query:
    post:
      description: Queries the DB for limit number of IPFIX templates that match the
        query parameters.
      operationId: post-v2.0-api-tenants-tenant_id-ipfixtemplates-query
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixTemplateScreen'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteIPFixPrefixAssociation'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query DB for IPFIX Templates (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/ipfixtemplates/{template_id}:
    delete:
      description: Delete an IPFIX template.
      operationId: delete-v2.0-api-tenants-tenant_id-ipfixtemplates-template_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX template.
        in: path
        name: template_id
        required: true
        schema: {description: Template Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixTemplate'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: IPFIX_TEMPLATE_IS_USED_IN_IPFIX_CONFIG}
                  - {code: IPFIX_TEMPLATE_IS_USED_IN_IPFIX_PROFILE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete IPFIX Template (v2.0)
      tags: [IPFIX]
    get:
      description: List an IPFIX template.
      operationId: get-v2.0-api-tenants-tenant_id-ipfixtemplates-template_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX template.
        in: path
        name: template_id
        required: true
        schema: {description: Template Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixTemplate'}
          description: Successful Operation
      summary: Get IPFIX Template (v2.0)
      tags: [IPFIX]
    put:
      description: Update an IPFIX template.
      operationId: put-v2.0-api-tenants-tenant_id-ipfixtemplates-template_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the IPFIX template.
        in: path
        name: template_id
        required: true
        schema: {description: Template Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixTemplateScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixTemplate'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: IPFIX_TEMPLATE_INVALID_FLOW_FIELDS}
                  - {code: IPFIX_TEMPLATE_FLOW_FIELDS_REQD}
                  - {code: IPFIX_TEMPLATE_INVALID_OPTIONS}
                  - {code: IPFIX_TEMPLATE_INVALID_TEMPLATE_EXPORT_TIMEOUT}
                  - {code: IPFIX_TEMPLATE_INVALID_OPTION_EXPORT_TIMEOUT}
                  - {code: IPFIX_TEMPLATE_DUPLICATE_FLOW_FIELDS}
                  - {code: IPFIX_TEMPLATE_DUPLICATE_OPTIONS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Template (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ipfix:
    get:
      description: List all IPFIX configs.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ipfix
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixConfig'}
          description: Successful Operation
      summary: Get IPFIX Configs (v2.0)
      tags: [IPFIX]
    post:
      description: Create an IPFIX config.
      operationId: post-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ipfix
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixConfigScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixConfig'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: IPFIX_COLLECTOR_INVALID_MAX_MESSAGE_SIZE}
                  - {code: IPFIX_COLLECTOR_INVALID_PROTOCOL}
                  - {code: IPFIX_COLLECTOR_HOST_REQUIRED}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_DUPLICATE_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_INTERVAL}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_SPACING}
                  - {code: IPFIX_FILTERS_MAX_SIZE}
                  - {code: IPFIX_INVALID_EXPORT_CACHE_TIMEOUT}
                  - {code: IPFIX_FILTER_INVALID_CONTEXT_IDS}
                  - {code: IPFIX_FILTER_DST_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_SRC_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_INVALID_WAN_PATH_DIRECTION}
                  - {code: IPFIX_FILTER_INVALID_APPDEF_IDS}
                  - {code: IPFIX_FILTER_INVALID_RTP_TRANSPORT_TYPE}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOLS}
                  - {code: IPFIX_FILTER_DUPLICATE_PROTOCOLS}
                  - {code: IPFIX_FILTER_INVALID_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_DUPLICATE_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOL_PORT}
                  - {code: IPFIX_FILTER_DUPLICATE_PORT_RANGE}
                  - {code: IPFIX_PORT_INVALID_VALUE}
                  - {code: IPFIX_INVALID_FILTER_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_COLLECTOR_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_APPDEF_IDS}
                  - {code: IPFIX_INVALID_PREFIX_IDS}
                  - {code: IPFIX_PORT_INVALID_RANGE}
                  - {code: IPFIX_INVALID_FILTER}
                  - {code: IPFIX_INVALID_TEMPLATE_ID}
                  - {code: IPFIX_CONFIG_INVALID_COLLECTOR_CONFIG}
                  - {code: IPFIX_CONFIG_PROFILE_ID_REQUIRED}
                  - {code: IPFIX_INVALID_PROFILE_ID}
                  - {code: IPFIX_CONFIG_MAX_EXCEEDED}
                  - {code: IPFIX_CONFIG_UNSUPPORTED_SWVERSION}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_TIME_BASED_CONFIG}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_NONE_CONFIG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFIX Config (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/elements/{element_id}/ipfix/{ipfix_id}:
    delete:
      description: Delete the IPFIX config.
      operationId: delete-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ipfix-ipfix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      - description: ID of the IPFIX.
        in: path
        name: ipfix_id
        required: true
        schema: {description: Ipfix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixConfig'}
          description: Successful Operation
      summary: Delete IPFIX Config (v2.0)
      tags: [IPFIX]
    get:
      description: List an IPFIX config.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ipfix-ipfix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      - description: ID of the IPFIX.
        in: path
        name: ipfix_id
        required: true
        schema: {description: Ipfix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixConfig'}
          description: Successful Operation
      summary: Get IPFIX Config (v2.0)
      tags: [IPFIX]
    put:
      description: Update an IPFIX config.
      operationId: put-v2.0-api-tenants-tenant_id-sites-site_id-elements-element_id-ipfix-ipfix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      - description: ID of the IPFIX.
        in: path
        name: ipfix_id
        required: true
        schema: {description: Ipfix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/IPFixConfigScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/IPFixConfig'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: IPFIX_COLLECTOR_INVALID_MAX_MESSAGE_SIZE}
                  - {code: IPFIX_COLLECTOR_INVALID_PROTOCOL}
                  - {code: IPFIX_COLLECTOR_HOST_REQUIRED}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_DUPLICATE_HOST_PORT}
                  - {code: IPFIX_COLLECTOR_INVALID_HOST}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_INTERVAL}
                  - {code: IPFIX_SAMPLER_INVALID_TIME_SPACING}
                  - {code: IPFIX_FILTERS_MAX_SIZE}
                  - {code: IPFIX_INVALID_EXPORT_CACHE_TIMEOUT}
                  - {code: IPFIX_FILTER_INVALID_CONTEXT_IDS}
                  - {code: IPFIX_FILTER_DST_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_SRC_PORTS_MAX_SIZE_EXCEED}
                  - {code: IPFIX_FILTER_INVALID_WAN_PATH_DIRECTION}
                  - {code: IPFIX_FILTER_INVALID_APPDEF_IDS}
                  - {code: IPFIX_FILTER_INVALID_RTP_TRANSPORT_TYPE}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOLS}
                  - {code: IPFIX_FILTER_DUPLICATE_PROTOCOLS}
                  - {code: IPFIX_FILTER_INVALID_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_DUPLICATE_PRIORITY_TRAFFIC_TYPES}
                  - {code: IPFIX_FILTER_INVALID_PROTOCOL_PORT}
                  - {code: IPFIX_FILTER_DUPLICATE_PORT_RANGE}
                  - {code: IPFIX_PORT_INVALID_VALUE}
                  - {code: IPFIX_INVALID_FILTER_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_COLLECTOR_CONTEXT_IDS}
                  - {code: IPFIX_INVALID_APPDEF_IDS}
                  - {code: IPFIX_INVALID_PREFIX_IDS}
                  - {code: IPFIX_PORT_INVALID_RANGE}
                  - {code: IPFIX_INVALID_FILTER}
                  - {code: IPFIX_INVALID_TEMPLATE_ID}
                  - {code: IPFIX_CONFIG_INVALID_COLLECTOR_CONFIG}
                  - {code: IPFIX_CONFIG_PROFILE_ID_REQUIRED}
                  - {code: IPFIX_INVALID_PROFILE_ID}
                  - {code: IPFIX_CONFIG_MAX_EXCEEDED}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_TIME_BASED_CONFIG}
                  - {code: IPFIX_SAMPLER_INVALID_ALGORITHM_NONE_CONFIG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Config (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/ipfixlocalprefixes:
    get:
      description: List all the IPFix site prefix associations.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-ipfixlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/BulkResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteIPFixPrefixAssociation'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get IPFix Site Prefix Association (v2.0)
      tags: [IPFIX]
    post:
      description: Create an IPFix site prefix association.
      operationId: post-v2.0-api-tenants-tenant_id-sites-site_id-ipfixlocalprefixes
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteIPFixPrefixAssociationScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteIPFixPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: SITE_NOT_FOUND}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
                  - {code: INVALID_IPV4_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create IPFix Site Prefix Association (v2.0)
      tags: [IPFIX]
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/ipfixlocalprefixes/{prefix_id}:
    delete:
      description: Delete an IPFIX site prefix association.
      operationId: delete-v2.0-api-tenants-tenant_id-sites-site_id-ipfixlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteIPFixPrefixAssociation'}
          description: Successful Operation
      summary: Delete IPFIX Site Prefix Association (v2.0)
      tags: [IPFIX]
    get:
      description: List the IPFIX site prefix association.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-ipfixlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteIPFixPrefixAssociation'}
          description: Successful Operation
      summary: Get IPFIX Site Prefix Association (v2.0)
      tags: [IPFIX]
    put:
      description: Update the IPFIX site prefix association.
      operationId: put-v2.0-api-tenants-tenant_id-sites-site_id-ipfixlocalprefixes-prefix_id
      parameters:
      - description: ID of the tenant.
        in: path
        name: tenant_id
        required: true
        schema: {description: Tenant Id, type: string}
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: ID of the prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteIPFixPrefixAssociationScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteIPFixPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: SITE_NOT_FOUND}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
                  - {code: INVALID_IPV4_PREFIX}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update IPFIX Site Prefix Association (v2.0)
      tags: [IPFIX]
servers:
- {url: 'https://api.us.cloudgenix.com'}
tags:
- {name: IPFIX}
