components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items:
            $ref: '#/components/schemas/AggregateData'
          type: array
        field:
          description: Field
          type: string
        name:
          description: 'The name of the query response.

            '
          type: string
        operator:
          description: Operator
          enum:
          - max
          - count
          - min
          - avg
          - sum
          type: string
        statistic:
          description: 'QoS application aggregate statistics.

            '
          type: string
        unit:
          description: 'The unit.

            '
          type: string
    AggregateData:
      properties:
        value:
          description: 'Value of the aggregate data.

            '
          type: number
        view:
          description: 'View the aggregate date.

            '
          type: object
    BasePolicyRule:
      properties:
        description:
          description: 'The description of the policy rule. Max size = 256.

            '
        disabled:
          description: "Lists if the policy rules are disabled. \n"
          type: boolean
        disabled_reason:
          description: 'Reason the policy rule is disabled.

            '
          type: string
        id:
          description: 'The policy rule ID.

            '
          type: string
        inactive:
          description: 'Lists if the policy rule is inactive.

            '
          type: boolean
        inactive_reason:
          description: 'Reason the policy rule is inactive.

            '
          type: string
        name:
          description: 'The name of the policy rule.

            '
          type: string
        policyset_id:
          description: 'The ID of the policy set.

            '
          type: string
        region:
          description: 'The region of the site.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the policy
            rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        tenant_id:
          description: 'Tenant Id: NotNull Digits(fraction = 0, integer = 20, TENANT_ID_INVALID_0001) '
          type: string
      required:
      - name
      - tenant_id
    BasePolicySet:
      properties:
        clone_from:
          description: 'The existing policy from Network Policies for cloning the
            policy set.

            '
          type: string
        description:
          description: 'The description of the network policy set. Max size = 256.

            '
        disabled:
          description: "Lists if the network policy sets are disabled. \n"
          type: boolean
        disabled_reason:
          description: 'Reason the network policy set is disabled.

            '
          type: string
        id:
          description: 'The network policy set ID.

            '
          type: string
        inactive:
          description: 'Lists if the network policy set is inactive.

            '
          type: boolean
        inactive_reason:
          description: 'Reason the network policy set is inactive.

            '
          type: string
        name:
          description: 'The name of the network policy set.

            '
          type: string
        policy_rules:
          description: "Lists the details of the policy rules. \n"
          items:
            $ref: '#/components/schemas/BasePolicyRule'
          type: array
        region:
          description: 'The region of the site.

            '
          type: string
        send_to_element:
          description: 'Send to element.

            '
          type: boolean
        tags:
          description: 'A information field that can be added to identify the network
            policy set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        tenant_id:
          description: 'Tenant Id: NotNull Digits(fraction = 0, integer = 20, TENANT_ID_INVALID_0001) '
          type: string
      required:
      - name
      - tenant_id
    BestPathConfig:
      properties:
        metric:
          description: 'Metric: Required(error = METRIC_PARAMETER_MISSING: Metric
            parameter is required) ValidateEnum(enumClass = classOf[LQMMetricType],
            error = NETWORK_POLICY_INVALID_PROBE_METRIC: Probe metric is invalid.
            It should be latency, jitter, packet_loss, init_failure_pct or dns_txn_failure_pct,
            nullAllowed = false) '
          enum:
          - LATENCY
          - JITTER
          - PACKET_LOSS
          - INIT_FAILURE_PCT
          - DNS_TXN_FAILURE_PCT
          type: string
        metric_type:
          description: 'Metric Type: Required(error = METRIC_TYPE_MISSING: Metric
            type is required) ValidateEnum(enumClass = classOf[ProbeMetricType], error
            = NETWORK_POLICY_INVALID_PROBE_METRIC_TYPE: Probe metric type is invalid.
            It should be lqm or probe, nullAllowed = false) '
          enum:
          - PROBE
          - LQM
          type: string
        probe_config_id:
          description: Probe Config Id
          type: string
      required:
      - metric_type
      - metric
    Error:
      properties:
        _error:
          items:
            $ref: '#/components/schemas/ErrorResponse'
          type: array
    ErrorResponse:
      properties:
        code:
          description: 'The error code.

            '
          type: string
        message:
          description: 'The error message.

            '
          type: string
    GlobalPrefixQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of global prefixes that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number global prefixes deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the global prefixs for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "Lists the global prefixes by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    GlobalPrefixQueryFilter:
      properties:
        description:
          description: 'The description of the security policy global prefix. Max
            size = 256.

            '
        id:
          description: 'The ID of the security policy global prefix.

            '
          type: string
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy global prefix.

            '
          items:
            description: Ipv4 Prefixes
            type: string
          type: array
        name:
          description: 'The name of the security policy global prefix. Max size =
            128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the security
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    ListQueryResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        deleted_count:
          description: 'The deleted number.

            '
          format: int64
          type: integer
        deleted_ids:
          description: 'The deleted IDs.

            '
          items:
            type: string
          type: array
        description:
          description: 'Description of the query. Max size = 256.

            '
        id:
          description: 'The ID.

            '
          type: string
        next_query:
          description: 'Details of the next query.

            '
          type: object
        tenant_id:
          description: 'The tenant ID.

            '
          type: string
        total_count:
          description: 'Total number.

            '
          format: int64
          type: integer
      type: object
    ListResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        id:
          description: 'The ID.

            '
          type: string
        tenant_id:
          description: 'The tenant ID.

            '
          type: string
      type: object
    NetworkPolicyGlobalPrefix:
      properties:
        description:
          description: 'The description of the network policy global prefix. Max size
            = 256.

            '
        id:
          description: 'The ID of the network policy global prefix.

            '
          type: string
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the network policy global prefix.

            '
          items:
            description: Ipv4 Prefixes
            type: string
          type: array
        name:
          description: 'The name of the network policy global prefix.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    NetworkPolicyGlobalPrefixV2N1:
      properties:
        description:
          description: 'The description of the network policy global prefix. Max size
            = 256.

            '
        id:
          description: 'The ID of the network policy global prefix.

            '
          type: string
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the network policy global prefix.

            '
          items:
            description: Ipv4 Prefixes
            type: string
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 addresses of the network policy global prefix.

            '
          items:
            description: Ipv6 Prefixes
            type: string
          type: array
        name:
          description: 'The name of the network policy global prefix.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    NetworkPolicyLocalPrefix:
      properties:
        description:
          description: 'The description of the network policy local prefix. Max size
            = 256.

            '
        id:
          description: 'The ID of the network policy local prefix.

            '
          type: string
        name:
          description: 'The name of the network policy local prefix.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy local prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    NetworkPolicyRule:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        destination_prefixes_id:
          description: 'The destination prefix ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. \
            \   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: "The network policy rule order number. The default order number\
            \ for a policy rule is 1024. \n"
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowed'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - paths_allowed
      - name
    NetworkPolicyRuleQueryFilterV2N3:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        best_path_config:
          $ref: '#/components/schemas/ProbeMetricInfo'
          description: 'Information on the best path configuration values.

            '
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        destination_prefixes_id:
          description: 'The destination prefix ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. \
            \   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: 'The network policy rule order number. The default order number
            for a policy rule is 1024.

            '
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowedV2N1'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        user_or_group:
          $ref: '#/components/schemas/UserGroup'
          description: 'The details of the user or group.

            '
      required:
      - paths_allowed
      - name
    NetworkPolicyRuleQueryFilterV2N4:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        best_path_config:
          $ref: '#/components/schemas/ProbeMetricInfo'
          description: 'Information on the best path configuration values.

            '
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items:
            description: Dest Device Ids
            type: string
          type: array
        destination_prefixes_id:
          description: 'The destination prefixes ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. True\
            \ or False.   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: 'The network policy rule order number. The default order number
            for a policy rule is 1024.

            '
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowedV2N1'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        src_device_ids:
          description: 'The ID of the src device.

            '
          items:
            description: Src Device Ids
            type: string
          type: array
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        user_or_group:
          $ref: '#/components/schemas/UserGroup'
          description: 'The details of the user or group.

            '
      required:
      - paths_allowed
      - name
    NetworkPolicyRuleScreenV2N3:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        best_path_config:
          $ref: '#/components/schemas/BestPathConfig'
          description: 'Information on the best path configuration values.

            '
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        destination_prefixes_id:
          description: 'The destination prefix ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. \
            \   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: "The network policy rule order number. The default order number\
            \ for a policy rule is 1024. \n"
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowedV2N1'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        user_or_group:
          $ref: '#/components/schemas/UserGroup'
          description: 'The details of the user or group.

            '
      required:
      - paths_allowed
      - name
    NetworkPolicyRuleScreenV2N4:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        best_path_config:
          $ref: '#/components/schemas/BestPathConfig'
          description: 'Information on the best path configuration values.

            '
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items:
            description: Dest Device Ids
            type: string
          type: array
        destination_prefixes_id:
          description: 'The destination prefixes ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. True\
            \ or False.   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: 'The network policy rule order number. The default order number
            for a policy rule is 1024.

            '
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowedV2N1'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        src_device_ids:
          description: 'The ID of the src device.

            '
          items:
            description: Src Device Ids
            type: string
          type: array
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        user_or_group:
          $ref: '#/components/schemas/UserGroup'
          description: "The details of the user or group. \n"
      required:
      - paths_allowed
      - name
    NetworkPolicyRuleV2N2:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        destination_prefixes_id:
          description: 'The destination prefix ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. \
            \   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: "The network policy rule order number. The default order number\
            \ for a policy rule is 1024. \n"
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowedV2N1'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        user_or_group:
          $ref: '#/components/schemas/UserGroup'
          description: 'The details of the user or group.

            '
      required:
      - paths_allowed
      - name
    NetworkPolicyRuleV2N3:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        best_path_config:
          $ref: '#/components/schemas/ProbeMetricInfo'
          description: 'Information on the best path configuration values.

            '
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        destination_prefixes_id:
          description: 'The destination prefix ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. \
            \   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: "The network policy rule order number. The default order number\
            \ for a policy rule is 1024. \n"
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowedV2N1'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        user_or_group:
          $ref: '#/components/schemas/UserGroup'
          description: 'The details of the user or group.

            '
      required:
      - paths_allowed
      - name
    NetworkPolicyRuleV2N4:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied network policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items:
            description: App Def Ids
            type: string
          type: array
        best_path_config:
          $ref: '#/components/schemas/ProbeMetricInfo'
          description: 'Information on the best path configuration values.

            '
        description:
          description: 'The description of the network policy rule. Max size = 256.

            '
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items:
            description: Dest Device Ids
            type: string
          type: array
        destination_prefixes_id:
          description: 'The destination prefixes ID.

            '
          type: string
        enabled:
          description: "The network policy rule based on rules that are enabled. True\
            \ or False.   \n"
          type: boolean
        id:
          description: 'The network policy rule ID.

            '
          type: string
        name:
          description: 'The name of the network policy rule. Max size = 128.

            '
          type: string
        network_context_id:
          description: "The  network context applied to the network policy rule. Network\
            \ context segments network traffic for the purpose of applying different\
            \ network policy rules for the same application. \n"
          type: string
        order_number:
          description: 'The network policy rule order number. The default order number
            for a policy rule is 1024.

            '
          type: integer
        paths_allowed:
          $ref: '#/components/schemas/PathsAllowedV2N1'
          description: 'The paths allowed for the application.

            '
        service_context:
          $ref: '#/components/schemas/ServiceContext'
          description: 'Information on the service context.

            '
        source_prefixes_id:
          description: 'The ID of the source prefixes.

            '
          type: string
        src_device_ids:
          description: 'The ID of the src device.

            '
          items:
            description: Src Device Ids
            type: string
          type: array
        tags:
          description: 'A information field that can be added to identify the network
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        user_or_group:
          $ref: '#/components/schemas/UserGroup'
          description: 'The details of the user or group.

            '
      required:
      - paths_allowed
      - name
    NetworkPolicySet:
      properties:
        clone_from:
          description: 'The existing policy from Network Policies for cloning the
            policy set.

            '
          type: string
        defaultrule_policyset:
          description: 'The default rule for the policy set.

            '
          type: boolean
        description:
          description: 'The description of the network policy set. Max size = 256.

            '
        disabled:
          description: "Lists if the network policy sets are disabled. \n"
          type: boolean
        disabled_reason:
          description: 'Reason the network policy set is disabled.

            '
          type: string
        id:
          description: 'The network policy set ID.

            '
          type: string
        inactive:
          description: 'Lists if the network policy set is inactive.

            '
          type: boolean
        inactive_reason:
          description: 'Reason the network policy set is inactive.

            '
          type: string
        name:
          description: 'The name of the network policy set.

            '
          type: string
        policy_req_version:
          description: 'The required version of the network policy.

            '
          type: string
        policy_rules:
          description: "Lists the details of the policy rules. \n"
          items:
            $ref: '#/components/schemas/NetworkPolicyRule'
          type: array
        region:
          description: 'The region of the site.

            '
          type: string
        send_to_element:
          description: 'Send to element.

            '
          type: boolean
        tags:
          description: 'A information field that can be added to identify the network
            policy set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        tenant_id:
          description: 'The tenant ID.

            '
          type: string
      required:
      - name
      - tenant_id
    NetworkPolicySetQueryFilter:
      properties:
        clone_from:
          description: 'The existing policy from Network Policies for cloning the
            policy set.

            '
          type: string
        defaultrule_policyset:
          description: 'The default rule for the policy set.

            '
          type: boolean
        description:
          description: 'The description of the network policy set. Max size = 256.

            '
        id:
          description: 'The network policy set ID.

            '
          type: string
        name:
          description: 'The name of the network policy set.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the network
            policy set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    NetworkPolicySetStack:
      properties:
        default_policysetstack:
          description: 'The default policy set stack.

            '
          type: boolean
        defaultrule_policyset:
          $ref: '#/components/schemas/NetworkPolicySet'
          description: 'The default rule of the policy set.

            '
        defaultrule_policyset_id:
          description: 'The default rule of the policy set ID.

            '
          type: string
        description:
          description: "The description of the network policy set stack. Max size\
            \ = 256. \n"
        disabled:
          description: "Lists if the network policy set stacks are disabled. \n"
          type: boolean
        disabled_reason:
          description: 'Reason the network policy set stack is disabled.

            '
          type: string
        id:
          description: 'The network policy set stack ID.

            '
          type: string
        inactive:
          description: 'Lists if the network policy set stack is inactive.

            '
          type: boolean
        inactive_reason:
          description: 'Reason the network policy set stack is inactive.

            '
          type: string
        legacy_policystack:
          description: 'The legacy policy stack.

            '
          type: boolean
        name:
          description: "The name of the policy set stack. Max size = 128.    \n"
          type: string
        policyset_ids:
          description: 'The ID of the policy set.

            '
          items:
            description: Policyset Ids
            type: string
          type: array
        policyset_ids_update:
          description: Policyset Ids Update
          type: boolean
        policysets:
          description: 'The details of the policy sets.

            '
          items:
            $ref: '#/components/schemas/BasePolicySet'
          type: array
        region:
          description: 'The region of the site.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the policy
            set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        tenant_id:
          description: 'The tenant ID.

            '
          type: string
      required:
      - defaultrule_policyset_id
      - name
      - tenant_id
    NetworkPolicySetStackQueryFilter:
      properties:
        default_policysetstack:
          description: 'The default policy set stack.

            '
          type: boolean
        defaultrule_policyset_id:
          description: 'The default rule of the policy set ID.

            '
          type: string
        description:
          description: "The description of the network policy set stack. Max size\
            \ = 256. \n"
        id:
          description: 'The network policy set stack ID.

            '
          type: string
        name:
          description: 'The name of the network policy set stack.

            '
          type: string
        policyset_ids:
          description: Policyset Ids
          items:
            description: Policyset Ids
            type: string
          type: array
        tags:
          description: 'A information field that can be added to identify the network
            policy set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
    NetworkPolicySetState:
      properties:
        id:
          description: 'The ID of the policy set.

            '
          type: string
        policy_req_state:
          description: 'The policy required state.

            '
          enum:
          - POLICY_REQ_PENDING
          - POLICY_CREATE_INPROGRESS
          - POLICY_CREATE_COMPLETE
          - POLICY_REPLICATE
          - POLICY_CREATE_FAILED
          - POLICY_REQ_COMPLETE
          type: string
        policyset_id:
          description: 'The ID of the policy set.

            '
          type: string
    PathsAllowed:
      properties:
        active_paths:
          description: 'The configured active path for the rule.

            '
          items:
            $ref: '#/components/schemas/WANPath'
          type: array
        backup_paths:
          description: 'The configured backup path for the rule.

            '
          items:
            $ref: '#/components/schemas/WANPath'
          type: array
        l3_failure_paths:
          description: "The configured l3 failure path for the rule. \n"
          items:
            $ref: '#/components/schemas/WANPath'
          type: array
      required:
      - active_paths
    PathsAllowedV2N1:
      properties:
        active_paths:
          description: 'The configured active path for the rule.

            '
          items:
            $ref: '#/components/schemas/WANPath'
          type: array
        backup_paths:
          description: 'The configured backup path for the rule.

            '
          items:
            $ref: '#/components/schemas/WANPath'
          type: array
        l3_failure_paths:
          description: 'The configured l3 failure path for the rule.

            '
          items:
            $ref: '#/components/schemas/WANPath'
          type: array
    PolicySetStackQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of policy set stacks that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number of policy set stacks deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the policy set stacks for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the policy set stacks by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    ProbeMetricInfo:
      properties:
        metric:
          description: 'The metric values.

            '
          enum:
          - LATENCY
          - JITTER
          - PACKET_LOSS
          - INIT_FAILURE_PCT
          - DNS_TXN_FAILURE_PCT
          type: string
        metric_type:
          description: 'The metric type.

            '
          enum:
          - PROBE
          - LQM
          type: string
        probe_config_id:
          description: 'The probe configuration ID.

            '
          type: string
    ServiceContext:
      properties:
        active_service_label_id:
          description: 'The configured active service label ID.

            '
          type: string
        active_service_label_type:
          description: 'The configured active service label type.

            '
          enum:
          - CG_TRANSIT
          - NON_CG_TRANSIT
          - SASE
          type: string
        backup_service_label_id:
          description: "The configured backup service label ID. \n"
          type: string
        backup_service_label_type:
          description: 'The configured backup service label type.

            '
          enum:
          - CG_TRANSIT
          - NON_CG_TRANSIT
          - SASE
          type: string
        type:
          description: 'The type of service context.

            '
          enum:
          - ALLOWED_TRANSIT
          - REQUIRED_TRANSIT
          type: string
      required:
      - active_service_label_id
      - type
    SiteNetworkPrefixAssociation:
      properties:
        id:
          description: 'The ID of the site prefix.

            '
          type: string
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items:
            description: Ipv4 Prefixes
            type: string
          type: array
        prefix_id:
          description: "The IP Prefix ID that associates with a site.   \n"
          type: string
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
    SiteNetworkPrefixAssociationV2N1:
      properties:
        id:
          description: 'The ID of the site prefix.

            '
          type: string
        ipv4_prefixes:
          description: 'The individual IPv4 prefixes address.

            '
          items:
            description: Ipv4 Prefixes
            type: string
          type: array
        ipv6_prefixes:
          description: 'The individual IPv4 prefixes address.

            '
          items:
            description: Ipv6 Prefixes
            type: string
          type: array
        prefix_id:
          description: "The IP Prefix ID that associates with a site.   \n"
          type: string
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
    SitePrefixAssociationQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of prefix associations that match the
            query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number of prefix associations deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the prefix associations for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "Lists the prefix associations by ID.    \n"
          type: string
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    SitePrefixAssociationQueryFilter:
      properties:
        id:
          description: 'The ID of the site prefix.

            '
          type: string
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items:
            description: Ipv4 Prefixes
            type: string
          type: array
        prefix_id:
          description: "The IP Prefix ID that associates with a site.   \n"
          type: string
        site_id:
          description: 'The site ID binding the prefix.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
    SitePrefixAssociationQueryFilterV2N1:
      properties:
        id:
          description: 'The ID of the site prefix.

            '
          type: string
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items:
            description: Ipv4 Prefixes
            type: string
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 address.

            '
          items:
            description: Ipv6 Prefixes
            type: string
          type: array
        prefix_id:
          description: "The IP Prefix ID that associates with a site.   \n"
          type: string
        site_id:
          description: 'The site ID binding the prefix.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
    UserGroup:
      properties:
        user_group_ids:
          description: 'The user group IDs.

            '
          items:
            description: User Group Ids
            type: string
          type: array
        user_ids:
          description: 'The user IDs.

            '
          items:
            description: User Ids
            type: string
          type: array
    WANPath:
      properties:
        label:
          description: 'The label to identify the path.

            '
          type: string
        path_type:
          description: "The path type.      \n"
          enum:
          - vpn
          - direct
          - servicelink
          type: string
      required:
      - path_type
info:
  contact: {}
  description: List of APIs used to manage the network path policy rules.
  title: Path Policy
  version: Latest
openapi: 3.0.0
paths:
  /v2.0/api/tenants/{tenant_id}/networkpolicyglobalprefixes:
    get:
      description: List all the network policy global prefixes.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicyglobalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicyGlobalPrefix'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Network Policy Global Prefixes (v2.0)
      tags:
      - Path Policy
    post:
      description: Create a new global prefix.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicyglobalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyGlobalPrefix'
        description: Details of the Network Global prefix  to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefix'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: INVALID_PREFIX
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_REQUEST_DUPLICATE_KEY
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Global Prefix (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicyglobalprefixes/query:
    post:
      description: Query all the network global prefixes.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicyglobalprefixes-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalPrefixQuery'
        description: Details to query the Network policy Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/GlobalPrefixQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Network Global Prefixes (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicyglobalprefixes/{prefix_id}:
    delete:
      description: Delete a network policy global prefix.
      operationId: delete-v2.0-api-tenants-tenant_id-networkpolicyglobalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network global prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefix'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: PREFIX_IS_USED_IN_POLICY
                  - code: ENTERPRISE_PREFIX_INVALID_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Network Policy Global Prefix (v2.0)
      tags:
      - Path Policy
    get:
      description: Get a specific network policy global prefix.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicyglobalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network global prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefix'
          description: Successful Operation
      summary: Get Network Policy Global Prefix (v2.0)
      tags:
      - Path Policy
    put:
      description: Update a network policy global prefix.
      operationId: put-v2.0-api-tenants-tenant_id-networkpolicyglobalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network global prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyGlobalPrefix'
        description: Details to update the Network policy Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefix'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: INVALID_PREFIX
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_REQUEST_DUPLICATE_KEY
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: ENTERPRISE_PREFIX_INVALID_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Network Policy Global Prefix (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicylocalprefixes:
    get:
      description: List a specific network policy local prefix.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicylocalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicyLocalPrefix'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Network Policy local Prefixes (v2.0)
      tags:
      - Path Policy
    post:
      description: Create a new network policy local prefix.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicylocalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyLocalPrefix'
        description: Details of the Network Policy Local prefix to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyLocalPrefix'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_REQUEST_DUPLICATE_KEY
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Network Policy local Prefix (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicylocalprefixes/query:
    post:
      description: Query the site network prefix associations.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicylocalprefixes-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SitePrefixAssociationQuery'
        description: Query details for site network prefix association
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/SitePrefixAssociationQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Site Network Prefix Association (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete a network policy local prefix.
      operationId: delete-v2.0-api-tenants-tenant_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyLocalPrefix'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: PREFIX_IS_ASSOCIATED_WITH_SITE
                  - code: PREFIX_IS_USED_IN_POLICY
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Network Policy Local Prefix (v2.0)
      tags:
      - Path Policy
    get:
      description: List a specific network policy local prefix.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyLocalPrefix'
          description: Successful Operation
      summary: Get Network Policy Local Prefix (v2.0)
      tags:
      - Path Policy
    put:
      description: Update a network policy local prefix.
      operationId: put-v2.0-api-tenants-tenant_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyLocalPrefix'
        description: Details to update the Network Policy local prefix.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyLocalPrefix'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_REQUEST_DUPLICATE_KEY
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Network Policy Local Prefix (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicysets:
    get:
      description: List all the network policy sets of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicysets
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicySet'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Network Policy Sets (v2.0)
      tags:
      - Path Policy
    post:
      description: Create a network policy set for a tenant.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicysets
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicySet'
        description: Details of the network policy to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySet'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: POLICY_SET_CONFIG_INVALID_CLONE_FROM
                  - code: INVALID_DEFAULTRULE_FLAG_OF_CLONE_FROM
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: POLICY_SET_CONFIG_CREATION_FAILED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Network Policy Set (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicysets/query:
    post:
      description: Query all the network policy sets.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicysets-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicySetQueryFilter'
        description: Query details for Network Policy Set
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicySetQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Network Policy Set (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicysets/{policy_set_id}:
    delete:
      description: Delete a network policy set.
      operationId: delete-v2.0-api-tenants-tenant_id-networkpolicysets-policy_set_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySet'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: POLICYSET_IS_IN_USE
                  - code: POLICYSET_INVALID_STATE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Network Policy Set (v2.0)
      tags:
      - Path Policy
    get:
      description: List a specific network policy set.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicysets-policy_set_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySet'
          description: Successful Operation
      summary: Get Network Policy Set (v2.0)
      tags:
      - Path Policy
    put:
      description: Update a network policy set.
      operationId: put-v2.0-api-tenants-tenant_id-networkpolicysets-policy_set_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicySet'
        description: NetworkPolicyset details to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySet'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: READONLY_CLONE_FROM
                  - code: READONLY_DEFAULTRULE_POLICYSET_FLAG
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Network Policy Set (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicysets/{policy_set_id}/status:
    get:
      description: List a specific network policy set status of tenant.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicysets-policy_set_id-status
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySetState'
          description: Successful Operation
      summary: Get Network Policy Set Status (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicysetstacks:
    get:
      description: List all the network policy set stacks of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicysetstacks
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicySetStack'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Network Policy Set Stacks (v2.0)
      tags:
      - Path Policy
    post:
      description: Create a network policy set stack.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicysetstacks
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicySetStack'
        description: Details of the network policy stack to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySetStack'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_DEFAULTRULE_POLICYSET_ID
                  - code: POLICYSETSTACK_CONFIG_DUPLICATE_POLICYSETID
                  - code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_IDS
                  - code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_STATE
                  - code: POLICYSETSTACK_CONFIG_INVALID_DEFAULTRULE_POLICYSET_STATE
                  - code: DEFAULT_POLICYSET_STACK_EXISTS
                  - code: POLICYSETSTACK_CONFIG_DEFAULTRULE_POLICYSET_REQUIRED
                  - code: INVALID_DEFAULTRULE_POLICYSET_ID_FORMAT
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Network Policy Set Stack (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicysetstacks/query:
    post:
      description: Query all the network policy set stacks.
      operationId: post-v2.0-api-tenants-tenant_id-networkpolicysetstacks-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolicySetStackQuery'
        description: Details to query the Network policy Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicySetStackQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Network Policy Set Stacks (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/networkpolicysetstacks/{policy_set_id}:
    delete:
      description: Delete a network policy set stack.
      operationId: delete-v2.0-api-tenants-tenant_id-networkpolicysetstacks-policy_set_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set stack ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySetStack'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: POLICYSETSTACK_IN_USE
                  - code: POLICYSETSTACK_CONFIG_DEFAULTRULE_POLICYSET_REQUIRED
                  - code: INVALID_DEFAULTRULE_POLICYSET_ID_FORMAT
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Network Policy Set Stack (v2.0)
      tags:
      - Path Policy
    get:
      description: List a specific network policy set stack of a tenant.
      operationId: get-v2.0-api-tenants-tenant_id-networkpolicysetstacks-policy_set_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set stack ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySetStack'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: POLICYSETSTACK_CONFIG_DEFAULTRULE_POLICYSET_REQUIRED
                  - code: INVALID_DEFAULTRULE_POLICYSET_ID_FORMAT
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Network Policy Set Stack (v2.0)
      tags:
      - Path Policy
    put:
      description: Update a network policy set stack.
      operationId: put-v2.0-api-tenants-tenant_id-networkpolicysetstacks-policy_set_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set stack ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicySetStack'
        description: Delete the Network policy id.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicySetStack'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: INVALID_DEFAULTRULE_POLICYSET_ID
                  - code: POLICYSETSTACK_CONFIG_DUPLICATE_POLICYSETID
                  - code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_IDS
                  - code: POLICYSETSTACK_CONFIG_INVALID_DEFAULTRULE_POLICYSET_STATE
                  - code: DEFAULT_POLICYSET_STACK_EXISTS
                  - code: POLICYSETSTACK_CONFIG_DEFAULTRULE_POLICYSET_REQUIRED
                  - code: INVALID_DEFAULTRULE_POLICYSET_ID_FORMAT
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Network Policy Set Stack (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/networkpolicylocalprefixes:
    get:
      description: List all the site network policy prefix associations.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Site Network Policy Prefix Associations (v2.0)
      tags:
      - Path Policy
    post:
      description: Create an association between the site and network local prefix.
      operationId: post-v2.0-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
        description: Details of the site Network Policy local prefix to be associated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: LOCAL_PREFIX_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Association between Site and Network Local Prefix (v2.0)
      tags:
      - Path Policy
  /v2.0/api/tenants/{tenant_id}/sites/{site_id}/networkpolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete an existing site network policy local prefix association.
      operationId: delete-v2.0-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: ID of the network policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Site Network Policy Local Prefix Association (v2.0)
      tags:
      - Path Policy
    get:
      description: List an existing site network policy local prefix association.
      operationId: get-v2.0-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: ID of the network policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Site Network Policy Local Prefix Association (v2.0)
      tags:
      - Path Policy
    put:
      description: Update a site network policy local prefix association.
      operationId: put-v2.0-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: ID of the network policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
        description: Details of the site Network policy local prefix to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: LOCAL_PREFIX_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Site Network Policy Local Prefix Association (v2.0)
      tags:
      - Path Policy
  /v2.1/api/tenants/{tenant_id}/networkpolicyglobalprefixes:
    get:
      description: List all the network policy global prefixes.
      operationId: get-v2.1-api-tenants-tenant_id-networkpolicyglobalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicyGlobalPrefixV2N1'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Network Policy Global Prefixes (v2.1)
      tags:
      - Path Policy
    post:
      description: Create a new global prefix.
      operationId: post-v2.1-api-tenants-tenant_id-networkpolicyglobalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyGlobalPrefixV2N1'
        description: Details of the Network Global prefix  to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefixV2N1'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: INVALID_PREFIX
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_REQUEST_DUPLICATE_KEY
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Global Prefix (v2.1)
      tags:
      - Path Policy
  /v2.1/api/tenants/{tenant_id}/networkpolicyglobalprefixes/query:
    post:
      description: Query all the network global prefixes.
      operationId: post-v2.1-api-tenants-tenant_id-networkpolicyglobalprefixes-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GlobalPrefixQuery'
        description: Details to query the Network policy Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/GlobalPrefixQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Network Global Prefixes (v2.1)
      tags:
      - Path Policy
  /v2.1/api/tenants/{tenant_id}/networkpolicyglobalprefixes/{prefix_id}:
    delete:
      description: Delete a network policy global prefix.
      operationId: delete-v2.1-api-tenants-tenant_id-networkpolicyglobalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network global prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefixV2N1'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: PREFIX_IS_USED_IN_POLICY
                  - code: ENTERPRISE_PREFIX_INVALID_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Network Policy Global Prefix (v2.1)
      tags:
      - Path Policy
    get:
      description: Get a specific network policy global prefix.
      operationId: get-v2.1-api-tenants-tenant_id-networkpolicyglobalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network global prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefixV2N1'
          description: Successful Operation
      summary: Get Network Policy Global Prefix (v2.1)
      tags:
      - Path Policy
    put:
      description: Update a network policy global prefix.
      operationId: put-v2.1-api-tenants-tenant_id-networkpolicyglobalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network global prefix ID.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyGlobalPrefixV2N1'
        description: Details to update the Network policy Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyGlobalPrefixV2N1'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: NAME_REQD
                  - code: INVALID_PREFIX
                  - code: DESCRIPTION_EXCEEDS_LIMIT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_REQUEST_DUPLICATE_KEY
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: ENTERPRISE_PREFIX_INVALID_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Network Policy Global Prefix (v2.1)
      tags:
      - Path Policy
  /v2.1/api/tenants/{tenant_id}/networkpolicylocalprefixes/query:
    post:
      description: Query the site network prefix associations.
      operationId: post-v2.1-api-tenants-tenant_id-networkpolicylocalprefixes-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SitePrefixAssociationQuery'
        description: Query details for site network prefix association
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/SitePrefixAssociationQueryFilterV2N1'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Site Network Prefix Association (v2.1)
      tags:
      - Path Policy
  /v2.1/api/tenants/{tenant_id}/sites/{site_id}/networkpolicylocalprefixes:
    get:
      description: List all the site network policy prefix associations.
      operationId: get-v2.1-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/SiteNetworkPrefixAssociationV2N1'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Site Network Policy Prefix Associations (v2.1)
      tags:
      - Path Policy
    post:
      description: Create an association between the site and network local prefix.
      operationId: post-v2.1-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SiteNetworkPrefixAssociationV2N1'
        description: Details of the site Network Policy local prefix to be associated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociationV2N1'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: LOCAL_PREFIX_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Association between Site and Network Local Prefix (v2.1)
      tags:
      - Path Policy
  /v2.1/api/tenants/{tenant_id}/sites/{site_id}/networkpolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete an existing site network policy local prefix association.
      operationId: delete-v2.1-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: ID of the network policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociation'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Site Network Policy Local Prefix Association (v2.1)
      tags:
      - Path Policy
    get:
      description: List an existing site network policy local prefix association.
      operationId: get-v2.1-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: ID of the network policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociationV2N1'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Site Network Policy Local Prefix Association (v2.1)
      tags:
      - Path Policy
    put:
      description: Update a site network policy local prefix association.
      operationId: put-v2.1-api-tenants-tenant_id-sites-site_id-networkpolicylocalprefixes-prefix_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: ID of the network policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema:
          description: Prefix Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SiteNetworkPrefixAssociationV2N1'
        description: Details of the site Network policy local prefix to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SiteNetworkPrefixAssociationV2N1'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: SITE_NOT_FOUND
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: LOCAL_PREFIX_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Site Network Policy Local Prefix Association (v2.1)
      tags:
      - Path Policy
  /v2.3/api/tenants/{tenant_id}/networkpolicyrules/query:
    post:
      description: Query all the network policy rules.
      operationId: post-v2.3-api-tenants-tenant_id-networkpolicyrules-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyRuleQueryFilterV2N3'
        description: Details to query the Network policy rules.
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicyRuleQueryFilterV2N3'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Network Policy Rules (v2.3)
      tags:
      - Path Policy
  /v2.3/api/tenants/{tenant_id}/networkpolicysets/{policy_set_id}/networkpolicyrules:
    get:
      description: List all the network policy rules of a tenant.
      operationId: get-v2.3-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/NetworkPolicyRuleV2N3'
                type: array
          description: Successful Operation
      summary: Get Network Policy Rules (v2.3)
      tags:
      - Path Policy
    post:
      description: Create a new network policy rule.
      operationId: post-v2.3-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyRuleScreenV2N3'
        description: Details of the network policy to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N3'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: DUPLICATE_APP_DEF_IDS
                  - code: APP_DEF_ID_LIST_SIZE_EXCEEDED
                  - code: INVALID_NETWORK_CONTEXT_ID
                  - code: INVALID_ORDER_NUMBER_MINIMUM_VAL
                  - code: INVALID_ORDER_NUMBER_MAXIMUM_VAL
                  - code: INVALID_SOURCE_PREFIXES_ID
                  - code: INVALID_DESTINATION_PREFIXES_ID
                  - code: PATHS_ALLOWED_REQUIRED
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: NETWORKPOLICYSET_NOT_PRESENT
                  - code: POLICYRULE_CREATION_NOT_ALLOWED
                  - code: INVALID_POLICYSET_STATE
                  - code: INVALID_DEFAULT_RULE_FLAG
                  - code: POLICYRULE_CONFIG_DEFAULT_RULE_NAME
                  - code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS
                  - code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT
                  - code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX
                  - code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX
                  - code: POLICYRULE_CONFIG_ACTIVE_BACKUP_SERVICELABELS_SAME
                  - code: POLICYRULE_CONFIG_SERVICELABEL_DOES_NOT_EXIST
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_ALLOWED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_REQUIRED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_CGTRANSIT_VPNPATH_MISSING
                  - code: POLICYRULE_CONFIG_DUPLICATE_PATHS_SPECIFIED
                  - code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS
                  - code: METRIC_PROBE_CONFIG_ID_REQUIRED_FOR_TYPE_PROBE
                  - code: METRIC_PROBE_CONFIG_ID_INVALID
                  - code: BACKUP_PATHS_NOT_ALLOWED_FOR_BEST_PATH_SELECTION
                  - code: NETWORKPOLICY_RULE_CONFIG_INVALID_ACTIVE_PATHS
                  - code: PROBE_CONFIG_APPLICABLE_ONLY_FOR_PROBE
                  - code: INVALID_CONFIG_PROTOCOL_FOR_BEST_PATH_SELECTION
                  - code: METRIC_PARAMETER_MISSING
                  - code: METRIC_TYPE_MISSING
                  - code: INVALID_METRIC_FOR_LQM_METRIC_TYPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Network Policy Rule (v2.3)
      tags:
      - Path Policy
  /v2.3/api/tenants/{tenant_id}/networkpolicysets/{policy_set_id}/networkpolicyrules/{policy_rule_id}:
    delete:
      description: Delete a network policy rule of a tenant.
      operationId: delete-v2.3-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules-policy_rule_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      - description: ID of the network policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema:
          description: Policy Rule Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N3'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: POLICY_SET_CLONE_IN_PROGRESS
                  - code: CANNOT_DELETE_DEFAULT_POLICY_RULE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Network Policy Rule (v2.3)
      tags:
      - Path Policy
    get:
      description: List a specific network policy rule of a tenant.
      operationId: get-v2.3-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules-policy_rule_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      - description: ID of the network policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema:
          description: Policy Rule Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N2'
          description: Successful Operation
      summary: Get Network Policy Rule (v2.3)
      tags:
      - Path Policy
    put:
      description: Update a network policy rule of a tenant.
      operationId: put-v2.3-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules-policy_rule_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      - description: ID of the network policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema:
          description: Policy Rule Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyRuleScreenV2N3'
        description: Details to update the Network Policy  policy rule.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N3'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: DUPLICATE_APP_DEF_IDS
                  - code: APP_DEF_ID_LIST_SIZE_EXCEEDED
                  - code: INVALID_NETWORK_CONTEXT_ID
                  - code: INVALID_ORDER_NUMBER_MINIMUM_VAL
                  - code: INVALID_ORDER_NUMBER_MAXIMUM_VAL
                  - code: INVALID_SOURCE_PREFIXES_ID
                  - code: INVALID_DESTINATION_PREFIXES_ID
                  - code: PATHS_ALLOWED_REQUIRED
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: NETWORKPOLICYSET_NOT_PRESENT
                  - code: INVALID_POLICYSET_STATE
                  - code: INVALID_DEFAULT_RULE_FLAG
                  - code: INVALID_DEFAULT_RULE_NAME
                  - code: INVALID_DEFAULT_RULE_APPDEFS_UPDATE
                  - code: INVALID_DEFAULT_RULE_DESTINATION_PREFIX_UPDATE
                  - code: INVALID_DEFAULT_RULE_SOURCE_PREFIX_UPDATE
                  - code: INVALID_DEFAULT_RULE_ENABLED_FLAG_UPDATE
                  - code: INVALID_DEFAULT_RULE_NETWORK_CONTEXT_UPDATE
                  - code: POLICY_RULE_UPDATE_INVALID_NETWORK_CONTEXT
                  - code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS
                  - code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT
                  - code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX
                  - code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX
                  - code: POLICYRULE_CONFIG_ACTIVE_BACKUP_SERVICELABELS_SAME
                  - code: POLICYRULE_CONFIG_SERVICELABEL_DOES_NOT_EXIST
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_ALLOWED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_REQUIRED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_CGTRANSIT_VPNPATH_MISSING
                  - code: POLICYRULE_CONFIG_DUPLICATE_PATHS_SPECIFIED
                  - code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS
                  - code: METRIC_PROBE_CONFIG_ID_REQUIRED_FOR_TYPE_PROBE
                  - code: METRIC_PROBE_CONFIG_ID_INVALID
                  - code: BACKUP_PATHS_NOT_ALLOWED_FOR_BEST_PATH_SELECTION
                  - code: NETWORKPOLICY_RULE_CONFIG_INVALID_ACTIVE_PATHS
                  - code: PROBE_CONFIG_APPLICABLE_ONLY_FOR_PROBE
                  - code: INVALID_CONFIG_PROTOCOL_FOR_BEST_PATH_SELECTION
                  - code: METRIC_PARAMETER_MISSING
                  - code: METRIC_TYPE_MISSING
                  - code: INVALID_METRIC_FOR_LQM_METRIC_TYPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Network Policy Rule (v2.3)
      tags:
      - Path Policy
  /v2.4/api/tenants/{tenant_id}/networkpolicyrules/query:
    post:
      description: Query all the network policy rules.
      operationId: post-v2.4-api-tenants-tenant_id-networkpolicyrules-query
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyRuleQueryFilterV2N4'
        description: Details to query the Network policy rules.
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/NetworkPolicyRuleQueryFilterV2N4'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Network Policy Rules (v2.4)
      tags:
      - Path Policy
  /v2.4/api/tenants/{tenant_id}/networkpolicysets/{policy_set_id}/networkpolicyrules:
    get:
      description: List all the network policy rules of a tenant.
      operationId: get-v2.4-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/NetworkPolicyRuleV2N4'
                type: array
          description: Successful Operation
      summary: Get Network Policy Rules (v2.4)
      tags:
      - Path Policy
    post:
      description: Create a new network policy rule.
      operationId: post-v2.4-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyRuleScreenV2N4'
        description: Details of the network policy to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N4'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: DUPLICATE_APP_DEF_IDS
                  - code: APP_DEF_ID_LIST_SIZE_EXCEEDED
                  - code: INVALID_NETWORK_CONTEXT_ID
                  - code: INVALID_ORDER_NUMBER_MINIMUM_VAL
                  - code: INVALID_ORDER_NUMBER_MAXIMUM_VAL
                  - code: INVALID_SOURCE_PREFIXES_ID
                  - code: INVALID_DESTINATION_PREFIXES_ID
                  - code: PATHS_ALLOWED_REQUIRED
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: NETWORKPOLICYSET_NOT_PRESENT
                  - code: POLICYRULE_CREATION_NOT_ALLOWED
                  - code: INVALID_POLICYSET_STATE
                  - code: INVALID_DEFAULT_RULE_FLAG
                  - code: POLICYRULE_CONFIG_DEFAULT_RULE_NAME
                  - code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS
                  - code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT
                  - code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX
                  - code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX
                  - code: POLICYRULE_CONFIG_ACTIVE_BACKUP_SERVICELABELS_SAME
                  - code: POLICYRULE_CONFIG_SERVICELABEL_DOES_NOT_EXIST
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_ALLOWED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_REQUIRED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_CGTRANSIT_VPNPATH_MISSING
                  - code: POLICYRULE_CONFIG_DUPLICATE_PATHS_SPECIFIED
                  - code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS
                  - code: METRIC_PROBE_CONFIG_ID_REQUIRED_FOR_TYPE_PROBE
                  - code: METRIC_PROBE_CONFIG_ID_INVALID
                  - code: BACKUP_PATHS_NOT_ALLOWED_FOR_BEST_PATH_SELECTION
                  - code: NETWORKPOLICY_RULE_CONFIG_INVALID_ACTIVE_PATHS
                  - code: PROBE_CONFIG_APPLICABLE_ONLY_FOR_PROBE
                  - code: INVALID_CONFIG_PROTOCOL_FOR_BEST_PATH_SELECTION
                  - code: METRIC_PARAMETER_MISSING
                  - code: METRIC_TYPE_MISSING
                  - code: INVALID_METRIC_FOR_LQM_METRIC_TYPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Network Policy Rule (v2.4)
      tags:
      - Path Policy
  /v2.4/api/tenants/{tenant_id}/networkpolicysets/{policy_set_id}/networkpolicyrules/{policy_rule_id}:
    delete:
      description: Delete a network policy rule of a tenant.
      operationId: delete-v2.4-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules-policy_rule_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      - description: ID of the network policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema:
          description: Policy Rule Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N4'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: POLICY_SET_CLONE_IN_PROGRESS
                  - code: CANNOT_DELETE_DEFAULT_POLICY_RULE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Network Policy Rule (v2.4)
      tags:
      - Path Policy
    get:
      description: List a specific network policy rule of a tenant.
      operationId: get-v2.4-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules-policy_rule_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      - description: ID of the network policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema:
          description: Policy Rule Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N4'
          description: Successful Operation
      summary: Get Network Policy Rule (v2.4)
      tags:
      - Path Policy
    put:
      description: Update a network policy rule of a tenant.
      operationId: put-v2.4-api-tenants-tenant_id-networkpolicysets-policy_set_id-networkpolicyrules-policy_rule_id
      parameters:
      - description: The tenant ID.
        in: path
        name: tenant_id
        required: true
        schema:
          description: Tenant Id
          type: string
      - description: The network policy set ID.
        in: path
        name: policy_set_id
        required: true
        schema:
          description: Policy Set Id
          type: string
      - description: ID of the network policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema:
          description: Policy Rule Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NetworkPolicyRuleScreenV2N4'
        description: Details to update the Network Policy  policy rule.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkPolicyRuleV2N4'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: DUPLICATE_APP_DEF_IDS
                  - code: APP_DEF_ID_LIST_SIZE_EXCEEDED
                  - code: INVALID_NETWORK_CONTEXT_ID
                  - code: INVALID_ORDER_NUMBER_MINIMUM_VAL
                  - code: INVALID_ORDER_NUMBER_MAXIMUM_VAL
                  - code: INVALID_SOURCE_PREFIXES_ID
                  - code: INVALID_DESTINATION_PREFIXES_ID
                  - code: PATHS_ALLOWED_REQUIRED
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: NETWORKPOLICYSET_NOT_PRESENT
                  - code: INVALID_POLICYSET_STATE
                  - code: INVALID_DEFAULT_RULE_FLAG
                  - code: INVALID_DEFAULT_RULE_NAME
                  - code: INVALID_DEFAULT_RULE_APPDEFS_UPDATE
                  - code: INVALID_DEFAULT_RULE_DESTINATION_PREFIX_UPDATE
                  - code: INVALID_DEFAULT_RULE_SOURCE_PREFIX_UPDATE
                  - code: INVALID_DEFAULT_RULE_ENABLED_FLAG_UPDATE
                  - code: INVALID_DEFAULT_RULE_NETWORK_CONTEXT_UPDATE
                  - code: POLICY_RULE_UPDATE_INVALID_NETWORK_CONTEXT
                  - code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS
                  - code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT
                  - code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX
                  - code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX
                  - code: POLICYRULE_CONFIG_ACTIVE_BACKUP_SERVICELABELS_SAME
                  - code: POLICYRULE_CONFIG_SERVICELABEL_DOES_NOT_EXIST
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_ALLOWED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_SERVICE_LABEL_REQUIRED_TRANSIT
                  - code: POLICYRULE_CONFIG_INVALID_CGTRANSIT_VPNPATH_MISSING
                  - code: POLICYRULE_CONFIG_DUPLICATE_PATHS_SPECIFIED
                  - code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS
                  - code: METRIC_PROBE_CONFIG_ID_REQUIRED_FOR_TYPE_PROBE
                  - code: METRIC_PROBE_CONFIG_ID_INVALID
                  - code: BACKUP_PATHS_NOT_ALLOWED_FOR_BEST_PATH_SELECTION
                  - code: NETWORKPOLICY_RULE_CONFIG_INVALID_ACTIVE_PATHS
                  - code: PROBE_CONFIG_APPLICABLE_ONLY_FOR_PROBE
                  - code: INVALID_CONFIG_PROTOCOL_FOR_BEST_PATH_SELECTION
                  - code: METRIC_PARAMETER_MISSING
                  - code: METRIC_TYPE_MISSING
                  - code: INVALID_METRIC_FOR_LQM_METRIC_TYPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Network Policy Rule (v2.4)
      tags:
      - Path Policy
servers:
- url: https://api.us.cloudgenix.com
tags:
- name: Path Policy
