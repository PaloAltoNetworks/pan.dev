components:
  schemas:
    Address:
      properties:
        city:
          description: 'The name of the city. Max size = 100.

            '
          type: string
        country:
          description: 'The name of the country. Max size = 100.

            '
          type: string
        post_code:
          description: 'The postal code. Max size = 100.

            '
          type: string
        state:
          description: 'The name of the state. Max size = 100.

            '
          type: string
        street:
          description: 'The name of the street. Max size = 100.

            '
          type: string
        street2:
          description: 'The name of the street. Max size = 100.

            '
          type: string
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items:
            $ref: '#/components/schemas/AggregateData'
          type: array
        field:
          description: 'The field value.

            '
          type: string
        name:
          description: 'The name of the query response.

            '
          type: string
        operator:
          description: 'The operator values.

            '
          enum:
          - max
          - count
          - min
          - avg
          - sum
          type: string
        statistic:
          description: 'The aggregate statistics.

            '
          type: string
        unit:
          description: 'The unit.

            '
          type: string
    AggregateData:
      properties:
        value:
          description: 'Value of the aggregate data.

            '
          type: number
        view:
          description: 'View the aggregate date.

            '
          type: object
    AnynetLinkBulkCreateV4:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        description:
          description: 'The anynet link description. Max size = 256.

            '
        ep1_hub_cluster_id:
          description: 'The endpoint 1 hub cluster ID.

            '
          type: string
        ep1_wan_interface_id:
          description: 'The endpoint 1 WAN interface ID.

            '
          type: string
        ep2_hub_cluster_id:
          description: 'The endpoint 2 hub cluster ID.

            '
          type: string
        ep2_site_id:
          description: 'The endpoint 2 site ID.

            '
          type: string
        ep2_wan_interface_id:
          description: 'The endpoint 2 WAN interface ID.

            '
          type: string
        forced:
          description: 'True or False.

            '
          type: boolean
        id:
          description: 'The anynet link ID.

            '
          type: string
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        type:
          description: "Anynet link type. \n"
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: 'The configuration of the VPN link.

            '
    AnynetLinkCreateBulkOperation:
      properties:
        description:
          description: 'The anynet link description. Max size = 256.

            '
        id:
          description: 'The anynet link ID.

            '
          type: string
        items:
          description: 'The anynet link bulk operations details.

            '
          items:
            $ref: '#/components/schemas/AnynetLinkBulkCreateV4'
          type: array
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - items
    AnynetLinkQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of anynet links that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number anynet links deleted for a time frame.

            '
          type: boolean
        group_by:
          description: Group By
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the anynet links by ID.    \n"
          type: string
        isReadPreferenceSecondary:
          description: 'IsReadPreferenceSecondary: JsonIgnore(value = true) '
          type: boolean
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    AnynetLinkQueryFilterV4:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        description:
          description: 'The anynet link description. Max size = 256.

            '
        ep1_hub_cluster_id:
          description: 'The endpoint 1 hub cluster ID.

            '
          type: string
        ep1_site_id:
          description: 'The endpoint 1 site ID.

            '
          type: string
        ep1_wan_interface_id:
          description: 'The endpoint 1 WAN interface ID.

            '
          type: string
        ep2_hub_cluster_id:
          description: 'The endpoint 2 hub cluster ID.

            '
          type: string
        ep2_site_id:
          description: 'The endpoint  2site ID.

            '
          type: string
        ep2_wan_interface_id:
          description: 'The endpoint 2 WAN interface ID.

            '
          type: string
        forced:
          description: 'True or False.

            '
          type: boolean
        id:
          description: 'The anynet link ID.

            '
          type: string
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        type:
          description: "Anynet link type. \n"
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: "The configuration of the VPN link. \n"
    AnynetLinkV3N4:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        description:
          description: 'The anynet link description. Max size = 256.

            '
        ep1_site_id:
          description: Ep1 Site Id
          type: string
        ep1_wan_if_id:
          description: Ep1 Wan If Id
          type: string
        ep2_site_id:
          description: Ep2 Site Id
          type: string
        ep2_wan_if_id:
          description: Ep2 Wan If Id
          type: string
        forced:
          description: Forced
          type: boolean
        id:
          description: 'The anynet link ID.

            '
          type: string
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        type:
          description: 'Anynet link type.

            '
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: 'The configuration of the VPN link.

            '
    AnynetLinkV4:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        description:
          description: 'The anynet link description. Max size = 256.

            '
        ep1_hub_cluster_id:
          description: 'The endpoint 1 hub cluster ID.

            '
          type: string
        ep1_site_id:
          description: 'The endpoint 1 site ID.

            '
          type: string
        ep1_wan_interface_id:
          description: 'The endpoint 1 WAN interface ID.

            '
          type: string
        ep2_hub_cluster_id:
          description: 'The endpoint 2 hub cluster ID.

            '
          type: string
        ep2_site_id:
          description: 'The endpoint  2site ID.

            '
          type: string
        ep2_wan_interface_id:
          description: 'The endpoint 2 WAN interface ID.

            '
          type: string
        forced:
          description: 'True or False.

            '
          type: boolean
        id:
          description: 'The anynet link ID.

            '
          type: string
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        type:
          description: "Anynet link type. \n"
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: 'The configuration of the VPN link.

            '
    Anynetlink:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        description:
          description: 'The anynet link description. Max size = 256.

            '
        disabled:
          description: Disabled
          type: boolean
        disabled_reason:
          description: 'Disabled Reason: Size(max = 5000, DISABLED_REASON_INVALID_0001,
            min = 0) '
          type: string
        ep1_branch_gateway:
          description: Ep1 Branch Gateway
          type: boolean
        ep1_hub_cluster_id:
          description: 'The hub cluster ID.

            '
          type: string
        ep1_site_id:
          description: 'The site ID.

            '
          type: string
        ep1_site_role:
          description: Ep1 Site Role
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep1_wan_interface_id:
          description: 'The WAN interface ID.

            '
          type: string
        ep2_branch_gateway:
          description: Ep2 Branch Gateway
          type: boolean
        ep2_hub_cluster_id:
          description: 'The hub cluster ID.

            '
          type: string
        ep2_site_id:
          description: 'The site ID.

            '
          type: string
        ep2_site_role:
          description: Ep2 Site Role
          enum:
          - NONE
          - HUB
          - SPOKE
          - PA_WE_BR
          - PA_WE_DC
          - PA_CONN
          type: string
        ep2_wan_interface_id:
          description: 'The WAN interface ID.

            '
          type: string
        id:
          description: 'The anynet link ID.

            '
          type: string
        inactive:
          description: Inactive
          type: boolean
        inactive_reason:
          description: 'Inactive Reason: Size(max = 5000, INACTIVE_REASON_INVALID_0001,
            min = 0) '
          type: string
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        region:
          description: Region
          type: string
        site_id:
          description: 'Site Id: Digits(fraction = 0, integer = 50, SITEID_INVALID)
            NotNull '
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        target_serviceendpoint_id:
          description: Target Serviceendpoint Id
          type: string
        type:
          description: "Anynet link type. \n"
          enum:
          - AUTO
          - MANUAL
          - AUTO_PRIVATE
          - MANUAL_PRIVATE
          - AUTO_SASE
          - DCI_MANUAL_PUBLIC
          - DCI_MANUAL_PRIVATE
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: "The configuration of the VPN link.         \n"
      required:
      - site_id
    BfdSession:
      properties:
        element_id:
          description: "The element ID.       \n"
          type: string
        peers:
          description: 'The peers details.

            '
          items:
            description: Peers
            type: string
          type: array
    BfdUnderlay:
      properties:
        bfd_sessions:
          description: "The element and peers information of the BFD sessions.   \
            \    \n"
          items:
            $ref: '#/components/schemas/BfdSession'
          type: array
        site_id:
          description: 'The site ID.

            '
          type: string
      required:
      - site_id
    BulkResponse:
      properties:
        count:
          description: 'The total number.

            '
          format: int32
          type: integer
        id:
          description: 'The ID.

            '
          type: string
    Error:
      properties:
        _error:
          items:
            $ref: '#/components/schemas/ErrorResponse'
          type: array
    ErrorResponse:
      properties:
        code:
          description: 'The error code.

            '
          type: string
        message:
          description: 'The error message.

            '
          type: string
    ListQueryResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        deleted_count:
          description: 'The deleted number.

            '
          format: int64
          type: integer
        deleted_ids:
          description: 'The deleted IDs.

            '
          items:
            type: string
          type: array
        description:
          description: 'Description of the query. Max size = 256.

            '
        id:
          description: 'The ID.

            '
          type: string
        next_query:
          description: 'Details of the next query.

            '
          type: object
        total_count:
          description: 'Total number.

            '
          format: int64
          type: integer
      type: object
    ListResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        id:
          description: 'The ID.

            '
          type: string
      type: object
    Location:
      properties:
        description:
          description: 'The description of the location.

            '
        latitude:
          description: 'The latitude of the location. Max range = 90.

            '
          format: float
          type: number
        longitude:
          description: 'The longitude of the location. Max range = 180.

            '
          format: float
          type: number
    NetworkLinkV2:
      properties:
        anynet_link_id:
          description: 'The anynet link ID..

            '
          type: string
        description:
          description: 'The anynet link description. Max size = 256.

            '
        id:
          description: 'The ID.

            '
          type: string
        network:
          description: Network
          type: string
        path_id:
          description: Path Id
          type: string
        source_node_id:
          description: 'The source node ID.

            '
          type: string
        source_wan_network:
          description: 'The source WAN network.

            '
          type: string
        source_wan_nw_id:
          description: 'The  The source WAN network ID.

            '
          type: string
        source_wan_path_id:
          description: Source Wan Path Id
          type: string
        state:
          description: State
          type: string
        status:
          description: 'The status of the link.

            '
          type: string
        sub_type:
          description: Sub Type
          type: string
        target_node_id:
          description: 'The target node ID.

            '
          type: string
        target_wan_network:
          description: 'The target WAN network.

            '
          type: string
        target_wan_nw_id:
          description: 'The  The target WAN network ID.

            '
          type: string
        target_wan_path_id:
          description: Target Wan Path Id
          type: string
        type:
          description: 'Anynet link type.

            '
          type: string
        vpnlinks:
          description: 'The VPN links info.

            '
          items:
            description: Vpnlinks
            type: string
          type: array
        wan_nw_id:
          description: 'The WAN network ID.

            '
          type: string
    NetworkLinkV3N4:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        anynet_link_id:
          description: 'The anynet link ID..

            '
          type: string
        cost:
          description: Cost
          format: int32
          type: integer
        description:
          description: 'The anynet link description. Max size = 256.

            '
        elem_interface_id:
          description: 'The element interface ID.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        in_use:
          description: In Use
          type: boolean
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        network:
          description: Network
          type: string
        path_id:
          description: Path Id
          type: string
        sep_id:
          description: Sep Id
          type: string
        sep_name:
          description: Sep Name
          type: string
        source_circuit_name:
          description: 'The name of the source circuit.

            '
          type: string
        source_elem_if_id:
          description: 'The source element ID.

            '
          type: string
        source_node_id:
          description: 'The source node ID.

            '
          type: string
        source_site_name:
          description: 'The source site name.

            '
          type: string
        source_wan_if_id:
          description: 'The source WAN ID.

            '
          type: string
        source_wan_network:
          description: 'The source WAN network.

            '
          type: string
        source_wan_nw_id:
          description: 'The  The source WAN network ID.

            '
          type: string
        status:
          description: 'The status of the link.

            '
          type: string
        sub_type:
          description: Sub Type
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        target_circuit_name:
          description: 'The target circuit name.

            '
          type: string
        target_elem_if_id:
          description: 'The target element ID.

            '
          type: string
        target_node_id:
          description: 'The target node ID.

            '
          type: string
        target_serviceendpoint_id:
          description: Target Serviceendpoint Id
          type: string
        target_serviceendpoint_name:
          description: Target Serviceendpoint Name
          type: string
        target_site_name:
          description: 'The target site name.

            '
          type: string
        target_wan_if_id:
          description: 'The target WAN ID.

            '
          type: string
        target_wan_network:
          description: 'The target WAN network.

            '
          type: string
        target_wan_nw_id:
          description: 'The  The target WAN network ID.

            '
          type: string
        type:
          description: 'Anynet link type.

            '
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: 'The configuration of the VPN link.

            '
        vpnlinks:
          description: 'The VPN links info.

            '
          items:
            description: Vpnlinks
            type: string
          type: array
        wan_if_id:
          description: 'The WAN ID.

            '
          type: string
        wan_nw_id:
          description: 'The WAN network ID.

            '
          type: string
        wan_nw_name:
          description: "The WAN network name. \n"
          type: string
    NetworkLinkV3N5:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        anynet_link_id:
          description: 'The anynet link ID..

            '
          type: string
        cost:
          description: Cost
          format: int32
          type: integer
        description:
          description: 'The anynet link description. Max size = 256.

            '
        elem_interface_id:
          description: 'The element interface ID.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        in_use:
          description: 'The link in use. True or False.

            '
          type: boolean
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        network:
          description: 'The anynet link network.

            '
          type: string
        path_id:
          description: 'The anynet link path ID.

            '
          type: string
        sep_id:
          description: 'The SEP ID.

            '
          type: string
        sep_name:
          description: 'The SEP name.

            '
          type: string
        source_circuit_name:
          description: 'The name of the source circuit.

            '
          type: string
        source_elem_if_id:
          description: 'The source element ID.

            '
          type: string
        source_node_id:
          description: 'The source node ID.

            '
          type: string
        source_site_name:
          description: 'The source site name.

            '
          type: string
        source_wan_if_id:
          description: 'The source WAN ID.

            '
          type: string
        source_wan_network:
          description: 'The source WAN network.

            '
          type: string
        source_wan_nw_id:
          description: 'The  The source WAN network ID.

            '
          type: string
        status:
          description: 'The status of the link.

            '
          type: string
        sub_type:
          description: Sub Type
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        target_circuit_name:
          description: 'The target circuit name.

            '
          type: string
        target_elem_if_id:
          description: 'The target element ID.

            '
          type: string
        target_node_id:
          description: 'The target node ID.

            '
          type: string
        target_serviceendpoint_id:
          description: 'The  The target WAN network service endpoint ID.

            '
          type: string
        target_serviceendpoint_name:
          description: 'The  The target WAN network service endpoint name.

            '
          type: string
        target_site_name:
          description: 'The target site name.

            '
          type: string
        target_wan_if_id:
          description: 'The target WAN ID.

            '
          type: string
        target_wan_network:
          description: 'The target WAN network.

            '
          type: string
        target_wan_nw_id:
          description: 'The  The target WAN network ID.

            '
          type: string
        type:
          description: 'Anynet link type.

            '
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: 'The configuration of the VPN link.

            '
        vpnlinks:
          description: 'The VPN links info.

            '
          items:
            description: Vpnlinks
            type: string
          type: array
        wan_if_id:
          description: 'The WAN ID.

            '
          type: string
        wan_nw_id:
          description: 'The WAN network ID.

            '
          type: string
        wan_nw_name:
          description: 'The WAN network name.

            '
          type: string
    NetworkLinkV3N6:
      properties:
        admin_up:
          description: 'The interface admin state.

            '
          type: boolean
        anynet_link_id:
          description: 'The anynet link ID..

            '
          type: string
        cost:
          description: Cost
          format: int32
          type: integer
        description:
          description: 'The anynet link description. Max size = 256.

            '
        elem_interface_id:
          description: 'The element interface ID.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        in_use:
          description: 'The link in use. True or False.

            '
          type: boolean
        name:
          description: 'The anynet link name. Max size = 128.

            '
          type: string
        network:
          description: 'The anynet link network.

            '
          type: string
        path_id:
          description: 'The anynet link path ID.

            '
          type: string
        sep_id:
          description: 'The SEP ID.

            '
          type: string
        sep_name:
          description: 'The SEP name.

            '
          type: string
        source_circuit_name:
          description: 'The name of the source circuit.

            '
          type: string
        source_elem_if_id:
          description: 'The source element ID.

            '
          type: string
        source_hub_cluster_id:
          description: Source Hub Cluster Id
          type: string
        source_hub_cluster_name:
          description: Source Hub Cluster Name
          type: string
        source_node_id:
          description: 'The source node ID.

            '
          type: string
        source_site_name:
          description: 'The source site name.

            '
          type: string
        source_wan_if_id:
          description: 'The source WAN ID.

            '
          type: string
        source_wan_network:
          description: 'The source WAN network.

            '
          type: string
        source_wan_nw_id:
          description: 'The  The source WAN network ID.

            '
          type: string
        status:
          description: 'The status of the link.

            '
          type: string
        sub_type:
          description: Sub Type
          type: string
        tags:
          description: 'A information field that can be added to identify the anynet
            link. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
        target_circuit_name:
          description: 'The target circuit name.

            '
          type: string
        target_elem_if_id:
          description: 'The target element ID.

            '
          type: string
        target_hub_cluster_id:
          description: 'The  The target WAN network cluster ID.

            '
          type: string
        target_hub_cluster_name:
          description: 'The  The target WAN network cluster name.

            '
          type: string
        target_node_id:
          description: 'The target node ID.

            '
          type: string
        target_serviceendpoint_id:
          description: 'The  The target WAN network service endpoint ID.

            '
          type: string
        target_serviceendpoint_name:
          description: 'The  The target WAN network service endpoint name.

            '
          type: string
        target_site_name:
          description: 'The target site name.

            '
          type: string
        target_wan_if_id:
          description: 'The target WAN ID.

            '
          type: string
        target_wan_network:
          description: 'The target WAN network.

            '
          type: string
        target_wan_nw_id:
          description: 'The  The target WAN network ID.

            '
          type: string
        type:
          description: 'Anynet link type.

            '
          type: string
        vpnlink_configuration:
          $ref: '#/components/schemas/VPNLinkConfiguration'
          description: 'The configuration of the VPN link.

            '
        vpnlinks:
          description: 'The VPN links info.

            '
          items:
            description: Vpnlinks
            type: string
          type: array
        wan_if_id:
          description: 'The WAN ID.

            '
          type: string
        wan_nw_id:
          description: 'The WAN network ID.

            '
          type: string
        wan_nw_name:
          description: 'The WAN network name.

            '
          type: string
    NetworkNodeV3N5:
      properties:
        address:
          $ref: '#/components/schemas/Address'
          description: 'The network address details.

            '
        elem_hw_id:
          description: 'The element hardware ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        is_sase:
          description: 'Sase connected. True or False.

            '
          type: boolean
        location:
          $ref: '#/components/schemas/Location'
          description: 'The network node location.

            '
        name:
          description: 'The network name. Max size = 128.

            '
          type: string
        role:
          description: Role
          type: string
        sase_properties:
          $ref: '#/components/schemas/SaseServiceEndpointProperties'
          description: 'The sase service endpoint properties.

            '
        site_id:
          description: "The site ID. \n"
          type: string
        spoke_cluster_elem_status:
          description: 'The spoke cluster element status.

            '
          type: string
        state:
          description: State
          type: string
        type:
          description: 'Anynet link type.

            '
          type: string
    NetworkTopologyV3N5:
      properties:
        links:
          description: Links
          items:
            $ref: '#/components/schemas/NetworkLinkV3N5'
          type: array
        nodes:
          description: Nodes
          items:
            $ref: '#/components/schemas/NetworkNodeV3N5'
          type: array
        type:
          description: Type
          enum:
          - ANYNET
          - ANYNETPLUS
          - BASENET
          - PHYSICAL
          type: string
    NetworkTopologyV3N6:
      properties:
        links:
          description: Links
          items:
            $ref: '#/components/schemas/NetworkLinkV3N6'
          type: array
        nodes:
          description: Nodes
          items:
            $ref: '#/components/schemas/NetworkNodeV3N5'
          type: array
        type:
          description: Type
          enum:
          - ANYNET
          - ANYNETPLUS
          - BASENET
          - PHYSICAL
          type: string
    SaseServiceEndpointProperties:
      properties:
        active:
          description: 'If SASE service endpoint is active. True or False.

            '
          type: boolean
        lqm_enabled:
          description: 'If LQM is enabled. True or False.

            '
          type: boolean
    SiteEndpoints:
      properties:
        ep1_id:
          description: Ep1 Id
          type: string
        ep2_id:
          description: Ep2 Id
          type: string
    SiteLocation:
      properties:
        end:
          $ref: '#/components/schemas/Location'
          description: End
        start:
          $ref: '#/components/schemas/Location'
          description: Start
    TopologyQueryV3N1:
      properties:
        links:
          description: 'Query the anynet links.

            '
          items:
            description: Links
            type: string
          type: array
        links_only:
          description: 'The anynet link ID. True/False.

            '
          type: boolean
        location:
          $ref: '#/components/schemas/SiteLocation'
          description: 'The anynet link start and end location.

            '
        nodes:
          description: Nodes
          items:
            description: Nodes
            type: string
          type: array
        servicelinks:
          description: 'Servicelinks present. True/False.

            '
          type: boolean
        site_id:
          description: "The site ID. \n"
          type: string
        sites:
          $ref: '#/components/schemas/SiteEndpoints'
          description: 'The site endpoints.

            '
        stub_links:
          description: Stub Links
          type: boolean
        type:
          description: 'Anynet link type.

            '
          enum:
          - ANYNET
          - ANYNETPLUS
          - BASENET
          - PHYSICAL
          type: string
    VPLAdminState:
      properties:
        al_id:
          description: Al Id
          type: string
        enabled:
          description: 'The enabled states on a VPN link.

            '
          type: boolean
        id:
          description: 'The ID of the VPN link.

            '
          type: string
    VPLOperations:
      properties:
        action:
          description: 'The actions performed on a VPN link.

            '
          enum:
          - refresh_vpnlink_status
          - refresh_vpnlink_psk
          type: string
        id:
          description: 'The ID of the VPN link.

            '
          type: string
    VPLOperationsScreen:
      properties:
        action:
          description: 'The actions performed on a VPN link.

            '
          enum:
          - refresh_vpnlink_status
          - refresh_vpnlink_psk
          type: string
        id:
          description: 'The ID of the VPN link.

            '
          type: string
    VPLStatusV2N1:
      properties:
        active:
          description: Active
          type: boolean
        common_cipher:
          description: Common Cipher
          type: string
        ep1_cipher:
          description: Ep1 Cipher
          type: string
        ep1_element_id:
          description: Ep1 Element Id
          type: string
        ep1_interface_id:
          description: Ep1 Interface Id
          type: string
        ep1_keep_alive_failure_count:
          description: Ep1 Keep Alive Failure Count
          type: integer
        ep1_keep_alive_interval:
          description: Ep1 Keep Alive Interval
          type: integer
        ep1_site_id:
          description: Ep1 Site Id
          type: string
        ep2_cipher:
          description: Ep2 Cipher
          type: string
        ep2_element_id:
          description: Ep2 Element Id
          type: string
        ep2_interface_id:
          description: Ep2 Interface Id
          type: string
        ep2_keep_alive_failure_count:
          description: Ep2 Keep Alive Failure Count
          type: integer
        ep2_keep_alive_interval:
          description: Ep2 Keep Alive Interval
          type: integer
        ep2_site_id:
          description: Ep2 Site Id
          type: string
        id:
          description: Id
          type: string
        link_up:
          description: Link Up
          type: boolean
        usable:
          description: Usable
          type: boolean
    VPLStatusV2N2:
      properties:
        active:
          description: Active
          type: boolean
        common_cipher:
          description: Common Cipher
          type: string
        ep1_cipher:
          description: Ep1 Cipher
          type: string
        ep1_element_id:
          description: Ep1 Element Id
          type: string
        ep1_interface_id:
          description: Ep1 Interface Id
          type: string
        ep1_keep_alive_failure_count:
          description: Ep1 Keep Alive Failure Count
          type: integer
        ep1_keep_alive_interval:
          description: Ep1 Keep Alive Interval
          type: integer
        ep1_site_id:
          description: Ep1 Site Id
          type: string
        ep2_cipher:
          description: Ep2 Cipher
          type: string
        ep2_element_id:
          description: Ep2 Element Id
          type: string
        ep2_interface_id:
          description: Ep2 Interface Id
          type: string
        ep2_keep_alive_failure_count:
          description: Ep2 Keep Alive Failure Count
          type: integer
        ep2_keep_alive_interval:
          description: Ep2 Keep Alive Interval
          type: integer
        ep2_site_id:
          description: Ep2 Site Id
          type: string
        id:
          description: Id
          type: string
        link_up:
          description: Link Up
          type: boolean
        type:
          description: Type
          type: string
        usable:
          description: Usable
          type: boolean
    VPNLinkConfiguration:
      properties:
        keep_alive_failure_count:
          description: 'The VPN keep alive failure count (must be between 3 and 30).

            '
          type: integer
        keep_alive_interval:
          description: 'The VPN keep alive timeout interval (must be between 100ms-1740,000ms).

            '
          type: integer
      required:
      - keep_alive_failure_count
      - keep_alive_interval
    VPNLinkQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of VPN links that match the query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number of VPN links deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the VPN links for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "Lists the VPN links by ID.    \n"
          type: string
        isReadPreferenceSecondary:
          description: 'IsReadPreferenceSecondary: JsonIgnore(value = true) '
          type: boolean
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    WanOverlay:
      properties:
        description:
          description: 'The WAN overlay config description. Max size = 256.

            '
        id:
          description: 'The ID of the WAN overlay.

            '
          type: string
        name:
          description: 'The WAN overlay config name. Max size = 128.

            '
          type: string
        vni:
          description: "The VNI value. \n"
          format: int32
          type: integer
      required:
      - vni
      - name
info:
  contact: {}
  description: List of APIs used to manage the network topology, WAN Overlays, VPN
    Links, and Secure Fabric Links.
  title: Topology
  version: Latest
openapi: 3.0.0
paths:
  /sdwan/v2.0/api/sites/{site_id}/anynetlinks/{anynet_id}:
    get:
      description: Get the anynet link.
      operationId: get-sdwan-v2.0-api-sites-site_id-anynetlinks-anynet_id
      parameters:
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The AnynetID.
        in: path
        name: anynet_id
        required: true
        schema:
          description: Anynet Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkLinkV2'
          description: Successful Operation
        '404':
          description: not found
      summary: Get Anynet Link (v2.0)
      tags:
      - Topology
  /sdwan/v2.0/api/vpnlinks/query:
    post:
      description: Query DB for VPN links that match query parameters.
      operationId: post-sdwan-v2.0-api-vpnlinks-query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VPNLinkQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/VPNLinkQuery'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: VPL_NOT_FOUND
                  - code: VPL_ACTION_INSUFFICIENT_VPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query DB for VPN Links (v2.0)
      tags:
      - Topology
  /sdwan/v2.0/api/vpnlinks/{vpn_link_id}/operations:
    post:
      description: Perform an operation on a VPN link.
      operationId: post-sdwan-v2.0-api-vpnlinks-vpn_link_id-operations
      parameters:
      - description: The ID of the VPN link.
        in: path
        name: vpn_link_id
        required: true
        schema:
          description: Vpn Link Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VPLOperationsScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VPLOperations'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: VPL_NOT_FOUND
                  - code: VPL_ACTION_INSUFFICIENT_VPE
                  - code: VPNLINK_INVALID_ACTION
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Perform Operation on VPN Link (v2.0)
      tags:
      - Topology
  /sdwan/v2.0/api/vpnlinks/{vpn_link_id}/state:
    get:
      description: List the admin state of the VPN link.
      operationId: get-sdwan-v2.0-api-vpnlinks-vpn_link_id-state
      parameters:
      - description: The ID of the VPN link.
        in: path
        name: vpn_link_id
        required: true
        schema:
          description: Vpn Link Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VPLAdminState'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: VPL_NOT_FOUND
                  - code: VPL_ACTION_INSUFFICIENT_VPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get VPN Link Admin State (v2.0)
      tags:
      - Topology
    put:
      description: Update the admin state of the VPN link.
      operationId: put-sdwan-v2.0-api-vpnlinks-vpn_link_id-state
      parameters:
      - description: The ID of the VPN link.
        in: path
        name: vpn_link_id
        required: true
        schema:
          description: Vpn Link Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VPLAdminState'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VPLAdminState'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: VPL_NOT_FOUND
                  - code: VPL_ACTION_INSUFFICIENT_VPE
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: ANYNET_VPL_ADMINSTATE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update VPN Link Admin State (v2.0)
      tags:
      - Topology
  /sdwan/v2.0/api/wanoverlays:
    get:
      description: List the app/wan contexts.
      operationId: get-sdwan-v2.0-api-wanoverlays
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/BulkResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/WanOverlay'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get App/WAN Contexts (v2.0)
      tags:
      - Topology
    post:
      description: Create a new app/wan context.
      operationId: post-sdwan-v2.0-api-wanoverlays
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WanOverlay'
        description: Details of the appwan context to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WanOverlay'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: WANOVERLAY_CONFIG_NAME_REQD
                  - code: NAME_EXCEEDS_LIMIT
                  - code: VNI_REQD
                  - code: INVALID_VNI_VALUE
                  - code: WANOVERLAY_CONFIG_DESCRIPTION_INVALID
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: WAN_CONTEXT_ADD_FAILED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create App/WAN Context (v2.0)
      tags:
      - Topology
  /sdwan/v2.0/api/wanoverlays/{wan_overlay_id}:
    delete:
      description: Create a new app/wan context.
      operationId: delete-sdwan-v2.0-api-wanoverlays-wan_overlay_id
      parameters:
      - description: The ID of the WAN overlay.
        in: path
        name: wan_overlay_id
        required: true
        schema:
          description: Wan Overlay Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WanOverlay'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: WANOVERLAY_IN_USE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete App/WAN Context (v2.0)
      tags:
      - Topology
    get:
      description: List the new app/wan context.
      operationId: get-sdwan-v2.0-api-wanoverlays-wan_overlay_id
      parameters:
      - description: The ID of the WAN overlay.
        in: path
        name: wan_overlay_id
        required: true
        schema:
          description: Wan Overlay Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WanOverlay'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: APPWAN_CONTEXT_DOES_NOT_EXIST
                  - code: APPWAN_CONTEXT_NAME_INVALID
                  - code: APPWAN_CONTEXT_DESCRIPTION_INVALID
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get App/WAN Context (v2.0)
      tags:
      - Topology
    put:
      description: Update a new app/wan context.
      operationId: put-sdwan-v2.0-api-wanoverlays-wan_overlay_id
      parameters:
      - description: The ID of the WAN overlay.
        in: path
        name: wan_overlay_id
        required: true
        schema:
          description: Wan Overlay Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WanOverlay'
        description: Details of the appwan context to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WanOverlay'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: APPWAN_CONTEXT_DOES_NOT_EXIST
                  - code: WANOVERLAY_CONFIG_NAME_REQD
                  - code: NAME_EXCEEDS_LIMIT
                  - code: VNI_REQD
                  - code: INVALID_VNI_VALUE
                  - code: WANOVERLAY_CONFIG_DESCRIPTION_INVALID
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update App/WAN Context (v2.0)
      tags:
      - Topology
  /sdwan/v2.1/api/vpnlinks/{vpn_link_id}/status:
    get:
      description: List the status of the VPN link.
      operationId: get-sdwan-v2.1-api-vpnlinks-vpn_link_id-status
      parameters:
      - description: The ID of the VPN link.
        in: path
        name: vpn_link_id
        required: true
        schema:
          description: Vpn Link Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VPLStatusV2N1'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: VPL_NOT_FOUND
                  - code: VPL_ACTION_INSUFFICIENT_VPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get VPN Link Status (v2.1)
      tags:
      - Topology
  /sdwan/v2.2/api/vpnlinks/{vpn_link_id}/status:
    get:
      description: Get the VPNLink status
      operationId: get-sdwan-v2.2-api-vpnlinks-vpn_link_id-status
      parameters:
      - description: VPN Link Id
        in: path
        name: vpn_link_id
        required: true
        schema:
          description: Vpn Link Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VPLStatusV2N2'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: VPL_NOT_FOUND
                  - code: VPL_ACTION_INSUFFICIENT_VPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get the VPNLink status (v2.2)
      tags:
      - Topology
  /sdwan/v3.0/api/sites/{site_id}/bfdpeers:
    get:
      description: Get the BFD peers.
      operationId: get-sdwan-v3.0-api-sites-site_id-bfdpeers
      parameters:
      - description: ID of the site.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/BulkResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/BfdUnderlay'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get BFD Peers (v2.0)
      tags:
      - Topology
  /sdwan/v3.4/api/anynetlinks:
    post:
      description: Create an anynet link.
      operationId: post-sdwan-v3.4-api-anynetlinks
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkV3N4'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Anynetlink'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Anynet Link (v3.4)
      tags:
      - Topology
  /sdwan/v3.4/api/anynetlinks/{anynet_id}:
    delete:
      description: Delete the anynet link.
      operationId: delete-sdwan-v3.4-api-anynetlinks-anynet_id
      parameters:
      - description: The anynet ID.
        in: path
        name: anynet_id
        required: true
        schema:
          description: Anynet Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Anynetlink'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
                  - code: NO_ANYNETLINK_FOUND
                  - code: INVALID_ANYNETLINK_TYPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Anynet Link (v3.4)
      tags:
      - Topology
    get:
      description: Get the anynet link.
      operationId: get-sdwan-v3.4-api-anynetlinks-anynet_id
      parameters:
      - description: The anynet ID.
        in: path
        name: anynet_id
        required: true
        schema:
          description: Anynet Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkLinkV3N4'
          description: Successful Operation
        '404':
          description: not found
      summary: Get Anynet Link (v3.4)
      tags:
      - Topology
    put:
      description: Update the anynet link.
      operationId: put-sdwan-v3.4-api-anynetlinks-anynet_id
      parameters:
      - description: The anynet ID.
        in: path
        name: anynet_id
        required: true
        schema:
          description: Anynet Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkV3N4'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Anynetlink'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Anynet Link (v3.4)
      tags:
      - Topology
  /sdwan/v3.5/api/topology:
    post:
      description: Query the topology elements of the tenant.
      operationId: post-sdwan-v3.5-api-topology
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TopologyQueryV3N1'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkTopologyV3N5'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_TOPO_QUERY
                  - code: INVALID_TOPOLOGY_STUB_LINKS_OPTION
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Topology (v3.5)
      tags:
      - Topology
  /sdwan/v3.6/api/topology:
    post:
      description: Query the topology elements of the tenant.
      operationId: post-sdwan-v3.6-api-topology
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TopologyQueryV3N1'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NetworkTopologyV3N6'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_TOPO_QUERY
                  - code: INVALID_TOPOLOGY_STUB_LINKS_OPTION
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Topology (v3.6)
      tags:
      - Topology
  /sdwan/v4.0/api/anynetlinks:
    post:
      description: Create an anynet link.
      operationId: post-sdwan-v4.0-api-anynetlinks
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkV4'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Anynetlink'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
                  - code: ANYNET_LINK_INVALID_SITE
                  - code: ANYNET_LINK_INVALID_WANINTERFACE
                  - code: ANYNET_LINK_INVALID_HUB_CLUSTER
                  - code: ANYNET_LINK_DUPLICATE_SITE
                  - code: ANYNET_LINK_MISMATCHED_WAN_INTERFACE_TYPE
                  - code: ANYNET_LINK_INVALID_SITE_ROLE
                  - code: ANYNET_LINK_INVALID_DCI_SITE_ROLE
                  - code: ANYNET_LINK_DUPLICATE_ANYNET
                  - code: ANYNET_LINK_HUB_CLUSTER_VALID_ONLY_FOR_DCI
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Anynet Link (v4.0)
      tags:
      - Topology
  /sdwan/v4.0/api/anynetlinks/query:
    post:
      description: Query Anynet Links
      operationId: post-sdwan-v4.0-api-anynetlinks-query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkQuery'
        description: Details of the hub cluster to be created
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/AnynetLinkQueryFilterV4'
                      type: array
                  type: object
          description: Successful operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_REQUEST_ERROR_0001
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad request
      summary: Query Anynet Links (v4.0)
      tags:
      - Topology
  /sdwan/v4.0/api/anynetlinks/{anynet_id}:
    delete:
      description: Delete the anynet link.
      operationId: delete-sdwan-v4.0-api-anynetlinks-anynet_id
      parameters:
      - description: The anynet ID.
        in: path
        name: anynet_id
        required: true
        schema:
          description: Anynet Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Anynetlink'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
                  - code: NO_ANYNETLINK_FOUND
                  - code: INVALID_ANYNETLINK_TYPE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete Anynet Link (v4.0)
      tags:
      - Topology
    get:
      description: Get the anynet link.
      operationId: get-sdwan-v4.0-api-anynetlinks-anynet_id
      parameters:
      - description: The anynet ID.
        in: path
        name: anynet_id
        required: true
        schema:
          description: Anynet Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AnynetLinkV4'
          description: Successful Operation
        '404':
          description: not found
      summary: Get Anynet Link (v4.0)
      tags:
      - Topology
    put:
      description: Update the anynet link.
      operationId: put-sdwan-v4.0-api-anynetlinks-anynet_id
      parameters:
      - description: The anynet ID.
        in: path
        name: anynet_id
        required: true
        schema:
          description: Anynet Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkV4'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Anynetlink'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Anynet Link (v4.0)
      tags:
      - Topology
  /sdwan/v4.0/api/sites/{ep1_site_id}/anynetlinks/bulkoperations:
    post:
      description: Create anynet links in bulk.
      operationId: post-sdwan-v4.0-api-sites-ep1_site_id-anynetlinks-bulkoperations
      parameters:
      - description: The endpoint 1 site ID.
        in: path
        name: ep1_site_id
        required: true
        schema:
          description: Ep1 Site Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AnynetLinkCreateBulkOperation'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/AnynetLinkV4'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: TOPOLOGY_REBUILD_IN_PROGRESS
                  - code: ANYNET_LINK_INVALID_SITE
                  - code: ANYNET_LINK_INVALID_WANINTERFACE
                  - code: ANYNET_LINK_INVALID_HUB_CLUSTER
                  - code: ANYNET_LINK_DUPLICATE_SITE
                  - code: ANYNET_LINK_MISMATCHED_WAN_INTERFACE_TYPE
                  - code: ANYNET_LINK_INVALID_SITE_ROLE
                  - code: ANYNET_LINK_INVALID_DCI_SITE_ROLE
                  - code: ANYNET_LINK_DUPLICATE_ANYNET
                  - code: ANYNET_LINK_HUB_CLUSTER_VALID_ONLY_FOR_DCI
                  - code: ANYNET_LINK_BULK_OPERATIONS_LIMIT_EXCEEDED
                  - code: ANYNET_LINK_MULTIPLE_TYPES_NOT_SUPPORTED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Anynet Links in Bulk (v4.0)
      tags:
      - Topology
servers:
- url: https://api.sase.paloaltonetworks.com
tags:
- name: Topology
