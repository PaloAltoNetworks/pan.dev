components:
  schemas:
    APNProfile:
      properties:
        apn:
          description: "Enter the APN name that provides access to the mobile broadband\
            \ network. APN information is provided by the operator. APN name must\
            \ be greater than one character and an allowed max length is 100. Valid\
            \ characters are alphanumeric, hyphen (-), and period (.). Must begin\
            \ and end with an alphanumeric character.   \n"
          type: string
        authentication:
          description: "Authentication mode to access the mobile broadband network.\
            \ \n"
          enum:
          - none
          - pap
          - chap
          - pap_or_chap
          type: string
        clear_password:
          description: 'Ignore this field.

            '
          type: boolean
        description:
          description: "Enter the name or acronym of the mobile broadband service\
            \ provider or any other information that might help you to identify the\
            \ APN profile. Max size = 256. \n"
        id:
          description: 'Ignore this field.

            '
          type: string
        name:
          description: "Enter the name or acronym to identify the APN profile. Max\
            \ size = 128. \n"
          type: string
        password:
          description: 'If you select an authentication mode, enter the password to
            access the mobile broadband network.

            '
          type: string
        tags:
          description: "Tags is a mechanism to group the content for easy retrieval.\
            \ It helps to organize the content. The tags associate a relevance to\
            \ the object. \n"
          items:
            description: Tags
            type: string
          type: array
        user_name:
          description: 'If you select an authentication mode, enter the user name
            to access the mobile broadband network. Size (max = 100,  min = 0).

            '
          type: string
      required:
      - authentication
      - name
    APNProfileQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of APN profiles that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number of APN profiles for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the APN profiles for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "Lists the APN profiles by ID.    \n"
          type: string
        isReadPreferenceSecondary:
          description: 'IsReadPreferenceSecondary: JsonIgnore(value = true) '
          type: boolean
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: "The total number of query parameters. \n"
          format: int64
          type: integer
    APNProfileQueryFilter:
      properties:
        apn:
          description: "Enter the APN name that provides access to the mobile broadband\
            \ network. APN information is provided by the operator. APN name must\
            \ be greater than one character and an allowed max length is 100. Valid\
            \ characters are alphanumeric, hyphen (-), and period (.). Must begin\
            \ and end with an alphanumeric character.   \n"
          type: string
        authentication:
          description: "Authentication mode to access the mobile broadband network.\
            \ \n"
          enum:
          - none
          - pap
          - chap
          - pap_or_chap
          type: string
        clear_password:
          description: 'Ignore this field.

            '
          type: boolean
        description:
          description: "Enter the name or acronym of the mobile broadband service\
            \ provider or any other information that might help you to identify the\
            \ APN profile. Max size = 256. \n"
        id:
          description: 'Ignore this field.

            '
          type: string
        name:
          description: "Enter the name or acronym to identify the APN profile. Max\
            \ size = 128. \n"
          type: string
        password:
          description: 'If you select an authentication mode, enter the password to
            access the mobile broadband network.

            '
          type: string
        tags:
          description: "Tags is a mechanism to group the content for easy retrieval.\
            \ It helps to organize the content. The tags associate a relevance to\
            \ the object. \n"
          items:
            description: Tags
            type: string
          type: array
        user_name:
          description: 'If you select an authentication mode, enter the user name
            to access the mobile broadband network. Size (max = 100,  min = 0).

            '
          type: string
      required:
      - authentication
      - name
    APNProfileScreen:
      properties:
        apn:
          description: "Enter the APN name that provides access to the mobile broadband\
            \ network. APN information is provided by the operator. APN name must\
            \ be greater than one character and an allowed max length is 100. Valid\
            \ characters are alphanumeric, hyphen (-), and period (.). Must begin\
            \ and end with an alphanumeric character.   \n"
          type: string
        authentication:
          description: "Authentication mode to access the mobile broadband network.\
            \ \n"
          enum:
          - none
          - pap
          - chap
          - pap_or_chap
          type: string
        clear_password:
          description: 'Ignore this field.

            '
          type: boolean
        description:
          description: 'Enter the name or acronym of the mobile broadband service
            provider or any other information that might help you to identify the
            APN profile. Max size = 256.

            '
        id:
          description: 'Ignore this field.

            '
          type: string
        name:
          description: 'Enter the name or acronym to identify the APN profile. Max
            size = 128.

            '
          type: string
        password:
          description: 'If you select an authentication mode, enter the password to
            access the mobile broadband network.

            '
          type: string
        tags:
          description: "Tags is a mechanism to group the content for easy retrieval.\
            \ It helps to organize the content. The tags associate a relevance to\
            \ the object. \n"
          items:
            description: Tags
            type: string
          type: array
        user_name:
          description: 'If you select an authentication mode, enter the user name
            to access the mobile broadband network. Size (max = 100,  min = 0).

            '
          type: string
      required:
      - authentication
      - name
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items:
            $ref: '#/components/schemas/AggregateData'
          type: array
        field:
          description: 'The field value.

            '
          type: string
        name:
          description: 'The name of the query response.

            '
          type: string
        operator:
          description: 'The operator values.

            '
          enum:
          - max
          - count
          - min
          - avg
          - sum
          type: string
        statistic:
          description: 'The aggregate statistics.

            '
          type: string
        unit:
          description: 'The unit.

            '
          type: string
    AggregateData:
      properties:
        value:
          description: 'Value of the aggregate data.

            '
          type: number
        view:
          description: 'View the aggregate date.

            '
          type: object
    BulkResponse:
      properties:
        count:
          description: 'The total number.

            '
          format: int32
          type: integer
        id:
          description: 'The ID.

            '
          type: string
    CellularModule:
      properties:
        description:
          description: 'The description of the cellular module. Max size = 256.

            '
        gps_enable:
          description: 'Indicates if the GPS is enabled for the cellular module.

            '
          type: boolean
        id:
          description: 'The ID of the cellular module.

            '
          type: string
        name:
          description: "The name of the cellular module. Max size = 128. \n"
          type: string
        primary_sim:
          description: 'The primary SIM attribute of the cellular module.

            '
          type: integer
        radio_on:
          description: 'Indicates the radio on attribute.

            '
          type: boolean
        tags:
          description: 'An information field that can be added to identify the cellular
            module. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - primary_sim
      - gps_enable
      - radio_on
      - name
    CellularModuleFirmwareConfig:
      properties:
        cellular_module_image_ids:
          description: "The cellular module image ID. \n"
          items:
            description: Cellular Module Image Ids
            type: string
          type: array
        download_interval:
          description: 'The firmware download interval. Valid download interval must
            be provided along with a valid download time

            '
          type: integer
        id:
          description: 'The ID of the cellular module firmware.

            '
          type: string
        interface_ids:
          description: 'The interface IDs of the firmware. Max interfaces allowed
            is 4.

            '
          items:
            description: Interface Ids
            type: string
          type: array
        scheduled_download:
          description: 'The scheduled download of the firmware.

            '
          type: string
        scheduled_upgrade:
          description: 'The scheduled upgrade of the firmware.

            '
          type: string
        upgrade_interval:
          description: 'The upgrade interval of the firmware. Valid upgrade interval
            must be provided along with a valid upgrade time.

            '
          type: integer
    CellularModuleFirmwareConfigScreen:
      properties:
        cellular_module_image_ids:
          description: "The cellular module image ID. \n"
          items:
            description: Cellular Module Image Ids
            type: string
          type: array
        download_interval:
          description: 'The firmware download interval. Valid download interval must
            be provided along with a valid download time.

            '
          type: integer
        id:
          description: 'The ID of the cellular module firmware.

            '
          type: string
        interface_ids:
          description: 'The interface IDs of the firmware. Max interfaces allowed
            is 4.

            '
          items:
            description: Interface Ids
            type: string
          type: array
        scheduled_download:
          description: 'The scheduled download of the firmware.

            '
          type: string
        scheduled_upgrade:
          description: 'The scheduled upgrade of the firmware.

            '
          type: string
        upgrade_interval:
          description: 'The upgrade interval of the firmware. Valid upgrade interval
            must be provided along with a valid upgrade time.

            '
          type: integer
    CellularModuleFirmwareImageStatus:
      properties:
        download_interval:
          description: 'The firmware download interval. Valid download interval must
            be provided along with a valid download time.

            '
          type: integer
        id:
          description: 'The ID of the cellular module firmware.

            '
          type: string
        image_states:
          description: 'List the image states.

            '
          items:
            $ref: '#/components/schemas/CellularModuleFirmwareUpgradeImageState'
          type: array
        last_state_change:
          description: 'Lists the last state change of the firmware.

            '
          type: string
        scheduled_download:
          description: 'The scheduled download of the firmware.

            '
          type: string
        scheduled_upgrade:
          description: 'The scheduled upgrade of the firmware.

            '
          type: string
        upgrade_interval:
          description: 'The upgrade interval of the firmware. Valid upgrade interval
            must be provided along with a valid upgrade time.

            '
          type: integer
    CellularModuleFirmwareStatusQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of cellular module firmware that match
            the query parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number of cellular module firmware for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the cellular module firmware for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "Lists the cellular module firmware by ID.    \n"
          type: string
        isReadPreferenceSecondary:
          description: 'IsReadPreferenceSecondary: JsonIgnore(value = true) '
          type: boolean
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    CellularModuleFirmwareStatusQueryFilter:
      properties:
        active_image_id:
          description: 'The active image ID.

            '
          type: string
        active_version:
          description: 'The active version.

            '
          type: string
        download_interval:
          description: 'The download interval.

            '
          type: integer
        download_percent:
          description: 'The download percent.

            '
          type: integer
        element_id:
          description: 'The element ID.

            '
          type: string
        failure_info:
          description: 'Detials of the failure information.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        previous_image_id:
          description: 'The previous image ID.

            '
          type: string
        rollback_version:
          description: 'The rollback version.

            '
          type: string
        scheduled_download:
          description: 'The scheduled download.

            '
          type: string
        scheduled_upgrade:
          description: 'The scheduled upgrade.

            '
          type: string
        upgrade_image_id:
          description: 'The upgrade image ID.

            '
          type: string
        upgrade_interval:
          description: 'The upgrade interval.

            '
          type: integer
        upgrade_state:
          description: 'The upgrade state.

            '
          enum:
          - pending
          - init
          - discovery
          - download
          - download_init
          - download_complete
          - download_scheduled
          - download_abort
          - download_cancelled
          - download_cancel_pending
          - upgrade
          - upgrade_init
          - upgrade_complete
          - upgrade_scheduled
          - upgrade_abort
          - upgrade_cancelled
          - upgrade_cancel_pending
          - upgrade_failed
          - upgrading
          - verify
          - in-progress
          - install
          - complete
          - done
          - failed
          - download_discovery
          - download_failed
          - downloading
          - install_failed
          - installing
          - verifying
          - waiting_download_time
          - waiting_install_time
          - abort_upgrade_pending
          - abort_download_pending
          - invalid
          type: string
    CellularModuleFirmwareUpgradeImageState:
      properties:
        current_carrier:
          description: Current Carrier
          type: string
        current_fw_version:
          description: Current Fw Version
          type: string
        current_pri_version:
          description: Current Pri Version
          type: string
        download_percent:
          description: Download Percent
          type: integer
        download_start:
          description: Download Start
          type: string
        status:
          description: Status
          type: string
        status_info:
          description: Status Info
          type: string
        upgrade_carrier:
          description: Upgrade Carrier
          type: string
        upgrade_fw_version:
          description: Upgrade Fw Version
          type: string
        upgrade_location:
          description: Upgrade Location
          type: string
        upgrade_pri_version:
          description: Upgrade Pri Version
          type: string
        upgrade_start:
          description: Upgrade Start
          type: string
    CellularModuleImage:
      properties:
        carrier:
          description: 'The name of the carrier.

            '
          type: string
        fw_version:
          description: 'The Fw version.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        inventory_id:
          description: 'The inventory ID.

            '
          type: string
        maximum_ion_version:
          description: 'The maximum ION version.

            '
          type: string
        minimum_ion_version:
          description: 'The minimum ION version.

            '
          type: string
        modem_models:
          description: 'The modem models.

            '
          items:
            description: Modem Models
            type: string
          type: array
        pri_version:
          description: 'The pri version.

            '
          type: string
        release_url:
          description: 'The release URL.

            '
          type: string
        state:
          description: 'The state of the image.

            '
          type: string
    CellularModuleQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of cellular module that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number of cellular module for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the cellular module for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "Lists the cellular module by ID.    \n"
          type: string
        isReadPreferenceSecondary:
          description: 'IsReadPreferenceSecondary: JsonIgnore(value = true) '
          type: boolean
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    CellularModuleQueryFilter:
      properties:
        description:
          description: 'The description of the cellular module. Max size = 256.

            '
        element_id:
          description: 'The element ID.

            '
          type: string
        gps_enable:
          description: 'Indicates if the GPS is enabled for the cellular module.

            '
          type: boolean
        id:
          description: 'The ID of the cellular module.

            '
          type: string
        name:
          description: "The name of the cellular module. Max size = 128.urce name.\
            \ \n"
          type: string
        primary_sim:
          description: 'The primary SIM attribute of the cellular module.

            '
          type: integer
        radio_on:
          description: 'Indicates the radio on attribute.

            '
          type: boolean
        tags:
          description: 'An information field that can be added to identify the cellular
            module. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - primary_sim
      - gps_enable
      - radio_on
      - name
    CellularModuleRevokedImage:
      properties:
        carrier:
          description: 'The name of the cellular module carrier.

            '
          type: string
        fw_version:
          description: 'The Firmware version.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        inventory_id:
          description: "The inventory ID.          \n"
          type: string
        pri_version:
          description: 'The cellular module firmware pri version.

            '
          type: string
        state:
          description: 'The cellular state.

            '
          type: string
      required:
      - carrier
      - pri_version
      - fw_version
    CellularModuleScreen:
      properties:
        description:
          description: 'The description of the cellular module. Max size = 256.

            '
        gps_enable:
          description: 'Indicates if the GPS is enabled for the cellular module.

            '
          type: boolean
        id:
          description: 'The ID of the cellular module.

            '
          type: string
        name:
          description: "The name of the cellular module. Max size = 128. \n"
          type: string
        primary_sim:
          description: 'The primary SIM attribute of the cellular module.

            '
          type: integer
        radio_on:
          description: 'Indicates the radio on attribute.

            '
          type: boolean
        tags:
          description: "An information field that can be added to identify the cellular\
            \ module. Maximum 10 unique tags of length 1024 each are allowed.    \
            \  \n"
          items:
            description: Tags
            type: string
          type: array
      required:
      - primary_sim
      - gps_enable
      - radio_on
      - name
    CellularModuleState:
      properties:
        activation_state:
          description: 'The activation state of the cellular module.

            '
          type: string
        active_sim:
          description: 'The active SIM of the cellular module.

            '
          type: integer
        carrier:
          description: 'The name of the carrier.

            '
          type: string
        firmware:
          description: 'The firmware of the cellular module.

            '
          items:
            $ref: '#/components/schemas/FirmwareState'
          type: array
        gps:
          $ref: '#/components/schemas/GPSState'
          description: 'The GPS, if enabled for the cellular module.

            '
        id:
          description: 'The ID of the cellular module.

            '
          type: string
        imei:
          description: 'The IMEI number.

            '
          type: string
        last_state_change:
          description: 'Lists the last state change of the firmware.

            '
          format: int64
          type: integer
        manufacturer:
          description: 'The manufacturer of the firmware.

            '
          type: string
        model_name:
          description: 'The model name.

            '
          type: string
        modem_error_reason:
          description: 'The reason for a modem error.

            '
          type: string
        modem_state:
          description: 'The modem state.

            '
          type: string
        network_registration_state:
          description: 'The network registration state.

            '
          type: string
        network_state:
          $ref: '#/components/schemas/NetworkState'
          description: 'The network state.

            '
        packet_service_state:
          description: 'The packet service state.

            '
          type: string
        serial_number:
          description: 'The serial number.

            '
          type: string
        signal_strength_indicator:
          description: 'The signal strength indicator.

            '
          type: string
        sim:
          description: 'Details of the SIM.

            '
          items:
            $ref: '#/components/schemas/SimState'
          type: array
        technology:
          description: 'The technology of the cellular module.

            '
          type: string
    CellularModuleStateV2N1:
      properties:
        activation_state:
          description: 'The activation state of the cellular module.

            '
          type: string
        active_sim:
          description: 'The active SIM of the cellular module.

            '
          type: integer
        carrier:
          description: 'The name of the carrier.

            '
          type: string
        cellular_module_id:
          description: 'The cellular module ID.

            '
          type: string
        element_id:
          description: Element Id
          type: string
        firmware:
          description: 'The firmware of the cellular module.

            '
          items:
            $ref: '#/components/schemas/FirmwareState'
          type: array
        gps:
          $ref: '#/components/schemas/GPSState'
          description: 'The GPS, if enabled for the cellular module.

            '
        id:
          description: 'The ID of the cellular module.

            '
          type: string
        imei:
          description: 'The IMEI number.

            '
          type: string
        last_state_change:
          description: 'Lists the last state change of the firmware.

            '
          format: int64
          type: integer
        manufacturer:
          description: 'The manufacturer of the firmware.

            '
          type: string
        model_name:
          description: 'The model name.

            '
          type: string
        modem_error_reason:
          description: 'The reason for a modem error.

            '
          type: string
        modem_state:
          description: 'The modem state.

            '
          type: string
        network_registration_state:
          description: 'The network registration state.

            '
          type: string
        network_state:
          $ref: '#/components/schemas/NetworkState'
          description: 'The network state.

            '
        packet_service_state:
          description: 'The packet service state.

            '
          type: string
        serial_number:
          description: 'The serial number.

            '
          type: string
        signal_strength_indicator:
          description: 'The signal strength indicator.

            '
          type: string
        sim:
          description: 'Details of the SIM.

            '
          items:
            $ref: '#/components/schemas/SimState'
          type: array
        technology:
          description: 'The technology of the cellular module.

            '
          type: string
    CellularModuleStatusQueryFilterV2N1:
      properties:
        activation_state:
          description: 'The activation state of the cellular module.

            '
          type: string
        active_sim:
          description: 'The active SIM of the cellular module.

            '
          type: integer
        carrier:
          description: 'The name of the carrier.

            '
          type: string
        cellular_module_id:
          description: 'The cellular module ID.

            '
          type: string
        element_id:
          description: Element Id
          type: string
        firmware:
          description: 'The firmware of the cellular module.

            '
          items:
            $ref: '#/components/schemas/FirmwareState'
          type: array
        gps:
          $ref: '#/components/schemas/GPSState'
          description: 'The GPS, if enabled for the cellular module.

            '
        id:
          description: 'The ID of the cellular module.

            '
          type: string
        imei:
          description: 'The IMEI number.

            '
          type: string
        last_state_change:
          description: 'Lists the last state change of the firmware.

            '
          format: int64
          type: integer
        manufacturer:
          description: 'The manufacturer of the firmware.

            '
          type: string
        model_name:
          description: 'The model name.

            '
          type: string
        modem_error_reason:
          description: 'The reason for a modem error.

            '
          type: string
        modem_state:
          description: 'The modem state.

            '
          type: string
        network_registration_state:
          description: 'The network registration state.

            '
          type: string
        network_state:
          $ref: '#/components/schemas/NetworkState'
          description: 'The network state.

            '
        packet_service_state:
          description: 'The packet service state.

            '
          type: string
        serial_number:
          description: 'The serial number.

            '
          type: string
        signal_strength_indicator:
          description: 'The signal strength indicator.

            '
          type: string
        sim:
          description: 'Details of the SIM.

            '
          items:
            $ref: '#/components/schemas/SimState'
          type: array
        technology:
          description: 'The technology of the cellular module.

            '
          type: string
    CellularModuleStatusQueryV2N1:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of cellular modules that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number cellular modules deleted for a time frame.

            '
          type: boolean
        group_by:
          description: 'Group the cellular modules for the query.

            '
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the cellular modules by ID.    \n"
          type: string
        isReadPreferenceSecondary:
          description: 'If the read preference is secondary. True or False.

            '
          type: boolean
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          description: 'Query using query parameters.

            '
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          description: 'Specify the order in which you want the search criteria to
            display.

            '
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    ElementAccessConfig:
      properties:
        account_disable_interval:
          description: 'The account disable interval.

            '
          format: int32
          type: integer
        id:
          description: 'The ID of the element.

            '
          type: string
        inactive_interval:
          description: 'The inactive interval.

            '
          format: int32
          type: integer
        otpkey_version:
          description: 'The version of the OTP key.

            '
          format: int32
          type: integer
        retry_login_count:
          description: 'The count for retry logins.

            '
          format: int32
          type: integer
        ssh_enabled:
          description: 'Indicates if SSH is enabled.

            '
          type: boolean
    ElementAccessConfigScreen:
      properties:
        account_disable_interval:
          description: 'The account disable interval.

            '
          format: int32
          type: integer
        id:
          description: 'The ID of the element.

            '
          type: string
        inactive_interval:
          description: 'The inactive interval.

            '
          format: int32
          type: integer
        otpkey_version:
          description: 'The version of the OTP key.

            '
          format: int32
          type: integer
        retry_login_count:
          description: 'The count for retry logins.

            '
          format: int32
          type: integer
        ssh_enabled:
          description: 'Indicates if SSH is enabled.

            '
          type: boolean
    ElementAccessConfigScreenV2N2:
      properties:
        account_disable_interval:
          description: 'The account disable interval.

            '
          format: int32
          type: integer
        id:
          description: 'The ID of the element.

            '
          type: string
        inactive_interval:
          description: 'The inactive interval.

            '
          format: int32
          type: integer
        otpkey_version:
          description: 'The version of the OTP key.

            '
          format: int32
          type: integer
        retry_login_count:
          description: 'The count for retry logins.

            '
          format: int32
          type: integer
        ssh_enabled:
          description: 'Indicates if SSH is enabled.

            '
          type: boolean
        ssh_outbound_enabled:
          description: 'Indicates if SSH is outbound enabled.

            '
          type: boolean
    ElementAccessConfigV2N2:
      properties:
        account_disable_interval:
          description: 'The account disable interval.

            '
          format: int32
          type: integer
        id:
          description: 'The ID of the element.

            '
          type: string
        inactive_interval:
          description: 'The inactive interval.

            '
          format: int32
          type: integer
        otpkey_version:
          description: 'The version of the OTP key.

            '
          format: int32
          type: integer
        retry_login_count:
          description: 'The count for retry logins.

            '
          format: int32
          type: integer
        ssh_enabled:
          description: 'Indicates if SSH is enabled.

            '
          type: boolean
        ssh_outbound_enabled:
          description: 'Indicates if SSH is outbound enabled.

            '
          type: boolean
    ElementAccessState:
      properties:
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID of the element.

            '
          type: string
        otpkey_version:
          description: 'The OTP key version.

            '
          format: int32
          type: integer
    ElementOTPAccess:
      properties:
        challenge_phrase:
          description: 'The challenge phrase.

            '
          type: string
        id:
          description: 'The ID of the element.

            '
          type: string
        response_phrase:
          description: 'The response phrase.

            '
          type: string
      required:
      - challenge_phrase
    Error:
      properties:
        _error:
          items:
            $ref: '#/components/schemas/ErrorResponse'
          type: array
    ErrorResponse:
      properties:
        code:
          description: 'The error code.

            '
          type: string
        message:
          description: 'The error message.

            '
          type: string
    FirmwareState:
      properties:
        active:
          description: Active
          type: boolean
        carrier:
          description: Carrier
          type: string
        firmware_state:
          description: Firmware State
          type: string
        fw_version:
          description: Fw Version
          type: string
        pri_version:
          description: Pri Version
          type: string
        storage_location:
          description: Storage Location
          type: string
    GPSState:
      properties:
        latitude:
          description: Latitude
          format: double
          type: number
        longitude:
          description: Longitude
          format: double
          type: number
        state:
          description: State
          type: string
    ListQueryResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        deleted_count:
          description: 'The deleted number.

            '
          format: int64
          type: integer
        deleted_ids:
          description: 'The deleted IDs.

            '
          items:
            type: string
          type: array
        description:
          description: 'Description of the query. Max size = 256.

            '
        id:
          description: 'The ID.

            '
          type: string
        next_query:
          description: 'Details of the next query.

            '
          type: object
        total_count:
          description: 'Total number.

            '
          format: int64
          type: integer
      type: object
    ListResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        id:
          description: 'The ID.

            '
          type: string
      type: object
    MachineCellularModuleFirmwareConfig:
      properties:
        cellular_module_image_ids:
          description: 'The cellular module image ID.

            '
          items:
            description: Cellular Module Image Ids
            type: string
          type: array
        id:
          description: "The ID of the cellular module firmware. \n"
          type: string
    MachineCellularModuleFirmwareConfigScreen:
      properties:
        cellular_module_image_ids:
          description: 'The cellular module image ID.

            '
          items:
            description: Cellular Module Image Ids
            type: string
          type: array
        id:
          description: 'The ID of the cellular module firmware.

            '
          type: string
    MachineCellularModuleFirmwareImageStatus:
      properties:
        download_interval:
          description: 'The firmware download interval. Valid download interval must
            be provided along with a valid download time.

            '
          type: integer
        id:
          description: 'The ID of the cellular module firmware.

            '
          type: string
        image_states:
          description: 'List the image states.

            '
          items:
            $ref: '#/components/schemas/CellularModuleFirmwareUpgradeImageState'
          type: array
        last_state_change:
          description: 'Lists the last state change of the firmware.

            '
          type: string
        scheduled_download:
          description: 'The scheduled download of the firmware.

            '
          type: string
        scheduled_upgrade:
          description: 'The scheduled upgrade of the firmware.

            '
          type: string
        upgrade_interval:
          description: 'The upgrade interval of the firmware. Valid upgrade interval
            must be provided along with a valid upgrade time.

            '
          type: integer
    MachineCellularModuleInfo:
      properties:
        id:
          description: 'The ID of the element.

            '
          type: string
        imei:
          description: 'The IMEI number.

            '
          type: string
        manufacturer:
          description: 'The manufacturer of the firmware.

            '
          type: string
        model_name:
          description: 'The model name.

            '
          type: string
        serial_number:
          description: "The serial number. \n"
          type: string
    MachineCellularModuleState:
      properties:
        activation_state:
          description: 'The activation state of the cellular module.

            '
          type: string
        active_sim:
          description: 'The active SIM of the cellular module.

            '
          type: integer
        carrier:
          description: 'The name of the carrier.

            '
          type: string
        firmware:
          description: 'The firmware of the cellular module.

            '
          items:
            $ref: '#/components/schemas/FirmwareState'
          type: array
        gps:
          $ref: '#/components/schemas/GPSState'
          description: 'The GPS, if enabled for the cellular module.

            '
        id:
          description: 'The ID of the cellular module.

            '
          type: string
        imei:
          description: 'The IMEI number.

            '
          type: string
        last_state_change:
          description: 'Lists the last state change of the firmware.

            '
          format: int64
          type: integer
        manufacturer:
          description: 'The manufacturer of the firmware.

            '
          type: string
        model_name:
          description: 'The model name.

            '
          type: string
        modem_error_reason:
          description: 'The reason for a modem error.

            '
          type: string
        modem_state:
          description: 'The modem state.

            '
          type: string
        network_registration_state:
          description: 'The network registration state.

            '
          type: string
        network_state:
          $ref: '#/components/schemas/NetworkState'
          description: 'The network state.

            '
        packet_service_state:
          description: 'The packet service state.

            '
          type: string
        serial_number:
          description: 'The serial number.

            '
          type: string
        signal_strength_indicator:
          description: 'The signal strength indicator.

            '
          type: string
        sim:
          description: 'Details of the SIM.

            '
          items:
            $ref: '#/components/schemas/SimState'
          type: array
        technology:
          description: 'The technology of the cellular module.

            '
          type: string
    NetworkState:
      properties:
        cell_id:
          description: Cell Id
          type: integer
        frequency_band:
          description: Frequency Band
          type: string
        mcc:
          description: Mcc
          type: integer
        mnc:
          description: Mnc
          type: integer
        roaming:
          description: Roaming
          type: boolean
    OTPAccessConfig:
      properties:
        id:
          description: 'The ID of the config.

            '
          type: string
        otp_attempts:
          description: 'The number of otp attempts. Min value = 2L / Max value = 20L.

            '
          format: int32
          type: integer
        otp_validity_minutes:
          description: 'The time for the otp validity. Min value = 5L / Max value
            = 60L.

            '
          format: int32
          type: integer
    SimSecurity:
      properties:
        description:
          description: 'The description of the sim security. Max size = 256.

            '
        id:
          description: 'The ID of the sim security.

            '
          type: string
        name:
          description: "The name of the sim security. Max size = 128. \n"
          type: string
        pin:
          description: 'The pin of the sim security. Pin must be numeric with minimum
            4 and maximum 8 digits.

            '
          type: string
        remove_pin:
          description: 'The remove pin of the sim security.

            '
          type: boolean
        slot_number:
          description: 'The slot number of the sim.

            '
          format: int32
          type: integer
        tags:
          description: 'An information field that can be added to identify the sim
            security. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - remove_pin
      - slot_number
      - name
    SimSecurityScreen:
      properties:
        description:
          description: 'The description of the sim security. Max size = 256.

            '
        id:
          description: 'The ID of the sim security.

            '
          type: string
        name:
          description: "The name of the sim security. Max size = 128.urce name. \n"
          type: string
        pin:
          description: 'The pin of the sim security. Pin must be numeric with minimum
            4 and maximum 8 digits.

            '
          type: string
        remove_pin:
          description: 'The remove pin of the sim security.

            '
          type: boolean
        slot_number:
          description: 'The slot number of the sim.

            '
          format: int32
          type: integer
        tags:
          description: 'An information field that can be added to identify the sim
            security. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - remove_pin
      - slot_number
      - name
    SimState:
      properties:
        carrier:
          description: Carrier
          type: string
        iccid:
          description: Iccid
          type: string
        imsi:
          description: Imsi
          type: string
        pin_state:
          description: Pin State
          type: string
        present:
          description: Present
          type: boolean
        remaining_attempts_pin_verify:
          description: Remaining Attempts Pin Verify
          type: integer
        remaining_attempts_puk_unblock:
          description: Remaining Attempts Puk Unblock
          type: integer
        slot_number:
          description: Slot Number
          type: integer
info:
  contact: {}
  description: List of APIs used to manage the cellular device, services, APN profiles,
    and modules.
  title: Cellular
  version: Latest
openapi: 3.0.0
paths:
  /sdwan/v2.0/api/apnprofiles:
    get:
      description: List all APN Profiles.
      operationId: get-sdwan-v2.0-api-apnprofiles
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/APNProfile'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get APN Profiles (v2.0)
      tags:
      - Cellular
    post:
      description: Create an APN Profile.
      operationId: post-sdwan-v2.0-api-apnprofiles
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/APNProfileScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/APNProfile'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: APN_PROFILE_MISSING_APN_NAME
                  - code: APN_PROFILE_NAME_INVALID
                  - code: APN_PROFILE_PASSWORD_INVALID
                  - code: APN_PROFILE_USERNAME_INVALID
                  - code: APN_PROFILE_INVALID_APN_AUTH
                  - code: APN_PROFILE_PASSWORD_WITHOUT_USERNAME
                  - code: APN_PROFILE_PASSWORD_PRESENT_WHEN_CLEAR_PASSWORD_TRUE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create an APN Profile (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/apnprofiles/query:
    post:
      description: Query the DB for limit number of APN profiles that match the query
        parameters.
      operationId: post-sdwan-v2.0-api-apnprofiles-query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/APNProfileQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/APNProfileQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
      summary: Query DB for APN Profiles (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/apnprofiles/{apnprofile_id}:
    delete:
      description: Delete an APN Profile.
      operationId: delete-sdwan-v2.0-api-apnprofiles-apnprofile_id
      parameters:
      - description: The ID of the APN profile.
        in: path
        name: apnprofile_id
        required: true
        schema:
          description: Apnprofile Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/APNProfile'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: APN_PROFILE_IN_USE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete APN Profile (v2.0)
      tags:
      - Cellular
    get:
      description: Get APN Profile.
      operationId: get-sdwan-v2.0-api-apnprofiles-apnprofile_id
      parameters:
      - description: The ID of the APN profile.
        in: path
        name: apnprofile_id
        required: true
        schema:
          description: Apnprofile Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/APNProfile'
          description: Successful Operation
      summary: Get APN Profile (v2.0)
      tags:
      - Cellular
    put:
      description: Update an APN Profile.
      operationId: put-sdwan-v2.0-api-apnprofiles-apnprofile_id
      parameters:
      - description: The ID of the APN profile.
        in: path
        name: apnprofile_id
        required: true
        schema:
          description: Apnprofile Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/APNProfileScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/APNProfile'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: APN_PROFILE_MISSING_APN_NAME
                  - code: APN_PROFILE_NAME_INVALID
                  - code: APN_PROFILE_PASSWORD_INVALID
                  - code: APN_PROFILE_USERNAME_INVALID
                  - code: APN_PROFILE_INVALID_APN_AUTH
                  - code: APN_PROFILE_PASSWORD_WITHOUT_USERNAME
                  - code: APN_PROFILE_PASSWORD_PRESENT_WHEN_CLEAR_PASSWORD_TRUE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update APN Profile (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/cellular_module_firmware/status/query:
    post:
      description: Query the cellular module firmware upgrade status of all tenant
        elements.
      operationId: post-sdwan-v2.0-api-cellular_module_firmware-status-query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CellularModuleFirmwareStatusQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/CellularModuleFirmwareStatusQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: BAD_QUERY_REQUEST
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Query Cellular Module Firmware Upgrade Status (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/cellular_module_images:
    get:
      description: Get an existing element cellular module image.
      operationId: get-sdwan-v2.0-api-cellular_module_images
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/CellularModuleImage'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Element Cellular Module Image (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/cellular_module_images/{image_id}:
    get:
      description: Get information about a element cellular module image.
      operationId: get-sdwan-v2.0-api-cellular_module_images-image_id
      parameters:
      - description: ID of the image.
        in: path
        name: image_id
        required: true
        schema:
          description: Image Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleImage'
          description: Successful Operation
      summary: Get Information about a Element Cellular Module Image (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/cellular_module_revoked_images:
    post:
      description: Create a revoked cellular image.
      operationId: post-sdwan-v2.0-api-cellular_module_revoked_images
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CellularModuleRevokedImage'
        description: Details of the cellular module revoked image
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleRevokedImage'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: CELLULAR_MODULE_FW_INVALID_VERSION_MISSING
                  - code: CELLULAR_MODULE_FW_INVALID_PRI_VERSION_MISSING
                  - code: CELLULAR_MODULE_FW_INVALID_CARRIER_MISSING
                  - code: CELLULAR_MODULE_FW_INVALID_VERSION
                  - code: CELLULAR_MODULE_FW_INVALID_PRI_VERSION
                  - code: CELLULAR_MODULE_FW_INVALID_CARRIER
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Revoked Cellular Image (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/cellular_modules/query:
    post:
      description: Query the DB for limit number of cellular modules that match query
        parameters.
      operationId: post-sdwan-v2.0-api-cellular_modules-query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CellularModuleQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/CellularModuleQueryFilter'
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Cellular Modules (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/cellular_modules:
    get:
      description: List all the cellular modules.
      operationId: get-sdwan-v2.0-api-elements-element_id-cellular_modules
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModule'
          description: Successful Operation
      summary: Get Cellular Modules (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/cellular_modules/{cellular_module_id}:
    get:
      description: List a specific cellular module.
      operationId: get-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModule'
          description: Successful Operation
      summary: Get Cellular Module (v2.0)
      tags:
      - Cellular
    put:
      description: Update a specific cellular module.
      operationId: put-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CellularModuleScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModule'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: CELLULAR_MODULE_CONFIG_RADIO_ON_REQD
                  - code: CELLULAR_MODULE_CONFIG_GPS_ENABLE_REQD
                  - code: CELLULAR_MODULE_CONFIG_PRIMARY_SIM_REQD
                  - code: CELLULAR_MODULE_CONFIG_INVALID_PRIMARY_SIM
                  - code: CELLULAR_MODULE_CONFIG_NAME_CHANGE_NOT_SUPPORTED
                  - code: CELLULAR_MODULE_CONFIG_UNSUPPORTED_DEVICESW
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Cellular Module (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/cellular_modules/{cellular_module_id}/firmware:
    get:
      description: List the cellular module firmware configuration.
      operationId: get-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id-firmware
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleFirmwareConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: FW_CONFIG_NOTFOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Cellular Module Firmware Configuration (v2.0)
      tags:
      - Cellular
    put:
      description: Update the cellular module firmware configuration.
      operationId: put-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id-firmware
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CellularModuleFirmwareConfigScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleFirmwareConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: FW_CONFIG_NOTFOUND
                  - code: FW_CONFIG_INVALID_MISSING_IMAGE_IDS
                  - code: FW_CONFIG_INVALID_IMAGE_IDS_COUNT
                  - code: FW_CONFIG_INVALID_INTERFACES
                  - code: FW_CONFIG_INVALID_DOWNLOAD_INTERVAL
                  - code: FW_CONFIG_INVALID_UPGRADE_INTERVAL
                  - code: FW_CONFIG_INVALID_DOWNLOAD_ONLY_NOTSUPPORTED
                  - code: FW_CONFIG_INVALID_DOWNLOAD_TIME
                  - code: FW_CONFIG_INVALID_UPGRADE_TIME
                  - code: FW_CONFIG_INCOMPATIBLE_SCHEDULES
                  - code: FW_CONFIG_IMAGES_FOR_SAME_CARRIER_NOT_ALLOWED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Cellular Module Firmware Configuration (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/cellular_modules/{cellular_module_id}/firmware/status:
    get:
      description: List the cellular module firmware configuration status.
      operationId: get-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id-firmware-status
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleFirmwareImageStatus'
          description: Successful Operation
      summary: Get Cellular Module Firmware Configuration Status (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/cellular_modules/{cellular_module_id}/sim_security:
    get:
      description: List all the cellular modules sim security information.
      operationId: get-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id-sim_security
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/BulkResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/SimSecurity'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Cellular Modules Sim Security (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/cellular_modules/{cellular_module_id}/sim_security/{sim_security_id}:
    get:
      description: List the cellular module sim security information.
      operationId: get-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id-sim_security-sim_security_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      - description: ID of the sim security.
        in: path
        name: sim_security_id
        required: true
        schema:
          description: Sim Security Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimSecurity'
          description: Successful Operation
      summary: Get Cellular Modules Sim Security (v2.0)
      tags:
      - Cellular
    put:
      description: Update the cellular modules sim security information.
      operationId: put-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id-sim_security-sim_security_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      - description: ID of the sim security.
        in: path
        name: sim_security_id
        required: true
        schema:
          description: Sim Security Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimSecurityScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimSecurity'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: SIM_SECURITY_CONFIG_REMOVE_PIN_REQD
                  - code: SIM_SECURITY_CONFIG_PIN_MUST_BE_EMPTY
                  - code: SIM_SECURITY_CONFIG_MUST_HAVE_PIN
                  - code: SIM_SECURITY_CONFIG_INVALID_PIN
                  - code: SIM_SECURITY_PIN_STORAGE_FAILURE
                  - code: SIM_SECURITY_CONFIG_UNSUPPORTED_DEVICESW
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Cellular Modules Sim Security (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/cellular_modules/{cellular_module_id}/status:
    get:
      description: Get the cellular module status.
      operationId: get-sdwan-v2.0-api-elements-element_id-cellular_modules-cellular_module_id-status
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleState'
          description: Successful Operation
      summary: Get Cellular Module Status (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/elementaccessstates:
    get:
      description: Get a specific element's access state.
      operationId: get-sdwan-v2.0-api-elements-element_id-elementaccessstates
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessState'
          description: Successful Operation
      summary: Get Element Access State (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/elements/{element_id}/otpaccess:
    post:
      description: Verify the challenge phrase and generate the response phrase.
      operationId: post-sdwan-v2.0-api-elements-element_id-otpaccess
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElementOTPAccess'
        description: Details of the challenge phrase and response phrase created
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementOTPAccess'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: CHALLENGE_VERIFICATION_FAILED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Verify Challenge Phrase and Generate Response Phrase (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/machines/{machine_id}/cellular_modules:
    get:
      description: List all cellular modules.
      operationId: get-sdwan-v2.0-api-machines-machine_id-cellular_modules
      parameters:
      - description: The ID of the device.
        in: path
        name: machine_id
        required: true
        schema:
          description: Machine Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/BulkResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/MachineCellularModuleInfo'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MACHINE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Cellular Modules (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/machines/{machine_id}/cellular_modules/{cellular_module_id}:
    get:
      description: List a specific cellular module.
      operationId: get-sdwan-v2.0-api-machines-machine_id-cellular_modules-cellular_module_id
      parameters:
      - description: The ID of the device.
        in: path
        name: machine_id
        required: true
        schema:
          description: Machine Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MachineCellularModuleInfo'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MACHINE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Cellular Module (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/machines/{machine_id}/cellular_modules/{cellular_module_id}/firmware:
    get:
      description: Get the cellular module firmware configuration.
      operationId: get-sdwan-v2.0-api-machines-machine_id-cellular_modules-cellular_module_id-firmware
      parameters:
      - description: The ID of the device.
        in: path
        name: machine_id
        required: true
        schema:
          description: Machine Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MachineCellularModuleFirmwareConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: CELLULAR_MODULE_FW_CONFIG_NOTFOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Cellular Module Firmware Configuration (v2.0)
      tags:
      - Cellular
    put:
      description: Update the cellular module firmware configuration.
      operationId: put-sdwan-v2.0-api-machines-machine_id-cellular_modules-cellular_module_id-firmware
      parameters:
      - description: The ID of the device.
        in: path
        name: machine_id
        required: true
        schema:
          description: Machine Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MachineCellularModuleFirmwareConfigScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MachineCellularModuleFirmwareConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: MACHINE_NOT_FOUND
                  - code: CELLULAR_MODULE_FW_CONFIG_NOTFOUND
                  - code: CELLULAR_MODULE_FW_CONFIG_UNSUPPORTED_DEVICE
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_MISSING_IMAGE_IDS
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_IMAGE_IDS_COUNT
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_NO_IMAGE_PRESENT
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_IMAGE_NOT_SUPPORTED
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_UNSUPPORTED_MIN_ELEMENT_VERSION
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_UNSUPPORTED_MAX_ELEMENT_VERSION
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_UNSUPPORTED_ELEMENT_VERSION
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_IMAGE_UNSUPPORTED_MODEMMODEL
                  - code: CELLULAR_MODULE_FW_CONFIG_INVALID_IMAGE_UPGRADE_INPROGESS
                  - code: CELLULAR_MODULE_FW_CONFIG_TENANT_NOTALLOCATED_FOR_IMAGE
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Cellular Module Firmware Configuration (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/machines/{machine_id}/cellular_modules/{cellular_module_id}/firmware/status:
    get:
      description: Get the cellular module firmware configuration status.
      operationId: get-sdwan-v2.0-api-machines-machine_id-cellular_modules-cellular_module_id-firmware-status
      parameters:
      - description: The ID of the device.
        in: path
        name: machine_id
        required: true
        schema:
          description: Machine Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MachineCellularModuleFirmwareImageStatus'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: CELLULAR_MODULE_FW_CONFIG_STATUS_NOTFOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Cellular Module Firmware Configuration Status (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/machines/{machine_id}/cellular_modules/{cellular_module_id}/status:
    get:
      description: Get the cellular module status.
      operationId: get-sdwan-v2.0-api-machines-machine_id-cellular_modules-cellular_module_id-status
      parameters:
      - description: The ID of the device.
        in: path
        name: machine_id
        required: true
        schema:
          description: Machine Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MachineCellularModuleState'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MACHINE_NOT_FOUND
                  - code: CELLULAR_MODULE_NOT_FOUND
                  - code: CELLULAR_MODULE_STATUS_NOT_REPORTED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Cellular Module Status (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/otpaccessconfigs:
    get:
      description: List all the otp access configs.
      operationId: get-sdwan-v2.0-api-otpaccessconfigs
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OTPAccessConfig'
          description: Successful Operation
      summary: Get OTP Access Configs (v2.0)
      tags:
      - Cellular
  /sdwan/v2.0/api/otpaccessconfigs/{otpaccess_config_id}:
    put:
      description: Update an OTP Access for all the elements under a Tenant.
      operationId: put-sdwan-v2.0-api-otpaccessconfigs-otpaccess_config_id
      parameters:
      - description: The OTP access config ID.
        in: path
        name: otpaccess_config_id
        required: true
        schema:
          description: Otpaccess Config Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OTPAccessConfig'
        description: Details of the challenge phrase and response phrase updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OTPAccessConfig'
          description: Successful Operation
      summary: Update OTP Access for Elements (v2.0)
      tags:
      - Cellular
  /sdwan/v2.1/api/cellular_module_images:
    get:
      description: Get an existing element cellular module image.
      operationId: get-sdwan-v2.1-api-cellular_module_images
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/CellularModuleImage'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Element Cellular Module Image (v2.1)
      tags:
      - Cellular
  /sdwan/v2.1/api/cellular_module_images/{image_id}:
    get:
      description: Get information about a element cellular module image.
      operationId: get-sdwan-v2.1-api-cellular_module_images-image_id
      parameters:
      - description: ID of the image.
        in: path
        name: image_id
        required: true
        schema:
          description: Image Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleImage'
          description: Successful Operation
      summary: Get Information about Element Cellular Module Image (v2.1)
      tags:
      - Cellular
  /sdwan/v2.1/api/cellular_modules/status/query:
    post:
      description: Query the DB for limit number of cellular module status that match
        query params.
      operationId: post-sdwan-v2.1-api-cellular_modules-status-query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CellularModuleStatusQueryV2N1'
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListQueryResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/CellularModuleStatusQueryFilterV2N1'
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Cellular Module Status (v2.1)
      tags:
      - Cellular
  /sdwan/v2.1/api/elements/{element_id}/cellular_modules/{cellular_module_id}/status:
    get:
      description: Get the cellular module status.
      operationId: get-sdwan-v2.1-api-elements-element_id-cellular_modules-cellular_module_id-status
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the cellular module.
        in: path
        name: cellular_module_id
        required: true
        schema:
          description: Cellular Module Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CellularModuleStateV2N1'
          description: Successful Operation
      summary: Get Cellular Module Status (v2.1)
      tags:
      - Cellular
  /sdwan/v2.1/api/elements/{element_id}/elementaccessconfigs:
    get:
      description: Get all the element access configuration.
      operationId: get-sdwan-v2.1-api-elements-element_id-elementaccessconfigs
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessConfig'
          description: Successful Operation
      summary: Get Element Access Config (v2.1)
      tags:
      - Cellular
    post:
      description: Create all the element access configuration.
      operationId: post-sdwan-v2.1-api-elements-element_id-elementaccessconfigs
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElementAccessConfigScreen'
        description: Post element access config
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: CHALLENGE_VERIFICATION_FAILED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Element Access Config (v2.1)
      tags:
      - Cellular
  /sdwan/v2.1/api/elements/{element_id}/elementaccessconfigs/{element_access_id}:
    get:
      description: Get a specific element access configuration.
      operationId: get-sdwan-v2.1-api-elements-element_id-elementaccessconfigs-element_access_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the element access config.
        in: path
        name: element_access_id
        required: true
        schema:
          description: Element Access Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessConfig'
          description: Successful Operation
      summary: Get Specific Element Access Config (v2.1)
      tags:
      - Cellular
    put:
      description: Update a specific element access configuration.
      operationId: put-sdwan-v2.1-api-elements-element_id-elementaccessconfigs-element_access_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the element access config.
        in: path
        name: element_access_id
        required: true
        schema:
          description: Element Access Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElementAccessConfigScreen'
        description: Details of the access config to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessConfig'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_ACCESS_CONFIG_INVALID_INACTIVE_INTERVAL
                  - code: ELEMENT_ACCESS_CONFIG_INVALID_RETRY_LOGIN_COUNT
                  - code: ELEMENT_ACCESS_CONFIG_INVALID_ACCOUNT_DISABLE_INTERVAL
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Element Access Config (v2.1)
      tags:
      - Cellular
  /sdwan/v2.2/api/elements/{element_id}/elementaccessconfigs:
    get:
      description: Get all the element access configuration.
      operationId: get-sdwan-v2.2-api-elements-element_id-elementaccessconfigs
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessConfigScreenV2N2'
          description: Successful Operation
      summary: Get Element Access Config (v2.2)
      tags:
      - Cellular
  /sdwan/v2.2/api/elements/{element_id}/elementaccessconfigs/{element_access_id}:
    get:
      description: Get a specific element access configuration.
      operationId: get-sdwan-v2.2-api-elements-element_id-elementaccessconfigs-element_access_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the element access config.
        in: path
        name: element_access_id
        required: true
        schema:
          description: Element Access Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessConfigScreenV2N2'
          description: Successful Operation
      summary: Get Specific Element Access Config (v2.2)
      tags:
      - Cellular
    put:
      description: Update a specific element access configuration.
      operationId: put-sdwan-v2.2-api-elements-element_id-elementaccessconfigs-element_access_id
      parameters:
      - description: ID of the element.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: ID of the element access config.
        in: path
        name: element_access_id
        required: true
        schema:
          description: Element Access Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElementAccessConfigScreenV2N2'
        description: Details of the access config to be updated
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementAccessConfigV2N2'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_ACCESS_CONFIG_INVALID_INACTIVE_INTERVAL
                  - code: ELEMENT_ACCESS_CONFIG_INVALID_RETRY_LOGIN_COUNT
                  - code: ELEMENT_ACCESS_CONFIG_INVALID_ACCOUNT_DISABLE_INTERVAL
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Update Element Access Config (v2.2)
      tags:
      - Cellular
servers:
- url: https://api.sase.paloaltonetworks.com
tags:
- name: Cellular
