components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items: {$ref: '#/components/schemas/AggregateData'}
          type: array
        field: {description: 'The field value.

            ', type: string}
        name: {description: 'The name of the query response.

            ', type: string}
        operator:
          description: 'The operator values.

            '
          enum: [max, count, min, avg, sum]
          type: string
        statistic: {description: 'The aggregate statistics.

            ', type: string}
        unit: {description: 'The unit.

            ', type: string}
    AggregateData:
      properties:
        value: {description: 'Value of the aggregate data.

            ', type: number}
        view: {description: 'View the aggregate date.

            ', type: object}
    ApplicationFilter:
      properties:
        app_transfer_types:
          description: 'Lists the app transfer types. Maximum 4 app transfer types
            are supported.

            '
          items:
            description: App Transfer Types
            enum: [RT_AUDIO, RT_VIDEO, TRANSACTIONAL, BULK]
            type: string
          type: array
        application_ids:
          description: 'Lists the application IDs in a rule. Maximum 64 applications
            can be specified in a rule.

            '
          items: {description: Application Ids, type: string}
          type: array
    BasePolicyRule:
      properties:
        description: {description: 'The description of the policy rule. Max size =
            256.

            '}
        disabled: {description: "Lists if the policy rules are disabled. \n", type: boolean}
        disabled_reason: {description: 'Reason the policy rule is disabled.

            ', type: string}
        id: {description: 'The policy rule ID.

            ', type: string}
        inactive: {description: 'Lists if the policy rule is inactive.

            ', type: boolean}
        inactive_reason: {description: 'Reason the policy rule is inactive.

            ', type: string}
        name: {description: 'The name of the policy rule.

            ', type: string}
        policyset_id: {description: 'The ID of the policy set.

            ', type: string}
        region: {description: 'The region of the site.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the policy
            rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    CircuitUtilizationMetricThresholds:
      properties:
        percentage_circuit_utilization: {description: 'The circuit utilization percentage
            configured for the threshold.

            ', type: integer}
    Error:
      properties:
        _error:
          items: {$ref: '#/components/schemas/ErrorResponse'}
          type: array
    ErrorResponse:
      properties:
        code: {description: 'The error code.

            ', type: string}
        message: {description: 'The error message.

            ', type: string}
    FlowMetricThresholds:
      properties:
        percentage_flow_utilization: {description: 'The flow utilization percentage
            configured for the threshold.

            ', type: integer}
    LQMThresholdConfig:
      properties:
        max_jitter: {description: 'The max jitter threshold range. Max = 100L.

            ', type: integer}
        max_latency: {description: 'The max latency threshold range. Max = 500L.

            ', type: integer}
        max_packet_loss: {description: "The max packet loss threshold range. Max =\
            \ 20L.      \n", type: integer}
    LQMThresholdConfigV2N1:
      properties:
        max_jitter: {description: Max Jitter, type: integer}
        max_latency: {description: Max Latency, type: integer}
        max_packet_loss: {description: Max Packet Loss, type: integer}
        min_mos: {description: Min Mos, type: integer}
    ListQueryResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        deleted_count: {description: 'The deleted number.

            ', format: int64, type: integer}
        deleted_ids:
          description: 'The deleted IDs.

            '
          items: {type: string}
          type: array
        description: {description: 'Description of the query. Max size = 256.

            '}
        id: {description: 'The ID.

            ', type: string}
        next_query: {description: 'Details of the next query.

            ', type: object}
        total_count: {description: 'Total number.

            ', format: int64, type: integer}
      type: object
    ListResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
      type: object
    PathFilter:
      properties:
        label: {description: 'The policy rule path label.

            ', type: string}
        path_type:
          description: 'The path type specified in the path filter.

            '
          enum: [vpn, direct, servicelink, all]
          type: string
      required: [path_type]
    PerfMgmtAction:
      properties:
        action_type:
          description: 'The values of the action type for the performance policy rule.

            '
          enum: [RAISE_ALARM, MOVE_FLOWS, MOVE_FLOWS_FORCED, FEC, VISIBILITY, APP_ACCELERATION,
            PACKET_DUPLICATION]
          type: string
        always_on: {description: Always On, type: boolean}
        app_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the application performance parameters.

            '}
        circuit_utilization_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters',
          description: 'Circuit Utilization Perf: Valid '}
        lqm_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the LQM performance parameters.

            '}
        probe_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'Probe
            Perf: Valid '}
        sys_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'Sys
            Perf: Valid '}
      required: [action_type]
    PerfMgmtActionParameters:
      properties:
        bad_health_thresholds: {$ref: '#/components/schemas/PerfMgmtHealthThresholds',
          description: 'Lists the valid / invalid performance policy rules thresholds.

            '}
        monitoring_approach:
          description: 'The monitoring approach for the rule.

            '
          enum: [AGGRESSIVE, MODERATE, CONSERVATIVE]
          type: string
      required: [monitoring_approach]
    PerfMgmtActionV2N1:
      properties:
        action_type:
          description: 'The values of the action type for the performance policy rule.

            '
          enum: [RAISE_ALARM, MOVE_FLOWS, MOVE_FLOWS_FORCED, FEC, VISIBILITY, APP_ACCELERATION,
            PACKET_DUPLICATION]
          type: string
        app_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the application performance parameters.

            '}
        circuit_utilization_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters',
          description: 'The details of the circuit utilization parameters.

            '}
        lqm_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the LQM performance parameters.

            '}
        probe_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the probe performance parameters.

            '}
        sys_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the system performance parameters.

            '}
      required: [action_type]
    PerfMgmtActionV2N2:
      properties:
        action_type:
          description: 'The values of the action type for the performance policy rule.

            '
          enum: [RAISE_ALARM, MOVE_FLOWS, MOVE_FLOWS_FORCED, FEC, VISIBILITY, APP_ACCELERATION,
            PACKET_DUPLICATION]
          type: string
        always_on: {description: 'True or False.

            ', type: boolean}
        app_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the application performance parameters.

            '}
        circuit_utilization_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters',
          description: 'The details of the circuit utilization parameters.

            '}
        lqm_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the LQM performance parameters.

            '}
        probe_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the probe performance parameters.

            '}
        sys_perf: {$ref: '#/components/schemas/PerfMgmtActionParameters', description: 'The
            details of the system performance parameters.

            '}
      required: [action_type]
    PerfMgmtHealthThresholds:
      properties:
        clear_below: {description: 'The clear_below value should be between 1-80L.

            ', format: int32, type: integer}
        raise_above: {description: 'The raise_above value should be between 10-100L.

            ', format: int32, type: integer}
    PerfMgmtPolicyRule:
      properties:
        actions:
          description: 'The valid actions configured for the performance policy rule.

            '
          items: {$ref: '#/components/schemas/PerfMgmtAction'}
          type: array
        app_filters: {$ref: '#/components/schemas/ApplicationFilter', description: 'The
            details of the app filters used for the performance policy rule.

            '}
        description: {description: 'The description of the performance policy rule.
            Max characters = 256.

            '}
        enabled: {description: 'Lists if the performance policy rule is enabled.

            ', type: boolean}
        id: {description: 'Lists the performance policy rules by ID.

            ', type: string}
        name: {description: 'The name of the performance policy rule.

            ', type: string}
        path_filters:
          description: 'Lists the path filters applied. Maximum 32 path filters are
            supported.

            '
          items: {$ref: '#/components/schemas/PathFilter'}
          type: array
        service_label_ids:
          description: 'Lists the service label IDs. Maximum 32 service labels are
            supported.

            '
          items: {description: Service Label Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the performance
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        thresholdprofile_id: {description: 'Lists the threshold profile IDs.

            ', type: string}
        type:
          description: 'The type of performance policy rule.

            '
          enum: [APP_CIRCUIT_HEALTH, SYSTEM_SITE_HEALTH]
          type: string
      required: [type, name]
    PerfMgmtPolicyRuleQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of performance policy rules that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number performance policy rules deleted for
            a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the performance policy rules for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "List the performance policy rules by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {description: 'Query using query parameters.

            ', type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {description: 'Specify the order in which you want the search
            criteria to display.

            ', type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    PerfMgmtPolicyRuleQueryFilterV2N1:
      properties:
        actions:
          description: 'The valid actions configured for the performance policy rule.

            '
          items: {$ref: '#/components/schemas/PerfMgmtActionV2N1'}
          type: array
        app_filters: {$ref: '#/components/schemas/ApplicationFilter', description: 'The
            details of the app filters used for the performance policy rule.

            '}
        description: {description: 'The description of the performance policy rule.
            Max characters = 256.

            '}
        enabled: {description: 'Lists if the performance policy rule is enabled.

            ', type: boolean}
        id: {description: 'Lists the performance policy rules by ID.

            ', type: string}
        name: {description: 'The name of the performance policy rule.

            ', type: string}
        path_filters:
          description: 'Lists the path filters applied. Maximum 32 path filters are
            supported.

            '
          items: {$ref: '#/components/schemas/PathFilter'}
          type: array
        service_label_ids:
          description: 'Lists the service label IDs. Maximum 32 service labels are
            supported.

            '
          items: {description: Service Label Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the performance
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        thresholdprofile_id: {description: 'Lists the threshold profile IDs.

            ', type: string}
        type:
          description: 'The type of performance policy rule.

            '
          enum: [APP_CIRCUIT_HEALTH, SYSTEM_SITE_HEALTH]
          type: string
      required: [type, name]
    PerfMgmtPolicyRuleScreenV2N1:
      properties:
        actions:
          description: 'Actions: Valid '
          items: {$ref: '#/components/schemas/PerfMgmtActionV2N1'}
          type: array
        app_filters: {$ref: '#/components/schemas/ApplicationFilter', description: 'App
            Filters: Valid '}
        description: {description: 'Description: Size(max = 256, error = DESCRIPTION_EXCEEDS_LIMIT:
            Description length exceeds limit, min = 0) ', type: string}
        enabled: {description: Enabled, type: boolean}
        id: {description: Id, type: string}
        name: {description: 'Name: Required(error = NAME_REQD: Please provide resource
            name.) Size(max = 128, error = NAME_EXCEEDS_LIMIT: Name of the resource
            exceeds limit., min = 0) ', type: string}
        path_filters:
          description: 'Path Filters: Valid Size(max = 32, error = PERFMGMT_PATH_FILTERS_LIST_SIZE_EXCEEDED:
            Maximum 32 path filters are supported, min = 0) '
          items: {$ref: '#/components/schemas/PathFilter'}
          type: array
        service_label_ids:
          description: 'Service Label Ids: ListString(allowDuplicate = false, allowEmpty
            = true, allowNull = true, length = 0, listMaxSize = 0, error = PERFMGMT_RULE_DUPLICATE_SERVICE_LABEL_IDS:
            Duplicate service label ids are not allowed in policy rule, noTrim = false,
            regex = , required = false) Size(max = 32, error = SERVICE_LABEL_ID_LIST_SIZE_EXCEEDED:
            Maximum 32 service labels are supported, min = 0) '
          items: {description: Service Label Ids, type: string}
          type: array
        tags:
          description: 'Tags: ListString(allowDuplicate = false, allowEmpty = true,
            allowNull = true, length = 128, listMaxSize = 10, error = INVALID_TAG:
            Maximum 10 Unique tags of length 1024 each are allowed, noTrim = false,
            regex = [^,\\s]+, required = false) '
          items: {description: Tags, type: string}
          type: array
        thresholdprofile_id: {description: Thresholdprofile Id, type: string}
        type:
          description: 'Type: ValidateEnum(enumClass = classOf[PerfMgmtRuleType],
            error = PERFMGMT_INVALID_RULE_TYPE: Performance policy rule type is invalid,
            nullAllowed = false) Required(error = PERFMGMT_RULE_TYPE_REQUIRED: Rule
            type is required) '
          enum: [APP_CIRCUIT_HEALTH, SYSTEM_SITE_HEALTH]
          type: string
      required: [type, name]
    PerfMgmtPolicyRuleScreenV2N2:
      properties:
        actions:
          description: 'The valid actions configured for the performance policy rule.

            '
          items: {$ref: '#/components/schemas/PerfMgmtActionV2N2'}
          type: array
        app_filters: {$ref: '#/components/schemas/ApplicationFilter', description: 'The
            details of the app filters used for the performance policy rule.

            '}
        description: {description: 'The description of the performance policy rule.
            Max characters = 256.

            '}
        enabled: {description: 'Lists if the performance policy rule is enabled.

            ', type: boolean}
        id: {description: 'Lists the performance policy rules by ID.

            ', type: string}
        name: {description: 'The name of the performance policy rule.

            ', type: string}
        network_context_ids:
          description: 'Network context IDs.

            '
          items: {description: Network Context Ids, type: string}
          type: array
        path_filters:
          description: 'Lists the path filters applied. Maximum 32 path filters are
            supported.

            '
          items: {$ref: '#/components/schemas/PathFilter'}
          type: array
        service_label_ids:
          description: 'Lists the service label IDs. Maximum 32 service labels are
            supported.

            '
          items: {description: Service Label Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the performance
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        thresholdprofile_id: {description: 'Lists the threshold profile IDs.

            ', type: string}
        type:
          description: 'The type of performance policy rule.

            '
          enum: [APP_CIRCUIT_HEALTH, SYSTEM_SITE_HEALTH]
          type: string
      required: [type, name]
    PerfMgmtPolicyRuleV2N1:
      properties:
        actions:
          description: 'The valid actions configured for the performance policy rule.

            '
          items: {$ref: '#/components/schemas/PerfMgmtActionV2N1'}
          type: array
        app_filters: {$ref: '#/components/schemas/ApplicationFilter', description: 'The
            details of the app filters used for the performance policy rule.

            '}
        description: {description: 'The description of the performance policy rule.
            Max characters = 256.

            '}
        enabled: {description: 'Lists if the performance policy rule is enabled.

            ', type: boolean}
        id: {description: 'Lists the performance policy rules by ID.

            ', type: string}
        name: {description: 'The name of the performance policy rule.

            ', type: string}
        path_filters:
          description: 'Lists the path filters applied. Maximum 32 path filters are
            supported.

            '
          items: {$ref: '#/components/schemas/PathFilter'}
          type: array
        service_label_ids:
          description: 'Lists the service label IDs. Maximum 32 service labels are
            supported.

            '
          items: {description: Service Label Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the performance
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        thresholdprofile_id: {description: 'Lists the threshold profile IDs.

            ', type: string}
        type:
          description: 'The type of performance policy rule.

            '
          enum: [APP_CIRCUIT_HEALTH, SYSTEM_SITE_HEALTH]
          type: string
      required: [type, name]
    PerfMgmtPolicyRuleV2N2:
      properties:
        actions:
          description: 'The valid actions configured for the performance policy rule.

            '
          items: {$ref: '#/components/schemas/PerfMgmtActionV2N2'}
          type: array
        app_filters: {$ref: '#/components/schemas/ApplicationFilter', description: 'The
            details of the app filters used for the performance policy rule.

            '}
        description: {description: 'The description of the performance policy rule.
            Max characters = 256.

            '}
        enabled: {description: 'Lists if the performance policy rule is enabled.

            ', type: boolean}
        id: {description: 'Lists the performance policy rules by ID.

            ', type: string}
        name: {description: 'The name of the performance policy rule.

            ', type: string}
        network_context_ids:
          description: Network Context Ids
          items: {description: Network Context Ids, type: string}
          type: array
        path_filters:
          description: 'Lists the path filters applied. Maximum 32 path filters are
            supported.

            '
          items: {$ref: '#/components/schemas/PathFilter'}
          type: array
        service_label_ids:
          description: 'Lists the service label IDs. Maximum 32 service labels are
            supported.

            '
          items: {description: Service Label Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the performance
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        thresholdprofile_id: {description: 'Lists the threshold profile IDs.

            ', type: string}
        type:
          description: 'The type of performance policy rule.

            '
          enum: [APP_CIRCUIT_HEALTH, SYSTEM_SITE_HEALTH]
          type: string
      required: [type, name]
    PerfMgmtPolicySet:
      properties:
        clone_from: {description: 'The performance policy set that has been cloned
            from.

            ', type: string}
        defaultrule_policyset: {description: "The default rule of the policy set.\
            \  \n", type: boolean}
        description: {description: 'The description of the performance policy set.
            Max size = 256.

            '}
        disabled: {description: "Lists if the performance policy sets are disabled.\
            \ \n", type: boolean}
        disabled_reason: {description: 'Reason the performance policy set is disabled.

            ', type: string}
        id: {description: 'The ID of the policy set.

            ', type: string}
        inactive: {description: 'Lists if the performance policy set is inactive.

            ', type: boolean}
        inactive_reason: {description: "Reason the performance policy set is inactive.\
            \ \n", type: string}
        link_health_policyrule_order:
          description: "The order of the policy rule. \n"
          items: {description: Link Health Policyrule Order, type: string}
          type: array
        link_health_rules:
          description: 'The details of the policy rule.

            '
          items: {$ref: '#/components/schemas/PerfMgmtPolicyRule'}
          type: array
        name: {description: 'The name of the performnace policy set. Max size = 128.

            ', type: string}
        policy_rules:
          description: 'Lists the details of the policy rules.

            '
          items: {$ref: '#/components/schemas/BasePolicyRule'}
          type: array
        region: {description: 'The region of the site.

            ', type: string}
        send_to_element: {description: Send To Element, type: boolean}
        tags:
          description: 'A information field that can be added to identify the policy
            set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    PerfMgmtPolicySetStack:
      properties:
        default_policysetstack: {description: "The default policy set stack. \n",
          type: boolean}
        defaultrule_policyset_id: {description: 'The ID of the default rule policy
            set.

            ', type: string}
        description: {description: 'The description of the policy set stack. Max size
            = 256.

            '}
        id: {description: 'The policy set stack ID.

            ', type: string}
        name: {description: 'Name: Required(error = NAME_REQD: Please provide resource
            name.) Size(max = 128, error = NAME_EXCEEDS_LIMIT: Name of the resource
            exceeds limit., min = 0) ', type: string}
        policyset_ids:
          description: 'The policy set IDs.Should be between 1-4.

            '
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the policy
            set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [defaultrule_policyset_id, name]
    PerfMgmtPolicySetState:
      properties:
        id: {description: 'The ID of the performance policy set.

            ', type: string}
        policy_req_state:
          description: 'The policy required state.

            '
          enum: [POLICY_REQ_PENDING, POLICY_CREATE_INPROGRESS, POLICY_CREATE_COMPLETE,
            POLICY_REPLICATE, POLICY_CREATE_FAILED, POLICY_REQ_COMPLETE]
          type: string
        policyset_id: {description: 'The ID of the policy set.

            ', type: string}
    PerfMgmtThresholdProfile:
      properties:
        circuit_utilization_metrics_thresholds: {$ref: '#/components/schemas/CircuitUtilizationMetricThresholds',
          description: "The circuit utilization metrics thresholds. \n"}
        description: {description: 'The description of the threshold profile. Max
            size = 256.

            '}
        disabled: {description: "Lists if the threshold profiles are disabled. \n",
          type: boolean}
        disabled_reason: {description: 'Reason the threshold profile is disabled.

            ', type: string}
        flow_metrics_thresholds: {$ref: '#/components/schemas/FlowMetricThresholds',
          description: "The flow 4metrics thresholds. \n"}
        hard_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfig',
          description: 'The hard limits set for the app metrics.

            '}
        id: {description: 'The ID of the threshold profile.

            ', type: string}
        inactive: {description: 'Lists if the threshold profile is inactive.

            ', type: boolean}
        inactive_reason: {description: "Reason the threshold profile is inactive.\
            \ \n", type: string}
        is_default: {description: 'The default threshold.

            ', type: boolean}
        lqm_thresholds: {$ref: '#/components/schemas/LQMThresholdConfig', description: 'Details
            of the LQM threshold configurations.

            '}
        name: {description: 'The name of the profile. Max size = 128.

            ', type: string}
        region: {description: 'The region of the site.

            ', type: string}
        soft_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfig',
          description: 'The soft limits set for the app metrics.

            '}
        synthetic_probe_thresholds: {$ref: '#/components/schemas/SyntheticProbeThresholds',
          description: 'The synthetic probe thresholds.

            '}
        system_health_metrics_thresholds: {$ref: '#/components/schemas/SystemHealthMetricThresholds',
          description: "The system health metrics thresholds.          \n"}
        tags:
          description: 'A information field that can be added to identify the profile.
            Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    PerfMgmtThresholdProfileScreen:
      properties:
        description: {description: 'The description of the threshold profile. Max
            size = 256.

            '}
        hard_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfig',
          description: 'The hard limits set for the app metrics.

            '}
        id: {description: 'The ID of the threshold profile.

            ', type: string}
        lqm_thresholds: {$ref: '#/components/schemas/LQMThresholdConfig', description: 'Details
            of the LQM threshold configurations.

            '}
        name: {description: 'The name of the profile. Max size = 128.

            ', type: string}
        soft_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfig',
          description: 'The soft limits set for the app metrics.

            '}
        tags:
          description: 'A information field that can be added to identify the profile.
            Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [hard_limit_app_metrics, name]
    PerfMgmtThresholdProfileScreenV2N1:
      properties:
        circuit_utilization_metrics_thresholds: {$ref: '#/components/schemas/CircuitUtilizationMetricThresholds',
          description: 'Circuit Utilization Metrics Thresholds: Valid '}
        description: {description: 'Description: Size(max = 256, error = DESCRIPTION_EXCEEDS_LIMIT:
            Description length exceeds limit, min = 0) ', type: string}
        flow_metrics_thresholds: {$ref: '#/components/schemas/FlowMetricThresholds',
          description: 'Flow Metrics Thresholds: Valid '}
        hard_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfigV2N1',
          description: 'Hard Limit App Metrics: Valid '}
        id: {description: Id, type: string}
        lqm_thresholds: {$ref: '#/components/schemas/LQMThresholdConfigV2N1', description: 'Lqm
            Thresholds: Valid '}
        name: {description: 'Name: Required(error = NAME_REQD: Please provide resource
            name.) Size(max = 128, error = NAME_EXCEEDS_LIMIT: Name of the resource
            exceeds limit., min = 0) ', type: string}
        soft_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfigV2N1',
          description: 'Soft Limit App Metrics: Valid '}
        synthetic_probe_thresholds: {$ref: '#/components/schemas/SyntheticProbeThresholds',
          description: 'Synthetic Probe Thresholds: Valid '}
        system_health_metrics_thresholds: {$ref: '#/components/schemas/SystemHealthMetricsThresholds',
          description: 'System Health Metrics Thresholds: Valid '}
        tags:
          description: 'Tags: ListString(allowDuplicate = false, allowEmpty = true,
            allowNull = true, length = 128, listMaxSize = 10, error = INVALID_TAG:
            Maximum 10 Unique tags of length 1024 each are allowed, noTrim = false,
            regex = [^,\\s]+, required = false) '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    PerfMgmtThresholdProfileV2N1:
      properties:
        circuit_utilization_metrics_thresholds: {$ref: '#/components/schemas/CircuitUtilizationMetricThresholds',
          description: "The circuit utilization metrics thresholds. \n"}
        description: {description: "The description of the threshold profile. Max\
            \ size = 256.          \n"}
        flow_metrics_thresholds: {$ref: '#/components/schemas/FlowMetricThresholds',
          description: "The flow 4metrics thresholds. \n"}
        hard_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfigV2N1',
          description: "The hard limits set for the app metrics.          \n"}
        id: {description: "The ID of the threshold profile.          \n", type: string}
        lqm_thresholds: {$ref: '#/components/schemas/LQMThresholdConfigV2N1', description: 'Details
            of the LQM threshold configurations.

            '}
        name: {description: "The name of the profile. Max size = 128.          \n",
          type: string}
        soft_limit_app_metrics: {$ref: '#/components/schemas/StaticAppMetricConfigV2N1',
          description: 'The soft limits set for the app metrics.

            '}
        synthetic_probe_thresholds: {$ref: '#/components/schemas/SyntheticProbeThresholds',
          description: 'The synthetic probe thresholds.

            '}
        system_health_metrics_thresholds: {$ref: '#/components/schemas/SystemHealthMetricsThresholds',
          description: "The system health metrics thresholds.          \n"}
        tags:
          description: 'A information field that can be added to identify the profile.
            Maximum 10 unique tags.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    ProbeConfig:
      properties:
        description: {description: "The description of the probe configuration. Max\
            \ size = 256.          \n"}
        enabled: {description: 'The probe configuration based on rules that are enabled.

            ', type: boolean}
        endpoints:
          description: 'Info on the probe endpoint configurations.

            '
          items: {$ref: '#/components/schemas/ProbeEndpoint'}
          type: array
        id: {description: 'The probe config ID.

            ', type: string}
        name: {description: 'The probe config name. Max size = 128.

            ', type: string}
        tags:
          description: 'An information field that can be added to identify the probe
            config. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [endpoints, name]
    ProbeConfigQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of probe configs that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number probe configs deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the probe configs for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "List the probe configs by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    ProbeConfigQueryFilter:
      properties:
        description: {description: "The description of the probe configuration. Max\
            \ size = 256.          \n"}
        enabled: {description: 'The probe configuration based on rules that are enabled.

            ', type: boolean}
        endpoints:
          description: 'Info on the probe endpoint configurations.

            '
          items: {$ref: '#/components/schemas/ProbeEndpoint'}
          type: array
        id: {description: 'The probe config ID.

            ', type: string}
        name: {description: 'The probe config name. Max size = 128.

            ', type: string}
        tags:
          description: "An information field that can be added to identify the probe\
            \ config. Maximum 10 unique tags of length 1024 each are allowed.    \
            \  \n"
          items: {description: Tags, type: string}
          type: array
      required: [endpoints, name]
    ProbeConfigScreen:
      properties:
        description: {description: "The description of the probe configuration. Max\
            \ size = 256.          \n"}
        enabled: {description: 'The probe configuration based on rules that are enabled.

            ', type: boolean}
        endpoints:
          description: 'Info on the probe endpoint configurations.

            '
          items: {$ref: '#/components/schemas/ProbeEndpoint'}
          type: array
        id: {description: 'The probe config ID.

            ', type: string}
        name: {description: 'The probe config name. Max size = 128.

            ', type: string}
        tags:
          description: "An information field that can be added to identify the probe\
            \ config. Maximum 10 unique tags of length 1024 each are allowed.  \n"
          items: {description: Tags, type: string}
          type: array
      required: [endpoints, name]
    ProbeEndpoint:
      properties:
        allow_insecure_https_connection: {description: "Allow insecure HTTPS connections.\
            \ True or False.         \n", type: boolean}
        dns_server_ip: {description: 'The DNS server IP address.

            ', type: string}
        fqdn: {description: 'The FQDN for the probe configuration.

            ', type: string}
        http_response_codes:
          description: 'The HTTP response codes.

            '
          items: {description: Http Response Codes, type: integer}
          type: array
        http_response_string: {description: 'The HTTP response string.

            ', type: string}
        ipv4_address: {description: 'The IPv4 address for the probe configuration.

            ', type: string}
        path_types:
          description: "The probe path type selected = Direct, Prisma SD-WAN VPN,\
            \ and Direct VPN.        \n"
          items:
            description: Path Types
            enum: [vpn, direct, servicelink, all]
            type: string
          type: array
        probe_count: {description: 'The probe count value which is a multiple of the
            probe cycle duration; multiples of 1, 2, and 3.

            ', format: int32, type: integer}
        probe_cycle_duration: {description: 'Probe cycle duration in minutes or seconds.

            ', format: int32, type: integer}
        protocol:
          description: 'The selected protocol = HTTP, HTTPS, DNS, and ICMP.

            '
          enum: [http, https, icmp, dns]
          type: string
    ProbeProfile:
      properties:
        description: {description: "The description of the probe profile. Max size\
            \ = 256.      \n"}
        id: {description: 'The probe profile ID.

            ', type: string}
        name: {description: 'The probe profile name. Max size = 128.

            ', type: string}
        probe_config_ids:
          description: 'The probe configuration IDs. Max number = 8.

            '
          items: {description: Probe Config Ids, type: string}
          type: array
        tags:
          description: "An information field that can be added to identify the probe\
            \ profile. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [probe_config_ids, name]
    ProbeProfileQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of probe profiles that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number probe profiles deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the probe profiles for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "List the probe profiles by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    ProbeProfileQueryFilter:
      properties:
        description: {description: "The description of the probe profile. Max size\
            \ = 256.      \n"}
        id: {description: 'The probe profile ID.

            ', type: string}
        name: {description: 'The probe profile name. Max size = 128.

            ', type: string}
        probe_config_ids:
          description: 'The probe configuration IDs. Max number = 8.

            '
          items: {description: Probe Config Ids, type: string}
          type: array
        tags:
          description: 'An information field that can be added to identify the probe
            profile. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [probe_config_ids, name]
    ProbeProfileScreen:
      properties:
        description: {description: "The description of the probe profile. Max size\
            \ = 256.      \n"}
        id: {description: 'The probe profile ID.

            ', type: string}
        name: {description: 'The probe profile name. Max size = 128.

            ', type: string}
        probe_config_ids:
          description: 'The probe configuration IDs. Max number = 8.

            '
          items: {description: Probe Config Ids, type: string}
          type: array
        tags:
          description: "An information field that can be added to identify the probe\
            \ profile. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [probe_config_ids, name]
    StaticAppMetricConfig:
      properties:
        max_init_failure_rate: {description: 'The max init failure rate threshold.
            Max = 100L.

            ', type: integer}
        max_rtt: {description: 'The max rtt threshold range. Max = 500L.

            ', type: integer}
        udp_trt: {description: 'The udp rtt threshold range. Max = 500L.

            ', type: integer}
    StaticAppMetricConfigV2N1:
      properties:
        max_init_failure_rate: {description: 'Max Init Failure Rate: Range(max = 100L,
            message = PERFMGMT_MAX_INIT_FAILURE_RATE_THRESHOLD_RANGE_INVALID, min
            = 0L) ', type: integer}
        max_rtt: {description: 'Max Rtt: Range(max = 500L, message = PERFMGMT_MAX_RTT_THRESHOLD_RANGE_INVALID,
            min = 0L) ', type: integer}
        udp_trt: {description: Udp Trt, type: integer}
    SyntheticProbeThreshold:
      properties:
        probe_config_id: {description: 'The probe config ID.

            ', type: string}
        value: {description: 'The threshold value.

            ', type: integer}
    SyntheticProbeThresholds:
      properties:
        dns_txn_failure_pct: {$ref: '#/components/schemas/SyntheticProbeThreshold',
          description: 'The DNS transaction failure percentage.

            '}
        init_failure_pct: {$ref: '#/components/schemas/SyntheticProbeThreshold', description: 'The
            init failure percentage.

            '}
        jitter: {$ref: '#/components/schemas/SyntheticProbeThreshold', description: 'The
            jitter value.

            '}
        latency: {$ref: '#/components/schemas/SyntheticProbeThreshold', description: 'The
            latency value.

            '}
        packet_loss: {$ref: '#/components/schemas/SyntheticProbeThreshold', description: "The\
            \ packet loss value.              \n"}
    SystemHealthMetricThresholds:
      properties:
        cpu_utilization: {description: 'The CPU utilization threshold range.

            ', type: integer}
        disk_utilization: {description: 'The disk utilization threshold range.

            ', type: integer}
        memory_utilization: {description: 'The memory utilization threshold range.

            ', type: integer}
    SystemHealthMetricsThresholds:
      properties:
        cpu_utilization: {description: Cpu Utilization, format: int32, type: integer}
        disk_utilization: {description: Disk Utilization, format: int32, type: integer}
        memory_utilization: {description: Memory Utilization, format: int32, type: integer}
info:
  contact: {}
  description: List of APIs used to manage performance policy.
  title: Performance Policy
  version: Latest
openapi: 3.0.0
paths:
  /sdwan/v2.0/api/perfmgmtpolicysets:
    get:
      description: Get details of the performance policy sets.
      operationId: get-sdwan-v2.0-api-perfmgmtpolicysets
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Performance Policy Sets (v2.0)
      tags: [Performance Policy]
    post:
      description: Create a new performance policy set.
      operationId: post-sdwan-v2.0-api-perfmgmtpolicysets
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
        description: Details of PERFMGMT Policy Set to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: POLICY_SET_CONFIG_CREATION_FAILED}
                  - {code: PERFMGMT_SET_CONFIG_INVALID_CLONE_FROM}
                  - {code: PERFMGMT_DUPLICATE_RULES_IN_RULES_ORDER}
                  - {code: PERFMGMT_INVALID_POLICY_SET_ORDER}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Performance Policy Set (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtpolicysets/query:
    post:
      description: Query details for the performance policy set.
      operationId: post-sdwan-v2.0-api-perfmgmtpolicysets-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
        description: Details of query for PERFMGMT Policy Set
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Performance Policy Set (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtpolicysets/{id}/status:
    get:
      description: Get the status of the performance policy sets.
      operationId: get-sdwan-v2.0-api-perfmgmtpolicysets-id-status
      parameters:
      - description: The performance policy set ID.
        in: path
        name: id
        required: true
        schema: {description: Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySetState'}
          description: Successful Operation
      summary: Get Performance Policy Set Status (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtpolicysets/{perfmgmtpolicyset_id}:
    delete:
      description: Delete a performance policy set.
      operationId: delete-sdwan-v2.0-api-perfmgmtpolicysets-perfmgmtpolicyset_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PERFMGMT_SET_IN_USE}
                  - {code: DEFAULT_POLICY_SET_CANNOT_BE_DELETED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Performance Policy Set (v2.0)
      tags: [Performance Policy]
    get:
      description: Get a performance policy set.
      operationId: get-sdwan-v2.0-api-perfmgmtpolicysets-perfmgmtpolicyset_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
          description: Successful Operation
      summary: Get Performance Policy Set (v2.0)
      tags: [Performance Policy]
    put:
      description: Update a performance policy set.
      operationId: put-sdwan-v2.0-api-perfmgmtpolicysets-perfmgmtpolicyset_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
        description: Details of PERFMGMT Policy Set to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICY_SET_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: READONLY_CLONE_FROM}
                  - {code: DEFAULT_RULES_CANNOT_BE_REMOVED_FROM_SET}
                  - {code: PERFMGMT_DEFAULT_RESOURCE_NAME_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_DUPLICATE_RULES_IN_RULES_ORDER}
                  - {code: PERFMGMT_INVALID_POLICY_SET_ORDER}
                  - {code: PERFMGMT_POLICY_DEFAULT_SET_UPDATE_NOT_ALLOWED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Performance Policy Set (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtpolicysetstacks:
    get:
      description: Get all the performance policy set stacks.
      operationId: get-sdwan-v2.0-api-perfmgmtpolicysetstacks
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Performance Policy Set Stacks (v2.0)
      tags: [Performance Policy]
    post:
      description: Create a new performance policy set stack.
      operationId: post-sdwan-v2.0-api-perfmgmtpolicysetstacks
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
        description: Details of PERFMGMT Policy Set Stack to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYSETSTACK_CONFIG_DUPLICATE_POLICYSETID}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_IDS}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_STATE}
                  - {code: PERFMGMT_STACKS_EXCEEDS_MAX_LIMIT_FOR_SETS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Performance Policy Set Stack (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtpolicysetstacks/query:
    post:
      description: Query details for the performance policy set stack.
      operationId: post-sdwan-v2.0-api-perfmgmtpolicysetstacks-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
        description: Details of query for PERFMGMT Policy Set Stack
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Performance Policy Set Stack (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtpolicysetstacks/{id}:
    delete:
      description: Delete a performance policy set stack.
      operationId: delete-sdwan-v2.0-api-perfmgmtpolicysetstacks-id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: id
        required: true
        schema: {description: Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: DEFAULT_POLICYSETSTACK_CAN_NOT_BE_DELETED}
                  - {code: POLICYSETSTACK_IN_USE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Performance Policy Set Stack (v2.0)
      tags: [Performance Policy]
    get:
      description: Get a performance set stack.
      operationId: get-sdwan-v2.0-api-perfmgmtpolicysetstacks-id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: id
        required: true
        schema: {description: Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
          description: Successful Operation
      summary: Get Performance Policy Set Stack (v2.0)
      tags: [Performance Policy]
    put:
      description: Update a performance policy set stack.
      operationId: put-sdwan-v2.0-api-perfmgmtpolicysetstacks-id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: id
        required: true
        schema: {description: Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
        description: Details of PERFMGMT Policy Set Stack to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYSETSTACK_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: DEFAULT_POLICY_SET_CANNOT_BE_REMOVED_FROM_DEFAULT_STACK}
                  - {code: PERFMGMT_DEFAULT_RESOURCE_NAME_CHANGE_NOT_ALLOWED}
                  - {code: DEFAULT_POLICYSET_STACK_EXISTS}
                  - {code: POLICYSETSTACK_CONFIG_DUPLICATE_POLICYSETID}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_IDS}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_STATE}
                  - {code: PERFMGMT_STACKS_EXCEEDS_MAX_LIMIT_FOR_SETS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Performance Policy Set Stack (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtthresholdprofiles:
    get:
      description: Get details of the threshold profile list.
      operationId: get-sdwan-v2.0-api-perfmgmtthresholdprofiles
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
      summary: Get Threshold Profile List (v2.0)
      tags: [Performance Policy]
    post:
      description: Create a new threshold profile.
      operationId: post-sdwan-v2.0-api-perfmgmtthresholdprofiles
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfileScreen'}
        description: Details of the Threshold profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PERFMGMT_HARD_LIMIT_APP_METRICS_REQUIRED}
                  - {code: PERFMGMT_MAX_INIT_FAILURE_RATE_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_LATENCY_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_JITTER_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_PACKET_LOSS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_RTT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_LQM}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_APP_METRICS}
                  - {code: PERFMGMT_LQM_OR_APP_THREHOLDS_EXPECTED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Threshold Profile (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtthresholdprofiles/query:
    post:
      description: Query the DB for limit number of tenant level threshold profiles
        that match the query params.
      operationId: post-sdwan-v2.0-api-perfmgmtthresholdprofiles-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
        description: Query Details for Threshold profile
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Tenant Level Threshold Profiles (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/perfmgmtthresholdprofiles/{profile_id}:
    delete:
      description: Delete a threshold profile.
      operationId: delete-sdwan-v2.0-api-perfmgmtthresholdprofiles-profile_id
      parameters:
      - description: The threshold ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PERFMGMT_THRESHOLD_PROFILE_IN_USE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Threshold Profile (v2.0)
      tags: [Performance Policy]
    get:
      description: Get a threshold profile.
      operationId: get-sdwan-v2.0-api-perfmgmtthresholdprofiles-profile_id
      parameters:
      - description: The threshold ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
      summary: Get Threshold Profile (v2.0)
      tags: [Performance Policy]
    put:
      description: Update a threshold profile.
      operationId: put-sdwan-v2.0-api-perfmgmtthresholdprofiles-profile_id
      parameters:
      - description: The threshold ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfileScreen'}
        description: Details of the Threshold profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PERFMGMT_HARD_LIMIT_APP_METRICS_REQUIRED}
                  - {code: PERFMGMT_MAX_INIT_FAILURE_RATE_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_LATENCY_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_JITTER_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_PACKET_LOSS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_RTT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_LQM}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_APP_METRICS}
                  - {code: PERFMGMT_CANNOT_UNSET_LQM_THRESHOLD_PROFILE_IN_USE}
                  - {code: PERFMGMT_CANNOT_SET_APP_METRICS_APP_FILTER_MISSING}
                  - {code: PERFMGMT_DEFAULT_RESOURCE_NAME_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_DEFAULT_THRESHOLD_PROFILE_LQM_UNSET_NOT_ALLOWED}
                  - {code: PERFMGMT_LQM_OR_APP_THREHOLDS_EXPECTED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Threshold Profile (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/probeconfigs:
    get:
      description: List all the probe configurations.
      operationId: get-sdwan-v2.0-api-probeconfigs
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeConfig'}
          description: Successful Operation
      summary: Get Probe Config List (v2.0)
      tags: [Performance Policy]
    post:
      description: Create a new probe configuration.
      operationId: post-sdwan-v2.0-api-probeconfigs
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ProbeConfigScreen'}
        description: Details of the PROBE profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeConfig'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PROBE_ENDPOINTS_REQUIRED}
                  - {code: PROBE_ENDPOINT_INVALID_IP}
                  - {code: PROBE_ENDPOINT_MULTICAST_IP_NOT_ALLOWED}
                  - {code: PROBE_ENDPOINT_DNS_SERVER_MULTICAST_IP_NOT_ALLOWED}
                  - {code: PROBE_ENDPOINT_INVALID_PROTOCOL}
                  - {code: PROBE_ENDPOINT_INVALID_PROBE_CYCLE_DURATION_RANGE}
                  - {code: PROBE_ENDPOINT_INVALID_PATH_TYPE}
                  - {code: PROBE_ENDPOINTS_SUPPORTED_SIZE_EXCEEDED}
                  - {code: PROBE_ENDPOINT_INVALID_PROBE_COUNT}
                  - {code: PROBE_ENDPOINT_EITHER_IP_OR_FQDN_MUST_BE_CONFIGURED}
                  - {code: PROBE_ENDPOINT_IP_AND_FQDN_CANNOT_CONFIGURED_TOGETHER}
                  - {code: PROBE_ENDPOINT_INSECURE_HTTPS_APPLICABLE_FOR_PROTOCOL_HTTPS}
                  - {code: PROBE_ENDPOINT_HTTP_CODE_STRING_NOT_SUPPORTED}
                  - {code: PROBE_ENDPOINT_PATH_TYPES_REQUIRED}
                  - {code: PROBE_ENDPOINT_DUPLICATE_PATH_TYPES}
                  - {code: PROBE_ENDPOINT_INVALID_DNS_SERVER_IP}
                  - {code: PROBE_ENDPOINT_DNS_SERVER_IP_NOT_APPLICABLE}
                  - {code: PROBE_ENDPOINT_DNS_SERVER_IP_REQUIRED}
                  - {code: PROBE_ENDPOINT_IP_NOT_ALLOWED_FOR_DNS}
                  - {code: PROBE_ENDPOINT_INVALID_PROBE_DURATION_MULTIPLE}
                  - {code: INVALID_FQDN_STRING_LENGTH}
                  - {code: INVALID_HTTP_RESPONSE_STRING_LENGTH}
                  - {code: PROBE_ENDPOINT_INVALID_HTTP_RESPONSE_STRING_ENCODING}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Probe Config (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/probeconfigs/query:
    post:
      description: Query the DB for the limit number of tenant level probe configs
        that match query parameters.
      operationId: post-sdwan-v2.0-api-probeconfigs-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ProbeConfigQuery'}
        description: Query Details for ProbeConfig
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/ProbeConfigQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for Tenant Level Probe Configs (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/probeconfigs/{config_id}:
    delete:
      description: Delete a probe config.
      operationId: delete-sdwan-v2.0-api-probeconfigs-config_id
      parameters:
      - description: The probe config ID.
        in: path
        name: config_id
        required: true
        schema: {description: Config Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeConfig'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PROBE_CONFIG_IN_USE_BY_PROBE_PROFILE}
                  - {code: PROBE_CONFIG_IN_USE_BY_WANINTERFACE_LABEL}
                  - {code: PROBE_CONFIG_IN_USE_BY_WANINTERFACE}
                  - {code: PROBE_CONFIG_IN_USE_BY_PERFMGMT_THRESHOLD_PROFILE}
                  - {code: PROBE_CONFIG_IN_USE_BY_NETWORK_POLICY_RULES}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete a Probe Config (v2.0)
      tags: [Performance Policy]
    get:
      description: List a probe config.
      operationId: get-sdwan-v2.0-api-probeconfigs-config_id
      parameters:
      - description: The probe config ID.
        in: path
        name: config_id
        required: true
        schema: {description: Config Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeConfig'}
          description: Successful Operation
      summary: Get a Probe Config (v2.0)
      tags: [Performance Policy]
    put:
      description: Update a probe config.
      operationId: put-sdwan-v2.0-api-probeconfigs-config_id
      parameters:
      - description: The probe config ID.
        in: path
        name: config_id
        required: true
        schema: {description: Config Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ProbeConfigScreen'}
        description: Details of the PROBE profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeConfig'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PROBE_ENDPOINTS_REQUIRED}
                  - {code: PROBE_ENDPOINT_INVALID_IP}
                  - {code: PROBE_ENDPOINT_MULTICAST_IP_NOT_ALLOWED}
                  - {code: PROBE_ENDPOINT_DNS_SERVER_MULTICAST_IP_NOT_ALLOWED}
                  - {code: PROBE_ENDPOINT_INVALID_PROTOCOL}
                  - {code: PROBE_ENDPOINT_INVALID_PROBE_CYCLE_DURATION_RANGE}
                  - {code: PROBE_ENDPOINT_INVALID_PATH_TYPE}
                  - {code: PROBE_ENDPOINTS_SUPPORTED_SIZE_EXCEEDED}
                  - {code: PROBE_ENDPOINT_INVALID_PROBE_COUNT}
                  - {code: PROBE_ENDPOINT_EITHER_IP_OR_FQDN_MUST_BE_CONFIGURED}
                  - {code: PROBE_ENDPOINT_IP_AND_FQDN_CANNOT_CONFIGURED_TOGETHER}
                  - {code: PROBE_ENDPOINT_INSECURE_HTTPS_APPLICABLE_FOR_PROTOCOL_HTTPS}
                  - {code: PROBE_ENDPOINT_HTTP_CODE_STRING_NOT_SUPPORTED}
                  - {code: PROBE_ENDPOINT_PATH_TYPES_REQUIRED}
                  - {code: PROBE_ENDPOINT_DUPLICATE_PATH_TYPES}
                  - {code: PROBE_ENDPOINT_INVALID_DNS_SERVER_IP}
                  - {code: PROBE_ENDPOINT_DNS_SERVER_IP_NOT_APPLICABLE}
                  - {code: PROBE_ENDPOINT_DNS_SERVER_IP_REQUIRED}
                  - {code: PROBE_ENDPOINT_IP_NOT_ALLOWED_FOR_DNS}
                  - {code: PROBE_ENDPOINT_INVALID_PROBE_DURATION_MULTIPLE}
                  - {code: INVALID_FQDN_STRING_LENGTH}
                  - {code: INVALID_HTTP_RESPONSE_STRING_LENGTH}
                  - {code: PROBE_ENDPOINT_INVALID_HTTP_RESPONSE_STRING_ENCODING}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update a Probe Config (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/probeprofiles:
    get:
      description: List all the probe profiles.
      operationId: get-sdwan-v2.0-api-probeprofiles
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeProfile'}
          description: Successful Operation
      summary: Get Probe Profile List (v2.0)
      tags: [Performance Policy]
    post:
      description: Create a new probe profile.
      operationId: post-sdwan-v2.0-api-probeprofiles
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ProbeProfileScreen'}
        description: Details of the PROBE profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PROBE_PROFILE_MAX_PROBE_CONFIGS_LIMIT_EXCEEDED_OR_DUPLICATES}
                  - {code: PROBE_ENDPOINT_DUPLICATE_IP_FQDN_PROTOCOL}
                  - {code: PROBE_PROFILE_IS_IN_USE_BY_WANINTERFACE_LABEL}
                  - {code: PROBE_PROFILE_IS_IN_USE_BY_WANINTERFACE}
                  - {code: PROBE_CONFIGS_INVALID}
                  - {code: PROBE_PROFILE_PROBE_CONFIG_IDS_REQUIRED}
                  - {code: PERFMGMT_CONFIG_IDS_IN_USE_BY_LABELS}
                  - {code: PERFMGMT_CONFIG_IDS_IN_USE_BY_WANINTERFACE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Probe Profile (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/probeprofiles/query:
    post:
      description: Query the DB for the limit number of tenant level probe profiles
        that match query parameters.
      operationId: post-sdwan-v2.0-api-probeprofiles-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ProbeProfileQuery'}
        description: Query Details for ProbeProfile
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/ProbeProfileQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for Tenant Level Probe Profiles (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.0/api/probeprofiles/{profile_id}:
    delete:
      description: Delete a probe profile.
      operationId: delete-sdwan-v2.0-api-probeprofiles-profile_id
      parameters:
      - description: The probe profile ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete a Probe Profile (v2.0)
      tags: [Performance Policy]
    get:
      description: List a probe profile.
      operationId: get-sdwan-v2.0-api-probeprofiles-profile_id
      parameters:
      - description: The probe profile ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeProfile'}
          description: Successful Operation
      summary: Get a Probe Profile (v2.0)
      tags: [Performance Policy]
    put:
      description: Update a probe profile.
      operationId: put-sdwan-v2.0-api-probeprofiles-profile_id
      parameters:
      - description: The probe profile ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ProbeProfileScreen'}
        description: Details of the PROBE profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ProbeProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PROBE_PROFILE_MAX_PROBE_CONFIGS_LIMIT_EXCEEDED_OR_DUPLICATES}
                  - {code: PROBE_ENDPOINT_DUPLICATE_IP_FQDN_PROTOCOL}
                  - {code: PROBE_PROFILE_IS_IN_USE_BY_WANINTERFACE_LABEL}
                  - {code: PROBE_PROFILE_IS_IN_USE_BY_WANINTERFACE}
                  - {code: PROBE_CONFIGS_INVALID}
                  - {code: PROBE_PROFILE_PROBE_CONFIG_IDS_REQUIRED}
                  - {code: PERFMGMT_CONFIG_IDS_IN_USE_BY_LABELS}
                  - {code: PERFMGMT_CONFIG_IDS_IN_USE_BY_WANINTERFACE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update a Probe Profile (v2.0)
      tags: [Performance Policy]
  /sdwan/v2.1/api/perfmgmtpolicyrules/query:
    post:
      description: Query the performance management policy rules.
      operationId: post-sdwan-v2.1-api-perfmgmtpolicyrules-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleQuery'}
        description: Details of the query for PERFMGMT Policy Rule
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtPolicyRuleQueryFilterV2N1'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Performance Policy Rules (v2.1)
      tags: [Performance Policy]
  /sdwan/v2.1/api/perfmgmtpolicysets/{perfmgmtpolicyset_id}/perfmgmtpolicyrules:
    get:
      description: List the policy rules of a policy set.
      operationId: get-sdwan-v2.1-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N1'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Policy Rules of Policy Set (v2.1)
      tags: [Performance Policy]
    post:
      description: Create a new performance policy rule.
      operationId: post-sdwan-v2.1-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleScreenV2N1'}
        description: Details of PERFMGMT Policy Rule to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: PERFMGMT_INVALID_PATH_TYPE}
                  - {code: PERFMGMT_INVALID_APP_TRANSFER_TYPE}
                  - {code: PERFMGMT_INVALID_ACTION_TYPE}
                  - {code: PERFMGMT_INVALID_MONITORING_APPROACH}
                  - {code: PERFMGMT_INVALID_RAISE_ABOVE}
                  - {code: PERFMGMT_INVALID_CLEAR_BELOW}
                  - {code: PERFMGMT_RULE_THRESHOLD_CLEAR_BELOW_INVALID}
                  - {code: PERFMGMT_INVALID_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_INVALID_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_POLICY_RULE_NOT_PRESENT}
                  - {code: PERFMGMT_MISSING_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_RULE_MISSING_ACTIONS}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_PERF_BAD_HEALTH_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_MISSING_ACTION_TYPE}
                  - {code: PERFMGMT_RULE_APP_FILTER_AND_APP_THRESHOLDS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING}
                  - {code: PERFMGMT_RULE_INVALID_APP_FILTER}
                  - {code: PERFMGMT_INVALID_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APP_TRANSFER_TYPES}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_LABEL_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_ACTION_TYPE}
                  - {code: PERFMGMT_RULE_TYPE_REQUIRED}
                  - {code: PERFMGMT_INVALID_RULE_TYPE}
                  - {code: PERFMGMT_APP_TRANSFER_TYPE_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_PATH_FILTERS_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_APP_FILTER_MANDATORY_FOR_APP_THRESHOLDS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY}
                  - {code: PERFMGMT_RULE_EXCEEDS_MAX_LIMIT}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_LABEL_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_FEC}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_MOVE_FLOWS}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLDS_REQUIRED_FOR_VISIBILITY}
                  - {code: PERFMGMT_RULE_VISIBILITY_APP_FILTERS_PROHIBITED}
                  - {code: PERFMGMT_RULE_FEC_APP_THRESHOLDS_PROHIBITED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING_FOR_LQM_PERF}
                  - {code: PERFMGMT_SERVICE_LABELS_NOT_ALLOWED_FOR_DIRECT_PATHS}
                  - {code: PERFMGMT_RULE_CANNOT_BE_CREATED_FOR_DEFAULT_SET}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED_64}
                  - {code: SERVICE_LABEL_ID_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_RULE_OVERLAPPING_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_PERF_PARAMETERS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_INVALID_ACTIONS_FOR_SYNTHETIC_PROBE_THRESHOLDS}
                  - {code: PERFMGMT_RULE_INCOMPATIBLE_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_RULE_INVALID_MATCH_CRITERIA_PARAMETERS_FOR_TYPE}
                  - {code: PERFMGMT_SYSTEM_RULE_ONLY_ALL_PATHS_ALLOWED}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_APP_CIRCUIT_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_RULE_TENANT_NOT_APP_ACCELERATION_ENABLED}
                  - {code: PERFMGMT_RULE_APP_ACCEL_ACTION_PATH_FILTER_MANDATORY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Performance Policy Rule (v2.1)
      tags: [Performance Policy]
  /sdwan/v2.1/api/perfmgmtpolicysets/{perfmgmtpolicyset_id}/perfmgmtpolicyrules/{perfmgmtpolicyrule_id}:
    delete:
      description: Delete a performance policy rule of the tenant.
      operationId: delete-sdwan-v2.1-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules-perfmgmtpolicyrule_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      - description: The performance policy rule ID.
        in: path
        name: perfmgmtpolicyrule_id
        required: true
        schema: {description: Perfmgmtpolicyrule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PERFMGMT_SET_NOT_FOUND}
                  - {code: PERFMGMT_POLICY_RULE_IN_USE}
                  - {code: DEFAULT_POLICY_RULE_CANNOT_BE_DELETED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Policy Rule (v2.1)
      tags: [Performance Policy]
    get:
      description: List a specific performance policy rule of the tenant.
      operationId: get-sdwan-v2.1-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules-perfmgmtpolicyrule_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      - description: The performance policy rule ID.
        in: path
        name: perfmgmtpolicyrule_id
        required: true
        schema: {description: Perfmgmtpolicyrule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N1'}
          description: Successful Operation
      summary: Get Policy Rule (v2.1)
      tags: [Performance Policy]
    put:
      description: Update a performance policy rule of the tenant.
      operationId: put-sdwan-v2.1-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules-perfmgmtpolicyrule_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      - description: The performance policy rule ID.
        in: path
        name: perfmgmtpolicyrule_id
        required: true
        schema: {description: Perfmgmtpolicyrule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleScreenV2N1'}
        description: Details of PERFMGMT Policy Rule to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: PERFMGMT_INVALID_PATH_TYPE}
                  - {code: PERFMGMT_INVALID_APP_TRANSFER_TYPE}
                  - {code: PERFMGMT_INVALID_ACTION_TYPE}
                  - {code: PERFMGMT_INVALID_MONITORING_APPROACH}
                  - {code: PERFMGMT_INVALID_RAISE_ABOVE}
                  - {code: PERFMGMT_INVALID_CLEAR_BELOW}
                  - {code: PERFMGMT_RULE_THRESHOLD_CLEAR_BELOW_INVALID}
                  - {code: PERFMGMT_INVALID_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_INVALID_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_POLICY_RULE_NOT_PRESENT}
                  - {code: PERFMGMT_MISSING_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_RULE_MISSING_ACTIONS}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_PERF_BAD_HEALTH_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_MISSING_ACTION_TYPE}
                  - {code: PERFMGMT_RULE_APP_FILTER_AND_APP_THRESHOLDS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING}
                  - {code: PERFMGMT_RULE_INVALID_APP_FILTER}
                  - {code: PERFMGMT_INVALID_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APP_TRANSFER_TYPES}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_LABEL_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_ACTION_TYPE}
                  - {code: PERFMGMT_DEFAULT_RESOURCE_NAME_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_DEFAULT_RULE_MATCH_CRITERIA_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_ACTION_CANNOT_BE_REMOVED_FROM_DEFAULT_RULE}
                  - {code: PERFMGMT_INVALID_RULE_TYPE}
                  - {code: PERFMGMT_APP_TRANSFER_TYPE_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_PATH_FILTERS_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_APP_FILTER_MANDATORY_FOR_APP_THRESHOLDS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY}
                  - {code: PERFMGMT_RULE_EXCEEDS_MAX_LIMIT}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_LABEL_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_FEC}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_MOVE_FLOWS}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLDS_REQUIRED_FOR_VISIBILITY}
                  - {code: PERFMGMT_RULE_VISIBILITY_APP_FILTERS_PROHIBITED}
                  - {code: PERFMGMT_RULE_FEC_APP_THRESHOLDS_PROHIBITED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING_FOR_LQM_PERF}
                  - {code: PERFMGMT_RULE_TYPE_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_CANNOT_SET_APP_METRICS_APP_FILTER_MISSING}
                  - {code: PERFMGMT_SERVICE_LABELS_NOT_ALLOWED_FOR_DIRECT_PATHS}
                  - {code: PERFMGMT_DEFAULT_RULE_CANNOT_BE_DISABLED}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED_64}
                  - {code: SERVICE_LABEL_ID_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_RULE_OVERLAPPING_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_PERF_PARAMETERS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_INVALID_ACTIONS_FOR_SYNTHETIC_PROBE_THRESHOLDS}
                  - {code: PERFMGMT_RULE_INCOMPATIBLE_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_RULE_INVALID_MATCH_CRITERIA_PARAMETERS_FOR_TYPE}
                  - {code: PERFMGMT_SYSTEM_RULE_ONLY_ALL_PATHS_ALLOWED}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_APP_CIRCUIT_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_RULE_TENANT_NOT_APP_ACCELERATION_ENABLED}
                  - {code: PERFMGMT_RULE_APP_ACCEL_ACTION_PATH_FILTER_MANDATORY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Policy Rule (v2.1)
      tags: [Performance Policy]
  /sdwan/v2.1/api/perfmgmtthresholdprofiles:
    get:
      description: List the performance policy threshold profiles.
      operationId: get-sdwan-v2.1-api-perfmgmtthresholdprofiles
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
      summary: Get Threshold Profile List (v2.1)
      tags: [Performance Policy]
    post:
      description: Create a new threshold profile.
      operationId: post-sdwan-v2.1-api-perfmgmtthresholdprofiles
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfileScreen'}
        description: Details of the Threshold profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PERFMGMT_HARD_LIMIT_APP_METRICS_REQUIRED}
                  - {code: PERFMGMT_MAX_INIT_FAILURE_RATE_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_LATENCY_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_JITTER_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_PACKET_LOSS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_RTT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_UDP_TRT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_LQM}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_APP_METRICS}
                  - {code: PERFMGMT_LQM_APP_OR_SYNTHETIC_PROBE_THREHOLDS_EXPECTED}
                  - {code: PERFMGMT_INVALID_CONFIG_IDS_IN_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_INVALID_CONFIG_PROTOCOL_IN_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_SYSTEM_CIRCUIT_OR_FLOW_THREHOLDS_EXPECTED}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_SYSTEM_HEALTH_METRICS}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_CIRCUIT_UTILIZATION_METRICS}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_FLOW_METRICS}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_JITTER_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_LATENCY_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_PACKET_LOSS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MIN_MOS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_INIT_FAILURE_PCT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_DNS_TXN_FAILURE_PCT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_NON_EMPTY_THREHOLDS_EXPECTED}
                  - {code: PERFMGMT_MUTUALLY_EXCLUSIVE_THRESHOLD_PARAMETERS_PRESENT}
                  - {code: PERFMGMT_INVALID_CONFIG_HETEROGENOUS_PROTOCOL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Threshold Profile (v2.1)
      tags: [Performance Policy]
  /sdwan/v2.1/api/perfmgmtthresholdprofiles/query:
    post:
      description: Query the DB for the limit number of tenant level threshold profiles
        that match query parameters.
      operationId: post-sdwan-v2.1-api-perfmgmtthresholdprofiles-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfileV2N1'}
        description: Query Details for Threshold profile
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtThresholdProfileV2N1'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Performance Policy Threshold Profiles (v2.1)
      tags: [Performance Policy]
  /sdwan/v2.1/api/perfmgmtthresholdprofiles/{profile_id}:
    delete:
      description: Delete a threshold profile.
      operationId: delete-sdwan-v2.1-api-perfmgmtthresholdprofiles-profile_id
      parameters:
      - description: The threshold profile ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfileV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PERFMGMT_THRESHOLD_PROFILE_IN_USE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Threshold Profile (v2.1)
      tags: [Performance Policy]
    get:
      description: List a threshold profile.
      operationId: get-sdwan-v2.1-api-perfmgmtthresholdprofiles-profile_id
      parameters:
      - description: The threshold profile ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfile'}
          description: Successful Operation
      summary: Get Threshold Profile (v2.1)
      tags: [Performance Policy]
    put:
      description: Update a threshold profile.
      operationId: put-sdwan-v2.1-api-perfmgmtthresholdprofiles-profile_id
      parameters:
      - description: The threshold profile ID.
        in: path
        name: profile_id
        required: true
        schema: {description: Profile Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfileScreenV2N1'}
        description: Details of the Threshold profile to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtThresholdProfileV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_VALUE}
                  - {code: PERFMGMT_HARD_LIMIT_APP_METRICS_REQUIRED}
                  - {code: PERFMGMT_MAX_INIT_FAILURE_RATE_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_LATENCY_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_JITTER_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_PACKET_LOSS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MAX_RTT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_UDP_TRT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_LQM}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_APP_METRICS}
                  - {code: PERFMGMT_CANNOT_UNSET_LQM_THRESHOLD_PROFILE_IN_USE}
                  - {code: PERFMGMT_CANNOT_SET_APP_METRICS_APP_FILTER_MISSING}
                  - {code: PERFMGMT_DEFAULT_RESOURCE_NAME_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_DEFAULT_THRESHOLD_PROFILE_LQM_UNSET_NOT_ALLOWED}
                  - {code: PERFMGMT_LQM_APP_OR_SYNTHETIC_PROBE_THREHOLDS_EXPECTED}
                  - {code: PERFMGMT_INVALID_CONFIG_IDS_IN_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_INVALID_CONFIG_PROTOCOL_IN_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_SYSTEM_CIRCUIT_OR_FLOW_THREHOLDS_EXPECTED}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_SYSTEM_HEALTH_METRICS}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_CIRCUIT_UTILIZATION_METRICS}
                  - {code: PERFMGMT_NON_EMPTY_THRESHOLDS_EXPECTED_FOR_FLOW_METRICS}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_JITTER_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_LATENCY_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_PACKET_LOSS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MIN_MOS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_INIT_FAILURE_PCT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_SYNTHETIC_PROBE_DNS_TXN_FAILURE_PCT_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_NON_EMPTY_THREHOLDS_EXPECTED}
                  - {code: PERFMGMT_MUTUALLY_EXCLUSIVE_THRESHOLD_PARAMETERS_PRESENT}
                  - {code: PERFMGMT_CPU_UTILIZATION_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_MEMORY_UTILIZATION_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_DISK_UTILIZATION_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_CIRCUIT_UTILIZATION_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_FLOW_METRICS_THRESHOLD_RANGE_INVALID}
                  - {code: PERFMGMT_PROFILE_IN_USE_BY_APP_CIRCUIT_HEALTH_RULE}
                  - {code: PERFMGMT_PROFILE_IN_USE_BY_SYSTEM_SITE_HEALTH_RULE}
                  - {code: PERFMGMT_INVALID_CONFIG_HETEROGENOUS_PROTOCOL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Threshold Profile (v2.1)
      tags: [Performance Policy]
  /sdwan/v2.2/api/perfmgmtpolicyrules/query:
    post:
      description: Query the performance management policy rules.
      operationId: post-sdwan-v2.2-api-perfmgmtpolicyrules-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleQuery'}
        description: Details of the query for PERFMGMT Policy Rule
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtPolicyRuleQueryFilterV2N1'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_QUERY_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Performance Policy Rules (v2.2)
      tags: [Performance Policy]
  /sdwan/v2.2/api/perfmgmtpolicysets/{perfmgmtpolicyset_id}/perfmgmtpolicyrules:
    get:
      description: List the policy rules of a policy set.
      operationId: get-sdwan-v2.2-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N2'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Policy Rules of Policy Set (v2.2)
      tags: [Performance Policy]
    post:
      description: Create a new performance policy rule.
      operationId: post-sdwan-v2.2-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleScreenV2N2'}
        description: Details of PERFMGMT Policy Rule to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: PERFMGMT_INVALID_PATH_TYPE}
                  - {code: PERFMGMT_INVALID_APP_TRANSFER_TYPE}
                  - {code: PERFMGMT_INVALID_ACTION_TYPE}
                  - {code: PERFMGMT_INVALID_MONITORING_APPROACH}
                  - {code: PERFMGMT_INVALID_RAISE_ABOVE}
                  - {code: PERFMGMT_INVALID_CLEAR_BELOW}
                  - {code: PERFMGMT_RULE_THRESHOLD_CLEAR_BELOW_INVALID}
                  - {code: PERFMGMT_INVALID_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_INVALID_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_POLICY_RULE_NOT_PRESENT}
                  - {code: PERFMGMT_MISSING_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_RULE_MISSING_ACTIONS}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_PERF_BAD_HEALTH_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_MISSING_ACTION_TYPE}
                  - {code: PERFMGMT_RULE_APP_FILTER_AND_APP_THRESHOLDS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING}
                  - {code: PERFMGMT_RULE_INVALID_APP_FILTER}
                  - {code: PERFMGMT_INVALID_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APP_TRANSFER_TYPES}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_LABEL_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_ACTION_TYPE}
                  - {code: PERFMGMT_RULE_TYPE_REQUIRED}
                  - {code: PERFMGMT_INVALID_RULE_TYPE}
                  - {code: PERFMGMT_APP_TRANSFER_TYPE_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_PATH_FILTERS_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_APP_FILTER_MANDATORY_FOR_APP_THRESHOLDS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY}
                  - {code: PERFMGMT_RULE_EXCEEDS_MAX_LIMIT}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_LABEL_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_FEC}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_MOVE_FLOWS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_MOVE_FLOWS_FORCED}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLDS_REQUIRED_FOR_VISIBILITY}
                  - {code: PERFMGMT_RULE_VISIBILITY_APP_FILTERS_PROHIBITED}
                  - {code: PERFMGMT_RULE_FEC_APP_THRESHOLDS_PROHIBITED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING_FOR_LQM_PERF}
                  - {code: PERFMGMT_SERVICE_LABELS_NOT_ALLOWED_FOR_DIRECT_PATHS}
                  - {code: PERFMGMT_RULE_CANNOT_BE_CREATED_FOR_DEFAULT_SET}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED_64}
                  - {code: SERVICE_LABEL_ID_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_RULE_OVERLAPPING_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_PERF_PARAMETERS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_INVALID_ACTIONS_FOR_SYNTHETIC_PROBE_THRESHOLDS}
                  - {code: PERFMGMT_RULE_INCOMPATIBLE_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_RULE_INVALID_MATCH_CRITERIA_PARAMETERS_FOR_TYPE}
                  - {code: PERFMGMT_SYSTEM_RULE_ONLY_ALL_PATHS_ALLOWED}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_APP_CIRCUIT_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_RULE_TENANT_NOT_APP_ACCELERATION_ENABLED}
                  - {code: PERFMGMT_RULE_DUPLICATE_NETWORK_CONTEXT_IDS}
                  - {code: PERFMGMT_RULE_INVALID_NETWORK_CONTEXT_IDS}
                  - {code: PERFMGMT_RULE_INVALID_ACTION_WITH_NETWORK_CONTEXT}
                  - {code: PERFMGMT_RULE_INVALID_ACTIONS_COMBINATION_WITH_NETWORK_CONTEXT}
                  - {code: PERFMGMT_SYSTEM_RULE_NETWORK_CONTEXT_NOT_APPLICABLE}
                  - {code: PERFMGMT_INVALID_ACTION_CONFIG_FOR_ALWAYS_ON}
                  - {code: PERFMGMT_MUTUALLY_EXCLUSIVE_ACTIONS_CONFIGURED}
                  - {code: PERFMGMT_PACKET_DUPLICATION_WITHOUT_MOVE_FLOWS}
                  - {code: PERFMGMT_FEC_WITHOUT_MOVE_FLOWS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Performance Policy Rule (v2.2)
      tags: [Performance Policy]
  /sdwan/v2.2/api/perfmgmtpolicysets/{perfmgmtpolicyset_id}/perfmgmtpolicyrules/{perfmgmtpolicyrule_id}:
    delete:
      description: Delete a performance policy rule of the tenant.
      operationId: delete-sdwan-v2.2-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules-perfmgmtpolicyrule_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      - description: The performance policy rule ID.
        in: path
        name: perfmgmtpolicyrule_id
        required: true
        schema: {description: Perfmgmtpolicyrule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PERFMGMT_SET_NOT_FOUND}
                  - {code: PERFMGMT_POLICY_RULE_IN_USE}
                  - {code: DEFAULT_POLICY_RULE_CANNOT_BE_DELETED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Policy Rule (v2.2)
      tags: [Performance Policy]
    get:
      description: List a specific performance policy rule of the tenant.
      operationId: get-sdwan-v2.2-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules-perfmgmtpolicyrule_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      - description: The performance policy rule ID.
        in: path
        name: perfmgmtpolicyrule_id
        required: true
        schema: {description: Perfmgmtpolicyrule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N2'}
          description: Successful Operation
      summary: Get Policy Rule (v2.2)
      tags: [Performance Policy]
    put:
      description: Update a performance policy rule of the tenant.
      operationId: put-sdwan-v2.2-api-perfmgmtpolicysets-perfmgmtpolicyset_id-perfmgmtpolicyrules-perfmgmtpolicyrule_id
      parameters:
      - description: The performance policy set ID.
        in: path
        name: perfmgmtpolicyset_id
        required: true
        schema: {description: Perfmgmtpolicyset Id, type: string}
      - description: The performance policy rule ID.
        in: path
        name: perfmgmtpolicyrule_id
        required: true
        schema: {description: Perfmgmtpolicyrule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleScreenV2N2'}
        description: Details of PERFMGMT Policy Rule to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PerfMgmtPolicyRuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: PERFMGMT_INVALID_PATH_TYPE}
                  - {code: PERFMGMT_INVALID_APP_TRANSFER_TYPE}
                  - {code: PERFMGMT_INVALID_ACTION_TYPE}
                  - {code: PERFMGMT_INVALID_MONITORING_APPROACH}
                  - {code: PERFMGMT_INVALID_RAISE_ABOVE}
                  - {code: PERFMGMT_INVALID_CLEAR_BELOW}
                  - {code: PERFMGMT_RULE_THRESHOLD_CLEAR_BELOW_INVALID}
                  - {code: PERFMGMT_INVALID_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_INVALID_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_POLICY_RULE_NOT_PRESENT}
                  - {code: PERFMGMT_MISSING_THRESHOLD_PROFILE_ID}
                  - {code: PERFMGMT_RULE_MISSING_ACTIONS}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_APP_PERF_BAD_HEALTH_THRESHOLD_MISSING}
                  - {code: PERFMGMT_RULE_MISSING_ACTION_TYPE}
                  - {code: PERFMGMT_RULE_APP_FILTER_AND_APP_THRESHOLDS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING}
                  - {code: PERFMGMT_RULE_INVALID_APP_FILTER}
                  - {code: PERFMGMT_INVALID_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APPLICATION_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_SERVICE_LABEL_IDS}
                  - {code: PERFMGMT_RULE_DUPLICATE_APP_TRANSFER_TYPES}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_LABEL_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_DUPLICATE_ACTION_TYPE}
                  - {code: PERFMGMT_DEFAULT_RESOURCE_NAME_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_DEFAULT_RULE_MATCH_CRITERIA_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_ACTION_CANNOT_BE_REMOVED_FROM_DEFAULT_RULE}
                  - {code: PERFMGMT_INVALID_RULE_TYPE}
                  - {code: PERFMGMT_APP_TRANSFER_TYPE_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_PATH_FILTERS_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_APP_FILTER_MANDATORY_FOR_APP_THRESHOLDS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY}
                  - {code: PERFMGMT_RULE_EXCEEDS_MAX_LIMIT}
                  - {code: PERFMGMT_RULE_DUPLICATE_PATH_LABEL_PATH_TYPE_IN_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_FEC}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_MOVE_FLOWS}
                  - {code: PERFMGMT_RULE_APP_FILTER_MANDATORY_FOR_ACTION_MOVE_FLOWS_FORCED}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLDS_REQUIRED_FOR_VISIBILITY}
                  - {code: PERFMGMT_RULE_VISIBILITY_APP_FILTERS_PROHIBITED}
                  - {code: PERFMGMT_RULE_FEC_APP_THRESHOLDS_PROHIBITED}
                  - {code: PERFMGMT_RULE_THRESHOLDS_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_APP_FILTER_MISSING_FOR_APP_PERF}
                  - {code: PERFMGMT_RULE_LQM_THRESHOLD_MISSING_FOR_LQM_PERF}
                  - {code: PERFMGMT_RULE_TYPE_CHANGE_NOT_ALLOWED}
                  - {code: PERFMGMT_CANNOT_SET_APP_METRICS_APP_FILTER_MISSING}
                  - {code: PERFMGMT_SERVICE_LABELS_NOT_ALLOWED_FOR_DIRECT_PATHS}
                  - {code: PERFMGMT_DEFAULT_RULE_CANNOT_BE_DISABLED}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED_64}
                  - {code: SERVICE_LABEL_ID_LIST_SIZE_EXCEEDED}
                  - {code: PERFMGMT_RULE_OVERLAPPING_PATH_FILTERS}
                  - {code: PERFMGMT_RULE_PERF_PARAMETERS_NOT_ALLOWED}
                  - {code: PERFMGMT_RULE_INVALID_ACTIONS_FOR_SYNTHETIC_PROBE_THRESHOLDS}
                  - {code: PERFMGMT_RULE_INCOMPATIBLE_THRESHOLD_PROFILE}
                  - {code: PERFMGMT_RULE_INVALID_MATCH_CRITERIA_PARAMETERS_FOR_TYPE}
                  - {code: PERFMGMT_SYSTEM_RULE_ONLY_ALL_PATHS_ALLOWED}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION}
                  - {code: PERFMGMT_SYSTEM_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_APP_CIRCUIT_HEALTH_RULE_UNSUPPORTED_ACTION_FIELDS}
                  - {code: PERFMGMT_RULE_TENANT_NOT_APP_ACCELERATION_ENABLED}
                  - {code: PERFMGMT_RULE_DUPLICATE_NETWORK_CONTEXT_IDS}
                  - {code: PERFMGMT_RULE_INVALID_NETWORK_CONTEXT_IDS}
                  - {code: PERFMGMT_RULE_INVALID_ACTION_WITH_NETWORK_CONTEXT}
                  - {code: PERFMGMT_RULE_INVALID_ACTIONS_COMBINATION_WITH_NETWORK_CONTEXT}
                  - {code: PERFMGMT_SYSTEM_RULE_NETWORK_CONTEXT_NOT_APPLICABLE}
                  - {code: PERFMGMT_INVALID_ACTION_CONFIG_FOR_ALWAYS_ON}
                  - {code: PERFMGMT_MUTUALLY_EXCLUSIVE_ACTIONS_CONFIGURED}
                  - {code: PERFMGMT_PACKET_DUPLICATION_WITHOUT_MOVE_FLOWS}
                  - {code: PERFMGMT_FEC_WITHOUT_MOVE_FLOWS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Policy Rule (v2.2)
      tags: [Performance Policy]
servers:
- {url: 'https://api.sase.paloaltonetworks.com'}
tags:
- {name: Performance Policy}
