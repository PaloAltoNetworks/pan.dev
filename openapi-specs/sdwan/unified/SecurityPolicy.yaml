components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items: {$ref: '#/components/schemas/AggregateData'}
          type: array
        field: {description: 'The field value.

            ', type: string}
        name: {description: 'The name of the query response.

            ', type: string}
        operator:
          description: 'The operator values.

            '
          enum: [max, count, min, avg, sum]
          type: string
        statistic: {description: 'The aggregate statistics.

            ', type: string}
        unit: {description: 'The unit.

            ', type: string}
    AggregateData:
      properties:
        value: {description: 'Value of the aggregate data.

            ', type: number}
        view: {description: 'View the aggregate date.

            ', type: object}
    ElementSecurityZone:
      properties:
        id: {description: 'The ID of the security zone.

            ', type: string}
        interface_ids:
          description: 'The element security zones interface IDs.

            '
          items: {description: Interface Ids, type: string}
          type: array
        lannetwork_ids:
          description: 'The element security zones LAN network IDs.

            '
          items: {description: Lannetwork Ids, type: string}
          type: array
        site_id: {description: "The site ID configured for the element security zone.\
            \    \n", type: string}
        waninterface_ids:
          description: 'The element security zones WAN interface IDs.

            '
          items: {description: Waninterface Ids, type: string}
          type: array
        wanoverlay_ids:
          description: 'The element security zones WAN overlay IDs.

            '
          items: {description: Wanoverlay Ids, type: string}
          type: array
        zone_id: {description: 'The element security zone ID.

            ', type: string}
      required: [zone_id, site_id]
    ElementSecurityZoneQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of element security zones that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of element security zones deleted for
            a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the element security zones for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the element security zones by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    ElementSecurityZoneQueryFilter:
      properties:
        element_id: {description: 'The ID of the element configured for the security
            zone.

            ', type: string}
        id: {description: 'The ID of the security zone.

            ', type: string}
        interface_ids:
          description: 'The element security zones interface IDs.

            '
          items: {description: Interface Ids, type: string}
          type: array
        lannetwork_ids:
          description: 'The element security zones LAN network IDs.

            '
          items: {description: Lannetwork Ids, type: string}
          type: array
        site_id: {description: "The site ID configured for the element security zone.\
            \    \n", type: string}
        waninterface_ids:
          description: 'The element security zones WAN interface IDs.

            '
          items: {description: Waninterface Ids, type: string}
          type: array
        wanoverlay_ids:
          description: 'The element security zones WAN overlay IDs.

            '
          items: {description: Wanoverlay Ids, type: string}
          type: array
        zone_id: {description: 'The element security zone ID.

            ', type: string}
      required: [zone_id, site_id]
    ElementSecurityZoneScreen:
      properties:
        id: {description: 'The ID of the security zone.

            ', type: string}
        interface_ids:
          description: 'The element security zones interface IDs.

            '
          items: {description: Interface Ids, type: string}
          type: array
        lannetwork_ids:
          description: 'The element security zones LAN network IDs.

            '
          items: {description: Lannetwork Ids, type: string}
          type: array
        site_id: {description: "The site ID configured for the element security zone.\
            \    \n", type: string}
        waninterface_ids:
          description: 'The element security zones WAN interface IDs.

            '
          items: {description: Waninterface Ids, type: string}
          type: array
        wanoverlay_ids:
          description: 'The element security zones WAN overlay IDs.

            '
          items: {description: Wanoverlay Ids, type: string}
          type: array
        zone_id: {description: 'The element security zone ID.

            ', type: string}
      required: [zone_id, site_id]
    Error:
      properties:
        _error:
          items: {$ref: '#/components/schemas/ErrorResponse'}
          type: array
    ErrorResponse:
      properties:
        code: {description: 'The error code.

            ', type: string}
        message: {description: 'The error message.

            ', type: string}
    Filter:
      properties:
        elements:
          description: 'Elements of the filter.

            '
          items: {description: Elements, type: string}
          type: array
        path:
          description: 'Path of the filter.

            '
          items: {description: Path, type: string}
          type: array
        site: {description: 'Site of the filter.

            ', type: object}
        type: {description: 'Type of the filter.

            ', type: string}
        wn_path:
          description: Wn Path
          items: {description: Wn Path, type: string}
          type: array
    GlobalPrefixQueryFilter:
      properties:
        description: {description: 'The description of the security policy global
            prefix. Max size = 256.

            '}
        id: {description: 'The ID of the security policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the security policy global prefix. Max size
            = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    GlobalPrefixQueryFilterV2N1:
      properties:
        description: {description: 'The description of the priority policy global
            prefix.

            '}
        id: {description: 'The ID of the priority policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the priority policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 addresses of the priority policy global prefix.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        name: {description: 'The name of the priority policy global prefix.

            ', type: string}
        tags:
          description: "A information field that can be added to identify the priority\
            \ policy global prefix. Maximum 10 unique tags of length 1024 each are\
            \ allowed.    \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    ListQueryResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        deleted_count: {description: 'The deleted number.

            ', format: int64, type: integer}
        deleted_ids:
          description: 'The deleted IDs.

            '
          items: {type: string}
          type: array
        description: {description: 'Description of the query. Max size = 256.

            '}
        id: {description: 'The ID.

            ', type: string}
        next_query: {description: 'Details of the next query.

            ', type: object}
        total_count: {description: 'Total number.

            ', format: int64, type: integer}
      type: object
    ListResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
      type: object
    PolicySetStackQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of policy set stacks that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of policy set stacks deleted for a time
            frame.

            ', type: boolean}
        group_by:
          description: 'Group the policy set stacks for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "List the policy set stacks by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    PortRange:
      properties:
        from: {description: 'The start port of the destination/source port range.

            ', type: integer}
        to: {description: 'The end port of the destination/source port range.

            ', type: integer}
    PrefixFilterAssociation:
      properties:
        filters:
          description: 'The details of the prefix filter.

            '
          items: {$ref: '#/components/schemas/Filter'}
          type: array
        id: {description: "The ID. \n", type: string}
        prefix_filter_id: {description: 'The ID of the prefix filter.

            ', type: string}
      required: [filters]
    PrefixFilterQueryScreen:
      properties:
        id: {description: 'The ID of the security prefix filter.

            ', type: string}
        query_params: {$ref: '#/components/schemas/QueryParam', description: 'Query
            Params: Valid '}
    QueryParam:
      properties:
        zone_id: {description: 'The element security zone ID.

            ', type: string}
      required: [zone_id]
    RuleOrder:
      properties:
        insert_before: {description: Insert Before, type: string}
        policyrule_id: {description: Policyrule Id, type: string}
    SecurityPolicyGlobalPrefix:
      properties:
        description: {description: 'The description of the security policy global
            prefix. Max size = 256.

            '}
        id: {description: 'The ID of the security policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the security policy global prefix. Max size
            = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyGlobalPrefixScreen:
      properties:
        description: {description: 'The description of the security policy global
            prefix. Max size = 256.

            '}
        id: {description: 'The ID of the security policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the security policy global prefix. Max size
            = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyGlobalPrefixScreenV2N1:
      properties:
        description: {description: 'The description of the security policy global
            prefix.

            '}
        id: {description: 'The ID of the security policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the priority policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 addresses of the security policy global prefix.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        name: {description: 'The name of the security policy global prefix.

            ', type: string}
        tags:
          description: "A information field that can be added to identify the security\
            \ policy global prefix. Maximum 10 unique tags of length 1024 each are\
            \ allowed.      \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyGlobalPrefixV2N1:
      properties:
        description: {description: 'The description of the security policy global
            prefix.

            '}
        id: {description: 'The ID of the security policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the priority policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 addresses of the security policy global prefix.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        name: {description: 'The name of the security policy global prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyRule:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        application_ids:
          description: 'The security policy rule application ID.

            '
          items: {description: Application Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        destination_filter_ids:
          description: 'The destination filter IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Filter Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        disabled_flag: {description: "The security policy rule, if disabled.    \n",
          type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        source_filter_ids:
          description: 'The source filter IDs. Max list of IDs is 16.

            '
          items: {description: Source Filter Ids, type: string}
          type: array
        source_zone_ids:
          description: "The source zone IDs. Max list of IDs is 16. \n"
          items: {description: Source Zone Ids, type: string}
          type: array
      required: [application_ids, destination_zone_ids, source_zone_ids, name]
    SecurityPolicyRuleQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of LAN networks that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of LAN networks deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the LAN networks for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the LAN networks by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SecurityPolicyRuleQueryFilter:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        application_ids:
          description: 'The security policy rule application ID.

            '
          items: {description: Application Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        destination_filter_ids:
          description: 'The destination filter IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Filter Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        disabled_flag: {description: "The security policy rule, if disabled.    \n",
          type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        security_policyset_id: {description: 'The security policy set ID.

            ', type: string}
        source_filter_ids:
          description: 'The source filter IDs. Max list of IDs is 16.

            '
          items: {description: Source Filter Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
      required: [application_ids, destination_zone_ids, source_zone_ids, name]
    SecurityPolicyRuleScreen:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        application_ids:
          description: 'The security policy rule application ID.

            '
          items: {description: Application Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        destination_filter_ids:
          description: 'The destination filter IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Filter Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        disabled_flag: {description: "The security policy rule, if disabled.    \n",
          type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        source_filter_ids:
          description: 'The source filter IDs. Max list of IDs is 16.

            '
          items: {description: Source Filter Ids, type: string}
          type: array
        source_zone_ids:
          description: "The source zone IDs. Max list of IDs is 16. \n"
          items: {description: Source Zone Ids, type: string}
          type: array
      required: [application_ids, destination_zone_ids, source_zone_ids, name]
    SecurityPolicySet:
      properties:
        description: {description: 'The description of the security policy set. Max
            size = 256.

            '}
        id: {description: 'The security policy set ID.

            ', type: string}
        name: {description: 'The name of the security policy set. Max size = 128.

            ', type: string}
        policyrule_order:
          description: "The order for the execution of the policy rule. \n"
          items: {description: Policyrule Order, type: string}
          type: array
      required: [name]
    SecurityPolicySetPOSTScreen:
      properties:
        clone_from: {description: "Policy sets cloned from existing policy sets. \n",
          type: string}
        description: {description: 'The description of the security policy set. Max
            size = 256.

            '}
        id: {description: 'The security policy set ID.

            ', type: string}
        name: {description: 'The name of the security policy set. Max size = 128.

            ', type: string}
        policyrule_order:
          description: 'The order for the execution of the policy rule.

            '
          items: {description: Policyrule Order, type: string}
          type: array
      required: [name]
    SecurityPolicySetQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of security policy sets that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of security policy sets deleted for
            a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the security policy sets for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the security policy sets by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SecurityPolicySetQueryFilter:
      properties:
        description: {description: 'Description of the security policy set.

            '}
        id: {description: 'ID of the security policy set.

            ', type: string}
        name: {description: "Name of the security policy set. Max 25 characters. \
            \   \n", type: string}
        policyrule_order:
          description: 'The order for the execution of the policy rule.

            '
          items: {description: Policyrule Order, type: string}
          type: array
      required: [name]
    SecurityPolicySetRuleOrderScreen:
      properties:
        id: {description: 'ID of the security policy set.

            ', type: string}
        operations:
          description: 'The actions to create the security policy set.

            '
          items: {$ref: '#/components/schemas/RuleOrder'}
          type: array
    SecurityPolicyV2LocalPrefix:
      properties:
        description: {description: 'The description of the security policy local prefix.
            Max size = 256.

            '}
        id: {description: 'The ID of the security policy local prefix.

            ', type: string}
        name: {description: 'The name of the security policy local prefix. Max size
            = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy local prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyV2LocalPrefixScreen:
      properties:
        description: {description: 'The description of the security policy local prefix.
            Max size = 256.

            '}
        id: {description: 'The ID of the security policy local prefix.

            ', type: string}
        name: {description: 'The name of the security policy local prefix. Max size
            = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy local prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyV2N1RuleQueryFilter:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        app_def_ids:
          description: 'The security policy rule application ID.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        destination_prefix_ids:
          description: 'The destination prefix IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Prefix Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        enabled: {description: "The security policy rule, if enabled.    \n", type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        policyset_id: {description: 'Policyset Id: Digits(fraction = 0, integer =
            30, INVALID_POLICYSET_ID) ', type: string}
        services:
          description: "The details of the source and destination ports along with\
            \ the protocol used.  \n"
          items: {$ref: '#/components/schemas/Service'}
          type: array
        source_prefix_ids:
          description: 'The source prefix IDs. Max list of IDs is 16.

            '
          items: {description: Source Prefix Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: 'The
            details of the user or group.

            '}
      required: [enabled, action, name]
    SecurityPolicyV2N2RuleQueryFilter:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        app_def_ids:
          description: 'The security policy rule application ID.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefix_ids:
          description: 'The destination prefix IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Prefix Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        enabled: {description: "The security policy rule, if enabled.    \n", type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        policyset_id: {description: 'Policyset Id: Digits(fraction = 0, integer =
            30, INVALID_POLICYSET_ID) ', type: string}
        services:
          description: "The details of the source and destination ports along with\
            \ the protocol used.  \n"
          items: {$ref: '#/components/schemas/Service'}
          type: array
        source_prefix_ids:
          description: 'The source prefix IDs. Max list of IDs is 16.

            '
          items: {description: Source Prefix Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: 'The
            details of the user or group.

            '}
      required: [enabled, action, name]
    SecurityPolicyV2N2RuleScreen:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        app_def_ids:
          description: 'The security policy rule application ID.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefix_ids:
          description: 'The destination prefix IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Prefix Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        enabled: {description: "The security policy rule, if enabled.    \n", type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        services:
          description: "The details of the source and destination ports along with\
            \ the protocol used.  \n"
          items: {$ref: '#/components/schemas/Service'}
          type: array
        source_prefix_ids:
          description: 'The source prefix IDs. Max list of IDs is 16.

            '
          items: {description: Source Prefix Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: 'The
            details of the user or group.

            '}
      required: [enabled, action, name]
    SecurityPolicyV2RuleQueryFilterV2N3:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        app_def_ids:
          description: 'The number of app definition IDs that have applied security
            policy rules. Maximum 16 applications can be specified in a rule.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device IDs.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefix_ids:
          description: 'The destination prefix IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Prefix Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        enabled: {description: "The security policy rules based on rules that are\
            \ enabled.    \n", type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        policyset_id: {description: 'The policy set ID.

            ', type: string}
        security_profile_group_id: {description: 'The security profile group ID.

            ', type: string}
        services:
          description: "The details of the source and destination ports along with\
            \ the protocol used..               \n"
          items: {$ref: '#/components/schemas/Service'}
          type: array
        source_prefix_ids:
          description: 'The source prefix IDs. Max list of IDs is 16.

            '
          items: {description: Source Prefix Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: "The\
            \ details of the user or group. \n"}
      required: [enabled, action, name]
    SecurityPolicyV2RuleScreenV2N3:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        app_def_ids:
          description: 'The number of app definition IDs that have applied security
            policy rules. Maximum 16 applications can be specified in a rule.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device IDs.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefix_ids:
          description: 'The destination prefix IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Prefix Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        enabled: {description: "The security policy rules based on rules that are\
            \ enabled.    \n", type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        security_profile_group_id: {description: 'The security profile group ID.

            ', type: string}
        services:
          description: "The details of the source and destination ports along with\
            \ the protocol used..               \n"
          items: {$ref: '#/components/schemas/Service'}
          type: array
        source_prefix_ids:
          description: 'The source prefix IDs. Max list of IDs is 16.

            '
          items: {description: Source Prefix Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: "The\
            \ details of the user or group.\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\
            \ \n    \n    \n\n\n\n  \n  \n          \n        \n\n\n\n\n\n\n\n\n\n\
            \n\n\n\n          \n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\
            \n        \n\n\n\n\n\n \n\n\n\n\n\n\n\n\n\n\n\n\n\n\n           \n\n\n\
            \n\n\n\n       \n          \n        \n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\
            \n\n\n\n\n\n          \n\n\n\n\n\n          \n\n\n\n\n\n\n\n      \n\n\
            \n     \n\n\n     \n\n\n          \n\n\n\n\n\n\n\n\n\n\n    \n    "}
      required: [enabled, action, name]
    SecurityPolicyV2RuleV2N2:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        app_def_ids:
          description: 'The security policy rule application ID.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefix_ids:
          description: 'The destination prefix IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Prefix Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        enabled: {description: "The security policy rule, if enabled.    \n", type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        services:
          description: "The details of the source and destination ports along with\
            \ the protocol used.  \n"
          items: {$ref: '#/components/schemas/Service'}
          type: array
        source_prefix_ids:
          description: 'The source prefix IDs. Max list of IDs is 16.

            '
          items: {description: Source Prefix Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: 'The
            details of the user or group.

            '}
      required: [enabled, action, name]
    SecurityPolicyV2RuleV2N3:
      properties:
        action: {description: "The action taken for traffic matching this rule. It\
            \ determine how the traffic from the specified source zone to the specified\
            \ destination zone should respond.       \n", type: string}
        app_def_ids:
          description: 'The number of app definition IDs that have applied security
            policy rules. Maximum 16 applications can be specified in a rule.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the security policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device IDs.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefix_ids:
          description: 'The destination prefix IDs. Max list of prefix IDs is 16.

            '
          items: {description: Destination Prefix Ids, type: string}
          type: array
        destination_zone_ids:
          description: 'The destination zone IDs. Max list of zone IDs is 16.

            '
          items: {description: Destination Zone Ids, type: string}
          type: array
        enabled: {description: "The security policy rules based on rules that are\
            \ enabled.    \n", type: boolean}
        id: {description: 'The security policy rule ID.

            ', type: string}
        name: {description: 'The name of the security policy rule. Max size = 128.

            ', type: string}
        security_profile_group_id: {description: 'The security profile group ID.

            ', type: string}
        services:
          description: "The details of the source and destination ports along with\
            \ the protocol used..               \n"
          items: {$ref: '#/components/schemas/Service'}
          type: array
        source_prefix_ids:
          description: 'The source prefix IDs. Max list of IDs is 16.

            '
          items: {description: Source Prefix Ids, type: string}
          type: array
        source_zone_ids:
          description: 'The source zone IDs. Max list of IDs is 16.

            '
          items: {description: Source Zone Ids, type: string}
          type: array
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: "The\
            \ details of the user or group. \n"}
      required: [enabled, action, name]
    SecurityPolicyV2Set:
      properties:
        clone_from: {description: "Policy sets cloned from existing policy sets. \n",
          type: string}
        defaultrule_policyset: {description: "The default rule of the policy set.\
            \   \n", type: boolean}
        description: {description: 'The description of the security policy set. Max
            size = 256.

            '}
        id: {description: 'The security policy set ID.

            ', type: string}
        name: {description: 'The name of the security policy set. Max size = 128.

            ', type: string}
        policyrule_order:
          description: 'The order for the execution of the policy rule.

            '
          items: {description: Policyrule Order, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyV2SetQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of security policies V2 that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of security policies V2 deleted for
            a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the security policies V2 for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the security policies V2 by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SecurityPolicyV2SetQueryFilter:
      properties:
        clone_from: {description: "Policy sets cloned from existing policy sets. \n",
          type: string}
        defaultrule_policyset: {description: "The default rule of the policy set.\
            \   \n", type: boolean}
        description: {description: 'The description of the security policy set. Max
            size = 256.

            '}
        id: {description: 'The security policy set ID.

            ', type: string}
        name: {description: 'The name of the security policy set. Max size = 128.

            ', type: string}
        policyrule_order:
          description: 'The order for the execution of the policy rule.

            '
          items: {description: Policyrule Order, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyV2SetScreen:
      properties:
        clone_from: {description: "Policy sets cloned from existing policy sets. \n",
          type: string}
        defaultrule_policyset: {description: "The default rule of the policy set.\
            \   \n", type: boolean}
        description: {description: 'The description of the security policy set. Max
            size = 256.

            '}
        id: {description: 'The security policy set ID.

            ', type: string}
        name: {description: 'The name of the security policy set. Max size = 128.

            ', type: string}
        policyrule_order:
          description: 'The order for the execution of the policy rule.

            '
          items: {description: Policyrule Order, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyV2SetStack:
      properties:
        defaultrule_policyset_id: {description: 'The default rule of the policy set
            ID.

            ', type: string}
        description: {description: "The description of the security policy set stack.\
            \ Max size = 256. \n"}
        id: {description: 'The security policy set stack ID.

            ', type: string}
        name: {description: 'The name of the security policy set stack.

            ', type: string}
        policyset_ids:
          description: 'The ID of the policy set. Policy sets must be between 1-4.

            '
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyV2SetStackQueryFilter:
      properties:
        defaultrule_policyset_id: {description: 'The default rule of the policy set
            ID.

            ', type: string}
        description: {description: "The description of the security policy set stack.\
            \ Max size = 256. \n"}
        id: {description: 'The security policy set stack ID.

            ', type: string}
        name: {description: 'The name of the security policy set stack.

            ', type: string}
        policyset_ids:
          description: 'The IDs of the policy set. Policy sets must be between 1-4.

            '
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: "A information field that can be added to identify the security\
            \ policy set stack. Maximum 10 unique tags of length 1024 each are allowed.\
            \ \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityPolicyV2SetStackScreen:
      properties:
        defaultrule_policyset_id: {description: 'The default rule of the policy set
            ID.

            ', type: string}
        description: {description: "The description of the security policy set stack.\
            \ Max size = 256. \n"}
        id: {description: 'The security policy set stack ID.

            ', type: string}
        name: {description: 'The name of the security policy set stack.

            ', type: string}
        policyset_ids:
          description: 'The IDs of the policy set. Policy sets must be between 1-4.

            '
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the security
            policy set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    SecurityZone:
      properties:
        description: {description: 'The description of the security zone. Max size
            = 256.

            '}
        id: {description: 'The security zone ID.

            ', type: string}
        name: {description: 'The name of the zone. Max size = 128.

            ', type: string}
      required: [name]
    SecurityZoneNetworkAssociation:
      properties:
        id: {description: 'The ID of the security zone.

            ', type: string}
        networks:
          description: "The details of the network and network type. \n"
          items: {$ref: '#/components/schemas/SiteContext'}
          type: array
        zone_id: {description: 'The ID of the security zone.

            ', type: string}
      required: [networks, zone_id]
    SecurityZoneQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of security zones that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of security zones deleted for a time
            frame.

            ', type: boolean}
        group_by:
          description: 'Group the security zones for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the security zones by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SecurityZoneQueryFilter:
      properties:
        description: {description: 'The description of the security zone. Max size
            = 256.

            '}
        id: {description: 'The security zone ID.

            ', type: string}
        name: {description: 'The name of the zone. Max size = 128.

            ', type: string}
    SecurityZoneQueryScreen:
      properties:
        id: {description: 'The ID of the security zone.

            ', type: string}
        query_params: {$ref: '#/components/schemas/QueryParam', description: 'Query
            Params: Valid '}
    SecurityZoneV2N1:
      properties:
        description: {description: 'The description of the security zone. Max size
            = 256.

            '}
        id: {description: 'The security zone ID.

            ', type: string}
        name: {description: 'The name of the zone. Max size = 128.

            ', type: string}
        tcp_allow_non_syn: {description: 'Allows TCP. True or False.

            ', type: boolean}
      required: [name]
    Service:
      properties:
        destination_ports:
          description: "The start and end port for Port Ranges. A maximum of 16 port\
            \ ranges is allowed, and port ranges can only be added for TCP or UDP\
            \ protocols.     \n"
          items: {$ref: '#/components/schemas/PortRange'}
          type: array
        protocol: {description: "The protocol type (TCP, or UDP, or TLS). \n", format: int32,
          type: integer}
        source_ports:
          description: 'The start value of the port range. A maximum of 16 port ranges
            are allowed, and port ranges can only be added for TCP or UDP protocols.

            '
          items: {$ref: '#/components/schemas/PortRange'}
          type: array
    SiteBasePrefixAssociationQueryFilter:
      properties:
        filters:
          description: 'The details of the prefix filter.

            '
          items: {$ref: '#/components/schemas/Filter'}
          type: array
        id: {description: "The ID. \n", type: string}
        prefix_filter_id: {description: 'The ID of the prefix filter.

            ', type: string}
        site_id: {description: 'The ID of the site.

            ', type: string}
      required: [filters]
    SiteContext:
      properties:
        network_id: {description: 'The ID of the network.

            ', type: string}
        network_type:
          description: 'The network type.

            '
          enum: [wan_network, lan_network, wan_overlay]
          type: string
      required: [network_type]
    SitePrefixAssociationQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of prefix associations that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of prefix associations deleted for a
            time frame.

            ', type: boolean}
        group_by:
          description: 'Group the prefix associations for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the prefix associations by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SitePrefixAssociationQueryFilter:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        site_id: {description: 'The site ID binding the prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    SitePrefixAssociationQueryFilterV2N1:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 address.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        site_id: {description: 'The site ID binding the prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    SiteSecurityPolicyV2N1PrefixAssociation:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 address.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    SiteSecurityPolicyV2N1PrefixAssociationScreen:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 address.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        tags:
          description: "A information field that can be added to identify the site\
            \ prefix. Maximum 10 unique tags of length 1024 each are allowed. \n"
          items: {description: Tags, type: string}
          type: array
    SiteSecurityPolicyV2PrefixAssociation:
      properties:
        id: {description: 'The ID of the security policy v2 prefix association.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy v2 prefix
            association.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        tags:
          description: 'An information field that can be added to identify the security
            policy v2 prefix association. Maximum 10 unique tags of length 1024 each
            are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    SiteSecurityPolicyV2PrefixAssociationScreen:
      properties:
        id: {description: 'The ID of the security policy v2 prefix association.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy v2 prefix
            association.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        tags:
          description: 'An information field that can be added to identify the security
            policy v2 prefix association. Maximum 10 unique tags of length 1024 each
            are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    SiteSecurityZoneNetworkAssociationQueryFilter:
      properties:
        id: {description: 'The ID of the security zone.

            ', type: string}
        networks:
          description: 'The details of the network and network type.

            '
          items: {$ref: '#/components/schemas/SiteContext'}
          type: array
        site_id: {description: "The site ID configured for the element security zone.\
            \               \n", type: string}
        zone_id: {description: 'The ID of the security zone.

            ', type: string}
      required: [site_id, networks, zone_id]
    UserGroup:
      properties:
        user_group_ids:
          description: 'The user group IDs.

            '
          items: {description: User Group Ids, type: string}
          type: array
        user_ids:
          description: 'The user IDs.

            '
          items: {description: User Ids, type: string}
          type: array
info:
  contact: {}
  description: List of APIs used to manage your security policy rules that include
    security zones, element security  zones, site security zones, global prefix filters,
    local prefix filters, security policy rule order, security policy rules, security
    policy sets, prefix filters, tenant filters, next generation firewall security
    policy prefixes, rules, policy sets, and policy set stacks.
  title: Security Policy
  version: Latest
openapi: 3.0.0
paths:
  /sdwan/v2.0/api/elementsecurityzones/query:
    post:
      description: Query the element security zones.
      operationId: post-sdwan-v2.0-api-elementsecurityzones-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ElementSecurityZoneQuery'}
        description: Details for Element Security Zone Query
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/ElementSecurityZoneQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Element Security Zones (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicyglobalprefixes:
    get:
      description: List all the security policy V2 global prefixes for a tenant.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicyglobalprefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefix'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Global Prefixes (v2.0)
      tags: [Security Policy]
    post:
      description: Create a security policy V2 global prefix.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicyglobalprefixes
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_PREFIX}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Global Prefix (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicyglobalprefixes/query:
    post:
      description: Query the security policy V2 global prefixes of a tenant.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicyglobalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GlobalPrefixQueryFilter'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/GlobalPrefixQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Security Policy Global Prefixes (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicyglobalprefixes/{prefix_id}:
    delete:
      description: Delete a security policy V2 global prefix by the tenant ID and
        the prefix ID.
      operationId: delete-sdwan-v2.0-api-ngfwsecuritypolicyglobalprefixes-prefix_id
      parameters:
      - description: ID of the security policy global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_USED_BY_RULE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Global Prefix (v2.0)
      tags: [Security Policy]
    get:
      description: List a security policy V2 global prefix by the tenant ID and the
        prefix ID.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicyglobalprefixes-prefix_id
      parameters:
      - description: ID of the security policy global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefix'}
          description: Successful Operation
      summary: Get Security Policy Global Prefix (v2.0)
      tags: [Security Policy]
    put:
      description: Update a security policy V2 global prefix by the tenant ID and
        the prefix ID.
      operationId: put-sdwan-v2.0-api-ngfwsecuritypolicyglobalprefixes-prefix_id
      parameters:
      - description: ID of the security policy global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_PREFIX}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Global Prefix (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicylocalprefixes:
    get:
      description: List all the security policy V2 local prefixes for a tenant.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicylocalprefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2LocalPrefix'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Local Prefixes (v2.0)
      tags: [Security Policy]
    post:
      description: Create a security policy V2 local prefix.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicylocalprefixes
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2LocalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2LocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Local Prefix (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicylocalprefixes/query:
    post:
      description: Query the security policy v2 local prefix site associations of
        a tenant.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicylocalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePrefixAssociationQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePrefixAssociationQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Security Policy Local Prefixes (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete a security policy V2 local prefix by the tenant ID and the
        prefix ID.
      operationId: delete-sdwan-v2.0-api-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: ID of the security policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2LocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Local Prefix (v2.0)
      tags: [Security Policy]
    get:
      description: List a security policy V2 local prefix by the tenant ID and the
        prefix ID.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: ID of the security policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2LocalPrefix'}
          description: Successful Operation
      summary: Get Security Policy Local Prefix (v2.0)
      tags: [Security Policy]
    put:
      description: Update a security policy V2 local prefix by the tenant ID and the
        prefix ID.
      operationId: put-sdwan-v2.0-api-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: ID of the security policy local prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2LocalPrefixScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2LocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Local Prefix (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicysets:
    get:
      description: List all the security policy V2 sets by the tenant ID.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicysets
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2Set'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Sets (v2.0)
      tags: [Security Policy]
    post:
      description: Create a security policy V2 set.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicysets
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2SetScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2Set'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Set (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicysets/query:
    post:
      description: Query the security policy v2 sets of a tenant.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicysets-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2SetQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2SetQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Security Policy Sets (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicysets/{policy_set_id}:
    delete:
      description: Delete a security policy V2 set by the tenant ID and the policy
        set ID.
      operationId: delete-sdwan-v2.0-api-ngfwsecuritypolicysets-policy_set_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2Set'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Set (v2.0)
      tags: [Security Policy]
    get:
      description: List a security policy V2 set by the tenant ID and the policy set
        ID.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicysets-policy_set_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2Set'}
          description: Successful Operation
      summary: Get Security Policy Set (v2.0)
      tags: [Security Policy]
    put:
      description: Update a security policy V2 set by the tenant ID and the policy
        set ID.
      operationId: put-sdwan-v2.0-api-ngfwsecuritypolicysets-policy_set_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2SetScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2Set'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Set (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicysetstacks:
    get:
      description: List all the security policy V2 set stacks by the tenant ID.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicysetstacks
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2SetStack'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Set Stacks (v2.0)
      tags: [Security Policy]
    post:
      description: Create a security policy V2 set stack.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicysetstacks
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2SetStackScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2SetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Set Stack (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicysetstacks/query:
    post:
      description: Query the security policy v2 set stacks of a tenant.
      operationId: post-sdwan-v2.0-api-ngfwsecuritypolicysetstacks-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PolicySetStackQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2SetStackQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Security Policy Set Stacks (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/ngfwsecuritypolicysetstacks/{policyset_stack_id}:
    delete:
      description: Delete an existing security policy V2 set stack using the tenant
        ID and stack ID.
      operationId: delete-sdwan-v2.0-api-ngfwsecuritypolicysetstacks-policyset_stack_id
      parameters:
      - description: ID of the policy v2 set stack.
        in: path
        name: policyset_stack_id
        required: true
        schema: {description: Policyset Stack Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2SetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NGFWSETSTACK_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Set Stack
      tags: [Security Policy]
    get:
      description: List an existing security policy V2 set stack using the tenant
        ID and stack ID.
      operationId: get-sdwan-v2.0-api-ngfwsecuritypolicysetstacks-policyset_stack_id
      parameters:
      - description: ID of the policy v2 set stack.
        in: path
        name: policyset_stack_id
        required: true
        schema: {description: Policyset Stack Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2SetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NGFWSETSTACK_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Policy Set Stack
      tags: [Security Policy]
    put:
      description: Update an existing security policy V2 set stack using the tenant
        ID and stack ID.
      operationId: put-sdwan-v2.0-api-ngfwsecuritypolicysetstacks-policyset_stack_id
      parameters:
      - description: ID of the policy v2 set stack.
        in: path
        name: policyset_stack_id
        required: true
        schema: {description: Policyset Stack Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2SetStackScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2SetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NGFWSETSTACK_NOT_FOUND}
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Set Stack
      tags: [Security Policy]
  /sdwan/v2.0/api/prefixfilters/query:
    post:
      description: Query the security prefix filter for NB API.
      operationId: post-sdwan-v2.0-api-prefixfilters-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePrefixAssociationQuery'}
        description: Query details for security prefix filter
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteBasePrefixAssociationQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_INVALID_QUERY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Security Prefix Filter (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securitypolicyrules/query:
    post:
      description: Query the DB for limit number of LAN networks that match query
        parameters.
      operationId: post-sdwan-v2.0-api-securitypolicyrules-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyRuleQuery'}
        description: Query details for LAN Networks
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyRuleQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for LAN Networks (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securitypolicysets:
    get:
      description: List the tenant security policy sets.
      operationId: get-sdwan-v2.0-api-securitypolicysets
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicySet'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Sets (v2.0)
      tags: [Security Policy]
    post:
      description: Create a new tenant security policy set.
      operationId: post-sdwan-v2.0-api-securitypolicysets
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicySetPOSTScreen'}
        description: Details of the policy set to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: SECURITY_POLICYSET_INVALID_NAME}
                  - {code: SECURITY_POLICYSET_NAME_REQUIRED}
                  - {code: SECURITY_POLICYSET_INVALID_DESCRIPTION}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Set (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securitypolicysets/query:
    post:
      description: Query the DB for limit number of security policy sets that match
        query parameters.
      operationId: post-sdwan-v2.0-api-securitypolicysets-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicySetQuery'}
        description: Query details for SecurityPolicySet
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicySetQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for Security Policy Sets (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securitypolicysets/{policy_set_id}:
    delete:
      description: Delete a security policy set.
      operationId: delete-sdwan-v2.0-api-securitypolicysets-policy_set_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicySet'}
          description: Successful Operation
      summary: Delete Security Policy Set (v2.0)
      tags: [Security Policy]
    get:
      description: List a tenant security policy set.
      operationId: get-sdwan-v2.0-api-securitypolicysets-policy_set_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicySet'}
          description: Successful Operation
      summary: Get Security Policy Set (v2.0)
      tags: [Security Policy]
    put:
      description: Update a tenant security policy set.
      operationId: put-sdwan-v2.0-api-securitypolicysets-policy_set_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicySet'}
        description: Details of the policy set to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: SECURITY_POLICYSET_INVALID_POLICYSET}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: INVALID_JSON}
                  - {code: SECURITY_POLICYSET_INVALID_DEFAULT}
                  - {code: SECURITY_POLICYSET_INVALID_NAME}
                  - {code: SECURITY_POLICYSET_NAME_REQUIRED}
                  - {code: SECURITY_POLICYSET_INVALID_DESCRIPTION}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Set (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securitypolicysets/{policy_set_id}/firewallpolicyruleorder:
    post:
      description: Update a tenant security policy set.
      operationId: post-sdwan-v2.0-api-securitypolicysets-policy_set_id-firewallpolicyruleorder
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicySetRuleOrderScreen'}
        description: Details of the policy set to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON}
                  - {code: SECURITY_POLICYSET_INVALID_DEFAULT}
                  - {code: SECURITY_POLICYSET_INVALID_NAME}
                  - {code: SECURITY_POLICYSET_NAME_REQUIRED}
                  - {code: SECURITY_POLICYSET_INVALID_DESCRIPTION}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Set (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securitypolicysets/{policy_set_id}/securitypolicyrules:
    get:
      description: List the tenant security policy rules.
      operationId: get-sdwan-v2.0-api-securitypolicysets-policy_set_id-securitypolicyrules
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyRule'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITY_POLICY_RULE_SOURCE_ZONE_REQUIRED}
                  - {code: SECURITY_POLICY_RULE_APPLICATION_NAME_REQUIRED}
                  - {code: SECURITY_POLICY_RULE_DESCRIPTION_INVALID}
                  - {code: SECURITY_POLICY_RULE_NAME_INVALID}
                  - {code: SECURITY_POLICYRULE_NAME_REQUIRED}
                  - {code: SECURITY_POLICY_RULE_DESTINATION_ZONE_REQUIRED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Policy Rules (v2.0)
      tags: [Security Policy]
    post:
      description: Create a new tenant security policy rule.
      operationId: post-sdwan-v2.0-api-securitypolicysets-policy_set_id-securitypolicyrules
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyRuleScreen'}
        description: Details of the policy rule to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyRule'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITY_POLICYRULE_DUPLICATE_POLICYRULE}
                  - {code: SECURITY_POLICYRULE_NAME_REQD}
                  - {code: SECURITY_POLICYRULE_NAME_EXCEEDS_LIMIT}
                  - {code: SECURITY_POLICYRULE_DESCRIPTION_INVALID}
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: SECURITY_POLICYRULE_INVALID_POLICYSET}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: SECURITY_POLICYRULE_INVALID_POLICYRULE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Rule (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securitypolicysets/{policy_set_id}/securitypolicyrules/{policy_rule_id}:
    delete:
      description: Delete a security policy rule.
      operationId: delete-sdwan-v2.0-api-securitypolicysets-policy_set_id-securitypolicyrules-policy_rule_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: ID of the security policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyRule'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITY_POLICYRULE_INVALID_OPERATION}
                  - {code: SECURITY_POLICYRULE_INVALID_POLICYSET}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Rule (v2.0)
      tags: [Security Policy]
    get:
      description: List a tenant security policy rule.
      operationId: get-sdwan-v2.0-api-securitypolicysets-policy_set_id-securitypolicyrules-policy_rule_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: ID of the security policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyRule'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITY_POLICY_RULE_SOURCE_ZONE_REQUIRED}
                  - {code: SECURITY_POLICY_RULE_APPLICATION_NAME_REQUIRED}
                  - {code: SECURITY_POLICY_RULE_DESCRIPTION_INVALID}
                  - {code: SECURITY_POLICY_RULE_NAME_INVALID}
                  - {code: SECURITY_POLICYRULE_NAME_REQUIRED}
                  - {code: SECURITY_POLICY_RULE_DESTINATION_ZONE_REQUIRED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Policy Rule (v2.0)
      tags: [Security Policy]
    put:
      description: Update a tenant security policy rule.
      operationId: put-sdwan-v2.0-api-securitypolicysets-policy_set_id-securitypolicyrules-policy_rule_id
      parameters:
      - description: ID of the security policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: ID of the security policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyRuleScreen'}
        description: Details of the policy rule to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyRule'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITY_POLICYRULE_DUPLICATE_POLICYRULE}
                  - {code: SECURITY_POLICYRULE_NAME_REQD}
                  - {code: SECURITY_POLICYRULE_NAME_EXCEEDS_LIMIT}
                  - {code: SECURITY_POLICYRULE_DESCRIPTION_INVALID}
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: SECURITY_POLICYRULE_INVALID_POLICYSET}
                  - {code: SECURITY_POLICYRULE_INVALID_POLICYRULE}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: SECURITY_POLICYRULE_INVALID_DEFAULT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Rule (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securityzones:
    get:
      description: List all the security zones.
      operationId: get-sdwan-v2.0-api-securityzones
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZone'}
          description: Successful Operation
      summary: Get Security Zones (v2.0)
      tags: [Security Policy]
    post:
      description: Create a new security zone.
      operationId: post-sdwan-v2.0-api-securityzones
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZone'}
        description: Details of the security zone to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_DUPLICATE_ZONE}
                  - {code: SECURITYZONE_NAME_INVALID}
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: SECURITYZONE_NAME_REQUIRED}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Zone (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securityzones/query:
    post:
      description: Query the DB for limit number of security zones that match query
        parameters.
      operationId: post-sdwan-v2.0-api-securityzones-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZoneQuery'}
        description: Query details for security zones
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityZoneQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query DB for Security Zones (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/securityzones/{zone_id}:
    delete:
      description: Delete an existing security zone.
      operationId: delete-sdwan-v2.0-api-securityzones-zone_id
      parameters:
      - description: ID of the security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZone'}
          description: Successful Operation
      summary: Delete Security Zone (v2.0)
      tags: [Security Policy]
    get:
      description: List a security zone.
      operationId: get-sdwan-v2.0-api-securityzones-zone_id
      parameters:
      - description: ID of the security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Zone (v2.0)
      tags: [Security Policy]
    put:
      description: Update an existing the security zone.
      operationId: put-sdwan-v2.0-api-securityzones-zone_id
      parameters:
      - description: ID of the security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZone'}
        description: Details of the security zone to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_DUPLICATE_ZONE}
                  - {code: SECURITYZONE_NAME_INVALID}
                  - {code: SECURITYZONE_DESCRIPTION_INVALID}
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: SECURITYZONE_NAME_REQUIRED}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Zone (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/elements/{element_id}/securityzones:
    get:
      description: List all the element security zones.
      operationId: get-sdwan-v2.0-api-sites-site_id-elements-element_id-securityzones
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/ElementSecurityZone'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Element Security Zones (v2.0)
      tags: [Security Policy]
    post:
      description: Create an association between the element and the security zone.
      operationId: post-sdwan-v2.0-api-sites-site_id-elements-element_id-securityzones
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ElementSecurityZoneScreen'}
        description: Details of the element security zone to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ElementSecurityZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: ELEMENT_SECURITYZONE_ELEMENT_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_ZONE_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_INVALID_ELEMENT_ROLE}
                  - {code: ELEMENT_SECURITYZONE_DUPLICATE_INTERFACE_IDS}
                  - {code: ELEMENT_SECURITYZONE_INTERFACE_ID_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_DUPLICATE_LANNETWORK_IDS}
                  - {code: ELEMENT_SECURITYZONE_LANNETWORK_ID_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_DUPLICATE_WANOVERLAY_IDS}
                  - {code: ELEMENT_SECURITYZONE_WANOVERLAY_ID_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_INVALID_INTERFACE_IDS}
                  - {code: ELEMENT_SECURITYZONE_ZONEID_REQUIRED}
                  - {code: ELEMENT_SECURITYZONE_INVALID_LANNETWORK_IDS}
                  - {code: ELEMENT_SECURITYZONE_INVALID_WANINTERFACE_IDS}
                  - {code: ELEMENT_SECURITYZONE_INVALID_WANOVERLAY_IDS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Association between Element and Security Zone (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/elements/{element_id}/securityzones/{security_zone_id}:
    delete:
      description: Delete an existing security zone.
      operationId: delete-sdwan-v2.0-api-sites-site_id-elements-element_id-securityzones-security_zone_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      - description: The ID of the security zone.
        in: path
        name: security_zone_id
        required: true
        schema: {description: Security Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ElementSecurityZone'}
          description: Successful Operation
      summary: Delete Element Security Zone (v2.0)
      tags: [Security Policy]
    get:
      description: List a security zone.
      operationId: get-sdwan-v2.0-api-sites-site_id-elements-element_id-securityzones-security_zone_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      - description: The ID of the security zone.
        in: path
        name: security_zone_id
        required: true
        schema: {description: Security Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ElementSecurityZone'}
          description: Successful Operation
      summary: Get Element Security Zone (v2.0)
      tags: [Security Policy]
    put:
      description: Update an existing security zone.
      operationId: put-sdwan-v2.0-api-sites-site_id-elements-element_id-securityzones-security_zone_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the element.
        in: path
        name: element_id
        required: true
        schema: {description: Element Id, type: string}
      - description: The ID of the security zone.
        in: path
        name: security_zone_id
        required: true
        schema: {description: Security Zone Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/ElementSecurityZoneScreen'}
        description: Details of the element security zone to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/ElementSecurityZone'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: ELEMENT_SECURITYZONE_ELEMENT_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_ZONE_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_INVALID_ELEMENT_ROLE}
                  - {code: ELEMENT_SECURITYZONE_DUPLICATE_INTERFACE_IDS}
                  - {code: ELEMENT_SECURITYZONE_INTERFACE_ID_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_DUPLICATE_LANNETWORK_IDS}
                  - {code: ELEMENT_SECURITYZONE_LANNETWORK_ID_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_DUPLICATE_WANOVERLAY_IDS}
                  - {code: ELEMENT_SECURITYZONE_WANOVERLAY_ID_NOT_FOUND}
                  - {code: ELEMENT_SECURITYZONE_INVALID_INTERFACE_IDS}
                  - {code: ELEMENT_SECURITYZONE_ZONEID_REQUIRED}
                  - {code: ELEMENT_SECURITYZONE_INVALID_LANNETWORK_IDS}
                  - {code: ELEMENT_SECURITYZONE_INVALID_WANINTERFACE_IDS}
                  - {code: ELEMENT_SECURITYZONE_INVALID_WANOVERLAY_IDS}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Element Security Zone (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/ngfwsecuritypolicylocalprefixes:
    get:
      description: List all the security policy v2 local prefix site associations
        for a site.
      operationId: get-sdwan-v2.0-api-sites-site_id-ngfwsecuritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteSecurityPolicyV2PrefixAssociation'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Local Prefix Site Association (v2.0)
      tags: [Security Policy]
    post:
      description: Create a security policy v2 local prefix site association.
      operationId: post-sdwan-v2.0-api-sites-site_id-ngfwsecuritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2PrefixAssociationScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Local Prefix Site Association (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/ngfwsecuritypolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete an existing security policy v2 local prefix site association.
      operationId: delete-sdwan-v2.0-api-sites-site_id-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The Prefix ID that associates with a site.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Local Prefix Site Association (v2.0)
      tags: [Security Policy]
    get:
      description: List an existing security policy v2 local prefix assocation by
        its ID.
      operationId: get-sdwan-v2.0-api-sites-site_id-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The Prefix ID that associates with a site.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Policy Local Prefix Site Association (v2.0)
      tags: [Security Policy]
    put:
      description: Update an existing security policy v2 local prefix site association.
      operationId: put-sdwan-v2.0-api-sites-site_id-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The Prefix ID that associates with a site.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2PrefixAssociationScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Local Prefix Site Association (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/prefixfilters:
    get:
      description: List all the site security filters.
      operationId: get-sdwan-v2.0-api-sites-site_id-prefixfilters
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PrefixFilterAssociation'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Site Security Filters (v2.0)
      tags: [Security Policy]
    post:
      description: Create an association between site and security prefix filter.
      operationId: post-sdwan-v2.0-api-sites-site_id-prefixfilters
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PrefixFilterAssociation'}
        description: Details of the site security prefix filter to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrefixFilterAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Association between Site and Security Prefix Filter (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/prefixfilters/query:
    post:
      description: Query the security prefix filter for the NB API.
      operationId: post-sdwan-v2.0-api-sites-site_id-prefixfilters-query
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PrefixFilterQueryScreen'}
        description: Query details for security prefix filter
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PrefixFilterQueryScreen'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_INVALID_QUERY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Security Prefix Filter (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/prefixfilters/{filter_id}:
    delete:
      description: Delete an existing security prefix filter.
      operationId: delete-sdwan-v2.0-api-sites-site_id-prefixfilters-filter_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the prefix filter association.
        in: path
        name: filter_id
        required: true
        schema: {description: Filter Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrefixFilterAssociation'}
          description: Successful Operation
      summary: Delete Security Prefix Filter (v2.0)
      tags: [Security Policy]
    get:
      description: List a site security prefix filter.
      operationId: get-sdwan-v2.0-api-sites-site_id-prefixfilters-filter_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the prefix filter association.
        in: path
        name: filter_id
        required: true
        schema: {description: Filter Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrefixFilterAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Site Security Prefix Filter (v2.0)
      tags: [Security Policy]
    put:
      description: Update an existing security prefix filter.
      operationId: put-sdwan-v2.0-api-sites-site_id-prefixfilters-filter_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the prefix filter association.
        in: path
        name: filter_id
        required: true
        schema: {description: Filter Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PrefixFilterAssociation'}
        description: Details of the site security prefix filter to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PrefixFilterAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_INVALID_CONTEXT}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: SECURITYZONE_INVALID_FILTER}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Prefix Filter (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/sitesecurityzones:
    get:
      description: List all the site security zones.
      operationId: get-sdwan-v2.0-api-sites-site_id-sitesecurityzones
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Site Security Zones (v2.0)
      tags: [Security Policy]
    post:
      description: Create an association between site and security zone.
      operationId: post-sdwan-v2.0-api-sites-site_id-sitesecurityzones
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
        description: Details of the site security zone to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: SECURITYZONE_INVALID_ZONE}
                  - {code: SECURITYZONE_INVALID_CONTEXT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Association between Site and Security Zone (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/sitesecurityzones/query:
    post:
      description: Query the security zones for NB API.
      operationId: post-sdwan-v2.0-api-sites-site_id-sitesecurityzones-query
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZoneQueryScreen'}
        description: Query details for Priority Policy set stacks
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_INVALID_QUERY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Security Zone (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sites/{site_id}/sitesecurityzones/{zone_id}:
    delete:
      description: Delete an existing security zone.
      operationId: delete-sdwan-v2.0-api-sites-site_id-sitesecurityzones-zone_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the site security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
          description: Successful Operation
      summary: Delete Security Zone (v2.0)
      tags: [Security Policy]
    get:
      description: Get a security zone.
      operationId: get-sdwan-v2.0-api-sites-site_id-sitesecurityzones-zone_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the site security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Zone (v2.0)
      tags: [Security Policy]
    put:
      description: Update an existing security zone.
      operationId: put-sdwan-v2.0-api-sites-site_id-sitesecurityzones-zone_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The ID of the site security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
        description: Details of the site security zone to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneNetworkAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: SECURITYZONE_INVALID_ZONE}
                  - {code: SECURITYZONE_INVALID_CONTEXT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Zone (v2.0)
      tags: [Security Policy]
  /sdwan/v2.0/api/sitesecurityzones/query:
    post:
      description: Query the security zone for an NB API.
      operationId: post-sdwan-v2.0-api-sitesecurityzones-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZoneQueryScreen'}
        description: Query details for Priority Policy set stacks
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteSecurityZoneNetworkAssociationQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_INVALID_QUERY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Security Zone for NB API (v2.0)
      tags: [Security Policy]
  /sdwan/v2.1/api/ngfwsecuritypolicyglobalprefixes:
    get:
      description: List all the security policy V2 global prefixes for a tenant.
      operationId: get-sdwan-v2.1-api-ngfwsecuritypolicyglobalprefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixV2N1'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Global Prefixes (v2.1)
      tags: [Security Policy]
    post:
      description: Create a security policy V2 global prefix.
      operationId: post-sdwan-v2.1-api-ngfwsecuritypolicyglobalprefixes
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixScreenV2N1'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_PREFIX}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Global Prefix (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/ngfwsecuritypolicyglobalprefixes/query:
    post:
      description: Query the security policy V2 global prefixes of a tenant.
      operationId: post-sdwan-v2.1-api-ngfwsecuritypolicyglobalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GlobalPrefixQueryFilterV2N1'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/GlobalPrefixQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Security Policy Global Prefixes (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/ngfwsecuritypolicyglobalprefixes/{prefix_id}:
    delete:
      description: Delete a security policy V2 global prefix by the tenant ID and
        the prefix ID.
      operationId: delete-sdwan-v2.1-api-ngfwsecuritypolicyglobalprefixes-prefix_id
      parameters:
      - description: ID of the security policy global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_USED_BY_RULE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Global Prefix (v2.1)
      tags: [Security Policy]
    get:
      description: List a security policy V2 global prefix by the tenant ID and the
        prefix ID.
      operationId: get-sdwan-v2.1-api-ngfwsecuritypolicyglobalprefixes-prefix_id
      parameters:
      - description: ID of the security policy global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
      summary: Get Security Policy Global Prefix (v2.1)
      tags: [Security Policy]
    put:
      description: Update a security policy V2 global prefix by the tenant ID and
        the prefix ID.
      operationId: put-sdwan-v2.1-api-ngfwsecuritypolicyglobalprefixes-prefix_id
      parameters:
      - description: ID of the security policy global prefix.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixScreenV2N1'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_PREFIX}
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_TAG}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Global Prefix (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/ngfwsecuritypolicylocalprefixes/query:
    post:
      description: Query the security policy v2 local prefix site associations of
        a tenant.
      operationId: post-sdwan-v2.1-api-ngfwsecuritypolicylocalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePrefixAssociationQuery'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePrefixAssociationQueryFilterV2N1'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Security Policy Local Prefixes (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/securityzones:
    get:
      description: List all the security zones.
      operationId: get-sdwan-v2.1-api-securityzones
      summary: Get Security Zones (v2.1)
      tags: [Security Policy]
    post:
      description: Create a new security zone.
      operationId: post-sdwan-v2.1-api-securityzones
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZoneV2N1'}
        description: Details of the security zone to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_DUPLICATE_ZONE}
                  - {code: SECURITYZONE_NAME_INVALID}
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: SECURITYZONE_NAME_REQUIRED}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Zone (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/securityzones/query:
    post:
      description: Query the DB for limit number of security zones that match query
        parameters.
      operationId: post-sdwan-v2.1-api-securityzones-query
      summary: Query DB for Security Zones (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/securityzones/{zone_id}:
    delete:
      description: Delete an existing security zone.
      operationId: delete-sdwan-v2.1-api-securityzones-zone_id
      parameters:
      - description: ID of the security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneV2N1'}
          description: Successful Operation
      summary: Delete Security Zone (v2.1)
      tags: [Security Policy]
    get:
      description: List a security zone.
      operationId: get-sdwan-v2.1-api-securityzones-zone_id
      parameters:
      - description: ID of the security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Zone (v2.1)
      tags: [Security Policy]
    put:
      description: Update an existing the security zone.
      operationId: put-sdwan-v2.1-api-securityzones-zone_id
      parameters:
      - description: ID of the security zone.
        in: path
        name: zone_id
        required: true
        schema: {description: Zone Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityZoneV2N1'}
        description: Details of the security zone to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityZoneV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SECURITYZONE_DUPLICATE_ZONE}
                  - {code: SECURITYZONE_NAME_INVALID}
                  - {code: SECURITYZONE_DESCRIPTION_INVALID}
                  - {code: JSON_SCHEMA_MISMATCH}
                  - {code: SECURITYZONE_NAME_REQUIRED}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Zone (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/sites/{site_id}/ngfwsecuritypolicylocalprefixes:
    get:
      description: List all the security policy v2 local prefix site associations
        for a site.
      operationId: get-sdwan-v2.1-api-sites-site_id-ngfwsecuritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SiteSecurityPolicyV2N1PrefixAssociation'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Local Prefix Site Association (v2.1)
      tags: [Security Policy]
    post:
      description: Create a security policy v2 local prefix site association.
      operationId: post-sdwan-v2.1-api-sites-site_id-ngfwsecuritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2N1PrefixAssociationScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2N1PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Local Prefix Site Association (v2.1)
      tags: [Security Policy]
  /sdwan/v2.1/api/sites/{site_id}/ngfwsecuritypolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete an existing security policy v2 local prefix site association.
      operationId: delete-sdwan-v2.1-api-sites-site_id-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The Prefix ID that associates with a site.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2N1PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Local Prefix Site Association (v2.1)
      tags: [Security Policy]
    get:
      description: List an existing security policy v2 local prefix assocation by
        its ID.
      operationId: get-sdwan-v2.1-api-sites-site_id-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The Prefix ID that associates with a site.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2N1PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Policy Local Prefix Site Association (v2.1)
      tags: [Security Policy]
    put:
      description: Update an existing security policy v2 local prefix site association.
      operationId: put-sdwan-v2.1-api-sites-site_id-ngfwsecuritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The Prefix ID that associates with a site.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2N1PrefixAssociationScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SiteSecurityPolicyV2N1PrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Local Prefix Site Association (v2.1)
      tags: [Security Policy]
  /sdwan/v2.2/api/ngfwsecuritypolicyrules/query:
    post:
      description: Query the security policy v2 rules of a tenant.
      operationId: post-sdwan-v2.2-api-ngfwsecuritypolicyrules-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2N2RuleQueryFilter'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2N1RuleQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query Security Policy Rules (v2.2)
      tags: [Security Policy]
  /sdwan/v2.2/api/ngfwsecuritypolicysets/{policy_set_id}/ngfwsecuritypolicyrules:
    get:
      description: List all the security policy V2 rules under a policy set.
      operationId: get-sdwan-v2.2-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules
      parameters:
      - description: ID of the security policy v2 set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N2'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Security Policy Rules under Policy Set (v2.2)
      tags: [Security Policy]
    post:
      description: Create a security policy V2 rule under a policy set.
      operationId: post-sdwan-v2.2-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules
      parameters:
      - description: ID of the security policy v2 set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2N2RuleScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Security Policy Rules under Policy Set (v2.2)
      tags: [Security Policy]
  /sdwan/v2.2/api/ngfwsecuritypolicysets/{policy_set_id}/ngfwsecuritypolicyrules/{policy_rule_id}:
    delete:
      description: Delete an existing security policy V2 rule under a policy set.
      operationId: delete-sdwan-v2.2-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules-policy_rule_id
      parameters:
      - description: ID of the security policy v2 set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: ID of the security policy v2 rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYRULE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Security Policy Rules under Policy Set (v2.2)
      tags: [Security Policy]
    get:
      description: List an existing security policy V2 rule under a policy set.
      operationId: get-sdwan-v2.2-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules-policy_rule_id
      parameters:
      - description: ID of the security policy v2 set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: ID of the security policy v2 rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYRULE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Security Policy Rules under Policy Set (v2.2)
      tags: [Security Policy]
    put:
      description: Update an existing security policy V2 rule under a policy set.
      operationId: put-sdwan-v2.2-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules-policy_rule_id
      parameters:
      - description: ID of the security policy v2 set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: ID of the security policy v2 rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2N2RuleScreen'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Security Policy Rules under Policy Set (v2.2)
      tags: [Security Policy]
  /sdwan/v2.3/api/ngfwsecuritypolicyrules/query:
    post:
      description: Query security policy v2 rules of a tenant
      operationId: post-sdwan-v2.3-api-ngfwsecuritypolicyrules-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleQueryFilterV2N3'}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2RuleQueryFilterV2N3'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Query security policy v2 rules of a tenant (v2.3)
      tags: [Security Policy]
  /sdwan/v2.3/api/ngfwsecuritypolicysets/{policy_set_id}/ngfwsecuritypolicyrules:
    get:
      description: Get all Security Policy V2 Rules under a policy set
      operationId: get-sdwan-v2.3-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules
      parameters:
      - description: Security Policy V2 Set ID
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N3'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get all Security Policy V2 Rules under a policy set (v2.3)
      tags: [Security Policy]
    post:
      description: Create a Security Policy V2 Rule under a policy set
      operationId: post-sdwan-v2.3-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules
      parameters:
      - description: Security Policy V2 Set ID
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleScreenV2N3'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N3'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create a Security Policy V2 Rule under a policy set (v2.3)
      tags: [Security Policy]
  /sdwan/v2.3/api/ngfwsecuritypolicysets/{policy_set_id}/ngfwsecuritypolicyrules/{policy_rule_id}:
    delete:
      description: Delete an existing Security Policy V2 Rule under a policy set
      operationId: delete-sdwan-v2.3-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules-policy_rule_id
      parameters:
      - description: Security Policy V2 Set ID
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: Security Policy V2 Rule ID
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N3'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYRULE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete an existing Security Policy V2 Rule under a policy set (v2.3)
      tags: [Security Policy]
    get:
      description: Get an existing Security Policy V2 Rule under a policy set
      operationId: get-sdwan-v2.3-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules-policy_rule_id
      parameters:
      - description: Security Policy V2 Set ID
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: Security Policy V2 Rule ID
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N3'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYRULE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get an existing Security Policy V2 Rule under a policy set (v2.3)
      tags: [Security Policy]
    put:
      description: Update an existing Security Policy V2 Rule under a policy set
      operationId: put-sdwan-v2.3-api-ngfwsecuritypolicysets-policy_set_id-ngfwsecuritypolicyrules-policy_rule_id
      parameters:
      - description: Security Policy V2 Set ID
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: Security Policy V2 Rule ID
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleScreenV2N3'}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SecurityPolicyV2RuleV2N3'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_INPUT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update an existing Security Policy V2 Rule under a policy set (v2.3)
      tags: [Security Policy]
servers:
- {url: 'https://api.sase.paloaltonetworks.com'}
tags:
- {name: Security Policy}
