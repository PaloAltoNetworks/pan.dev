components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items: {$ref: '#/components/schemas/AggregateData'}
          type: array
        field: {description: 'The field value.

            ', type: string}
        name: {description: 'The name of the query response.

            ', type: string}
        operator:
          description: 'The operator values.

            '
          enum: [max, count, min, avg, sum]
          type: string
        statistic: {description: 'The aggregate statistics.

            ', type: string}
        unit: {description: 'The unit.

            ', type: string}
    AggregateData:
      properties:
        value: {description: 'Value of the aggregate data.

            ', type: number}
        view: {description: 'View the aggregate date.

            ', type: object}
    BandwidthAllocationSchemeV2:
      properties:
        bandwidth_range: {$ref: '#/components/schemas/BandwidthRange', description: 'The
            defined bandwidth range.

            '}
        business_priorities:
          description: 'The defined business priorities.

            '
          items: {$ref: '#/components/schemas/BusinessPriorityV2'}
          type: array
    BandwidthRange:
      properties:
        high: {description: 'The high range of the bandwidth allocated.

            ', format: double, type: number}
        low: {description: 'The low range of the bandwidth allocated.

            ', format: double, type: number}
    BandwidthSplit:
      properties:
        bulk: {description: Bulk, format: double, type: number}
        rt_audio: {description: Rt Audio, format: double, type: number}
        rt_video: {description: Rt Video, format: double, type: number}
        transactional: {description: Transactional, format: double, type: number}
    BusinessPriorityNameMapper:
      properties:
        priority_name: {description: 'The business priority name.

            ', type: string}
        priority_num: {description: 'The business priority number.

            ', format: int32, type: integer}
    BusinessPriorityV2:
      properties:
        bandwidth_allocation: {description: 'The bandwidth allocated.

            ', format: double, type: number}
        bandwidth_split_per_type: {$ref: '#/components/schemas/BandwidthSplit', description: 'The
            bandwidth allocated split by type.

            '}
        priority_number: {description: 'The priority number for a type.

            ', format: int32, type: integer}
    DSCP:
      properties:
        value: {description: 'The DSCP value.

            ', format: int32, type: integer}
    DefaultRuleDSCPMapping:
      properties:
        dscp:
          description: 'The default DSCP rule mapping.

            '
          items: {description: Dscp, type: integer}
          type: array
        priority_number: {description: 'The priority number of the rule.

            ', format: int32, type: integer}
        transfer_type:
          description: 'The transfer type.

            '
          enum: [RT_AUDIO, RT_VIDEO, TRANSACTIONAL, BULK]
          type: string
    Error:
      properties:
        _error:
          items: {$ref: '#/components/schemas/ErrorResponse'}
          type: array
    ErrorResponse:
      properties:
        code: {description: 'The error code.

            ', type: string}
        message: {description: 'The error message.

            ', type: string}
    GlobalPrefixQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of global prefixes that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number global prefixes deleted for a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the global prefixs for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the global prefixes by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    GlobalPrefixQueryFilter:
      properties:
        description: {description: 'The description of the security policy global
            prefix. Max size = 256.

            '}
        id: {description: 'The ID of the security policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the security policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the security policy global prefix. Max size
            = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the security
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    GlobalPrefixQueryFilterV2N1:
      properties:
        description: {description: 'The description of the priority policy global
            prefix.

            '}
        id: {description: 'The ID of the priority policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the priority policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 addresses of the priority policy global prefix.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        name: {description: 'The name of the priority policy global prefix.

            ', type: string}
        tags:
          description: "A information field that can be added to identify the priority\
            \ policy global prefix. Maximum 10 unique tags of length 1024 each are\
            \ allowed.    \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    ListQueryResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        deleted_count: {description: 'The deleted number.

            ', format: int64, type: integer}
        deleted_ids:
          description: 'The deleted IDs.

            '
          items: {type: string}
          type: array
        description: {description: 'Description of the query. Max size = 256.

            '}
        id: {description: 'The ID.

            ', type: string}
        next_query: {description: 'Details of the next query.

            ', type: object}
        total_count: {description: 'Total number.

            ', format: int64, type: integer}
      type: object
    ListResponse:
      properties:
        count: {description: 'The actual count.

            ', format: int32, type: integer}
        id: {description: 'The ID.

            ', type: string}
      type: object
    PolicySetStackQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of policy set stacks that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of policy set stacks deleted for a time
            frame.

            ', type: boolean}
        group_by:
          description: 'Group the policy set stacks for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "List the policy set stacks by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    PriorityPolicyGlobalPrefix:
      properties:
        description: {description: 'The description of the priority policy global
            prefix.

            '}
        id: {description: 'The ID of the priority policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the priority policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        name: {description: 'The name of the priority policy global prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the priority
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    PriorityPolicyGlobalPrefixV2N1:
      properties:
        description: {description: 'The description of the priority policy global
            prefix.

            '}
        id: {description: 'The ID of the priority policy global prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the IPv4 addresses of the priority policy global prefix.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 addresses of the priority policy global prefix.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        name: {description: 'The name of the priority policy global prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the priority
            policy global prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [name]
    PriorityPolicyLocalPrefix:
      properties:
        description: {description: 'The description of the priority policy local prefix.
            Max Size = 256.

            '}
        id: {description: 'The ID of the priority policy local prefix.

            ', type: string}
        name: {description: 'The name of the priority policy local prefix. Max Size
            = 128.

            ', type: string}
        tags:
          description: "A information field that can be added to identify the priority\
            \ policy local prefix. Maximum 10 unique tags of length 1024 each are\
            \ allowed. \n"
          items: {description: Tags, type: string}
          type: array
      required: [name]
    PriorityPolicyRuleQueryFilterV2N1:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied priority policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the priority policy rule. Max
            size = 256.

            '}
        destination_prefixes_id: {description: 'The destination prefix ID.

            ', type: string}
        dscp: {$ref: '#/components/schemas/DSCP', description: 'The DSCP value - When
            policy rules with marked DSCP bits are applied to a site, the ION branch
            device will honor the bits in the first packet on an unknown application
            flow, and queue the flow in the specified priority class.

            '}
        enabled: {description: "The priority policy rules based on rules that are\
            \ enabled.    \n", type: boolean}
        id: {description: 'The priority policy rule ID.

            ', type: string}
        name: {description: 'The name of the priority policy rule. Max size = 128.

            ', type: string}
        network_context_id: {description: "The  network context applied to the priority\
            \ policy rule. Network context segments network traffic for the purpose\
            \ of applying different QoS policy rules for the same application. \n",
          type: string}
        order_number: {description: "The order number. The default order number for\
            \ a policy rule is 1024. \n", type: integer}
        policyset_id: {description: "The policy set ID. \n", type: string}
        priority_number: {description: 'The priority number. Priority determines the
            relative priority of network resources assigned to each application.

            ', format: int32, type: integer}
        source_prefixes_id: {description: 'The source prefix ID. Source prefix filters
            are often added as exceptions.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the priority
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: 'The
            details of the user or group.

            '}
      required: [priority_number, name]
    PriorityPolicyRuleQueryFilterV2N2:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied priority policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the priority policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefixes_id: {description: 'The destination prefix ID.

            ', type: string}
        dscp: {$ref: '#/components/schemas/DSCP', description: 'The DSCP value - When
            policy rules with marked DSCP bits are applied to a site, the ION branch
            device will honor the bits in the first packet on an unknown application
            flow, and queue the flow in the specified priority class.

            '}
        enabled: {description: "The priority policy rules based on rules that are\
            \ enabled.    \n", type: boolean}
        id: {description: 'The priority policy rule ID.

            ', type: string}
        name: {description: 'The name of the priority policy rule. Max size = 128.

            ', type: string}
        network_context_id: {description: "The  network context applied to the priority\
            \ policy rule. Network context segments network traffic for the purpose\
            \ of applying different QoS policy rules for the same application. \n",
          type: string}
        order_number: {description: "The order number. The default order number for\
            \ a policy rule is 1024. \n", type: integer}
        policyset_id: {description: "The policy set ID. \n", type: string}
        priority_number: {description: 'The priority number. Priority determines the
            relative priority of network resources assigned to each application.

            ', format: int32, type: integer}
        source_prefixes_id: {description: 'The source prefix ID. Source prefix filters
            are often added as exceptions.

            ', type: string}
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the priority
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: 'The
            details of the user or group.

            '}
      required: [priority_number, name]
    PriorityPolicyRuleQueryV2N1:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of priority policy rules that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of priority policy rules deleted for
            a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the priority policy rules for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the priority policy rules by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    PriorityPolicyRuleQueryV2N2:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of priority policy rules that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of priority policy rules deleted for
            a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the priority policy rules for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the priority policy rules by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {description: 'Query using query parameters.

            ', type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    PriorityPolicyRuleV2N1:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied priority policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the priority policy rule. Max
            size = 256.

            '}
        destination_prefixes_id: {description: 'The destination prefix ID.

            ', type: string}
        dscp: {$ref: '#/components/schemas/DSCP', description: 'The DSCP value - When
            policy rules with marked DSCP bits are applied to a site, the ION branch
            device will honor the bits in the first packet on an unknown application
            flow, and queue the flow in the specified priority class.

            '}
        enabled: {description: "The priority policy rules based on rules that are\
            \ enabled.    \n", type: boolean}
        id: {description: 'The priority policy rule ID.

            ', type: string}
        name: {description: 'The name of the priority policy rule. Max size = 128.

            ', type: string}
        network_context_id: {description: "The  network context applied to the priority\
            \ policy rule. Network context segments network traffic for the purpose\
            \ of applying different QoS policy rules for the same application. \n",
          type: string}
        order_number: {description: "The order number. The default order number for\
            \ a policy rule is 1024. \n", type: integer}
        priority_number: {description: 'The priority number. Priority determines the
            relative priority of network resources assigned to each application.

            ', format: int32, type: integer}
        source_prefixes_id: {description: 'The source prefix ID. Source prefix filters
            are often added as exceptions.

            ', type: string}
        tags:
          description: "A information field that can be added to identify the priority\
            \ policy rule. Maximum 10 unique tags of length 1024 each are allowed.\
            \ \n"
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: "The\
            \ details of the user or group. \n"}
      required: [priority_number, name]
    PriorityPolicyRuleV2N2:
      properties:
        app_def_ids:
          description: 'The app definition IDs that have applied priority policy rules.
            The number of applications for a policy rule is limited to 256.

            '
          items: {description: App Def Ids, type: string}
          type: array
        description: {description: 'The description of the priority policy rule. Max
            size = 256.

            '}
        dest_device_ids:
          description: 'The destination device ID. Max number = 10.

            '
          items: {description: Dest Device Ids, type: string}
          type: array
        destination_prefixes_id: {description: 'The destination prefix ID.

            ', type: string}
        dscp: {$ref: '#/components/schemas/DSCP', description: 'The DSCP value - When
            policy rules with marked DSCP bits are applied to a site, the ION branch
            device will honor the bits in the first packet on an unknown application
            flow, and queue the flow in the specified priority class.

            '}
        enabled: {description: "The priority policy rules based on rules that are\
            \ enabled.    \n", type: boolean}
        id: {description: 'The priority policy rule ID.

            ', type: string}
        name: {description: 'The name of the priority policy rule. Max size = 128.

            ', type: string}
        network_context_id: {description: "The  network context applied to the priority\
            \ policy rule. Network context segments network traffic for the purpose\
            \ of applying different QoS policy rules for the same application. \n",
          type: string}
        order_number: {description: "The order number. The default order number for\
            \ a policy rule is 1024. \n", type: integer}
        priority_number: {description: 'The priority number. Priority determines the
            relative priority of network resources assigned to each application.

            ', format: int32, type: integer}
        source_prefixes_id: {description: 'The source prefix ID. Source prefix filters
            are often added as exceptions.

            ', type: string}
        src_device_ids:
          description: 'The ID of the src device.

            '
          items: {description: Src Device Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the priority
            policy rule. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        user_or_group: {$ref: '#/components/schemas/UserGroup', description: 'The
            details of the user or group.

            '}
      required: [priority_number, name]
    PriorityPolicySet:
      properties:
        bandwidth_allocation_schemes:
          description: 'The bandwidth allocation schemes configured for a given policy
            set. Upto four bandwidth allocation schemes can be configured.

            '
          items: {$ref: '#/components/schemas/BandwidthAllocationSchemeV2'}
          type: array
        business_priority_names:
          description: 'The business priority names. Max list size = 4.

            '
          items: {$ref: '#/components/schemas/BusinessPriorityNameMapper'}
          type: array
        clone_from: {description: 'The existing policy from Stacked Policies for cloning
            the policy set.

            ', type: string}
        default_rule_dscp_mappings:
          description: "The default rule of the DSCP mappings. \n"
          items: {$ref: '#/components/schemas/DefaultRuleDSCPMapping'}
          type: array
        defaultrule_policyset: {description: "The default rule of the policy set.\
            \   \n", type: boolean}
        description: {description: 'The description of the policy set. Max size =
            256.

            '}
        id: {description: 'The priority policy set ID.

            ', type: string}
        name: {description: 'The name of the policy set. Max size = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the policy
            set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        template: {description: 'The template used for cloning the policy rule. The
            template option creates a set of rules from a predefined template can
            be customized after creation.

            ', type: boolean}
      required: [business_priority_names, bandwidth_allocation_schemes, name]
    PriorityPolicySetQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of priority policy sets that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of priority policy sets deleted for
            a time frame.

            ', type: boolean}
        group_by:
          description: 'Group the priority policy sets for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the priority policy sets by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    PriorityPolicySetQueryFilter:
      properties:
        bandwidth_allocation_schemes:
          description: 'The bandwidth allocation schemes configured for a given policy
            set. Upto four bandwidth allocation schemes can be configured.

            '
          items: {$ref: '#/components/schemas/BandwidthAllocationSchemeV2'}
          type: array
        business_priority_names:
          description: 'The business priority names. Max list size = 4.

            '
          items: {$ref: '#/components/schemas/BusinessPriorityNameMapper'}
          type: array
        clone_from: {description: 'The existing policy from Stacked Policies for cloning
            the policy set.

            ', type: string}
        default_rule_dscp_mappings:
          description: "The default rule of the DSCP mappings. \n"
          items: {$ref: '#/components/schemas/DefaultRuleDSCPMapping'}
          type: array
        defaultrule_policyset: {description: "The default rule of the policy set.\
            \   \n", type: boolean}
        description: {description: 'The description of the policy set. Max size =
            256.

            '}
        id: {description: 'The priority policy set ID.

            ', type: string}
        name: {description: 'The name of the policy set. Max size = 128.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the policy
            set. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
        template: {description: 'The template used for cloning the policy rule. The
            template option creates a set of rules from a predefined template can
            be customized after creation.

            ', type: boolean}
      required: [business_priority_names, bandwidth_allocation_schemes, name]
    PriorityPolicySetStack:
      properties:
        default_policysetstack: {description: "The default policy set stack. \n",
          type: boolean}
        defaultrule_policyset_id: {description: 'The ID of the default rule policy
            set.

            ', type: string}
        description: {description: 'The description of the priority policy set stack.
            Max size = 256.

            '}
        id: {description: 'The priority policy set stack ID.

            ', type: string}
        name: {description: 'The name of the priority policy set stack. Max size =
            128.

            ', type: string}
        policyset_ids:
          description: "The ID of the policy set.    \n"
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the priority
            policy set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [defaultrule_policyset_id, name]
    PriorityPolicySetStackQueryFilter:
      properties:
        default_policysetstack: {description: "The default policy set stack. \n",
          type: boolean}
        defaultrule_policyset_id: {description: 'The ID of the default rule policy
            set.

            ', type: string}
        description: {description: 'The description of the priority policy set stack.
            Max size = 256.

            '}
        id: {description: 'The priority policy set stack ID.

            ', type: string}
        name: {description: 'The name of the priority policy set stack. Max size =
            128.

            ', type: string}
        policyset_ids:
          description: "The ID of the policy set.    \n"
          items: {description: Policyset Ids, type: string}
          type: array
        tags:
          description: 'A information field that can be added to identify the priority
            policy set stack. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
      required: [defaultrule_policyset_id, name]
    PriorityPolicySetState:
      properties:
        id: {description: 'The ID of the policy set.

            ', type: string}
        policy_req_state:
          description: 'The policy required state.

            '
          enum: [POLICY_REQ_PENDING, POLICY_CREATE_INPROGRESS, POLICY_CREATE_COMPLETE,
            POLICY_REPLICATE, POLICY_CREATE_FAILED, POLICY_REQ_COMPLETE]
          type: string
        policyset_id: {description: 'The ID of the policy set.

            ', type: string}
    SitePrefixAssociationQuery:
      properties:
        aggregate: {$ref: '#/components/schemas/Aggregate', description: 'The aggregate
            number of prefix associations that match the query parameters.

            '}
        dest_page: {description: 'The destination page.

            ', format: int32, type: integer}
        getDeleted: {description: 'The number of prefix associations deleted for a
            time frame.

            ', type: boolean}
        group_by:
          description: 'Group the prefix associations for the query.

            '
          items: {description: Group By, type: string}
          type: array
        id: {description: "Lists the prefix associations by ID.    \n", type: string}
        isReadPreferenceSecondary: {description: 'IsReadPreferenceSecondary: JsonIgnore(value
            = true) ', type: boolean}
        last_query_ts: {description: 'Return the ID of a specified query in the current
            session.

            ', format: int64, type: integer}
        limit: {description: 'The query limit.

            ', format: int32, type: integer}
        next_query: {description: 'The  limit.

            ', type: object}
        query_params: {type: object}
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items: {description: Retrieved Fields, type: string}
          type: array
        retrieved_fields_mask: {description: 'List the fields to query.

            ', type: boolean}
        sort_case_insensitive: {description: Sort Case Insensitive, type: boolean}
        sort_params: {type: object}
        total_count: {description: 'The total number of query parameters.

            ', format: int64, type: integer}
    SitePrefixAssociationQueryFilter:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        site_id: {description: 'The site ID binding the prefix.

            ', type: string}
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    SitePriorityPrefixAssociation:
      properties:
        id: {description: 'The site priority prefix association ID.

            ', type: string}
        ipv4_prefixes:
          description: 'Lists the prefix of the IPv4 addresses.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        prefix_id: {description: 'Lists the prefix ID of the IPv4 addresses.

            ', type: string}
        tags:
          description: "A information field that can be added to identify the site\
            \ priority prefix association. Maximum 10 unique tags of length 1024 each\
            \ are allowed.              \n"
          items: {description: Tags, type: string}
          type: array
    SitePriorityPrefixAssociationV2N1:
      properties:
        id: {description: 'The ID of the site prefix.

            ', type: string}
        ipv4_prefixes:
          description: 'The individual IP address.

            '
          items: {description: Ipv4 Prefixes, type: string}
          type: array
        ipv6_prefixes:
          description: 'Lists the IPv6 address.

            '
          items: {description: Ipv6 Prefixes, type: string}
          type: array
        prefix_id: {description: "The IP Prefix ID that associates with a site.  \
            \ \n", type: string}
        tags:
          description: 'A information field that can be added to identify the site
            prefix. Maximum 10 unique tags of length 1024 each are allowed.

            '
          items: {description: Tags, type: string}
          type: array
    UserGroup:
      properties:
        user_group_ids:
          description: 'The user group IDs.

            '
          items: {description: User Group Ids, type: string}
          type: array
        user_ids:
          description: 'The user IDs.

            '
          items: {description: User Ids, type: string}
          type: array
info:
  contact: {}
  description: List of APIs used to manage the QoS policies.
  title: QOS Policy
  version: Latest
openapi: 3.0.0
paths:
  /sdwan/v2.0/api/prioritypolicyglobalprefixes:
    get:
      description: List all the priority policy global prefixes.
      operationId: get-sdwan-v2.0-api-prioritypolicyglobalprefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefix'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Priority Policy GLobal Prefixes (v2.0)
      tags: [QOS Policy]
    post:
      description: Create a new priority global prefix.
      operationId: post-sdwan-v2.0-api-prioritypolicyglobalprefixes
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefix'}
        description: Details of the Priority Global prefix  to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: INVALID_PREFIX}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Priority Global Prefix (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicyglobalprefixes/query:
    post:
      description: Query the priority global prefixes.
      operationId: post-sdwan-v2.0-api-prioritypolicyglobalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GlobalPrefixQuery'}
        description: Query Details for Priority Global
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/GlobalPrefixQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Priority Global Prefixes (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicyglobalprefixes/{priority_policy_id}:
    delete:
      description: Delete a priority policy global prefix.
      operationId: delete-sdwan-v2.0-api-prioritypolicyglobalprefixes-priority_policy_id
      parameters:
      - description: The ID of the priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_USED_IN_POLICY}
                  - {code: ENTERPRISE_PREFIX_INVALID_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Priority Policy Global Prefix (v2.0)
      tags: [QOS Policy]
    get:
      description: List a specific priority policy global prefix.
      operationId: get-sdwan-v2.0-api-prioritypolicyglobalprefixes-priority_policy_id
      parameters:
      - description: The ID of the priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefix'}
          description: Successful Operation
      summary: Get Priority Policy Global Prefix (v2.0)
      tags: [QOS Policy]
    put:
      description: Update a priority policy global prefix.
      operationId: put-sdwan-v2.0-api-prioritypolicyglobalprefixes-priority_policy_id
      parameters:
      - description: The ID of the priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefix'}
        description: Details to update the Priority policy Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: INVALID_PREFIX}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: ENTERPRISE_PREFIX_INVALID_REQUEST}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Priority Policy Global Prefix (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicylocalprefixes:
    get:
      description: List all the priority policy local prefixes.
      operationId: get-sdwan-v2.0-api-prioritypolicylocalprefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicyLocalPrefix'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Priority Policy Local Prefixes (v2.0)
      tags: [QOS Policy]
    post:
      description: Create a new priority policy local prefix.
      operationId: post-sdwan-v2.0-api-prioritypolicylocalprefixes
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyLocalPrefix'}
        description: Details of the Priority Policy Local prefix to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Priority Policy Local Prefix (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicylocalprefixes/query:
    post:
      description: Query the site priority prefix association.
      operationId: post-sdwan-v2.0-api-prioritypolicylocalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePrefixAssociationQuery'}
        description: Query details for site priority prefix association
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePrefixAssociationQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Site Priority Prefix Association (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicylocalprefixes/{priority_policy_id}:
    delete:
      description: Delete a priority policy local prefix.
      operationId: delete-sdwan-v2.0-api-prioritypolicylocalprefixes-priority_policy_id
      parameters:
      - description: The ID of the local priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_ASSOCIATED_WITH_SITE}
                  - {code: PREFIX_IS_USED_IN_POLICY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Priority Policy Local Prefix (v2.0)
      tags: [QOS Policy]
    get:
      description: List a specific priority policy local prefix.
      operationId: get-sdwan-v2.0-api-prioritypolicylocalprefixes-priority_policy_id
      parameters:
      - description: The ID of the local priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyLocalPrefix'}
          description: Successful Operation
      summary: Get Priority Policy Local Prefix (v2.0)
      tags: [QOS Policy]
    put:
      description: Update a priority policy local prefix.
      operationId: put-sdwan-v2.0-api-prioritypolicylocalprefixes-priority_policy_id
      parameters:
      - description: The ID of the local priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyLocalPrefix'}
        description: Details to update the Priority Policy local prefix.
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyLocalPrefix'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Priority Policy Local Prefix (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicysets:
    get:
      description: Get all the priority policy sets of a tenant.
      operationId: get-sdwan-v2.0-api-prioritypolicysets
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicySet'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Priority Policy Sets (v2.0)
      tags: [QOS Policy]
    post:
      description: Create a new priority policy set.
      operationId: post-sdwan-v2.0-api-prioritypolicysets
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicySet'}
        description: Details of the priority policy to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_POLICYSET_TEMPLATE}
                  - {code: BANDWIDTH_ALLOCATION_SCHEME_REQD}
                  - {code: BUSINESS_PRIORITY_NAMES_REQD}
                  - {code: BUSINESS_PRIORITY_NAME_LIST_SIZE_EXCEEDED}
                  - {code: INVALID_DSCP_MAPPINGS}
                  - {code: INVALID_DSCP_VALUE}
                  - {code: DUPLICATE_DSCP_VALUE}
                  - {code: BUSINESS_PRIORITY_NAME_REQD}
                  - {code: BUSINESS_PRIORITY_NAME_EXCEEDS_LIMIT}
                  - {code: INVALID_PRIORITY_NUMBER}
                  - {code: BUSINESS_PRIORITY_NUMBER_REQD}
                  - {code: POLICY_SET_INVALID_BANDWIDTH_RANGE}
                  - {code: POLICY_SET_CONFIG_INVALID_CLONE_FROM}
                  - {code: POLICY_SET_CONFIG_CREATION_FAILED}
                  - {code: INVALID_DEFAULTRULE_FLAG_OF_CLONE_FROM}
                  - {code: INVALID_TEMPLATE_CONFIG_FOR_DEFAULTRULE_POLICYSET}
                  - {code: INVALID_TEMPLATE_AND_CLONE_FROM}
                  - {code: TENANT_APPDEF_VERSION_NOT_FOUND}
                  - {code: BANDWIDTH_ALLOCATION_SCHEME_LIST_SIZE_EXCEEDED}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Priority Policy Set (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicysets/query:
    post:
      description: Query all the priority policy sets.
      operationId: post-sdwan-v2.0-api-prioritypolicysets-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicySetQuery'}
        description: Query details for Priority Policy sets
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicySetQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Priority Policy Sets (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicysets/{policy_set_id}:
    delete:
      description: Delete a priority policy set.
      operationId: delete-sdwan-v2.0-api-prioritypolicysets-policy_set_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYSET_IS_IN_USE}
                  - {code: POLICYSET_INVALID_STATE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Priority Policy Set (v2.0)
      tags: [QOS Policy]
    get:
      description: Get a specific priority policy set of a tenant.
      operationId: get-sdwan-v2.0-api-prioritypolicysets-policy_set_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySet'}
          description: Successful Operation
      summary: Get Priority Policy Set (v2.0)
      tags: [QOS Policy]
    put:
      description: Update a priority policy set.
      operationId: put-sdwan-v2.0-api-prioritypolicysets-policy_set_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicySet'}
        description: Details of the priority policy to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySet'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: READONLY_CLONE_FROM}
                  - {code: READONLY_DEFAULTRULE_POLICYSET_FLAG}
                  - {code: BANDWIDTH_ALLOCATION_SCHEME_REQD}
                  - {code: BANDWIDTH_ALLOCATION_SCHEME_LIST_SIZE_EXCEEDED}
                  - {code: BUSINESS_PRIORITY_NAMES_REQD}
                  - {code: BUSINESS_PRIORITY_NAME_LIST_SIZE_EXCEEDED}
                  - {code: INVALID_DSCP_MAPPINGS}
                  - {code: INVALID_PRIORITY_NUMBER}
                  - {code: INVALID_DSCP_VALUE}
                  - {code: DUPLICATE_DSCP_VALUE}
                  - {code: BUSINESS_PRIORITY_NAME_REQD}
                  - {code: BUSINESS_PRIORITY_NUMBER_REQD}
                  - {code: BUSINESS_PRIORITY_NAME_EXCEEDS_LIMIT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Priority Policy Set (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicysets/{policy_set_id}/status:
    get:
      description: List a specific priority policy set status of a tenant.
      operationId: get-sdwan-v2.0-api-prioritypolicysets-policy_set_id-status
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySetState'}
          description: Successful Operation
      summary: Get Priority Policy Set Status (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicysetstacks:
    get:
      description: List all the priority policy set stacks of a tenant.
      operationId: get-sdwan-v2.0-api-prioritypolicysetstacks
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicySetStack'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Priority Policy Set Stacks (v2.0)
      tags: [QOS Policy]
    post:
      description: Create a priority policy set stack.
      operationId: post-sdwan-v2.0-api-prioritypolicysetstacks
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicySetStack'}
        description: Details of the Priority policy stack to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_DEFAULTRULE_POLICYSET_ID}
                  - {code: POLICYSETSTACK_CONFIG_DUPLICATE_POLICYSETID}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_IDS}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_STATE}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_DEFAULTRULE_POLICYSET_STATE}
                  - {code: DEFAULT_POLICYSET_STACK_EXISTS}
                  - {code: POLICYSETSTACK_CONFIG_DEFAULTRULE_POLICYSET_REQUIRED}
                  - {code: INVALID_DEFAULTRULE_POLICYSET_ID_FORMAT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Priority Policy Set Stacks (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicysetstacks/query:
    post:
      description: Query the priority policy set stacks.
      operationId: post-sdwan-v2.0-api-prioritypolicysetstacks-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PolicySetStackQuery'}
        description: Query details for Priority Policy set stacks
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicySetStackQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Priority Policy Set Stacks (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/prioritypolicysetstacks/{policy_set_id}:
    delete:
      description: Delete a Priority Policy Set Stack.
      operationId: delete-sdwan-v2.0-api-prioritypolicysetstacks-policy_set_id
      parameters:
      - description: The ID of the priority policy set stack.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICYSETSTACK_IN_USE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Priority Policy Set Stack (v2.0)
      tags: [QOS Policy]
    get:
      description: Get a specific priority policy set stack of a tenant.
      operationId: get-sdwan-v2.0-api-prioritypolicysetstacks-policy_set_id
      parameters:
      - description: The ID of the priority policy set stack.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySetStack'}
          description: Successful Operation
      summary: Get Priority Policy Set Stack (v2.0)
      tags: [QOS Policy]
    put:
      description: Update a Priority Policy Set Stack.
      operationId: put-sdwan-v2.0-api-prioritypolicysetstacks-policy_set_id
      parameters:
      - description: The ID of the priority policy set stack.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicySetStack'}
        description: Details of the Priority Policy Set Stack to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicySetStack'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: INVALID_DEFAULTRULE_POLICYSET_ID}
                  - {code: POLICYSETSTACK_CONFIG_DUPLICATE_POLICYSETID}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_POLICYSET_IDS}
                  - {code: POLICYSETSTACK_CONFIG_INVALID_DEFAULTRULE_POLICYSET_STATE}
                  - {code: DEFAULT_POLICYSET_STACK_EXISTS}
                  - {code: POLICYSETSTACK_CONFIG_DEFAULTRULE_POLICYSET_REQUIRED}
                  - {code: INVALID_DEFAULTRULE_POLICYSET_ID_FORMAT}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Priority Policy Set Stack (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/sites/{site_id}/prioritypolicylocalprefixes:
    get:
      description: List all the site priority policy prefix associations.
      operationId: get-sdwan-v2.0-api-sites-site_id-prioritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePriorityPrefixAssociation'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Site Priority Policy Prefix Associations (v2.0)
      tags: [QOS Policy]
    post:
      description: Create an association between the site and the priority local prefix.
      operationId: post-sdwan-v2.0-api-sites-site_id-prioritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociation'}
        description: Details of the site Priority Policy local prefix to be associated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NETWRK_POLICY_PREFIX_INVALID}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Association between Site and Priority Local Prefix (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.0/api/sites/{site_id}/prioritypolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete an existing site priority policy local prefix association.
      operationId: delete-sdwan-v2.0-api-sites-site_id-prioritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The priority policy local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Site Priority Policy Local Prefix Association (v2.0)
      tags: [QOS Policy]
    get:
      description: List a site priority policy local prefix association.
      operationId: get-sdwan-v2.0-api-sites-site_id-prioritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The priority policy local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Site Priority Policy Local Prefix Association (v2.0)
      tags: [QOS Policy]
    put:
      description: Update an existing site priority policy local prefix association.
      operationId: put-sdwan-v2.0-api-sites-site_id-prioritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The priority policy local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociation'}
        description: Details of the site Priority policy local prefix to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociation'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: PRIORITY_POLICY_PREFIX_INVALID}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Site Priority Policy Local Prefix Association (v2.0)
      tags: [QOS Policy]
  /sdwan/v2.1/api/prioritypolicyglobalprefixes:
    get:
      description: List all the priority policy global prefixes.
      operationId: get-sdwan-v2.1-api-prioritypolicyglobalprefixes
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefixV2N1'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Priority Policy GLobal Prefixes (v2.1)
      tags: [QOS Policy]
    post:
      description: Create a new priority global prefix.
      operationId: post-sdwan-v2.1-api-prioritypolicyglobalprefixes
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefixV2N1'}
        description: Details of the Priority Global prefix  to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: INVALID_PREFIX}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Priority Global Prefix (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/prioritypolicyglobalprefixes/query:
    post:
      description: Query the priority global prefixes.
      operationId: post-sdwan-v2.1-api-prioritypolicyglobalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/GlobalPrefixQuery'}
        description: Query Details for Priority Global
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/GlobalPrefixQueryFilterV2N1'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Priority Global Prefixes (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/prioritypolicyglobalprefixes/{priority_policy_id}:
    delete:
      description: Delete a priority policy global prefix.
      operationId: delete-sdwan-v2.1-api-prioritypolicyglobalprefixes-priority_policy_id
      parameters:
      - description: The ID of the priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: PREFIX_IS_USED_IN_POLICY}
                  - {code: ENTERPRISE_PREFIX_INVALID_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Priority Policy Global Prefix (v2.1)
      tags: [QOS Policy]
    get:
      description: List a specific priority policy global prefix.
      operationId: get-sdwan-v2.1-api-prioritypolicyglobalprefixes-priority_policy_id
      parameters:
      - description: The ID of the priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
      summary: Get Priority Policy Global Prefix (v2.1)
      tags: [QOS Policy]
    put:
      description: Update a priority policy global prefix.
      operationId: put-sdwan-v2.1-api-prioritypolicyglobalprefixes-priority_policy_id
      parameters:
      - description: The ID of the priority policy.
        in: path
        name: priority_policy_id
        required: true
        schema: {description: Priority Policy Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefixV2N1'}
        description: Details to update the Priority policy Global prefix.
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyGlobalPrefixV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: NAME_REQD}
                  - {code: INVALID_PREFIX}
                  - {code: DESCRIPTION_EXCEEDS_LIMIT}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_REQUEST_DUPLICATE_KEY}
                  - {code: ENTERPRISE_PREFIX_INVALID_REQUEST}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Priority Policy Global Prefix (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/prioritypolicylocalprefixes/query:
    post:
      description: Query the site priority prefix association.
      operationId: post-sdwan-v2.1-api-prioritypolicylocalprefixes-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePrefixAssociationQuery'}
        description: Query details for site priority prefix association
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePrefixAssociationQueryFilter'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Site Priority Prefix Association (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/prioritypolicyrules/query:
    post:
      description: Query the priority policy rules.
      operationId: post-sdwan-v2.1-api-prioritypolicyrules-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyRuleQueryV2N1'}
        description: Query details for Priority Policy rules
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicyRuleQueryFilterV2N1'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Priority Policy Rules (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/prioritypolicysets/{policy_set_id}/prioritypolicyrules:
    get:
      description: List the priority policy rules of a tenant.
      operationId: get-sdwan-v2.1-api-prioritypolicysets-policy_set_id-prioritypolicyrules
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicyRuleV2N1'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Priority Policy Rules (v2.1)
      tags: [QOS Policy]
    post:
      description: Create a new priority policy rule.
      operationId: post-sdwan-v2.1-api-prioritypolicysets-policy_set_id-prioritypolicyrules
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N1'}
        description: Details of the priority policy to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: DUPLICATE_APP_DEF_IDS}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED}
                  - {code: INVALID_NETWORK_CONTEXT_ID}
                  - {code: INVALID_ORDER_NUMBER_MINIMUM_VAL}
                  - {code: INVALID_ORDER_NUMBER_MAXIMUM_VAL}
                  - {code: INVALID_SOURCE_PREFIXES_ID}
                  - {code: INVALID_DESTINATION_PREFIXES_ID}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: PRIORITYPOLICYSET_NOT_PRESENT}
                  - {code: POLICYRULE_CREATION_NOT_ALLOWED}
                  - {code: INVALID_POLICYSET_STATE}
                  - {code: INVALID_DEFAULT_RULE_FLAG}
                  - {code: POLICYRULE_CONFIG_DEFAULT_RULE_NAME}
                  - {code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS}
                  - {code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT}
                  - {code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX}
                  - {code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX}
                  - {code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS}
                  - {code: INVALID_PRIORITY_NUMBER}
                  - {code: PRIORITY_NUMBER_MINIMUM_VAL}
                  - {code: PRIORITY_NUMBER_MAXIMUM_VAL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Priority Policy Rule (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/prioritypolicysets/{policy_set_id}/prioritypolicyrules/{policy_rule_id}:
    delete:
      description: Delete a priority policy rule of a tenant.
      operationId: delete-sdwan-v2.1-api-prioritypolicysets-policy_set_id-prioritypolicyrules-policy_rule_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: The ID of the priority policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICY_SET_CLONE_IN_PROGRESS}
                  - {code: CANNOT_DELETE_DEFAULT_POLICY_RULE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Priority Policy Rule (v2.1)
      tags: [QOS Policy]
    get:
      description: Get a specific priority policy rule of a tenant.
      operationId: get-sdwan-v2.1-api-prioritypolicysets-policy_set_id-prioritypolicyrules-policy_rule_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: The ID of the priority policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N1'}
          description: Successful Operation
      summary: Get Priority Policy Rule (v2.1)
      tags: [QOS Policy]
    put:
      description: Update a priority policy rule of a tenant.
      operationId: put-sdwan-v2.1-api-prioritypolicysets-policy_set_id-prioritypolicyrules-policy_rule_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: The ID of the priority policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N1'}
        description: Query details for Priority Policy rules
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: DUPLICATE_APP_DEF_IDS}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED}
                  - {code: INVALID_NETWORK_CONTEXT_ID}
                  - {code: INVALID_ORDER_NUMBER_MINIMUM_VAL}
                  - {code: INVALID_ORDER_NUMBER_MAXIMUM_VAL}
                  - {code: INVALID_SOURCE_PREFIXES_ID}
                  - {code: INVALID_DESTINATION_PREFIXES_ID}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: PRIORITYPOLICYSET_NOT_PRESENT}
                  - {code: INVALID_POLICYSET_STATE}
                  - {code: INVALID_DEFAULT_RULE_FLAG}
                  - {code: INVALID_DEFAULT_RULE_NAME}
                  - {code: INVALID_DEFAULT_RULE_APPDEFS_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_DESTINATION_PREFIX_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_SOURCE_PREFIX_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_ENABLED_FLAG_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_NETWORK_CONTEXT_UPDATE}
                  - {code: POLICY_RULE_UPDATE_INVALID_NETWORK_CONTEXT}
                  - {code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS}
                  - {code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT}
                  - {code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX}
                  - {code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX}
                  - {code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS}
                  - {code: INVALID_PRIORITY_NUMBER}
                  - {code: PRIORITY_NUMBER_MINIMUM_VAL}
                  - {code: PRIORITY_NUMBER_MAXIMUM_VAL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Priority Policy Rule (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/sites/{site_id}/prioritypolicylocalprefixes:
    get:
      description: List all the site priority policy prefix associations.
      operationId: get-sdwan-v2.1-api-sites-site_id-prioritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/SitePriorityPrefixAssociationV2N1'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Site Priority Policy Prefix Associations (v2.1)
      tags: [QOS Policy]
    post:
      description: Create an association between the site and the priority local prefix.
      operationId: post-sdwan-v2.1-api-sites-site_id-prioritypolicylocalprefixes
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociationV2N1'}
        description: Details of the site Priority Policy local prefix to be associated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociationV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: NETWRK_POLICY_PREFIX_INVALID}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Association between Site and Priority Local Prefix (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.1/api/sites/{site_id}/prioritypolicylocalprefixes/{prefix_id}:
    delete:
      description: Delete an existing site priority policy local prefix association.
      operationId: delete-sdwan-v2.1-api-sites-site_id-prioritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The priority policy local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociationV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Site Priority Policy Local Prefix Association (v2.1)
      tags: [QOS Policy]
    get:
      description: List a site priority policy local prefix association.
      operationId: get-sdwan-v2.1-api-sites-site_id-prioritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The priority policy local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociationV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Get Site Priority Policy Local Prefix Association (v2.1)
      tags: [QOS Policy]
    put:
      description: Update an existing site priority policy local prefix association.
      operationId: put-sdwan-v2.1-api-sites-site_id-prioritypolicylocalprefixes-prefix_id
      parameters:
      - description: The ID of the site.
        in: path
        name: site_id
        required: true
        schema: {description: Site Id, type: string}
      - description: The priority policy local prefix ID.
        in: path
        name: prefix_id
        required: true
        schema: {description: Prefix Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociationV2N1'}
        description: Details of the site Priority policy local prefix to be updated
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/SitePriorityPrefixAssociationV2N1'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: SITE_NOT_FOUND}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: PRIORITY_POLICY_PREFIX_INVALID}
                  - {code: LOCAL_PREFIX_NOT_FOUND}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Site Priority Policy Local Prefix Association (v2.1)
      tags: [QOS Policy]
  /sdwan/v2.2/api/prioritypolicyrules/query:
    post:
      description: Query the priority policy rules.
      operationId: post-sdwan-v2.2-api-prioritypolicyrules-query
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyRuleQueryV2N2'}
        description: Query details for Priority Policy rules
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListQueryResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicyRuleQueryFilterV2N2'}
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: BAD_REQUEST}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Query Priority Policy Rules (v2.2)
      tags: [QOS Policy]
  /sdwan/v2.2/api/prioritypolicysets/{policy_set_id}/prioritypolicyrules:
    get:
      description: List the priority policy rules of a tenant.
      operationId: get-sdwan-v2.2-api-prioritypolicysets-policy_set_id-prioritypolicyrules
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - {$ref: '#/components/schemas/ListResponse'}
                - properties:
                    items:
                      items: {$ref: '#/components/schemas/PriorityPolicyRuleV2N2'}
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Priority Policy Rules (v2.2)
      tags: [QOS Policy]
    post:
      description: Create a new priority policy rule.
      operationId: post-sdwan-v2.2-api-prioritypolicysets-policy_set_id-prioritypolicyrules
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N2'}
        description: Details of the priority policy to be created
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: DUPLICATE_APP_DEF_IDS}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED}
                  - {code: INVALID_NETWORK_CONTEXT_ID}
                  - {code: INVALID_ORDER_NUMBER_MINIMUM_VAL}
                  - {code: INVALID_ORDER_NUMBER_MAXIMUM_VAL}
                  - {code: INVALID_SOURCE_PREFIXES_ID}
                  - {code: INVALID_DESTINATION_PREFIXES_ID}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: PRIORITYPOLICYSET_NOT_PRESENT}
                  - {code: POLICYRULE_CREATION_NOT_ALLOWED}
                  - {code: INVALID_POLICYSET_STATE}
                  - {code: INVALID_DEFAULT_RULE_FLAG}
                  - {code: POLICYRULE_CONFIG_DEFAULT_RULE_NAME}
                  - {code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS}
                  - {code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT}
                  - {code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX}
                  - {code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX}
                  - {code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS}
                  - {code: INVALID_PRIORITY_NUMBER}
                  - {code: PRIORITY_NUMBER_MINIMUM_VAL}
                  - {code: PRIORITY_NUMBER_MAXIMUM_VAL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Create Priority Policy Rule (v2.2)
      tags: [QOS Policy]
  /sdwan/v2.2/api/prioritypolicysets/{policy_set_id}/prioritypolicyrules/{policy_rule_id}:
    delete:
      description: Delete a priority policy rule of a tenant.
      operationId: delete-sdwan-v2.2-api-prioritypolicysets-policy_set_id-prioritypolicyrules-policy_rule_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: The ID of the priority policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: POLICY_SET_CLONE_IN_PROGRESS}
                  - {code: CANNOT_DELETE_DEFAULT_POLICY_RULE}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Delete Priority Policy Rule (v2.2)
      tags: [QOS Policy]
    get:
      description: Get a specific priority policy rule of a tenant.
      operationId: get-sdwan-v2.2-api-prioritypolicysets-policy_set_id-prioritypolicyrules-policy_rule_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: The ID of the priority policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N2'}
          description: Successful Operation
      summary: Get Priority Policy Rule (v2.2)
      tags: [QOS Policy]
    put:
      description: Update a priority policy rule of a tenant.
      operationId: put-sdwan-v2.2-api-prioritypolicysets-policy_set_id-prioritypolicyrules-policy_rule_id
      parameters:
      - description: The ID of the priority policy set.
        in: path
        name: policy_set_id
        required: true
        schema: {description: Policy Set Id, type: string}
      - description: The ID of the priority policy rule.
        in: path
        name: policy_rule_id
        required: true
        schema: {description: Policy Rule Id, type: string}
      requestBody:
        content:
          application/json:
            schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N2'}
        description: Query details for Priority Policy rules
      responses:
        '200':
          content:
            application/json:
              schema: {$ref: '#/components/schemas/PriorityPolicyRuleV2N2'}
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - {code: DUPLICATE_APP_DEF_IDS}
                  - {code: APP_DEF_ID_LIST_SIZE_EXCEEDED}
                  - {code: INVALID_NETWORK_CONTEXT_ID}
                  - {code: INVALID_ORDER_NUMBER_MINIMUM_VAL}
                  - {code: INVALID_ORDER_NUMBER_MAXIMUM_VAL}
                  - {code: INVALID_SOURCE_PREFIXES_ID}
                  - {code: INVALID_DESTINATION_PREFIXES_ID}
                  - {code: INVALID_JSON_EXTRA_ATTRIBUTE}
                  - {code: INVALID_JSON_COMMON_ATTR_MISMATCH}
                  - {code: PRIORITYPOLICYSET_NOT_PRESENT}
                  - {code: INVALID_POLICYSET_STATE}
                  - {code: INVALID_DEFAULT_RULE_FLAG}
                  - {code: INVALID_DEFAULT_RULE_NAME}
                  - {code: INVALID_DEFAULT_RULE_APPDEFS_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_DESTINATION_PREFIX_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_SOURCE_PREFIX_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_ENABLED_FLAG_UPDATE}
                  - {code: INVALID_DEFAULT_RULE_NETWORK_CONTEXT_UPDATE}
                  - {code: POLICY_RULE_UPDATE_INVALID_NETWORK_CONTEXT}
                  - {code: POLICYRULE_CONFIG_INVALID_APPDEF_IDS}
                  - {code: POLICYRULE_CONFIG_INVALID_NETWORK_CONTEXT}
                  - {code: POLICYRULE_CONFIG_INVALID_SOURCE_PREFIX}
                  - {code: POLICYRULE_CONFIG_INVALID_DESTINATION_PREFIX}
                  - {code: POLICYRULE_CONFIG_ALREADY_EXISTS_FOR_THE_APPS}
                  - {code: INVALID_PRIORITY_NUMBER}
                  - {code: PRIORITY_NUMBER_MINIMUM_VAL}
                  - {code: PRIORITY_NUMBER_MAXIMUM_VAL}
              schema: {$ref: '#/components/schemas/Error'}
          description: Bad Request
      summary: Update Priority Policy Rule (v2.2)
      tags: [QOS Policy]
servers:
- {url: 'https://api.sase.paloaltonetworks.com'}
tags:
- {name: QOS Policy}
