components:
  schemas:
    Aggregate:
      properties:
        data:
          description: 'The details of the aggregate data.

            '
          items:
            $ref: '#/components/schemas/AggregateData'
          type: array
        field:
          description: 'The field value.

            '
          type: string
        name:
          description: 'The name of the query response.

            '
          type: string
        operator:
          description: 'The operator values.

            '
          enum:
          - max
          - count
          - min
          - avg
          - sum
          type: string
        statistic:
          description: 'The aggregate statistics.

            '
          type: string
        unit:
          description: 'The unit.

            '
          type: string
    AggregateData:
      properties:
        value:
          description: 'Value of the aggregate data.

            '
          type: number
        view:
          description: 'View the aggregate date.

            '
          type: object
    ClientDetails:
      properties:
        aging_timeout:
          description: 'The aging timeout value.

            '
          type: integer
        auth_type:
          description: 'The authentication type. Only closed mode and single host
            authentication is supported.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        interface_id:
          description: 'The interface ID.

            '
          type: string
        mac_address:
          description: 'The MAC address.

            '
          type: string
        reauth_timeout:
          description: "The re-auth timeout.  \n"
          type: integer
        reauthentication_timeout:
          description: 'The ION device authenticates or reinitializes the client after
            a session timeout based on the value of the Termination-Action. Values
            1 and 0.

            '
          type: integer
        stats_start_time_ms:
          description: 'The stats start time in milliseconds.

            '
          format: int64
          type: integer
        uptime:
          description: 'Indicates the time for which the neighbor has been up.

            '
          format: int64
          type: integer
        username:
          description: 'The client user name.

            '
          type: string
        vlan_id:
          description: 'The virtual LAN ID.

            '
          type: integer
    ElementRadius:
      properties:
        description:
          description: 'The element radius description. Max limit = 256.

            '
        id:
          description: 'The element radius ID.

            '
          type: string
        name:
          description: 'The name of the radius server.

            '
          type: string
        override_indicator:
          description: 'The override indicator. Certain configurations can be overridden
            from RADIUS server using the user policy.

            '
          items:
            description: Override Indicator
            type: string
          type: array
        radius_configuration:
          description: 'The details of the radius configuration parameters.

            '
          items:
            $ref: '#/components/schemas/RadiusConfiguration'
          type: array
        radius_profile_id:
          description: "The radius profile ID.  \n"
          type: string
        source_interface_id:
          description: "The source interface ID of the selected port. \n"
          type: string
        tags:
          description: 'A information field that can be added to identify the radius
            server. Maximum 10 unique tags of length 128 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    ElementRadiusScreen:
      properties:
        description:
          description: 'The element radius description. Max limit = 256.

            '
        id:
          description: 'The element radius ID.

            '
          type: string
        name:
          description: 'The name of the radius server.

            '
          type: string
        override_indicator:
          description: 'The override indicator. Certain configurations can be overridden
            from RADIUS server using the user policy.

            '
          items:
            description: Override Indicator
            type: string
          type: array
        radius_configuration:
          description: 'The details of the radius configuration parameters.

            '
          items:
            $ref: '#/components/schemas/RadiusConfiguration'
          type: array
        radius_profile_id:
          description: "The radius profile ID.  \n"
          type: string
        source_interface_id:
          description: "The source interface ID of the selected port. \n"
          type: string
        tags:
          description: 'A information field that can be added to identify the radius
            server. Maximum 10 unique tags of length 128 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    ElementRadiusStatus:
      properties:
        accounting_port:
          description: 'The radius configuration accounting port number between 1-65535.
            Default port is 1813.

            '
          format: int32
          type: integer
        authentication_port:
          description: 'The radius configuration authentication port number between
            1-65535. Default port is 1812.

            '
          format: int32
          type: integer
        description:
          description: 'The element radius description. Max limit = 256.

            '
        disabled:
          description: 'Indicates if the element radius server is disabled.

            '
          type: boolean
        disabled_reason:
          description: 'The reason for the element radius server to be disabled.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        inactive:
          description: 'Indicates if the element radius server is inactive.

            '
          type: boolean
        inactive_reason:
          description: 'The reason for the element radius server to be inactive.

            '
          type: string
        ip_address:
          description: 'The IP address.

            '
          type: string
        ip_version:
          description: 'The version of the IP.

            '
          format: int32
          type: integer
        name:
          description: 'The name of the radius server.

            '
          type: string
        priority:
          description: 'The priority value between 1-255. Lower the priority number,
            higher is the priority.

            '
          format: int32
          type: integer
        radius_id:
          description: "The radius ID. \n"
          type: string
        region:
          description: "The region of the radius.  \n"
          type: string
        source_interface_id:
          description: 'The source interface ID of the selected port.

            '
          type: string
    Error:
      properties:
        _error:
          items:
            $ref: '#/components/schemas/ErrorResponse'
          type: array
    ErrorResponse:
      properties:
        code:
          description: 'The error code.

            '
          type: string
        message:
          description: 'The error message.

            '
          type: string
    InterfaceAuthenticationState:
      properties:
        authorized:
          description: 'The interface authentication state authorization.

            '
          type: boolean
        client_details:
          description: 'The client details.

            '
          items:
            $ref: '#/components/schemas/ClientDetails'
          type: array
        eapol_recvd_version:
          description: 'The eapol recieved version.

            '
          type: integer
        eapol_src_mac_address:
          description: 'The eapol src mac address.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        interface_id:
          description: 'The interface ID.

            '
          type: string
        operational_state:
          description: 'The operational state.

            '
          type: string
    InterfaceLldpNeighborsState:
      properties:
        asset_id:
          description: 'The LLDP asset ID.

            '
          type: string
        chassis_id:
          description: 'The authentication type. Only closed mode and single host
            authentication is supported.

            '
          type: string
        device_class:
          description: 'The device class.

            '
          type: string
        device_description:
          description: 'The device description.

            '
          type: string
        device_name:
          description: 'The name of the device.

            '
          type: string
        firmware_revision:
          description: "The firmware revision.  \n"
          type: string
        hardware_revision:
          description: 'The hardware revision.

            '
          type: string
        ip_address:
          description: 'The Ip address of the LLDP neighbors.

            '
          type: string
        mac_address:
          description: Mac Address
          type: string
        manufacturer_name:
          description: 'The name of the manufacturer.

            '
          type: string
        model_name:
          description: 'The device model name.

            '
          type: string
        power_priority:
          description: 'The power usage priority.

            '
          type: string
        power_required:
          description: "The powere required.  \n"
          format: float
          type: number
        power_source:
          description: 'The power source.

            '
          type: string
        power_type:
          description: 'The power type.

            '
          type: string
        serial_number:
          description: 'The serial number.

            '
          type: string
        software_revision:
          description: 'The software revision.

            '
          type: string
        username:
          description: Username
          type: string
    ListResponse:
      properties:
        count:
          description: 'The actual count.

            '
          format: int32
          type: integer
        id:
          description: 'The ID.

            '
          type: string
      type: object
    LldpNeighborsState:
      properties:
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        interface_id:
          description: 'The interface ID.

            '
          type: string
        lldp_neighbors_state:
          description: 'Details of the LLDP neighbors state.

            '
          items:
            $ref: '#/components/schemas/InterfaceLldpNeighborsState'
          type: array
        lldp_state:
          description: 'The lldp state.

            '
          type: string
        lldp_type:
          description: 'The lldp type.

            '
          type: string
    MSTPInstance:
      properties:
        description:
          description: 'The MSTP instance description. Max limit = 256.

            '
        id:
          description: 'The MSTP instance ID.

            '
          type: string
        instance_number:
          description: 'The MSTP instance number. Valid values are 0-255.

            '
          format: int32
          type: integer
        instance_priority:
          description: 'The MSTP instance priority. Valid values are 0-15.

            '
          type: integer
        name:
          description: 'The name of the MSTP instance. Max limit = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the MSTP
            instance. Maximum 10 unique tags of length 128 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    MSTPInstanceQuery:
      properties:
        aggregate:
          $ref: '#/components/schemas/Aggregate'
          description: 'The aggregate number of MSTP instances that match the query
            parameters.

            '
        dest_page:
          description: 'The destination page.

            '
          format: int32
          type: integer
        getDeleted:
          description: 'The number MSTP instances deleted for a time frame.

            '
          type: boolean
        group_by:
          description: Group By
          items:
            description: Group By
            type: string
          type: array
        id:
          description: "List the MSTP instances by ID.    \n"
          type: string
        isReadPreferenceSecondary:
          description: 'IsReadPreferenceSecondary: JsonIgnore(value = true) '
          type: boolean
        last_query_ts:
          description: 'Return the ID of a specified query in the current session.

            '
          format: int64
          type: integer
        limit:
          description: 'The query limit.

            '
          format: int32
          type: integer
        next_query:
          description: 'The  limit.

            '
          type: object
        query_params:
          type: object
        retrieved_fields:
          description: 'Retrieve information from a field.

            '
          items:
            description: Retrieved Fields
            type: string
          type: array
        retrieved_fields_mask:
          description: 'List the fields to query.

            '
          type: boolean
        sort_params:
          type: object
        total_count:
          description: 'The total number of query parameters.

            '
          format: int64
          type: integer
    MSTPInstanceQueryFilter:
      properties:
        description:
          description: 'The MSTP instance description. Max limit = 256.

            '
        id:
          description: 'The MSTP instance ID.

            '
          type: string
        instance_number:
          description: 'The MSTP instance number. Valid values are 0-255.

            '
          format: int32
          type: integer
        instance_priority:
          description: 'The MSTP instance priority. Valid values are 0-15.

            '
          type: integer
        name:
          description: 'The name of the MSTP instance. Max limit = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the MSTP
            instance. Maximum 10 unique tags of length 128 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    MSTPInstanceScreen:
      properties:
        description:
          description: 'The MSTP instance description. Max limit = 256.

            '
        id:
          description: 'The MSTP instance ID.

            '
          type: string
        instance_number:
          description: 'The MSTP instance number. Valid values are 0-255.

            '
          format: int32
          type: integer
        instance_priority:
          description: 'The MSTP instance priority. Valid values are 0-15.

            '
          type: integer
        name:
          description: 'The name of the MSTP instance. Max limit = 128.

            '
          type: string
        tags:
          description: 'A information field that can be added to identify the MSTP
            instance. Maximum 10 unique tags of length 128 each are allowed.

            '
          items:
            description: Tags
            type: string
          type: array
      required:
      - name
    MSTPInstanceStatus:
      properties:
        bridge_mac_address:
          description: 'The bridge MAC address.

            '
          type: string
        disabled:
          description: 'Indicates if the MSTP instance is disabled.

            '
          type: boolean
        disabled_reason:
          description: 'The reason for the MSTP instance to be disabled.

            '
          type: string
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The ID.

            '
          type: string
        inactive:
          description: 'Indicates if the MSTP instance is inactive.

            '
          type: boolean
        inactive_reason:
          description: 'The reason for the MSTP instance to be inactive.

            '
          type: string
        instance_number:
          description: 'The MSTP instance number. Valid values are 0-255.

            '
          type: integer
        instance_priority:
          description: 'The MSTP instance priority. Valid values are 0-15.

            '
          type: integer
        mstp_instance_id:
          description: 'The MSTP instance ID.

            '
          type: string
        region:
          description: 'The region of the radius.

            '
          type: string
        site_id:
          description: 'The site ID.

            '
          type: string
        stp_mode:
          description: "The STP Mode is RSTP (default). \n"
          type: string
      required:
      - site_id
    MacAddressEntry:
      properties:
        mac_address:
          description: 'The MAC address.

            '
          type: string
        port:
          description: 'The switch port.

            '
          type: string
        type:
          description: 'Dynamic or Static.

            '
          type: string
        vlan:
          description: 'The number of conncected VLANs.

            '
          format: int32
          type: integer
    MacAddressState:
      properties:
        element_id:
          description: 'The element ID.

            '
          type: string
        id:
          description: 'The MAC address state ID.

            '
          type: string
        mac_address_table:
          description: 'Details of the MAC address entries.

            '
          items:
            $ref: '#/components/schemas/MacAddressEntry'
          type: array
    RadiusConfiguration:
      properties:
        accounting_port:
          description: 'The radius configuration accounting port number between 1-65535.
            Default port is 1813.

            '
          format: int32
          type: integer
        authentication_port:
          description: 'The radius configuration authentication port number between
            1-65535. Default port is 1812.

            '
          format: int32
          type: integer
        ip_version:
          description: 'The version of the IP.

            '
          format: int32
          type: integer
        priority:
          description: 'The priority value between 1-255. Lower the priority number,
            higher is the priority.

            '
          format: int32
          type: integer
        retain_shared_secret:
          description: "If the shared secret is retained. Value = True/False. \n"
          type: boolean
        server_ip_address:
          description: "The valid IP address (IPv4).  \n"
          type: string
        shared_secret:
          description: 'The shared secret, the length of the secret must be between
            8-64 characters.

            '
          type: string
        shared_secret_encrypted:
          description: 'The encrypted shared secret. Value = True.

            '
          type: string
    SwitchPort:
      properties:
        admin_state:
          description: 'The admin state of the switch port.

            '
          type: string
        id:
          description: 'The ID of the switch port.

            '
          type: string
        name:
          description: 'The name of the switch port.

            '
          type: string
        type:
          description: 'The type of switch port.

            '
          type: string
    SwitchPortVlanPorts:
      properties:
        admin_state:
          description: 'The admin state of the switch ports.

            '
          type: string
        id:
          description: 'The ID of the switch port.

            '
          type: string
        name:
          description: 'The name of the switch port.

            '
          type: string
        type:
          description: 'The type of switch port.

            '
          type: string
        vlans:
          description: 'The details of the VLANs ports.

            '
          items:
            $ref: '#/components/schemas/VlanPort'
          type: array
    VlanPort:
      properties:
        admin_state:
          description: 'The admin state of the VLAN ports.

            '
          type: string
        id:
          description: 'The ID of the VLAN port.

            '
          type: string
        name:
          description: 'The name of the VLAN port.

            '
          type: string
        vlan_id:
          description: 'The VLAN ID.

            '
          type: integer
        voice_enabled:
          description: 'Value = True/False.

            '
          type: boolean
    VlanPortSwitchPorts:
      properties:
        admin_state:
          description: 'The admin state of the VLAN ports.

            '
          type: string
        id:
          description: 'The ID of the VLAN port.

            '
          type: string
        ports:
          description: 'The details of the switch ports.

            '
          items:
            $ref: '#/components/schemas/SwitchPort'
          type: array
        vlan_id:
          description: 'The VLAN ID.

            '
          type: integer
        vlan_name:
          description: 'The name of the VLAN port.

            '
          type: string
        voice_enabled:
          description: "Value = True/False. \n"
          type: boolean
info:
  contact: {}
  description: List of APIs to read and configure switching capabilities.
  title: Device Switch Config
  version: Latest
openapi: 3.0.0
paths:
  /sdwan/v2.0/api/elements/{element_id}/interface_authentication/status:
    get:
      description: List all the interface authentication status for an element.
      operationId: get-sdwan-v2.0-api-elements-element_id-interface_authentication-status
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/InterfaceAuthenticationState'
                      type: array
                  type: object
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INTERFACE_AUTHENTICATION_STATE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Get Interface Authentication Status for Element (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/elements/{element_id}/lldp_neighbors/status:
    get:
      description: List all the lldp neighbor(s) status for an element.
      operationId: get-sdwan-v2.0-api-elements-element_id-lldp_neighbors-status
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/LldpNeighborsState'
                      type: array
                  type: object
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: LLDP_NEIGHBORS_STATE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Get LLDP Neighbor Status for Element (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/elements/{element_id}/mac_addresses/status:
    get:
      description: List all the MAC address status for a tenant.
      operationId: get-sdwan-v2.0-api-elements-element_id-mac_addresses-status
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MacAddressState'
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MAC_ADDRESS_STATUS_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Get MAC Address Status (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/elements/{element_id}/radii:
    get:
      description: List the radius configuration of an element in a tenant.
      operationId: get-sdwan-v2.0-api-elements-element_id-radii
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/ElementRadius'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get Radius Configuration of Element (v2.0)
      tags:
      - Device Switch Configuration
    post:
      description: Create the radius configuration of an element.
      operationId: post-sdwan-v2.0-api-elements-element_id-radii
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElementRadiusScreen'
        description: Details of the element radius configuration to be created.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementRadius'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_NOT_FOUND
                  - code: RADIUS_CONFIG_INVALID_MISSING_RADIUS_CONFIG
                  - code: RADIUS_CONFIG_INVALID_DUPLICATE_IP_ADDRESS
                  - code: RADIUS_CONFIG_DUPLICATE_AUTH_ACCT_PORT
                  - code: RADIUS_CONFIG_SHARED_SECRET_INVALID
                  - code: RADIUS_CONFIG_INVALID_IP_ADDRESS
                  - code: RADIUS_CONFIG_MISSING_SERVER_IP_ADDRESS
                  - code: RADIUS_CONFIG_INVALID_IP_VERSION
                  - code: RADIUS_SHARED_SECRET_STORAGE_FAILURE
                  - code: RADIUS_CONFIG_INVALID_SOURCE_INTERFACE_ID
                  - code: RADIUS_CONFIG_INVALID_PROFILE_ID
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create Element Radius (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/elements/{element_id}/radii/{radius_id}:
    delete:
      description: Delete radius configuration in an element.
      operationId: delete-sdwan-v2.0-api-elements-element_id-radii-radius_id
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The radius configuration ID.
        in: path
        name: radius_id
        required: true
        schema:
          description: Radius Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementRadius'
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_RADIUS_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Delete Radius Configuration of Element (v2.0)
      tags:
      - Device Switch Configuration
    get:
      description: List the radius configuration of an element.
      operationId: get-sdwan-v2.0-api-elements-element_id-radii-radius_id
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The radius configuration ID.
        in: path
        name: radius_id
        required: true
        schema:
          description: Radius Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementRadius'
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_RADIUS_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Get Radius Configuration of Element (v2.0)
      tags:
      - Device Switch Configuration
    put:
      description: Update the radius configuration of an element.
      operationId: put-sdwan-v2.0-api-elements-element_id-radii-radius_id
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The radius configuration ID.
        in: path
        name: radius_id
        required: true
        schema:
          description: Radius Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ElementRadiusScreen'
        description: Details of the element radius to be updated.
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementRadius'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: RADIUS_CONFIG_INVALID_MISSING_RADIUS_CONFIG
                  - code: RADIUS_CONFIG_INVALID_DUPLICATE_IP_ADDRESS
                  - code: RADIUS_CONFIG_DUPLICATE_AUTH_ACCT_PORT
                  - code: RADIUS_CONFIG_SHARED_SECRET_INVALID
                  - code: RADIUS_CONFIG_INVALID_IP_ADDRESS
                  - code: RADIUS_CONFIG_MISSING_SERVER_IP_ADDRESS
                  - code: RADIUS_CONFIG_INVALID_IP_VERSION
                  - code: RADIUS_SHARED_SECRET_STORAGE_FAILURE
                  - code: RADIUS_CONFIG_INVALID_SOURCE_INTERFACE_ID
                  - code: RADIUS_CONFIG_INVALID_PROFILE_ID
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_RADIUS_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Update Radius Configuration of Element (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/elements/{element_id}/radii/{radius_id}/status:
    get:
      description: List a specific radius configuration status for a radius configuration
        corresponding to a tenant and element.
      operationId: get-sdwan-v2.0-api-elements-element_id-radii-radius_id-status
      parameters:
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The radius configuration ID.
        in: path
        name: radius_id
        required: true
        schema:
          description: Radius Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ElementRadiusStatus'
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_RADIUS_STATUS_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Get Specific Radius Configuration Status (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/mstp_instances/query:
    post:
      description: Query the DB for limit number of MSTP Instances that match query
        params.
      operationId: post-sdwan-v2.0-api-mstp_instances-query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MSTPInstanceQuery'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MSTPInstanceQueryFilter'
          description: Successful Operation
      summary: Query DB for MSTP Instances (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/sites/{site_id}/elements/{element_id}/mstp_instances:
    get:
      description: List all the MSTP Instances.
      operationId: get-sdwan-v2.0-api-sites-site_id-elements-element_id-mstp_instances
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/MSTPInstance'
                      type: array
                  type: object
          description: Successful Operation
      summary: Get MSTP Instances (v2.0)
      tags:
      - Device Switch Configuration
    post:
      description: Create an MSTP Instance.
      operationId: post-sdwan-v2.0-api-sites-site_id-elements-element_id-mstp_instances
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MSTPInstanceScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MSTPInstance'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MSTP_INSTANCE_CONFIG_CREATE_NOT_SUPPORTED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Create MSTP Instance (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/sites/{site_id}/elements/{element_id}/mstp_instances/{mstp_instance_id}:
    delete:
      description: Delete MSTP instance for an element.
      operationId: delete-sdwan-v2.0-api-sites-site_id-elements-element_id-mstp_instances-mstp_instance_id
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The MSTP instance ID.
        in: path
        name: mstp_instance_id
        required: true
        schema:
          description: Mstp Instance Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MSTPInstance'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MSTP_INSTANCE_CONFIG_DELETE_NOT_SUPPORTED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Delete MSTP Instance (v2.0)
      tags:
      - Device Switch Configuration
    get:
      description: List a MSTP instance for an element.
      operationId: get-sdwan-v2.0-api-sites-site_id-elements-element_id-mstp_instances-mstp_instance_id
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The MSTP instance ID.
        in: path
        name: mstp_instance_id
        required: true
        schema:
          description: Mstp Instance Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MSTPInstance'
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MSTP_INSTANCE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Get MSTP Instance (v2.0)
      tags:
      - Device Switch Configuration
    put:
      description: Update a MSTP instance for an element.
      operationId: put-sdwan-v2.0-api-sites-site_id-elements-element_id-mstp_instances-mstp_instance_id
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The MSTP instance ID.
        in: path
        name: mstp_instance_id
        required: true
        schema:
          description: Mstp Instance Id
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MSTPInstanceScreen'
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MSTPInstance'
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: INVALID_JSON_INPUT
                  - code: INVALID_JSON_EXTRA_ATTRIBUTE
                  - code: INVALID_JSON_COMMON_ATTR_MISMATCH
                  - code: MSTP_INSTANCE_INVALID_INSTANCE_NUMBER
                  - code: MSTP_INSTANCE_INVALID_STP_PRIORITY
                  - code: MSTP_CONFIG_INVALID_DEFAULT_INSTANCE_NUMBER_CANNOT_BE_MODIFIED
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MSTP_INSTANCE_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Update MSTP Instance (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/sites/{site_id}/elements/{element_id}/mstp_instances/{mstp_instance_id}/status:
    get:
      description: List the MSTP instance status for a specific ID.
      operationId: get-sdwan-v2.0-api-sites-site_id-elements-element_id-mstp_instances-mstp_instance_id-status
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      - description: The MSTP instance ID.
        in: path
        name: mstp_instance_id
        required: true
        schema:
          description: Mstp Instance Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MSTPInstanceStatus'
          description: Successful Operation
        '404':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: MSTP_INSTANCE_STATUS_NOT_FOUND
              schema:
                $ref: '#/components/schemas/Error'
          description: Not Found
      summary: Get MSTP Instance Status (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/sites/{site_id}/elements/{element_id}/port_vlan_members:
    get:
      description: Get switch port to VLAN port mapping information for an element.
      operationId: get-sdwan-v2.0-api-sites-site_id-elements-element_id-port_vlan_members
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/SwitchPortVlanPorts'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_NOT_FOUND
                  - code: SWITCH_PORT_VLAN_MEMBERS_UNSUPPORTED_ELEMENT_MODEL
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get Switch Port to VLAN Port Mapping (v2.0)
      tags:
      - Device Switch Configuration
  /sdwan/v2.0/api/sites/{site_id}/elements/{element_id}/vlan_port_members:
    get:
      description: Get VLAN port to switch port mapping information for an element.
      operationId: get-sdwan-v2.0-api-sites-site_id-elements-element_id-vlan_port_members
      parameters:
      - description: The site ID.
        in: path
        name: site_id
        required: true
        schema:
          description: Site Id
          type: string
      - description: The element ID.
        in: path
        name: element_id
        required: true
        schema:
          description: Element Id
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                allOf:
                - $ref: '#/components/schemas/ListResponse'
                - properties:
                    items:
                      items:
                        $ref: '#/components/schemas/VlanPortSwitchPorts'
                      type: array
                  type: object
          description: Successful Operation
        '400':
          content:
            application/json:
              example:
                value:
                  _error:
                  - code: ELEMENT_NOT_FOUND
                  - code: SWITCH_PORT_VLAN_MEMBERS_UNSUPPORTED_ELEMENT_MODEL
              schema:
                $ref: '#/components/schemas/Error'
          description: Bad Request
      summary: Get VLAN Port to Switch Port Mapping (v2.0)
      tags:
      - Device Switch Configuration
servers:
- url: https://api.sase.paloaltonetworks.com
tags:
- name: Device Switch Configuration
