---
title: Policy API beta
date: 2026-07-07
slug: policy-api-beta
summary: "First release of the Prisma Browser Policy API (beta). Manage all your policy types via the API, plus zero-downtime cloud-storage credential rotation."
changes:
  - type: new
    text: "Initial release of the Policy API. You can now manage all your policy types via the API. This is a beta release: based on feedback, we may introduce breaking changes until it reaches GA."
  - type: new
    endpoint: "Replace a cloud storage provider"
    to: "/prisma-browser/api/replace-a-cloud-storage-provider"
    text: "New cloud-storage integration API: rotate a provider's credentials programmatically. Credentials are replaced with zero downtime, so there is no interruption for your users."
    example: |
      https PUT api.sase.paloaltonetworks.com/seb-api/v1/integrations/cloud-storage/{providerId} \
        "Authorization: Bearer $TOKEN"
---

First release of the Prisma Browser Policy API (beta). Manage all your policy
types via the API, plus zero-downtime cloud-storage credential rotation.

{/* truncate */}
