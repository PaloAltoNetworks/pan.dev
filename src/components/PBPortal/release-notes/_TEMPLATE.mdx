---
# Copy this file to a dated file, e.g. 2026-07-07-policy-api-beta.mdx.
# Files beginning with "_" are ignored by the blog build, so this template is
# never published. One file = one release = one item in the RSS feed.
#
# Required front matter:
title: Short release title
date: 2026-07-07            # YYYY-MM-DD. Drives ordering, the on-page date, and the RSS pubDate.
slug: short-release-title  # URL is /prisma-browser/release-notes/<slug>
summary: One-sentence headline shown as the card summary and the first line of the RSS item.

# The UI (timeline card + collapsible body) and the RSS feed are both generated
# from this `changes` list. No per-release UI code is needed.
changes:
  - type: new              # one of: breaking | new | changed | deprecated | removed | fixed
    text: What changed. Supports inline <code>...</code> and <b>...</b>.
    # Optional fields:
    endpoint: Affected endpoint name   # renders a link chip
    to: /prisma-browser/api/some-endpoint
    todo: What the customer needs to do (shown as a callout).
    before: old value      # `before` + `after` render a red/green diff
    after: new value
    example: |             # renders a copyable code panel
      https GET api.sase.paloaltonetworks.com/seb-api/v1/...
---

Write a one or two sentence human summary here. This body is what feed readers
fall back to and is not shown in the custom on-site timeline.

{/* truncate */}
