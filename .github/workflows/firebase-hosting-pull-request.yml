name: Deploy Preview

on:
  workflow_run:
    workflows: [CodeQL]
    types:
      - completed

jobs:
  build_and_preview:
    name: Build and Preview
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Download workflow artifact
        uses: dawidd6/action-download-artifact@b9571484721e8187f1fd08147b497129f8972c74 # v2.14.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          workflow: codeql-analysis.yml
          run_id: ${{ github.event.workflow_run.id }}

      - name: Get PR number
        id: pr_num
        run: echo ::set-output name=content::$(cat ./pr_num/pr_num.txt)
          
      - name: Checkout repository
        uses: actions/checkout@5a4ac9002d0be2fb38bd78e4b4dbde5606d7042f # v2.3.4

      - name: Build site
        run: yarn install && yarn run build

      - name: Deploy to Firebase
        id: deploy_preview
        uses: FirebaseExtended/action-hosting-deploy@276388dd6c2cde23455b30293105cc866c22282d # v0.6-alpha
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PAN_DEV_F1B58 }}'
          projectId: pan-dev-f1b58
          channelId: 'pr${{ steps.pr_num.outputs.content }}'
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          
      - name: 'Comment PR'
        uses: actions/github-script@a3e7071a34d7e1f219a8a4de9a5e0a34d1ee1293 # v4.0.2
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.issues.createComment({
              issue_number: ${{ steps.pr_num.outputs.content }},
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🔥 Deploy Preview URL: ${{ steps.deploy_preview.outputs.details_url }} \n\n🦝 Build Logs: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}'
            })
    environment:
      name: build
      url: ${{ steps.deploy_preview.outputs.details_url }}
